---
title: "WikiSeeker: Rethinking the Role of Vision-Language Models in Knowledge-Based Visual Question Answering"
title_zh: WikiSeeker：重新思考视觉语言模型在基于知识的视觉问答中的作用
authors: "Yingjian Zhu, Xinming Wang, Kun Ding, Ying Wang, Bin Fan, Shiming Xiang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05818v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 基于知识的视觉问答中的视觉语言模型
tldr: WikiSeeker是一种创新的多模态检索增强生成框架，针对基于知识的视觉问答任务中现有方法过度依赖图像作为检索键而忽视视觉语言模型(VLMs)潜力的问题。该框架引入多模态检索器，并将VLMs重新定义为Refiner和Inspector两个专门代理，显著提升了检索准确性和答案质量，在多个基准测试中实现了最先进性能。
selection_source: fresh_fetch
motivation: 现有KB-VQA方法主要依赖图像作为检索键，忽视或误用VLMs的潜力，未能充分利用其能力。
method: WikiSeeker引入多模态检索器，将VLMs重新定义为Refiner和Inspector两个代理，前者重写查询提升检索性能，后者选择性路由上下文或依赖内部知识生成答案。
result: 在EVQA、InfoSeek和M2KR基准测试中，WikiSeeker实现了最先进性能，显著提升了检索准确性和答案质量。
conclusion: 通过重新定义VLMs在多模态RAG框架中的角色，WikiSeeker有效解决了现有方法的局限性，为KB-VQA任务提供了更有效的解决方案。
---

## 摘要
多模态检索增强生成(RAG)已成为基于知识的视觉问答(KB-VQA)的一种非常有效的范式。尽管最近取得了进展，但现有方法主要仍依赖图像作为检索键，并且经常忽视或误用视觉语言模型(VLMs)的作用，从而未能充分利用其潜力。在本文中，我们介绍了WikiSeeker，一种新颖的多模态RAG框架，通过提出多模态检索器并重新定义VLMs的作用来弥合这些差距。VLMs不再仅仅作为答案生成器，我们为其分配了两个专门的代理：一个精炼器(Refiner)和一个检查器(Inspector)。精炼器利用VLMs根据输入图像重写文本查询的能力，显著提高了多模态检索器的性能。检查器通过将有价值的检索上下文选择性路由到另一个LLM进行答案生成，实现了解耦的生成策略，同时在检索不可靠时依赖VLM的内部知识。在EVQA、InfoSeek和M2KR上的大量实验表明，WikiSeeker取得了最先进的性能，在检索准确性和答案质量方面都有显著提高。我们的代码将在https://github.com/zhuyjan/WikiSeeker上发布。

## Abstract
Multi-modal Retrieval-Augmented Generation (RAG) has emerged as a highly effective paradigm for Knowledge-Based Visual Question Answering (KB-VQA). Despite recent advancements, prevailing methods still primarily depend on images as the retrieval key, and often overlook or misplace the role of Vision-Language Models (VLMs), thereby failing to leverage their potential fully. In this paper, we introduce WikiSeeker, a novel multi-modal RAG framework that bridges these gaps by proposing a multi-modal retriever and redefining the role of VLMs. Rather than serving merely as answer generators, we assign VLMs two specialized agents: a Refiner and an Inspector. The Refiner utilizes the capability of VLMs to rewrite the textual query according to the input image, significantly improving the performance of the multimodal retriever. The Inspector facilitates a decoupled generation strategy by selectively routing reliable retrieved context to another LLM for answer generation, while relying on the VLM's internal knowledge when retrieval is unreliable. Extensive experiments on EVQA, InfoSeek, and M2KR demonstrate that WikiSeeker achieves state-of-the-art performance, with substantial improvements in both retrieval accuracy and answer quality. Our code will be released on https://github.com/zhuyjan/WikiSeeker.

---

## 论文详细总结（自动生成）

# WikiSeeker论文总结

## 1. 论文的核心问题与整体含义

WikiSeeker针对基于知识的视觉问答(KB-VQA)领域中的两个关键局限性：
- **视觉-only检索限制**：现有方法主要依赖图像作为检索键，忽视了用户文本查询中的语义信息，导致视觉内容模糊时检索结果相关性差
- **VLM角色误用**：多模态RAG系统中，视觉语言模型(VLMs)通常仅作为答案生成器，未能充分利用其潜力

研究背景表明，多模态检索增强生成(RAG)已成为KB-VQA的有效范式，但现有方法在检索准确性和答案质量方面仍有提升空间，需要重新思考VLMs在多模态RAG框架中的角色。

## 2. 论文提出的方法论

### 核心思想
WikiSeeker引入多模态检索器，并将VLMs重新定义为两个专门代理：Refiner(精炼器)和Inspector(检查器)，实现更有效的知识检索和答案生成。

### 关键技术细节
- **多模态检索器**：
  - 构建由<图像,段落>对组成的多模态知识库
  - 使用加权连接策略融合视觉和文本特征：`v = Concat[α·Φ_vis(I), (1-α)·Φ_text(T)]`
  - 通过余弦相似度计算查询与知识库条目的匹配度

- **Refiner**：
  - 利用VLMs的视觉线索重写原始查询
  - 采用链式思维(CoT)推理过程增强视觉理解
  - 通过GRPO算法优化，结合检索奖励和格式奖励

- **Inspector**：
  - 评估检索上下文是否充分且与视觉证据一致
  - 实现解耦生成策略：可靠上下文路由到LLM生成器，不可靠时依赖VLM内部知识

### 算法流程
1. **检索阶段**：Refiner重写查询，多模态检索器进行密集检索
2. **重排序阶段**：多模态重排序器过滤最相关候选部分
3. **生成阶段**：Inspector评估上下文，动态决定答案生成路径

## 3. 实验设计

### 数据集
- **EVQA**：约100万个VQA样本，221,000个独特问答对，16.7k个细粒度实体
- **InfoSeek**：130万个训练问题，8.9k个人类标注评估样本，11k+视觉实体
- **M2KR**：综合框架，包含EVQA、OKVQA-GS和OVEN等子任务

### 评估指标
- **EVQA**：BEM分数
- **InfoSeek**：标准VQA准确率和宽松VQA准确率
- **M2KR**：Recall@K和Pseudo Recall@K

### 对比方法
- Wiki-LLaVA、EchoSight、ReflectiVA、OMGM
- RoRA-VLM、LLM-RA、mR²AG、CoRe-MMRAG、MI-RAG

## 4. 资源与算力

- **硬件**：4个NVIDIA A800 40GB SXM4 GPU
- **Refiner训练**：全局批大小32，学习率1×10⁻⁶，组大小5，rollout温度0.7，总训练600步
- **框架**：使用LlamaFactory进行LLM微调，HybridFlow进行RL训练

## 5. 实验数量与充分性

### 实验数量
- 三个主要基准测试(EVQA、InfoSeek、M2KR)上的性能评估
- 消融研究：Refiner和Inspector组件贡献分析
- 参数搜索：模态权重α(0.59-0.63)和多模态重排序权重β(0.6-0.8)
- 查询策略对比：原始查询、图像描述、零样本扩展等
- 解耦生成策略有效性分析
- Inspector模块性能评估

### 实验充分性
- 实验覆盖多个数据集和评估指标
- 进行全面的消融研究验证各组件贡献
- 提供详细的参数分析和敏感性研究
- 使用多种基线方法进行公平对比
- 遵循标准评估协议，结果客观可靠

## 6. 论文的主要结论与发现

### 主要结论
- WikiSeeker在EVQA、InfoSeek和M2KR基准测试上实现最先进性能
- Refiner显著提高检索质量，是最终KB-VQA性能提升的主要因素
- Inspector实现有效的解耦生成策略，进一步提升性能
- 多模态检索策略优于单模态方法，视觉语义权重略高于文本语义

### 主要发现
- VLMs在利用检索上下文提取答案方面不如LLMs有效
- 当检索结果不可靠时，VLMs的内部知识可提供可靠答案
- 查询重写对提高检索质量至关重要
- 解耦生成策略能有效结合LLMs的文本理解能力和VLMs的视觉推理能力

## 7. 优点

### 方法亮点
- 重新定义VLMs在多模态RAG框架中的角色，充分发挥其潜力
- 引入多模态检索器，同时利用视觉和文本信息进行检索
- 设计Refiner和Inspector两个专门代理，各司其职
- 使用强化学习(GRPO)优化Refiner，无需昂贵标注数据
- 实现解耦生成策略，根据检索可靠性动态路由

### 实验设计亮点
- 在多个基准测试上验证方法有效性
- 进行全面的消融研究，验证各组件贡献
- 提供详细的参数分析和敏感性研究
- 使用多种基线方法进行公平对比
- 提供直观案例研究，展示方法工作机制

## 8. 不足与局限

- **路由机制局限**：当前采用硬路由规则，可能不是最优的LLM-VLM协作方式
- **检索能力限制**：仅支持单次检索，无法处理多跳问题
- **知识库覆盖**：实验主要集中在英文维基百科知识，存在语言和文化偏差
- **扩展性未验证**：未探索模型在不同规模知识库上的扩展性
- **计算效率**：多代理架构增加了推理时间，虽然比一些基线更快
- **泛化能力**：未充分探索模型在开放域问题上的泛化能力

（完）
