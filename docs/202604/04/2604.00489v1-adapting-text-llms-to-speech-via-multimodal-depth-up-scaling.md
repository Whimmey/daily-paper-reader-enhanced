---
title: Adapting Text LLMs to Speech via Multimodal Depth Up-Scaling
title_zh: 通过多模态深度上缩放将文本大语言模型适配到语音
authors: "Kazuki Yano, Jun Suzuki, Shinji Watanabe"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00489v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 通过多模态深度上缩放将文本LLM适配到语音
tldr: 本研究提出多模态深度上采样方法，通过在冻结的文本LLM中插入新层并仅训练这些层于语音数据，实现了文本LLM到语音LLM的高效转换。实验表明，该方法在保持ASR性能的同时，显著减少了文本能力下降，且参数效率更高。
selection_source: fresh_fetch
motivation: 将文本LLM适配为语音LLM时，传统方法常导致原有文本能力显著下降。
method: 多模态深度上采样方法在冻结的文本LLM中插入新层，仅训练这些新增层于语音数据。
result: "深度上采样在ASR性能上接近完全微调，同时显著减少文本能力下降，且结合E-Branchformer架构可进一步降低75%以上的文本退化。"
conclusion: 多模态深度上采样是一种高效的方法，能够在保持文本LLM原有能力的同时有效适配语音任务。
---

## 摘要
通过在语音数据上进行持续预训练将预训练的文本大语言模型(LLMs)适配为语音语言模型(Speech LMs)是有前景的，但这往往会降低原始文本能力。我们提出了多模态深度上缩放(Multimodal Depth Upscaling)，这是持续LLM预训练中新兴策略的一种扩展，该方法将新的transformer层插入到冻结的文本LLM中，并且只有新增的层在语音数据上进行训练。在48k小时的英语自动语音识别(ASR)数据上对SmolLM2-360M和SmolLM2-1.7B进行的实验表明，深度上缩放实现了与完全微调相当的ASR性能，同时引起的文本能力下降远小于完全微调和低秩适配(LoRA)。我们进一步证明，将专为语音识别设计的E-Branchformer架构作为插入层，在较大模型上实现了与或超越完全微调的ASR性能，同时减少了75%以上的文本能力下降，并且可训练参数减少了60%。

## Abstract
Adapting pre-trained text Large Language Models (LLMs) into Speech Language Models (Speech LMs) via continual pretraining on speech data is promising, but often degrades the original text capabilities. We propose Multimodal Depth Upscaling, an extension of an emerging strategy in continual LLM pre-training, where new transformer layers are inserted into a frozen text LLM and only the added layers are trained on speech data. Experiments with SmolLM2-360M and SmolLM2-1.7B on 48k hours of English Automatic Speech Recognition (ASR) data show that depth up-scaling achieves ASR comparable to full fine-tuning while causing far less text degradation than both full fine-tuning and Low-Rank Adaptation (LoRA). We further show that incorporating E-Branchformer, an architecture designed for speech recognition, as the inserted layers achieves ASR that matches or surpasses full fine-tuning on the larger model while reducing text degradation by over 75% with 60% fewer trainable parameters.

---

## 论文详细总结（自动生成）

# 论文总结：通过多模态深度上缩放将文本大语言模型适配到语音

## 1. 论文的核心问题与整体含义

- **研究动机**：将预训练的文本大语言模型(LLMs)适配为语音语言模型(Speech LMs)时，传统方法（如完全微调和LoRA）往往会显著降低原始文本能力。
- **背景问题**：通过在语音数据上进行持续预训练将文本LLMs适配为Speech LMs是有前景的，但这种方法会导致模型"遗忘"原有的文本能力。
- **现有局限**：现有解决方案（如文本数据回放和LoRA）存在各种限制，如文本数据回放需要额外文本数据且增加训练成本，而大多数开源LLM不发布预训练数据；LoRA可能缺乏学习新模态的足够容量。

## 2. 论文提出的方法论

- **核心思想**：多模态深度上缩放(Multimodal Depth Upscaling)，即在冻结的文本LLM中插入新的transformer层，并仅训练这些新增层于语音数据。
- **关键技术细节**：
  - **词汇扩展**：将语音token添加到原始文本词汇表中，扩展tokenizer
  - **深度上缩放**：插入m个新的transformer层到原始n层模型中，所有原始层保持冻结
  - **函数保持初始化**：零初始化新增层的输出投影矩阵，使新增层在初始化时表现为恒等函数
  - **层放置策略**：测试了5种放置策略（INTERLEAVED、BOTTOM、MIDDLE、TOP、SANDWICH）
  - **新增层架构**：标准transformer层和专为语音设计的E-Branchformer层
  - **推理时层丢弃**：可以在推理时移除新增层，完全恢复原始预训练模型
- **算法流程**：
  1. 扩展词汇表以包含语音token
  2. 在冻结的文本LLM中插入新的transformer层
  3. 使用函数保持初始化方法初始化新增层
  4. 仅训练新增层于语音数据
  5. 推理时可以选择保留新增层（用于语音任务）或移除它们（恢复原始文本能力）

## 3. 实验设计

- **数据集/场景**：
  - 训练数据：OWSM v3.2套件中的英语ASR子集（约48k小时）
  - ASR评估：LibriSpeech测试集（clean和other子集）
  - 文本能力评估：8个基准测试（ARC-Easy、ARC-Challenge、BoolQ、HellaSwag、OpenBookQA、PIQA、WinoGrande、MMLU）
- **对比方法**：
  - 完全微调(Full fine-tuning)：更新所有原始参数
  - LoRA：低秩适配，训练低秩适配器
  - 深度上缩放：5种不同层放置策略
  - 不同新增层架构：标准transformer层 vs E-Branchformer层
- **评估指标**：
  - ASR性能：词错误率(WER)
  - 文本能力：8个基准测试的平均准确率

## 4. 资源与算力

- 论文中未明确提到使用的GPU型号、数量或训练时长等算力信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个模型规模：SmolLM2-360M和SmolLM2-1.7B
  - 5种层放置策略的对比
  - 2种新增层架构的对比
  - 函数保持初始化vs随机初始化的对比
  - 零样本指令跟随任务的定性分析
- **充分性与公平性**：
  - 实验设计较为全面，涵盖了不同模型规模、不同层放置策略、不同新增层架构
  - 对比方法包括完全微调和LoRA等主流方法
  - 为公平比较，LoRA的rank设置使其可训练参数数量与深度上缩放相当
  - 排除了文本数据回放的影响，以隔离深度上缩放的效果
  - 实验结果客观呈现，包括ASR性能和文本能力下降的量化指标

## 6. 论文的主要结论与发现

- 深度上缩放实现了与完全微调相当的ASR性能，同时显著减少了文本能力下降
- 在1.7B模型上，结合E-Branchformer的新增层实现了与或超越完全微调的ASR性能，同时减少了75%以上的文本退化，可训练参数减少60%
- 层放置策略影响性能：INTERLEAVED放置在ASR任务上表现最佳，而TOP放置在文本能力保留上表现最佳
- 函数保持初始化对训练稳定性至关重要
- 深度上缩放保留了基础LLM的文本指令跟随能力，即使在仅训练ASR任务的情况下

## 7. 优点：方法或实验设计上有哪些亮点

- **方法亮点**：
  - 创新性地将深度上缩放应用于跨模态适应，解决了文本到语音转换中的能力退化问题
  - 通过冻结原始层并仅训练新增层，完全保留了原始文本能力，并可随时通过移除新增层恢复
  - 结合E-Branchformer架构，专为语音任务设计的层进一步提升了性能
  - 方法简单有效，不需要额外的文本数据回放
- **实验设计亮点**：
  - 全面评估了不同层放置策略的影响
  - 对比了多种方法（完全微调、LoRA、不同深度上缩放变体）
  - 不仅评估了ASR性能，还评估了文本能力保留情况
  - 进行了零样本指令跟随任务的定性分析，验证了模型的语言理解能力

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅在英语ASR任务上进行了验证，未测试其他语言或其他语音任务
  - 未探索不同比例的新增层对性能的影响
  - 未测试模型在不同规模上的扩展性
- **应用限制**：
  - 虽然E-Branchformer提升了性能，但增加了参数数量，可能影响推理效率
  - 实验主要在学术数据集上进行，未在真实世界场景中验证
  - 未探索深度上缩放与其他技术（如文本数据回放）结合的可能性
- **其他潜在问题**：
  - 模型在语音和文本任务之间切换需要额外的推理逻辑
  - 新增层的放置策略可能需要针对不同任务进行调优

（完）
