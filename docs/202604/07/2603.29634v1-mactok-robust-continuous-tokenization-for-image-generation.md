---
title: "MacTok: Robust Continuous Tokenization for Image Generation"
title_zh: MacTok：用于图像生成的鲁棒连续分词
authors: "Hengyu Zeng, Xin Gao, Guanghao Li, Yuxiang Yan, Jiaoyang Ruan, Junpeng Ma, Haoyu Albert Wang, Jian Pu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29634v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 带正则化的掩码增强令牌化器
tldr: MacTok是一种基于掩码增强的1D连续图像标记化方法，通过随机掩码和DINO引导的语义掩码防止后验崩溃，结合全局和局部表示对齐在高度压缩的1D潜在空间中保留丰富判别信息，仅需64或128个标记即可在ImageNet上实现高质量图像生成，显著减少标记使用量。
selection_source: fresh_fetch
motivation: 现有基于变分框架的连续图像标记器在使用较少标记时容易出现后验崩溃问题，导致编码器无法将有效特征编码到压缩的潜在空间中。
method: MacTok通过随机掩码和DINO引导的语义掩码来正则化潜在学习，并利用全局和局部表示对齐来保留丰富的判别信息。
result: MacTok在ImageNet上实现了具有竞争力的gFID分数（256×256分辨率为1.44，512×512分辨率为1.52），同时将标记使用量减少了最多64倍。
conclusion: 掩码和语义指导共同防止了后验崩溃，实现了高效、高保真的标记化。
---

## 摘要
连续图像分词器能够实现高效的视觉生成，基于变分框架的分词器可以通过KL正则化学习平滑、结构化的潜在表示。然而，当使用较少的分词时，这常常导致后验崩溃，即编码器无法将有效特征编码到压缩的潜在空间中。为解决这一问题，我们引入了MacTok，这是一种掩码增强的一维连续分词器，它利用图像掩码和表示对齐来防止崩溃，同时学习紧凑且鲁棒的表示。MacTok应用随机掩码来正则化潜在学习，并应用DINO引导的语义掩码来强调图像中的信息区域，迫使模型从不完整的视觉证据中编码鲁棒的语义。结合全局和局部表示对齐，MacTok在高度压缩的一维潜在空间中保留了丰富的判别信息，仅需64或128个分词。在ImageNet上，MacTok实现了256×256分辨率下具有竞争力的1.44 gFID，以及使用SiT-XL在512×512分辨率下达到最先进的1.52，同时将分词使用量减少了最多64倍。这些结果证实，掩码和语义指导共同防止了后验崩溃，实现了高效、高保真的分词。

## Abstract
Continuous image tokenizers enable efficient visual generation, and those based on variational frameworks can learn smooth, structured latent representations through KL regularization. Yet this often leads to posterior collapse when using fewer tokens, where the encoder fails to encode informative features into the compressed latent space. To address this, we introduce \textbf{MacTok}, a \textbf{M}asked \textbf{A}ugmenting 1D \textbf{C}ontinuous \textbf{Tok}enizer that leverages image masking and representation alignment to prevent collapse while learning compact and robust representations. MacTok applies both random masking to regularize latent learning and DINO-guided semantic masking to emphasize informative regions in images, forcing the model to encode robust semantics from incomplete visual evidence. Combined with global and local representation alignment, MacTok preserves rich discriminative information in a highly compressed 1D latent space, requiring only 64 or 128 tokens. On ImageNet, MacTok achieves a competitive gFID of 1.44 at 256$\times$256 and a state-of-the-art 1.52 at 512$\times$512 with SiT-XL, while reducing token usage by up to 64$\times$. These results confirm that masking and semantic guidance together prevent posterior collapse and achieve efficient, high-fidelity tokenization.

---

## 论文详细总结（自动生成）

# MacTok论文总结

## 1. 论文的核心问题与整体含义
- **研究动机**：基于变分框架的连续图像分词器在使用较少标记时容易出现后验崩溃问题，导致编码器无法将有效特征编码到压缩的潜在空间中。
- **背景**：连续图像分词器能够实现高效的视觉生成，但现有方法在强压缩条件下存在后验崩溃问题，限制了其在实际应用中的效率和质量。
- **整体含义**：MacTok旨在解决连续分词器在强压缩条件下的后验崩溃问题，实现高效且高质量的图像生成。

## 2. 论文提出的方法论
- **核心思想**：MacTok是一种掩码增强的一维连续分词器，通过图像掩码和表示对齐来防止后验崩溃，学习紧凑且鲁棒的表示。
- **关键技术细节**：
  - **随机掩码**：随机选择图像块进行掩码（掩码比例在[0, 0.7]之间随机采样），迫使模型从不完整信息中重建完整图像。
  - **DINO引导的语义掩码**：基于DINOv2特征计算分类标记与图像块标记的余弦相似度，选择语义上重要的区域进行掩码，公式为：$s_i = \frac{c^T p_i}{\|c\|\|p_i\|}$，其中$c$是分类标记，$p_i$是第$i$个图像块标记。
  - **全局和局部表示对齐**：将潜在表示与预训练的DINOv2特征对齐，包括局部对齐（将潜在标记扩展到与图像块数量匹配）和全局对齐（对潜在标记进行平均池化），损失函数为：$L_{RA} = -\frac{1}{N+1}\left(\sum_{i=1}^{N} \text{sim}(o_{loc,i}, p_i) + \text{sim}(o_{glob}, c)\right)$。
- **算法流程**：编码器将图像和潜在标记输入，应用随机掩码或语义掩码策略，然后通过解码器重建完整图像，同时使用表示对齐损失来优化潜在空间的语义结构。

## 3. 实验设计
- **数据集**：ImageNet（256×256和512×512分辨率）
- **Benchmark**：
  - 重建质量：rFID、PSNR、SSIM
  - 生成质量：gFID、IS、Precision和Recall
- **对比方法**：
  - 连续分词器：VA-VAE、MAETok、SoftVQ-VAE、SD-VAE、MAR-VAE、l-DeTok
  - 离散分词器：ViT-VQGAN、MaskGIT、TiTok
  - 生成模型：DiT-XL、SiT-XL、LightningDiT

## 4. 资源与算力
- **模型规模**：使用ViT-Base作为骨干网络，总参数量为176M
- **训练配置**：
  - 256×256分辨率：训练250K迭代，批量大小256
  - 512×512分辨率：训练500K迭代，批量大小128
- **优化器**：AdamW优化器（β1=0.9，β2=0.95，权重衰减1×10⁻⁴）
- **学习率**：余弦退火调度，峰值1×10⁻⁴，线性预热5K/10K步
- **未明确信息**：论文未提及具体GPU型号、数量和训练时长

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验：在256×256和512×512分辨率下评估MacTok的性能
  - 消融实验：分析不同掩码比例、掩码策略、表示对齐方法的影响
  - 潜在空间分析：可视化潜在空间结构，进行线性探测分析
  - 与多种基线方法进行对比
- **充分性与公平性**：
  - 实验设计全面，覆盖了不同分辨率、不同标记数量、不同模型配置
  - 使用标准评估指标，与多种基线方法进行公平比较
  - 消融实验充分验证了各组件的有效性

## 6. 论文的主要结论与发现
- MacTok有效缓解了连续分词器中的后验崩溃问题
- 结合随机掩码和语义掩码能够学习更鲁棒和语义化的潜在表示
- MacTok在ImageNet上实现了具有竞争力的生成质量，仅需64或128个标记
- 表示对齐进一步增强了潜在空间的语义结构，提高了生成质量
- 适当掩码比例（70%）能显著提升生成性能

## 7. 优点
- **方法创新**：结合掩码增强和表示对齐两种策略，有效解决后验崩溃问题
- **高效压缩**：仅需64或128个标记即可实现高质量生成，比传统方法减少最多64倍标记使用量
- **实验全面**：包含多种消融实验、潜在空间分析和可视化，验证方法有效性
- **性能优越**：在ImageNet上实现了具有竞争力的gFID分数（256×256: 1.44，512×512: 1.52）

## 8. 不足与局限
- **数据集局限**：仅在ImageNet数据集上进行了验证，缺乏对其他数据集的泛化能力测试
- **压缩率探索不足**：未探讨模型在不同压缩率下的性能极限
- **效率分析缺失**：未分析计算效率和推理速度
- **潜在偏差**：可能存在对特定类型图像的偏好偏差
- **理论分析有限**：对后验崩溃问题的理论分析不够深入

（完）
