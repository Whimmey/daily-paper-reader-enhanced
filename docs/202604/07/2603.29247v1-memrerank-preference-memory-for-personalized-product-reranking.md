---
title: "MemRerank: Preference Memory for Personalized Product Reranking"
title_zh: MemRerank：用于个性化产品重排序的偏好记忆
authors: "Zhiyuan Peng, Xuyang Wu, Huaixiao Tou, Yi Fang, Yi Gong"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29247v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: LLM重排的偏好记忆
tldr: MemRerank是一个针对个性化产品重排序的偏好记忆框架，解决了LLM购物代理中直接使用原始购买历史导致噪声、长度和相关性不匹配的问题。该方法将用户购买历史提炼为简洁的查询无关信号，通过强化学习训练记忆提取器，实验证明其在1-in-5选择任务上比基线方法提高最多10.61个百分点准确率。
selection_source: fresh_fetch
motivation: 基于LLM的购物代理依赖长期购买历史和多轮交互进行个性化，但直接将原始历史附加到提示中往往因噪声、长度和相关性不匹配而效果不佳。
method: MemRerank框架将用户购买历史提炼为简洁的查询无关信号用于个性化产品重排序，并通过强化学习使用下游重排序性能作为监督来训练记忆提取器。
result: 实验表明，MemRerank在两个基于LLM的重排序器上持续优于无记忆、原始历史和现成记忆基线，在1-in-5准确率上提高最多10.61个百分点。
conclusion: 显式偏好记忆是代理电商系统中个性化的一种实用且有效的构建模块。
---

## 摘要
基于大型语言模型的购物代理越来越多地依赖长期购买历史和多轮交互来实现个性化，然而简单地将原始历史记录附加到提示中往往因为噪声、长度和相关性不匹配而效果不佳。我们提出了MemRerank，一种偏好记忆框架，它将用户的购买历史提炼为简洁的、与查询无关的信号，用于个性化产品重排序。为了研究这一问题，我们构建了一个端到端的基准和评估框架，围绕基于大型语言模型的1选5选择任务展开，该任务同时衡量记忆质量和下游重排序效用。我们进一步使用强化训练(RL)训练记忆提取器，以下游重排序性能作为监督。基于两种基于大型语言模型的重排序器的实验表明，MemRerank持续优于无记忆、原始历史记录和现成记忆基线，在1选5准确率上最高提升10.61个百分点。这些结果表明，显式偏好记忆是代理电子商务系统中个性化的一种实用且有效的构建模块。

## Abstract
LLM-based shopping agents increasingly rely on long purchase histories and multi-turn interactions for personalization, yet naively appending raw history to prompts is often ineffective due to noise, length, and relevance mismatch. We propose MemRerank, a preference memory framework that distills user purchase history into concise, query-independent signals for personalized product reranking. To study this problem, we build an end-to-end benchmark and evaluation framework centered on an LLM-based \textbf{1-in-5} selection task, which measures both memory quality and downstream reranking utility. We further train the memory extractor with reinforcement learning (RL), using downstream reranking performance as supervision. Experiments with two LLM-based rerankers show that MemRerank consistently outperforms no-memory, raw-history, and off-the-shelf memory baselines, yielding up to \textbf{+10.61} absolute points in 1-in-5 accuracy. These results suggest that explicit preference memory is a practical and effective building block for personalization in agentic e-commerce systems.

---

## 论文详细总结（自动生成）

# MemRerank论文总结

## 1. 论文的核心问题与整体含义

- **核心问题**：基于LLM的购物代理依赖长期购买历史和多轮交互实现个性化，但简单将原始历史附加到提示中往往因噪声、长度和相关性不匹配而效果不佳。
- **研究背景**：推荐系统在电商等领域作为引导用户浏览大量商品集合的关键工具，LLM使推荐系统从静态候选列表向代理行为发展。记忆是LLM代理的核心组件，包括工作记忆、情景记忆和语义记忆，但简单将购买历史附加到LLM提示中往往只能带来边际收益甚至降低性能。

## 2. 论文提出的方法论

- **核心思想**：提出MemRerank，一种偏好记忆框架，将用户购买历史提炼为简洁的、与查询无关的信号用于个性化产品重排序。
- **关键技术细节**：
  - 构建端到端的基准和评估框架，围绕基于LLM的1选5选择任务展开
  - 使用强化训练(RL)训练记忆提取器，以下游重排序性能作为监督
  - 区分within-category（同类）和cross-category（跨类）购物偏好记忆
  - 使用GRPO(Groun Truth-based Reinforcement Learning from Pairwise Comparisons)训练记忆提取器
- **算法流程**：
  1. 记忆提取阶段：给定用户购买历史，记忆提取器生成简洁的文本偏好记忆
  2. 下游重排序阶段：将提取的记忆插入LLM重排序提示中，与用户查询和候选产品集一起
  3. 使用1-in-5任务作为下游训练和评估目标
- **奖励函数**：
  - 格式奖励：检查生成的输出是否符合所需结构
  - 重排序奖励：将生成的记忆插入下游1-in-5重排序提示中，使用LLM重排序器选择最佳候选产品

## 3. 实验设计

- **数据集**：
  - Amazon-Review-2023：包含超过5.7亿评论和4800万商品，涵盖33个类别
  - Amazon-C4：产品搜索数据集，通过提示ChatGPT将Amazon-Review-2023中的5星评价重写为复杂上下文查询
- **Benchmark**：
  - 构建端到端基准和评估框架，围绕基于LLM的1-in-5选择任务展开
  - 每个示例围绕一个目标正例产品、其关联查询、4个负例产品和用户的历史购买构建
- **对比方法**：
  - 无记忆：直接进行个性化产品重排序
  - 产品上下文基线：同类产品、同类+跨类产品
  - LLM提取记忆基线：基础提取器记忆、GPT-5.2记忆
  - 外部记忆基线：MR.Rec和Mem0
  - 本文方法：MemRerank使用RL训练的记忆提取器

## 4. 资源与算力

- 论文中未明确提到使用的GPU型号、数量或训练时长等算力信息
- 仅提到了使用的模型（如GPT-4.1-mini和o4-mini作为重排序器，Qwen2.5-7B-Instruct作为记忆提取器），但没有详细说明训练这些模型所需的算力资源

## 5. 实验数量与充分性

- **实验数量**：
  - 主要实验在Electronics类别上进行，包含905个训练实例、194个开发实例和194个测试实例
  - 进行了不同记忆提取提示的对比实验（v1、v2、v3三种变体）
  - 研究了购买历史长度对下游个性化产品重排序的影响
  - 对比了多种基线方法
- **实验充分性**：
  - 实验设计较为全面，涵盖了不同类型的基线方法
  - 进行了消融研究，验证了不同组件的有效性
  - 使用了两个不同的LLM重排序器进行验证
- **公平性**：
  - 所有方法在相同的数据集和评估协议下进行比较
  - 使用相同的1-in-5任务作为评估标准

## 6. 论文的主要结论与发现

- **主要结论**：
  - 显式偏好记忆是代理电商系统中个性化的一种实用且有效的构建模块
  - 提炼的偏好记忆比直接附加原始历史记录更有效
  - RL训练的记忆提取器比基础提取器和更强的专有LLM更有效
- **主要发现**：
  - 简单增加原始购买历史并不总是提高重排序性能，效果取决于重排序器和同类与跨类上下文的平衡
  - 记忆提取提示设计对下游性能有显著影响，半结构化、基于证据的提示比完全固定的方面提示或完全无约束的提示更有效
  - "think tag"（显式推理）进一步提高了性能，表明更好的记忆和显式推理是互补的

## 7. 优点

- **方法亮点**：
  - 提出将用户购买历史提炼为简洁的、查询无关的偏好记忆，解决了直接使用原始历史导致的问题
  - 使用强化学习直接优化记忆提取器以最大化下游重排序性能，避免了需要收集大量偏好记忆标注数据
  - 区分同类和跨类购物偏好记忆，捕捉更全面的用户偏好模式
  - 设计半结构化、基于证据的记忆提取提示，平衡了结构化和灵活性
- **实验设计亮点**：
  - 构建了专门的基准和评估框架，同时衡量记忆质量和下游重排序效用
  - 对比了多种类型的基线方法，从简单到复杂
  - 进行了全面的消融研究，验证了不同组件的有效性
  - 使用两个不同的LLM重排序器验证方法的鲁棒性

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅在Electronics类别上进行了评估，未测试其他领域的泛化能力
  - 仅使用产品元数据进行记忆提取，未考虑更丰富的信号
  - 仅研究离线1-in-5重排序，未研究更大规模的检索设置
- **偏差风险**：
  - 数据集存在类别不平衡问题
  - 仅使用Amazon数据，可能存在平台特定偏差
  - 评估基于1-in-5任务，可能无法完全反映真实场景中的排序性能
- **应用限制**：
  - 方法依赖于LLM，可能存在计算成本高的问题
  - 记忆提取过程需要额外的训练步骤，增加了系统复杂性
  - 未考虑实时更新记忆的机制，可能难以适应用户偏好的动态变化

（完）
