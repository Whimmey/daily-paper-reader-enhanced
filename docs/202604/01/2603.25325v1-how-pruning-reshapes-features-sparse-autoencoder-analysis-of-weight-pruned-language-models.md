---
title: "How Pruning Reshapes Features: Sparse Autoencoder Analysis of Weight-Pruned Language Models"
title_zh: 剪枝如何重塑特征：对权重剪枝语言模型的稀疏自编码器分析
authors: "Hector Borobia, Elies Seguí-Mas, Guillermina Tormo-Carbó"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25325v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 语言模型中的权重剪枝
tldr: 本研究首次系统探讨了非结构化剪枝如何重塑语言模型的特征几何，使用稀疏自编码器作为可解释性探针。通过对三个模型家族、两种剪枝方法和六个稀疏度水平的实验，发现稀有特征比频繁特征在剪枝中存活更好，Wanda剪枝比幅度剪枝更好地保留特征结构，且几何特征生存不预测因果重要性。
selection_source: fresh_fetch
motivation: 权重剪枝虽是压缩大型语言模型的标准技术，但其对学习到的内部表征的影响仍不清楚。
method: 使用稀疏自编码器作为可解释性探针，系统研究了非结构化剪枝对语言模型特征几何的影响。
result: 研究发现稀有SAE特征比频繁特征在剪枝中存活得更好，Wanda剪枝比幅度剪枝更好地保留特征结构，且几何特征生存不预测因果重要性。
conclusion: 剪枝作为一种隐式特征选择机制，优先销毁高频通用特征，同时保留专业化稀有特征，这对压缩模型的可解释性有重要启示。
---

## 摘要
权重剪枝是压缩大型语言模型的标准技术，然而它对学习到的内部表征的影响仍 poorly understood。我们首次系统地研究了非结构化剪枝如何重塑语言模型的特征几何结构，使用稀疏自编码器(SAEs)作为可解释性探针。跨越三个模型家族(Gemma 3 1B, Gemma 2 2B, Llama 3.2 1B)、两种剪枝方法(幅度和Wanda)和六个稀疏度水平(0-60%)，我们研究了五个研究问题，包括种子稳定性、特征存活率、SAE可转移性、特征脆弱性和因果相关性。我们最引人注目的发现是，罕见的SAE特征——那些具有低激活率的特征——比频繁特征在剪枝中存活得好得多，在17个实验条件中的11个条件下，条件内的Spearman相关系数为rho = -1.0。这一反直觉的结果表明，剪枝充当了隐式特征选择，优先破坏高频通用特征，同时保留专门的稀有特征。我们进一步证明，Wanda剪枝比幅度剪枝更好地保留特征结构，高达3.7倍，预训练的SAEs在Wanda剪枝的模型上保持有效，稀疏度高达50%，并且几何特征存活率不能预测因果重要性——这种分离对压缩条件下的可解释性有影响。

## Abstract
Weight pruning is a standard technique for compressing large language models, yet its effect on learned internal representations remains poorly understood. We present the first systematic study of how unstructured pruning reshapes the feature geometry of language models, using Sparse Autoencoders (SAEs) as interpretability probes. Across three model families (Gemma 3 1B, Gemma 2 2B, Llama 3.2 1B), two pruning methods (magnitude and Wanda), and six sparsity levels (0--60%), we investigate five research questions spanning seed stability, feature survival, SAE transferability, feature fragility, and causal relevance. Our most striking finding is that rare SAE features--those with low firing rates--survive pruning far better than frequent ones, with within-condition Spearman correlations of rho = -1.0 in 11 of 17 experimental conditions. This counter-intuitive result suggests that pruning acts as implicit feature selection, preferentially destroying high-frequency generic features while preserving specialized rare ones. We further show that Wanda pruning preserves feature structure up to 3.7x better than magnitude pruning, that pre-trained SAEs remain viable on Wanda-pruned models up to 50% sparsity, and that geometric feature survival does not predict causal importance--a dissociation with implications for interpretability under compression.

---

## 论文详细总结（自动生成）

# 论文总结：剪枝如何重塑特征：对权重剪枝语言模型的稀疏自编码器分析

## 1. 论文的核心问题与整体含义

论文研究权重剪枝(weight pruning)如何影响大型语言模型的内部特征表示。虽然剪枝是压缩语言模型的标准技术，但其对学习到的内部表征的影响仍不清楚。研究动机是填补这一知识空白，理解剪枝如何重塑模型的特征几何结构，这对于在压缩模型上保持可解释性具有重要意义。这项研究首次系统探讨了非结构化剪枝对语言模型特征几何的影响，为模型压缩与可解释性之间的权衡提供了新见解。

## 2. 论文提出的方法论

核心思想：使用稀疏自编码器(Sparse Autoencoders, SAEs)作为可解释性探针，系统研究非结构化剪枝对语言模型特征几何的影响。

关键技术细节：
- 使用TopK稀疏自编码器架构，将模型激活向量分解为稀疏、可解释的特征
- SAE架构：z = TopK(W_enc * x_hat + b_enc, k)，其中x_hat = (x - μ)/σ（标准化输入）
- 重构公式：x_hat_rec = W_dec * z + b_dec
- 使用三种特征匹配指标：单向匹配率、互最近邻(MNN)率、贪心1对1匹配率
- 在阈值τ ∈ {0.5, 0.6, 0.7, 0.8, 0.9}下评估特征匹配，主要使用τ=0.7
- 使用单特征消融实验评估特征因果重要性，通过KL散度测量特征消融对输出的影响

## 3. 实验设计

数据集/场景：
- 三个模型家族：Gemma 3 1B（主要模型）、Gemma 2 2B（锚点模型）、Llama 3.2 1B（跨家族验证）
- 两种剪枝方法：全局幅度剪枝(magnitude pruning)和Wanda剪枝
- 六个稀疏度水平：0%、20%、30%、40%、50%、60%
- 使用WikiText-2评估模型质量（困惑度）
- 使用FineWeb-Edu数据集提取激活和进行评估

对比方法：
- 幅度剪枝 vs Wanda剪枝
- 不同稀疏度水平的比较
- 不同模型家族的比较
- 不同随机种子的SAE训练比较

## 4. 资源与算力

论文中未明确提到使用的GPU型号、数量或训练时长等算力信息。在SAE训练部分提到了一些超参数（如学习率5×10^-5，使用余弦退火，20,000训练步骤，批量大小2,048-4,096），但没有具体的硬件资源信息。

## 5. 实验数量与充分性

实验数量：
- 三个模型家族
- 两种剪枝方法
- 六个稀疏度水平
- 每个条件训练3-5个不同随机种子的SAE
- 总共22个成功完成的实验运行（计划24个，2个因float16溢出失败）

充分性：
- 实验覆盖了多种模型架构（Gemma和Llama）
- 测试了不同的剪枝方法和稀疏度
- 使用了多种评估指标（困惑度、特征匹配率、因果重要性等）
- 进行了种子稳定性分析，确保结果不是随机初始化的产物
- 实验设计较为全面，但主要限于1-2B参数规模的模型

## 6. 论文的主要结论与发现

主要结论：
1. 稀有SAE特征（低激活率）比频繁特征在剪枝中存活得更好，在17个实验条件中的15个条件下，激活率与存活率之间的Spearman相关系数ρ≤-0.8
2. Wanda剪枝比幅度剪枝更好地保留特征结构，高达3.7倍
3. 预训练的SAEs在Wanda剪枝的模型上保持有效，稀疏度高达50%
4. 几何特征存活率不能预测因果重要性：健壮和脆弱特征在消融时产生几乎相同的KL散差异常值（<1.2%差异）
5. 基于激活率和稀疏度的简单逻辑回归模型可以预测特征存活，AUC>0.79

## 7. 优点

1. 首次系统研究了剪枝对语言模型特征几何的影响，填补了研究空白
2. 使用稀疏自编码器作为可解释性探针，提供了对内部表征的直接洞察
3. 实验设计全面，覆盖多种模型、剪枝方法和稀疏度
4. 发现了反直觉的结果（稀有特征比频繁特征更耐剪枝），为理解剪机制提供了新视角
5. 提供了实用的预剪枝诊断工具，帮助识别脆弱特征
6. 开源了代码、数据和训练好的SAE，提高了研究的可复现性

## 8. 不足与局限

1. 所有实验模型都在1-2B参数范围内，不清楚这些发现在更大规模模型（7B、70B）上是否成立
2. 使用的上下文长度为256个token，短于模型的完整上下文窗口，可能影响特征激活
3. 研究的是一次性剪枝而没有微调，剪枝后重新训练可能会有不同的特征动态
4. SAE特征在不同随机种子之间的绝对匹配率较低（2-4%），虽然模式一致但具体识别的特征可能不同
5. 仅研究了残差流激活，没有探索其他类型的激活（如MLP输出）
6. 因果重要性评估仅基于单特征消融，可能无法捕捉特征间的复杂交互

（完）
