---
title: Who Wrote the Book? Detecting and Attributing LLM Ghostwriters
title_zh: 谁写了这本书？检测和归因LLM代笔作者
authors: "Anudeex Shetty, Qiongkai Xu, Olga Ohrimenko, Jey Han Lau"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28054v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 使用指纹识别进行LLM作者归属
tldr: 本研究引入了GhostWriteBench数据集用于检测和归因LLM生成的长篇文本作者，并提出了一种名为TRACE的新型指纹识别方法。TRACE通过捕捉token级转换模式创建可解释且轻量级的指纹，适用于开源和闭源模型。实验表明，该方法在多个分布外维度上表现优异，具有最先进的性能和鲁棒性。
selection_source: fresh_fetch
motivation: 随着LLM生成文本的普及，需要有效的方法来检测和归因这些文本的作者，特别是在面对不同领域和未见过的模型时。
method: TRACE方法通过捕捉token级转换模式（如词排名）创建可解释且轻量级的指纹，用于LLM作者归属。
result: 在GhostWriteBench数据集上，TRACE实现了最先进的性能，在分布外设置中保持鲁棒性，并且在有限训练数据场景中表现良好。
conclusion: TRACE为LLM作者归属问题提供了一个有效、可解释且实用的解决方案，能够应对多种挑战性场景。
---

## 摘要
在本文中，我们介绍了GhostWriteBench，一个用于LLM作者归属的数据集。它包含由前沿LLM生成的长篇文本（每本书5万+单词），旨在测试跨多个分布外（OOD）维度的泛化能力，包括领域和未见过的LLM作者。我们还提出了TRACE——一种可解释且轻量级的指纹识别方法——它适用于开源和闭源模型。TRACE通过捕获由另一个轻量级语言模型估计的token级转换模式（例如，词序排名）来创建指纹。在GhostWriteBench上的实验表明，TRACE达到了最先进的性能，在OOD设置中保持鲁棒性，并且在有限训练数据场景下表现良好。

## Abstract
In this paper, we introduce GhostWriteBench, a dataset for LLM authorship attribution. It comprises long-form texts (50K+ words per book) generated by frontier LLMs, and is designed to test generalisation across multiple out-of-distribution (OOD) dimensions, including domain and unseen LLM author. We also propose TRACE -- a novel fingerprinting method that is interpretable and lightweight -- that works for both open- and closed-source models. TRACE creates the fingerprint by capturing token-level transition patterns (e.g., word rank) estimated by another lightweight language model. Experiments on GhostWriteBench demonstrate that TRACE achieves state-of-the-art performance, remains robust in OOD settings, and works well in limited training data scenarios.

---

## 论文详细总结（自动生成）

# 论文总结：谁写了这本书？检测和归因LLM代笔作者

## 1. 论文的核心问题与整体含义
- **研究动机**：随着LLM生成文本的普及，特别是长篇文本如书籍的"代笔"现象日益增多，需要有效方法来检测和归因这些文本的作者。
- **背景**：现有数据集大多关注短文本和二元分类（人类vs AI），缺乏针对长篇文本的多类别作者归属方法，且在跨领域和未见过的模型泛化能力上有限。
- **问题重要性**：LLM代笔书籍可能影响出版市场，需要可靠的技术来识别文本来源，确保内容真实性和透明度。

## 2. 论文提出的方法论
- **核心思想**：通过捕捉LLM生成文本中的token级转换模式创建独特"指纹"，实现作者归属。
- **关键技术**：
  - **GhostWriteBench数据集**：包含10种前沿LLM生成的长篇书籍（每本5万+单词），涵盖多种领域。
  - **TRACE方法**：两种变体
    - **基于排名(Rank-based)**：使用轻量级语言模型估计每个token的排名，捕获相邻token排名转换模式
    - **基于熵(Entropy-based)**：估计每个token的熵值，使用核密度估计创建连续密度图
  - **指纹匹配**：使用Jensen-Shannon距离（排名）或法线均值距离（熵）比较测试文本与参考指纹的相似度
- **算法流程**：
  1. 训练阶段：为每个LLM创建参考指纹库
  2. 测试阶段：为测试文本创建指纹，与参考指纹库比较
  3. 归属决策：选择最相似的参考指纹，应用置信度阈值进行最终判断

## 3. 实验设计
- **数据集**：GhostWriteBench，包含10种前沿LLM（如GPT-4.1、GPT-5.1、Claude等）生成的325本书
- **评估场景**：
  - **ID评估**：同领域已知作者的文本
  - **OOD-Domain**：跨领域文本（如从历史传记转向社会科学）
  - **OOD-Author**：未见过的LLM生成的文本
  - **低资源/高资源**：分别使用3-5本和10-35本训练文本
- **Benchmark**：宏F1分数，平衡各类LLM的性能
- **对比方法**：
  - 基于度量的方法：RANK、ENTROPY、GLTR
  - 基于模型的方法：N-GRAM、BERT-AA、DETECTIVE

## 4. 资源与算力
- **生成成本**：GhostWriteBench数据集生成总成本约530美元
- **实验环境**：单个A100 GPU，CUDA 12.4，PyTorch 2.10.0
- **API服务**：使用OpenRouter进行LLM API调用
- **未明确说明**：训练时长未在论文中明确提及

## 5. 实验数量与充分性
- **实验规模**：
  - 主实验：在低资源和高资源设置下评估多种方法
  - 消融研究：评估不同评估语言模型(GPT-2、OLMo、Gemma)的影响
  - 参数研究：测试不同指纹大小和顺序的影响
  - 额外实验：评估区分人类与LLM作者的能力
- **实验充分性**：
  - 使用3个随机分割确保结果鲁棒性
  - 考虑多种OOD维度，全面评估泛化能力
  - 包含定量和定性分析（指纹可视化）
- **公平性**：所有方法使用相同的训练/测试分割和评估标准

## 6. 论文的主要结论与发现
- **TRACE性能**：在多个设置下实现最先进性能，特别是在OOD场景中保持鲁棒性
- **关键发现**：
  - 基于熵的TRACE变体在区分人类与LLM作者时表现完美
  - 基线方法在OOD设置中性能显著下降，而TRACE保持稳定
  - 方法在有限训练数据场景下表现良好，适合实际应用
  - 不同评估语言模型对TRACE性能影响较小，表明方法鲁棒性

## 7. 优点
- **轻量且可解释**：生成的二维指纹易于理解和可视化
- **通用性**：适用于开源和闭源模型，无需白盒访问
- **强泛化能力**：在跨领域和未见过的作者场景下表现优异
- **数据效率**：需要少量训练数据即可有效工作
- **多任务适用**：既能用于多类别作者归属，也能用于二进制人类vs AI检测

## 8. 不足与局限
- **语言限制**：数据集仅限于英语文本，未考虑跨语言场景
- **文本长度限制**：主要针对长文本(>10K词)，对短文本适应性未充分研究
- **作者数量限制**：仅研究单作者设置，未考虑多作者协作情况
- **对抗攻击**：对抗对抗攻击的鲁棒性尚未充分评估
- **评估一致性**：人类评估的标注者一致性较低(Krippendorff's α = 0.17)
- **成本考虑**：高质量数据集生成成本较高(530美元)，可能限制研究可复制性

（完）
