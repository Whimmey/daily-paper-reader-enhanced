---
title: Degradation-Consistent Paired Training for Robust AI-Generated Image Detection
title_zh: 面向鲁棒AI生成图像检测的退化一致配对训练
authors: "Zongyou Yang, Yinghan Hou, Xiaokun Yang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10102v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 退化一致性配对训练
tldr: "AI生成图像检测器在真实世界图像退化条件下性能显著下降。本文提出DCPT训练策略，通过构建清晰-退化图像对并施加特征和预测一致性约束，明确增强模型对退化的鲁棒性。实验表明，DCPT在Synthbuster基准上提高退化条件准确率9.1个百分点，同时仅牺牲0.9%的清晰准确率，且不增加额外参数和推理开销。"
selection_source: fresh_fetch
motivation: 现有AI生成图像检测器将退化鲁棒性视为数据增强的副产品，而非明确训练目标，导致在真实世界退化条件下性能显著下降。
method: DCPT通过构建清晰-退化图像对并施加特征和预测一致性约束，明确增强模型对图像退化的鲁棒性。
result: "在Synthbuster基准测试上，DCPT比基线提高退化条件平均准确率9.1个百分点，JPEG压缩条件下提升最为显著，同时仅牺牲0.9%的清晰准确率。"
conclusion: 对于AI生成图像检测的退化鲁棒性，改进训练目标比增加架构组件更有效，DCPT证明了简单训练策略的优越性。
---

## 摘要
AI生成图像检测器在JPEG压缩、高斯模糊和分辨率下采样等真实世界图像退化条件下性能显著下降。我们观察到，包括B-Free在内的最先进方法将退化鲁棒性视为数据增强的副产品，而非明确的训练目标。在这项工作中，我们提出了退化一致配对训练(DCPT)，一种简单而有效的训练策略，通过配对一致性约束明确强制鲁棒性。对于每个训练图像，我们构建一个清晰视图和一个退化视图，然后施加两个约束：最小化清晰和退化表示之间余弦距离的特征一致性损失，以及基于对称KL对齐跨视图输出分布的预测一致性损失。DCPT不增加任何额外参数和推理开销。在Synthbuster基准测试(9个生成器，8种退化条件)上的实验表明，与没有配对训练的相同基线相比，DCPT将退化条件下的平均准确率提高了9.1个百分点，同时仅牺牲了0.9%的清晰准确率。改进在JPEG压缩下最为显著(+15.7%至+17.9%)。消融实验进一步揭示，添加架构组件会导致在有限训练数据上过拟合，证实了对于退化鲁棒性，训练目标的改进比架构增强更有效。

## Abstract
AI-generated image detectors suffer significant performance degradation under real-world image corruptions such as JPEG compression, Gaussian blur, and resolution downsampling. We observe that state-of-the-art methods, including B-Free, treat degradation robustness as a byproduct of data augmentation rather than an explicit training objective. In this work, we propose Degradation-Consistent Paired Training (DCPT), a simple yet effective training strategy that explicitly enforces robustness through paired consistency constraints. For each training image, we construct a clean view and a degraded view, then impose two constraints: a feature consistency loss that minimizes the cosine distance between clean and degraded representations, and a prediction consistency loss based on symmetric KL divergence that aligns output distributions across views. DCPT adds zero additional parameters and zero inference overhead. Experiments on the Synthbuster benchmark (9 generators, 8 degradation conditions) demonstrate that DCPT improves the degraded-condition average accuracy by 9.1 percentage points compared to an identical baseline without paired training, while sacrificing only 0.9% clean accuracy. The improvement is most pronounced under JPEG compression (+15.7% to +17.9%). Ablation further reveals that adding architectural components leads to overfitting on limited training data, confirming that training objective improvement is more effective than architectural augmentation for degradation robustness.

---

## 论文详细总结（自动生成）

# 论文总结：面向鲁棒AI生成图像检测的退化一致配对训练

## 1. 论文的核心问题与整体含义

**核心问题**：AI生成图像检测器在真实世界图像退化条件（如JPEG压缩、高斯模糊、分辨率下采样等）下性能显著下降，现有方法（如B-Free）将退化鲁棒性视为数据增强的副产品，而非明确的训练目标。

**整体含义**：论文提出了一种简单而有效的训练策略，通过明确增强模型对图像退化的鲁棒性，提高AI生成图像检测器在实际应用中的可靠性和实用性，解决了检测器在真实场景中的性能退化问题。

## 2. 论文提出的方法论

**核心思想**：退化一致配对训练(DCPT)，通过构建清晰-退化图像对并施加一致性约束，明确增强模型对退化的鲁棒性。

**关键技术细节**：
- 为每个训练图像构建清晰视图和退化视图
- 清晰视图：原始图像加上标准增强（随机裁剪到504×504，随机水平翻转）
- 退化视图：以概率pdeg=0.5应用三种退化类型之一（JPEG压缩、高斯模糊、下采样）
- 施加两个约束：
  1. 特征一致性损失：最小化清晰和退化表示之间的余弦距离
  2. 预测一致性损失：使用对称KL散度对齐跨视图输出分布

**公式**：
- 特征一致性损失：Lfeat = 1 - (fc·fd)/(∥fc∥·∥fd∥)
- 预测一致性损失：Lpred = KL(¯pc∥pd) + KL(pd∥¯pc)，其中¯pc = sg[pc]表示停止梯度
- 总损失：L = CE(ˆyc,y) + CE(ˆyd,y) + λf·Lfeat + λp·Lpred，其中λf=0.5，λp=0.1

## 3. 实验设计

**数据集/场景**：
- 训练数据：B-Free训练集（51,517张MS-COCO真实图像与51,518张SD 2.1生成的假图像配对）
- 评估数据：Synthbuster（9,000张图像，来自9个生成器：DALL-E 2/3、Firefly、GLIDE、Midjourney v5、SD 1.3/1.4/2/XL）
- 退化条件：JPEG压缩（QF∈{30,50,70}）、高斯模糊（σ∈{1,2,3}）、双三次下采样（scale∈{0.5,0.25}）

**Benchmark**：Synthbuster基准测试（9个生成器×8种退化条件）

**对比方法**：
- B-Free官方模型（端到端微调主干，86.5M参数，5裁剪推理）
- 基线方法（冻结DINOv2主干+197K分类器头，无配对训练）

## 4. 资源与算力

- 使用单个NVIDIA RTX 4090D（24GB）GPU
- 混合精度训练(AMP)
- 20个训练周期
- 批量大小为64
- 优化器为AdamW，学习率为10^-4，权重衰减为0.01

## 5. 实验数量与充分性

**实验数量**：
- 主要实验：在Synthbuster基准上评估DCPT与基线方法的性能对比
- 消融实验：分析DCPT的两个损失组件（Lfeat和Lpred）的贡献
- 每生成器分析：评估DCPT在9个不同生成器上的表现
- 每退化类型分析：评估DCPT在不同退化条件下的表现

**充分性与公平性**：
- 实验设计较为充分，覆盖了多种生成器和退化类型
- 使用冻结主干确保公平比较，隔离训练目标的贡献
- 消融实验验证了各组件的有效性
- 对比了不同参数设置下的性能

## 6. 论文的主要结论与发现

- DCPT显著提高了AI生成图像检测器在退化条件下的鲁棒性
- 在Synthbuster基准上，DCPT将退化条件下的平均准确率提高了9.1个百分点
- JPEG压缩条件下提升最为显著（+15.7%至+17.9%）
- 仅牺牲0.9%的清晰准确率
- 训练目标的改进比架构增强更有效，添加架构组件会导致在有限训练数据上过拟合

## 7. 优点

**方法亮点**：
- 简单而有效：DCPT仅通过添加一致性约束，不增加任何参数和推理开销
- 双路径一致性：同时施加特征和预测层面的约束，全面增强鲁棒性
- 退化类型不可知：测试时无需知道退化类型
- 与数据增强互补：DCPT不替代数据增强，而是添加正交训练信号

**实验设计亮点**：
- 使用冻结主干确保公平比较
- 全面的评估覆盖多种生成器和退化类型
- 深入的消融实验验证各组件贡献
- 分析不同退化严重程度的影响

## 8. 不足与局限

- 仅评估了有限的退化类型（JPEG、模糊、下采样），可能无法覆盖所有实际场景
- 使用冻结主干，未评估在完全微调模型上的表现
- 训练数据有限（约10万对），可能影响泛化能力
- 未评估在真实世界混合退化条件下的性能
- 未探索DCPT与其他鲁棒性增强方法的结合
- 实验主要在Synthbuster基准上进行，可能存在数据集偏差

（完）
