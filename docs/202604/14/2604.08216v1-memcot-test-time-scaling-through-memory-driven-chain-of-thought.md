---
title: "MemCoT: Test-Time Scaling through Memory-Driven Chain-of-Thought"
title_zh: MemCoT：基于记忆驱动的思维链的测试时扩展
authors: "Haodong Lei, Junming Liu, Yirong Chen, Ding Wang, Hongsong Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08216v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 创新的LLM推理记忆框架
tldr: MemCoT是一种测试时内存扩展框架，解决了大型语言模型在长上下文因果推理中的幻觉和灾难性遗忘问题。通过将推理转变为迭代的状态化信息搜索，并引入多视图长期内存感知模块和任务条件双短期记忆系统，MemCoT实现了更精确的证据定位和上下文重建，显著提升了模型在长上下文任务中的性能。
selection_source: carryover_cache
motivation: 现有内存机制将检索视为静态单步过程，导致语义稀释和上下文碎片化，无法有效处理长上下文推理。
method: MemCoT通过多视图长期内存感知模块和任务条件双短期记忆系统，将长上下文推理转变为迭代的状态化信息搜索过程。
result: 实证评估显示MemCoT实现了最先进性能，多个模型在LoCoMo和LongMemEval-S基准上达到SOTA结果。
conclusion: MemCoT通过重新定义推理过程为内存驱动的思维链，有效解决了长上下文推理中的关键挑战。
---

## 摘要
大型语言模型(LLMs)在处理大量、碎片化的长上下文进行因果推理时，仍然存在严重的幻觉和灾难性遗忘问题。现有的记忆机制通常将检索视为静态的、单步的被动匹配过程，导致严重的语义稀释和上下文碎片化。为了克服这些基本瓶颈，我们提出了MemCoT，一种测试时内存扩展框架，它通过将长上下文推理转变为迭代式、有状态的信息搜索来重新定义推理过程。MemCoT引入了一个多视角长期记忆感知模块，实现了"放大"证据定位和"缩小"上下文扩展，使模型能够首先识别相关证据所在的位置，然后重建推理所需的周围因果结构。此外，MemCoT采用了一个任务条件化的双短期记忆系统，由语义状态记忆和情节轨迹记忆组成。这种短期记忆记录历史搜索决策，并在迭代过程中动态指导查询分解和剪枝。实证评估表明，MemCoT达到了最先进的性能水平。借助MemCoT，多个开源和闭源模型在LoCoMo基准测试和LongMemEval-S基准测试上实现了最先进的性能。

## Abstract
Large Language Models (LLMs) still suffer from severe hallucinations and catastrophic forgetting during causal reasoning over massive, fragmented long contexts. Existing memory mechanisms typically treat retrieval as a static, single-step passive matching process, leading to severe semantic dilution and contextual fragmentation. To overcome these fundamental bottlenecks, we propose MemCoT, a test-time memory scaling framework that redefines the reasoning process by transforming long-context reasoning into an iterative, stateful information search. MemCoT introduces a multi-view long-term memory perception module that enables Zoom-In evidence localization and Zoom-Out contextual expansion, allowing the model to first identify where relevant evidence resides and then reconstruct the surrounding causal structure necessary for reasoning. In addition, MemCoT employs a task-conditioned dual short-term memory system composed of semantic state memory and episodic trajectory memory. This short-term memory records historical search decisions and dynamically guides query decomposition and pruning across iterations. Empirical evaluations demonstrate that MemCoT establishes a state-of-the-art performance. Empowered by MemCoT, several open- and closed-source models achieve SOTA performance on the LoCoMo benchmark and LongMemEval-S benchmark.

---

## 论文详细总结（自动生成）

# MemCoT论文总结

## 1. 论文的核心问题与整体含义

大型语言模型(LLMs)在处理大量、碎片化的长上下文进行因果推理时，仍然存在严重的幻觉和灾难性遗忘问题。现有记忆机制将检索视为静态的、单步的被动匹配过程，导致严重的语义稀释和上下文碎片化。论文的核心创新在于提出MemCoT框架，将长上下文推理转变为迭代式、有状态的信息搜索过程，从根本上重新定义了内存与推理的关系。

## 2. 论文提出的方法论

### 核心思想
MemCoT通过将内存从被动数据仓库转变为内存驱动的推理引擎，实现了推理过程的可追溯性和可纠正性。

### 关键技术细节
- **多视图长期记忆感知模块**：
  - Zoom-In证据定位：精确检索相关实体
  - Zoom-Out上下文扩展：获取周围语义上下文
  - 全景视觉锚定：处理多模态记忆

- **任务条件双短期记忆系统**：
  - 语义状态记忆：存储事实性语义信息
  - 情节轨迹记忆：记录历史搜索决策和失败轨迹

### 算法流程
1. 每次迭代j开始时，通过感知模块ψ_perceive(q_j, M)对长期记忆M进行深度提取
2. 进化模块f_evolve提炼高价值信息，更新动态短期记忆状态m_j
3. 判断过程f_judge评估m_j的充分性：若足够，响应代理生成最终答案；否则，识别"知识盲点"形成下一个子查询q_{j+1}
4. 这一过程将复杂查询分解为原子子问题，通过迭代优化解决

## 3. 实验设计

### 数据集与场景
- **LoCoMo基准**：包含841个单跳、282个多跳、96个开放领域和321个时间推理挑战
- **LongMemEval-S基准**：包含70个会话，涵盖133个单会话用户(SSU)、30个多会话(MS)、30个单会话偏好(SSP)、133个时间推理(TR)、78个知识更新(KU)和56个单会话助手(SSA)实例

### 对比方法
- 基线方法：Full Context、RAG、Mem0、MemoryOS、HippoRAG、A-Mem、CAM、CompassMem等
- 测试模型：GPT-4o-mini、Qwen2.5-14B、Qwen2.5-7B等

## 4. 资源与算力

论文中未明确提及具体的GPU型号、数量或训练时长等算力信息。实验使用了预训练的GPT-4o-mini和Qwen2.5系列模型，但没有详细说明训练或推理所需的计算资源。

## 5. 实验数量与充分性

### 实验数量
- 主要实验：在LoCoMo和LongMemEval-S基准上的性能比较
- 消融研究：测试多视图长期记忆感知模块各组件的贡献
- 超参数分析：研究相邻窗口大小(W)、最大进化迭代次数(J)和细粒度TopK(K)的影响
- 额外实验：计算成本分析、检索模块分析、更多模型实验

### 充分性与公平性
- 实验覆盖了多种模型规模，从小型(Qwen2.5-7B)到大型(GPT-4o-mini)模型
- 对比了多种基线方法，包括传统RAG和最新的记忆增强方法
- 进行了详细的消融研究验证各组件贡献
- 提供了超参数敏感性分析，确保结果稳健性
- 实验设计较为全面和客观，采用标准评估指标(F1分数、LLM-as-Judge分数)

## 6. 论文的主要结论与发现

- MemCoT在LoCoMo基准上实现了SOTA性能，GPT-4o-mini达到58.03的F1分数，Qwen2.5-14B达到57.06
- 在LongMemEval-S基准上，MemCoT达到88.0的LLM-as-a-Judge分数
- 多视图长期记忆感知模块和任务条件双短期记忆系统有效解决了长上下文推理中的关键挑战
- 静态单步检索是导致语义稀释和上下文碎片化的根本原因
- 将推理转变为迭代式、有状态的信息搜索可以显著提升长上下文推理性能

## 7. 优点

### 方法亮点
- 提出了首个自演化架构，将测试时推理与动态内存演化相结合
- 多视图长期记忆感知模块平衡了精确实体搜索与全局上下文感知
- 任务条件双短期记忆系统实现了可追溯的推理，动态分解和剪枝查询
- 框架是即插即用的、无需训练的，专为复杂长上下文内存管理设计

### 实验设计亮点
- 在多种模型规模上验证了方法的泛化能力
- 进行了详细的消融研究验证各组件贡献
- 提供了超参数敏感性分析
- 在两个不同的基准测试上验证了方法的有效性

## 8. 不足与局限

- 实验主要集中在问答任务上，可能无法完全反映方法在其他任务上的表现
- 虽然在多种模型上进行了测试，但主要集中在GPT和Qwen系列模型，可能存在模型偏差
- 没有探讨方法在极长上下文(超过百万token)场景下的表现
- 计算成本分析显示MemCoT比一些基线方法消耗更多token，可能影响实际应用效率
- 没有探讨方法在多模态任务中的全面表现，尽管提到了视觉记忆组件
- 实验主要在英文数据集上进行，未验证在多语言场景下的表现

（完）
