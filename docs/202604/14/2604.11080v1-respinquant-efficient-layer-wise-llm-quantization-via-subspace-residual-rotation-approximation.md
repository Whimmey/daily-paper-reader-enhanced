---
title: "ReSpinQuant: Efficient Layer-Wise LLM Quantization via Subspace Residual Rotation Approximation"
title_zh: "ReSpinQuant: 基于子空间残差旋转近似的高效层级LLM量化"
authors: "Suyoung Kim, Sunghyun Wee, Hyeonjin Kim, Kyomin Hwang, Hyunho Lee, Nojun Kwak"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11080v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 新型LLM量化方法
tldr: ReSpinQuant是一种新型LLM量化框架，解决了层级变换方法的高计算开销问题。通过离线激活旋转融合和高效的残差子空间旋转技术，该方法结合了全局旋转方法的推理效率和层级变换方法的高表达能力，在W4A4和W3A3量化实验中实现了最先进的性能。
selection_source: fresh_fetch
motivation: 现有全局旋转方法表达能力有限，而层级变换方法计算开销大，需要一种兼顾两者优势的量化方法。
method: ReSpinQuant利用离线激活旋转融合和残差子空间旋转匹配基，实现层级适应与推理效率的平衡。
result: 在W4A4和W3A3量化实验中，ReSpinQuant超越了全局旋转方法，并以最小开销匹配了层级变换方法的精度。
conclusion: ReSpinQuant通过创新的子空间残差旋转近似技术，实现了高效层级LLM量化，为模型部署提供了新方案。
---

## 摘要
基于旋转的训练后量化（PTQ）已成为缓解大型语言模型（LLMs）量化中激活异常值的一种有前景的解决方案。全局旋转方法通过将激活旋转融合到注意力（attention）和前馈网络（FFN）块中实现推理效率，但由于它们被限制在所有层中使用单个可学习旋转矩阵，因此表达能力有限。为了解决这个问题，层级变换方法应运而生，通过局部适应实现了更高的准确性。然而，层级方法无法将激活旋转矩阵融合到权重中，需要在线计算并造成显著的开销。在本文中，我们提出了ReSpinQuant，一种量化框架，通过利用离线激活旋转融合和使用高效的残差子空间旋转进行匹配基，解决了这种开销问题。这种设计协调了层级适应的高表达能力，仅带来可忽略的推理开销。W4A4和W3A3量化上的大量实验表明，ReSpinQuant达到了最先进的性能，优于全局旋转方法，并以最小的开销匹配了计算成本高昂的层级方法的准确性。

## Abstract
Rotation-based Post-Training Quantization (PTQ) has emerged as a promising solution for mitigating activation outliers in the quantization of Large Language Models (LLMs). Global rotation methods achieve inference efficiency by fusing activation rotations into attention and FFN blocks, but suffer from limited expressivity as they are constrained to use a single learnable rotation matrix across all layers. To tackle this, layer-wise transformation methods emerged, achieving superior accuracy through localized adaptation. However, layer-wise methods cannot fuse activation rotation matrices into weights, requiring online computations and causing significant overhead. In this paper, we propose ReSpinQuant, a quantization framework that resolves such overhead by leveraging offline activation rotation fusion and matching basis using efficient residual subspace rotation. This design reconciles the high expressivity of layer-wise adaptation with only negligible inference overhead. Extensive experiments on W4A4 and W3A3 quantization demonstrate that ReSpinQuant achieves state-of-the-art performance, outperforming global rotation methods and matching the accuracy of computationally expensive layer-wise methods with minimal overhead.

---

## 论文详细总结（自动生成）

# ReSpinQuant 论文总结

## 1. 论文的核心问题与整体含义
- **核心问题**：大型语言模型(LLMs)在资源受限设备上部署面临内存占用和计算成本大的挑战；训练后量化(PTQ)中激活通道的极端异常值会显著扩大动态范围，导致严重量化误差。
- **现有方法局限**：
  - 全局旋转方法：使用单一旋转矩阵，推理效率高但表达能力有限
  - 层级变换方法：为每层使用独特旋转矩阵，表达能力强但无法将激活旋转矩阵融合到权重中，需要在线计算造成显著开销
- **研究意义**：旨在解决全局旋转方法表达能力有限和层级变换方法计算开销大的矛盾，提出一种兼顾两者优势的量化方法。

## 2. 论文提出的方法论
- **核心思想**：ReSpinQuant框架通过离线激活旋转融合和高效的残差子空间旋转解决层级变换方法的开销问题，结合全局旋转方法的推理效率和层级变换方法的高表达能力。
- **关键技术细节**：
  - **层级旋转分配**：为不同网络阶段分配不同可学习正交矩阵
    - Ri^1：旋转MHSA块输入激活和FFN块输出激活
    - Ri^2：旋转FFN块输入和MHSA块输出
    - Ri^3：注意力机制中的中间旋转
    - R4, R5：通过快速Hadamard变换实现的结构化旋转
  - **离线权重合并**：将旋转矩阵数学吸收到MHSA或FFN中的线性变换
  - **子空间残差旋转近似**：
    - 经验观察：使用Cayley优化器优化的旋转矩阵与初始状态偏差小
    - 主子空间识别：对偏差矩阵进行奇异值分解(SVD)识别主要方向
    - 正交子空间投影：将完整转换矩阵投影到子空间
    - 旋转近似：在识别的子空间内应用变换，同时保持正交性

## 3. 实验设计
- **数据集/场景**：
  - 模型：Llama 2 (7B, 13B), Llama 3 (8B), Llama 3.2 (1B, 3B)
  - 数据集：WikiText-2用于校准和困惑度(PPL)评估
  - 零样本推理任务：9个多样化下游任务(BoolQ, PIQA, SIQA, HellaSwag, WinoGrande, ARC-Easy, ARC-Challenge, OBQA, LAMBADA)
- **Benchmark**：
  - 困惑度(PPL)在WikiText-2上
  - 零样本准确率
- **对比方法**：
  - 基础PTQ：RTN, GPTQ
  - 基于旋转的全局旋转：QuaRot, SpinQuant
  - 层级方法：OSTQuant, FlatQuant

## 4. 资源与算力
- **GPU型号**：NVIDIA H100 (80GB)
- **训练时长**：
  - LLaMA-2 7B: 43分钟
  - LLaMA-2 13B: 61分钟
  - LLaMA-3 8B: 42分钟
- **实验环境**：所有实验在单个NVIDIA H100 GPU上进行

## 5. 实验数量与充分性
- **实验数量**：
  - 在W4A4和W3A3量化设置下进行了大量实验
  - 对比了多种基线方法
  - 进行了消融研究，特别是关于近似秩r的影响
  - 测试了不同模型规模(1B到13B参数)
- **充分性与公平性**：
  - 实验设计较为充分，覆盖不同模型规模和量化设置
  - 使用标准协议进行评估，确保公平比较
  - 所有方法使用相同的校准数据集和评估指标
  - ReSpinQuant与基线方法使用相同的交叉熵损失进行优化

## 6. 论文的主要结论与发现
- **主要结论**：
  - ReSpinQuant在W4A4和W3A3量化设置下实现了最先进的性能
  - 超越了全局旋转方法，并以最小开销匹配了计算成本高昂的层级方法的精度
  - 在LLaMA-3 8B上，W4A4设置下PPL达到7.24，优于QuaRot(7.82)和SpinQuant(7.50)
  - 在W3A3设置下，ReSpinQuant在LLaMA-3.2 1B上PPL降低19.8点(69.70→49.90)
- **关键发现**：
  - 旋转矩阵优化后与初始状态偏差小，残差旋转矩阵呈现强对角占优
  - 基础不匹配主要集中在一个低维子空间中
  - 即使使用最小的子空间秩也能获得显著改进

## 7. 优点
- **方法亮点**：
  - "训练大，推理小"策略：训练时使用大量参数(O(L·D²))，推理时压缩到低秩子空间
  - 完全融合层级旋转到权重矩阵，保持高表达能力
  - 子空间残差旋转近似将计算复杂度从O(D²)降低到O(D)
  - 仅使用标准交叉熵损失进行优化，无需专门的目标函数
- **实验设计亮点**：
  - 在多种模型规模上验证方法有效性
  - 在具有挑战性的W3A3设置下验证方法鲁棒性
  - 详细的效率分析，包括参数效率、计算复杂度和推理延迟
  - 消融研究验证了近似秩r的选择合理性

## 8. 不足与局限
- **实验覆盖局限**：
  - 仅关注标准全局校准目标下的结构创新，未探索专门的层级损失函数或训练方案
  - 计算限制(单GPU)限制了评估范围，仅测试了最多13B参数的模型
  - 未探索更大规模模型(>13B)上的性能
- **应用限制**：
  - 虽然验证了理论效率，但专门的硬件高效内核实现仍需未来工作
  - 可能存在模型特定偏差，主要在Llama系列模型上验证
- **其他局限**：
  - 未考虑量化方法在不同硬件平台上的实际部署效果
  - 未探索与其他量化技术的组合可能性

（完）
