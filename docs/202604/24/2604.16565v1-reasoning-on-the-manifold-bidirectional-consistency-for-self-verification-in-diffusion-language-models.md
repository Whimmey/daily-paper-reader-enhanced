---
title: "Reasoning on the Manifold: Bidirectional Consistency for Self-Verification in Diffusion Language Models"
title_zh: 流形上的推理：扩散语言模型中用于自验证的双向一致性
authors: "Jiaoyang Ruan, Xin Gao, Yinda Chen, Hengyu Zeng, Liang Du, Guanghao Li, Jie Fu, Jian Pu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16565v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 引入双向流形一致性用于自验证
tldr: 针对扩散大语言模型推理验证难的问题，本文提出“流形推理”视角，假设有效轨迹位于高密度流形上。为此引入双向流形一致性（BMC）这一无需训练的指标，通过前向掩码和后向重构量化稳定性。BMC 可用于诊断、推理重采样及对齐奖励，有效提升模型推理能力。
selection_source: fresh_fetch
motivation: 扩散大语言模型虽具备全局规划优势，但如何高效验证其推理轨迹的正确性仍是一大挑战。
method: 提出双向流形一致性（BMC），一种无需训练的无监督指标，通过前向掩码和后向重构循环量化生成序列的稳定性。
result: 实验表明BMC在诊断、推理重采样及对齐中均表现出色，能作为密集几何奖励指导模型自我进化。
conclusion: 研究证实了内在的几何稳定性是扩散大语言模型正确性的一个稳健指标。
---

## 摘要
虽然扩散大语言模型在全局规划方面具有结构优势，但如何高效验证其是否通过有效的推理轨迹得出正确答案，仍是一个关键挑战。在本工作中，我们提出了一种几何视角：流形上的推理。我们假设，有效的生成轨迹作为稳定吸引子存在于学习分布的高密度流形上，而无效路径则表现出流形偏离。为了实现这一目标，我们引入了双向流形一致性（BMC），这是一种无需训练的无监督指标，通过前向掩码和后向重构循环来量化生成序列的稳定性。实验结果表明，BMC在整个推理生命周期中具有广泛的适用性：(1) 在诊断中，它作为解决方案有效性的鲁棒判别器，无需真实答案；(2) 在推理中，它支持拒绝重采样，从而有效地将计算资源集中在复杂的推理任务上；(3) 在对齐中，它充当密集的几何奖励，将稀疏的结果监督转化为细粒度的指导，赋能模型超越标准基线实现自我进化。我们的结果确立了内在几何稳定性是扩散大语言模型正确性的一个鲁棒指标。

## Abstract
While Diffusion Large Language Models (dLLMs) offer structural advantages for global planning, efficiently verifying that they arrive at correct answers via valid reasoning traces remains a critical challenge. In this work, we propose a geometric perspective: Reasoning on the Manifold. We hypothesize that valid generation trajectories reside as stable attractors on the high-density manifold of the learned distribution, whereas invalid paths exhibit off-manifold drift. To operationalize this, we introduce Bidirectional Manifold Consistency (BMC), a training-free, unsupervised metric that quantifies the stability of the generated sequence through a forward-masking and backward-reconstruction cycle. Empirically, we demonstrate BMC's versatility across the full reasoning lifecycle: (1) in Diagnosis, it serves as a robust discriminator of solution validity without ground truth answer; (2) in Inference, it enables rejection resampling to effectively concentrate computational resources on complex reasoning tasks; and (3) in Alignment, it functions as a dense geometric reward that transforms sparse outcome supervision into fine-grained guidance, empowering models to self-evolve beyond standard baselines. Our results establish intrinsic geometric stability as a robust indicator of correctness for dLLMs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心问题**：扩散大语言模型虽然具备全局规划和迭代修正的结构优势，但在复杂推理任务中，如何高效验证其生成的推理轨迹是否正确（即自验证问题）仍是一个关键挑战。现有的验证方法（如过程奖励模型 PRM、自一致性 Self-Consistency）通常依赖昂贵的外部监督信号或计算密集的采样，且在困难样本上容易失效。
*   **研究动机与背景**：随着模型在复杂任务中的部署，幻觉和推理链中的错误累积风险增加。作者提出从“黑盒”外部监督转向“白盒”内部发现，探索 dLLMs 的生成过程本身是否包含与解的有效性相关的内在信号。
*   **整体含义**：论文提出了一种几何视角——“流形上的推理”，假设有效的推理轨迹位于学习分布的高密度流形上，表现为稳定的吸引子；而无效路径则表现出流形偏离。这一视角为无需训练的模型自验证提供了理论基础。

### 2. 论文提出的方法论

*   **核心思想**：引入**双向流形一致性**，这是一种无需训练、无监督的指标。其核心假设是：正确的解在经过前向掩码扰动和后向去噪重构后，能够保持高度一致（即位于流形上的稳定点）；而错误的解则会发生显著的语义漂移。
*   **关键技术细节**：
    *   **前向扰动**：对生成的完整序列 $x_0$ 应用掩码策略（掩码比例 $\gamma=0.9$），得到部分观测状态 $\tilde{x}_t$。
    *   **后向重构**：利用预训练的去噪器 $p_\theta$，从 $\tilde{x}_t$ 出发执行 $K$ 步截断的去噪重构（$K \ll T$，例如 $K=16$），得到重构序列 $\hat{x}_0$。
    *   **一致性度量**：计算原始序列 $x_0$ 与重构序列 $\hat{x}_0$ 之间的相似度。BMC 分数 $S_{BMC}$ 是六个维度的加权组合：
        *   **Token Accuracy**：精确的词元重构率。
        *   **Semantic Similarity**：句子嵌入的余弦相似度。
        *   **Number Retention**：数学推理中关键数字的保留率。
        *   **Final Answer Match**：最终答案是否匹配。
        *   **Character Similarity**：字符级别的编辑距离。
        *   **Intrinsic Confidence**：重构过程中的平均预测概率。
*   **算法与应用流程**：
    *   **诊断**：直接计算 $S_{BMC}$ 作为判别器，分数高则认为解有效。
    *   **推理**：提出**流形引导拒绝采样**。生成样本后计算 BMC，若分数超过阈值 $\tau$ 则直接接受；否则进行重采样，直到找到稳定解或达到预算上限。
    *   **对齐**：在强化学习中，将 BMC 作为密集奖励信号。奖励函数设计为 $r(x_0) = I(y_{pred}=y^*) \cdot [r_{base} + \alpha_t \cdot S_{BMC}(x_0)]$，确保只有在答案正确的前提下，模型才会因为几何稳定性高而获得额外奖励。

### 3. 实验设计

*   **数据集**：使用了四个不同复杂度的推理基准数据集：
    *   **GSM8K**：小学数学应用题。
    *   **MATH**：高中竞赛数学题。
    *   **ARC-Challenge**：知识密集的科学推理。
    *   **GPQA**：专家级科学问题（研究生水平）。
*   **基准模型**：在两个最先进的 dLLMs 上进行实现：
    *   **LLaDA-8B-Instruct**
    *   **Dream-v0-Instruct-7B**
*   **对比方法**：
    *   **诊断阶段**：对比了 Model Confidence（模型置信度）、Self-Consistency（自一致性）、Self-Evaluation（自评估）。
    *   **推理阶段**：对比了 Standard Sampling（标准采样）、Best-of-N (Confidence)（基于置信度的优选）、Self-Consistency。
    *   **对齐阶段**：对比了 SFT（监督微调）、Outcome RL（结果强化学习），以及部分 AR 模型（如 LLaMA3-8B, Qwen2.5-7B）作为参考。

### 4. 资源与算力

*   **算力情况**：论文提供的文本中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练的具体墙钟时间。
*   **计算复杂度分析**：文中重点分析了计算步骤的复杂度。指出 BMC 的验证过程仅需 $K$ 步截断重构（$K \approx 16$），远小于完整生成的步数 $T$（通常为 1024）。因此，BMC 的计算开销 $C_{BMC} \approx T$，远低于需要多次完整采样的 Self-Consistency 方法（$C_{SC} = N_{SC} \times T$）。

### 5. 实验数量与充分性

*   **实验数量**：实验非常充分，涵盖了推理的全生命周期：
    *   **诊断实验**：在 4 个数据集上对比了 2 个模型，使用 AUROC 和 AUPR 指标。
    *   **推理实验**：对比了不同采样策略（N=3, N=10）的准确率和样本效率。
    *   **对齐实验**：在不同生成长度（128, 256, 512 tokens）下评估了模型性能。
    *   **消融实验**：详细分析了超参数（向后步数 $K$、掩码率 $\gamma$、集成大小 $N_{BMC}$）以及 BMC 各个组成指标的影响。
    *   **几何验证**：通过核密度估计（KDE）验证了流形密度与推理质量之间的相关性。
*   **充分性与客观性**：实验设计严谨，涵盖了从简单到复杂的任务，且不仅对比了准确率，还引入了样本效率等指标。消融实验细致，证明了各个组件的必要性。整体实验客观地展示了 BMC 的优势与适用边界。

### 6. 论文的主要结论与发现

*   **几何稳定性即正确性**：实验证实，内在的几何稳定性（通过 BMC 衡量）是 dLLMs 正确性的一个鲁棒指标。正确的解在高密度流形上表现出稳定的吸引子特性。
*   **BMC 的优越性**：在错误诊断任务中，BMC 的 AUROC 显著高于基线方法（如在 GPQA 上比 Self-Consistency 高出约 14%）。
*   **效率提升**：MGRS 策略能够根据任务难度自适应地调整计算资源，在保持或提升准确率的同时，大幅提高了样本效率（例如在 GSM8K 上效率达到 3.98）。
*   **对齐效果**：将 BMC 作为密集奖励信号引入 RL，能够有效缓解奖励黑客问题，使模型在数学推理任务上超越了标准的 Outcome RL 基线。

### 7. 优点

*   **无需训练**：BMC 是一种完全无监督、无需训练的指标，可以直接应用于预训练的 dLLMs，降低了部署成本。
*   **理论扎实**：方法不仅有直观的几何解释，还建立了与 ELBO（证据下界）和收缩映射理论的数学联系。
*   **高效性**：利用截断重构进行验证，计算开销极低，适合实时推理和大规模筛选。
*   **通用性强**：统一框架涵盖了诊断、推理增强和模型对齐三个阶段，展示了广泛的适用性。
*   **细粒度评估**：通过多维度的复合指标，能够有效区分“流畅但错误”的幻觉和“正确但表述不同”的有效解。

### 8. 不足与局限

*   **模型依赖性**：方法的有效性依赖于预训练模型本身的质量，即假设模型的高密度流形与逻辑正确性是对齐的。如果模型本身存在严重的系统性偏差（即高密度区域也是错误的），BMC 可能会失效（尽管论文通过几何验证表明这种情况较少）。
*   **适用范围特定**：目前方法主要针对扩散语言模型（dLLMs），对于主流的自回归（AR）模型不能直接应用，需要适配。
*   **超参数敏感性**：虽然给出了默认值，但 BMC 的性能仍受掩码率 $\gamma$ 和重构步数 $K$ 等超参数的影响，可能需要针对特定任务进行微调。
*   **复合指标权重**：最终的 BMC 分数是多个指标的加权组合，虽然实验表明优化权重效果更好，但确定最优权重可能需要验证集上的调优。

（完）
