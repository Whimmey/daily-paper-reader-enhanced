---
title: Diffusion Autoencoder for Unsupervised Artifact Restoration in Handheld Fundus Images
title_zh: 用于手持式眼底图像无监督伪影修复的扩散自编码器
authors: "Mathumetha Palani, Kavya Puthumana, Ayantika Das, Ganapathy Krishnamurthi"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15723v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 无监督伪影修复
tldr: "针对手持眼底成像设备易受伪影影响的问题，本文提出了一种无监督扩散自编码器。该方法结合上下文编码器与去噪过程，仅利用高质量台式眼底图像进行训练，无需成对数据即可修复手持图像中的非结构化伪影。实验表明，该方法能有效提升图像质量，并在未见数据集上将诊断准确率提升至81.17%。"
selection_source: fresh_fetch
motivation: 针对手持眼底图像中常见的非结构化伪影，现有依赖成对监督或预定义结构的生成模型适应性较差。
method: 提出一种无监督扩散自编码器，通过集成上下文编码器与去噪过程，仅利用高质量台式眼底图像学习语义表示。
result: "在未见过的数据集和多种伪影条件下，该方法将诊断准确率提升至81.17%。"
conclusion: 该方法有效解决了手持眼底图像的伪影问题，显著提升了图像质量及下游诊断的准确性。
---

## 摘要
手持式眼底成像设备的问世使得眼科诊断和疾病筛查更加普及、高效且具有成本效益。然而，这些设备拍摄的图像通常受到闪光反射、曝光变化和运动引起的模糊等伪影的影响，降低了图像质量并阻碍了下游分析。虽然生成模型在图像修复方面效果显著，但大多数模型依赖于成对监督或预定义的伪影结构，使其难以适应手持式眼底图像中常见的非结构化退化。为了解决这一问题，我们提出了一种无监督扩散自编码器，该模型将上下文编码器与去噪过程相结合，以学习用于伪影修复的语义上有意义的表示。该模型仅在高质量台式眼底图像上进行训练，并推断以修复受伪影影响的手持式采集图像。我们通过定量和定性评估验证了修复效果，并表明在未见过的数据集和多种伪影条件下，诊断准确率提高到了 81.17%。

## Abstract
The advent of handheld fundus imaging devices has made ophthalmologic diagnosis and disease screening more accessible, efficient, and cost-effective. However, images captured from these setups often suffer from artifacts such as flash reflections, exposure variations, and motion-induced blur, which degrade image quality and hinder downstream analysis. While generative models have been effective in image restoration, most depend on paired supervision or predefined artifact structures, making them less adaptable to unstructured degradations commonly observed in handheld fundus images. To address this, we propose an unsupervised diffusion autoencoder that integrates a context encoder with the denoising process to learn semantically meaningful representations for artifact restoration. The model is trained only on high-quality table-top fundus images and infers to restore artifact-affected handheld acquisitions. We validate the restorations through quantitative and qualitative evaluations, and have shown that diagnostic accuracy increases to 81.17% on an unseen dataset and multiple artifact conditions

---

## 论文详细总结（自动生成）

### 论文总结：用于手持式眼底图像无监督伪影修复的扩散自编码器

#### 1. 论文的核心问题与整体含义
*   **研究背景**：手持式眼底成像设备虽然提高了眼科诊断的可及性和成本效益，但其采集的图像常受闪光反射、曝光不均和运动模糊等非结构化伪影影响，导致图像质量下降，阻碍了下游的自动分析。
*   **核心问题**：现有的生成模型在图像修复任务中通常依赖于成对的监督数据（即同一场景的清晰图和带伪影图）或预定义的伪影结构（如固定形状的掩码）。这使得它们难以适应手持设备中常见的、非结构化的复杂伪影。此外，在高质量台式眼底图像上训练的AI模型，往往无法直接处理分布差异较大的手持设备图像。
*   **整体含义**：本文旨在解决如何仅利用高质量台式眼底图像，来修复受伪影影响的手持式眼底图像，从而提升图像质量并保证下游诊断任务的准确性，推动便携式眼底筛查设备的临床应用。

#### 2. 论文提出的方法论
*   **核心思想**：提出了一种**无监督扩散自编码器**。该方法结合了上下文编码器与去噪过程，仅在高质量台式眼底图像上进行自编码训练，学习语义上有意义的表示；在推理阶段，利用这些表示对受伪影影响的手持图像进行修复。
*   **关键技术细节**：
    *   **DiffAE 架构**：集成了一个可学习的上下文编码器 $Enc_\phi$ 和一个去噪 UNet $f_\theta$。编码器将清晰图像 $x_0$ 映射为紧凑的潜在向量 $z$，捕获全局视网膜结构作为语义先验。
    *   **训练过程**：在高质量台式图像上进行标准的扩散模型训练。前向过程添加高斯噪声，反向过程学习预测去噪后的图像 $\hat{x}_0$，损失函数为 MSE 损失 $L_{MSE} = \|\hat{x}_0 - x_0\|^2$。
    *   **推理与修复**：利用训练好的模型进行推理时，引入伪影掩码 $M$。通过公式 $x_{t}^{inpaint} = M \odot x_t + (1-M) \odot x_{noisy}^0$ 在每个去噪时间步 $t$ 引导修复过程。反向过程由潜在向量 $z$（来自编码器）和掩码 $M$ 共同条件控制，从而在保留上下文信息的同时填充伪影区域。

#### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：EyePACS 数据集，包含 12,098 张高质量的健康台式眼底图像。
    *   **测试集**：
        *   **真实场景**：mBRSET 数据集，包含 197 张含有过曝、欠曝和闪光伪影的手持眼底图像。
        *   **合成场景**：DRIVE 数据集，合成了 40 对“伪影-清晰”图像对，用于定量评估。
*   **评估指标**：
    *   图像质量：PSNR、SSIM。
    *   结构保持：血管分割的 Dice 分数。
    *   临床质量：三级质量评估（Good, Usable, Reject）。
    *   下游任务：糖尿病视网膜病变（DR）分类准确率。
*   **对比方法**：MISF（CNN）、EdgeConnect（边缘感知GAN）、ShiftNet（移位层GAN）、DeepFillV2（门控卷积GAN）、ClarityNet（扩散模型）。

#### 4. 资源与算力
*   **硬件配置**：使用了一块 **NVIDIA H100 GPU**（80 GB 显存）。
*   **软件框架**：PyTorch。
*   **训练参数**：图像尺寸 512×512，批大小为 4，训练 150 个 Epoch。
*   **说明**：文中未明确提及具体的训练总时长（小时或天数）。

#### 5. 实验数量与充分性
*   **实验数量**：
    *   在合成数据集上进行了 PSNR、SSIM 和血管分割对比。
    *   在真实数据集上进行了质量评估和 DR 分类任务对比。
    *   进行了一项消融实验，验证了上下文编码器中不同潜在向量表示（仅使用伪影图特征 vs 混合清晰图特征）对修复质量的影响。
*   **充分性与客观性**：
    *   实验设计较为全面，涵盖了合成数据（有 Ground Truth）和真实数据（无 Ground Truth）两种场景。
    *   对比基线涵盖了 CNN、GAN 和 Diffusion 等多种主流范式，具有代表性。
    *   不仅评估了图像复原质量，还评估了下游任务（血管分割、疾病分级）的性能，证明了方法的临床相关性。
    *   消融实验有效地支撑了核心组件（上下文编码器）的必要性。

#### 6. 论文的主要结论与发现
*   **定量结果**：在合成数据集上，该方法在 PSNR（38.64 dB）和血管分割 Dice（0.891）上均优于所有基线模型。
*   **定性结果**：在真实手持图像上，该方法生成的“Good”质量图像比例最高（35.8%），且修复后的图像在视觉上更自然，血管结构更连续。
*   **下游任务**：修复后的图像将 DR 分类的准确率提升至 **81.17%**，显著高于未修复时的 77.11% 以及其他基线方法的修复效果。
*   **发现**：引入上下文编码器能够有效捕获语义信息，指导扩散模型在去噪过程中保持视网膜结构的完整性，特别是在处理大面积伪影时优势明显。

#### 7. 优点
*   **无监督学习**：无需成对的“伪影-清晰”训练数据，仅需高质量图像即可训练，降低了数据获取难度。
*   **语义感知**：通过上下文编码器引入全局语义先验，解决了传统扩散模型在无约束修复中可能丢失上下文（如血管结构）的问题。
*   **非结构化适应性**：不依赖预定义的伪影掩码形状，能够处理手持设备中复杂多变的非结构化伪影。
*   **临床价值**：不仅提升了图像指标，还直接验证了提升下游疾病诊断准确率的能力。

#### 8. 不足与局限
*   **掩码生成限制**：目前的伪影掩码生成依赖于基础的处理流程（识别、处理、二值化），文中提到未来需要集成 AI 驱动的伪影定位来进一步提升精度。
*   **训练数据范围**：模型仅在健康眼底图像上训练，虽然测试集包含疾病图像，但训练数据的单一性可能限制了对某些病理特征与伪影复杂交互情况的处理能力。
*   **算力需求**：基于扩散模型的方法通常计算开销较大，虽然使用了 H100 GPU，但在实际临床部署或边缘设备（如手持设备端）上的实时性可能面临挑战。

（完）
