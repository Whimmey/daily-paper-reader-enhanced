---
title: "RawGen: Learning Camera Raw Image Generation"
title_zh: RawGen：学习相机原始图像生成
authors: "Dongyoung Kim, Junyong Lee, Abhijith Punnappurath, Mahmoud Afifi, Sangmin Han, Alex Levinshtein, Michael S. Brown"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00093v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 基于扩散的原始图像生成
tldr: RawGen是首个基于扩散的框架，支持任意目标相机的文本到原始图像生成及sRGB到原始图像转换。它利用大规模sRGB扩散模型的生成先验，通过在潜在和像素空间的专门处理合成物理一致的线性输出。通过构建多对一逆ISP数据集并微调模型，RawGen能够有效处理多样ISP流程，在传统逆ISP方法上表现更优，且其合成数据可增强下游低级视觉任务。
selection_source: fresh_fetch
motivation: 大规模原始图像数据集收集困难且现有数据集受限于特定相机硬件，阻碍了低级视觉任务的发展。
method: RawGen利用大规模sRGB扩散模型的生成先验，通过构建多对一逆ISP数据集并微调条件去噪器和专用解码器，实现文本到原始图像生成和sRGB到原始图像转换。
result: RawGen在处理多样ISP流程方面优于传统逆ISP方法，其生成的合成数据可有效增强下游低级视觉任务性能。
conclusion: RawGen为解决原始图像数据稀缺问题提供了新方法，通过生成物理一致的原始图像促进了低级视觉任务的发展。
---

## 摘要
相机捕获场景参考的线性原始图像，这些图像通过板载图像信号处理器(ISP)处理成显示参考的8位sRGB输出。虽然原始数据对于底层视觉任务更为忠实，但收集大规模原始数据集仍然是一个主要瓶颈，因为现有数据集有限且与特定相机硬件绑定。生成模型为解决这种稀缺性提供了有前景的方法——然而，现有的扩散框架旨在合成成品sRGB图像，而不是物理上一致的线性表示。本文介绍了RawGen，据我们所知，这是第一个基于扩散的框架，能够为任意目标相机实现文本到原始图像的生成，以及sRGB到原始图像的反演。RawGen利用大规模sRGB扩散模型的生成先验，通过在潜空间和像素空间中的专门处理，合成具有物理意义的线性输出，如CIE XYZ或相机特定的原始表示。为了处理扩散模型训练数据中未知且多样的ISP流程和照片处理效果，我们构建了一个多对一的逆ISP数据集，其中使用不同ISP参数生成的同一场景的多个sRGB渲染版本锚定到一个共同的场景参考目标。在该数据集上微调条件去噪器和专用解码器，使RawGen能够获得以相机为中心的线性重建，有效反转渲染流程。我们证明了RawGen相对于假设固定ISP的传统逆ISP方法的优越性能。此外，我们表明，使用RawGen的可扩展、文本驱动的合成数据增强训练流程可以有益于下游底层视觉任务。

## Abstract
Cameras capture scene-referred linear raw images, which are processed by onboard image signal processors (ISPs) into display-referred 8-bit sRGB outputs. Although raw data is more faithful for low-level vision tasks, collecting large-scale raw datasets remains a major bottleneck, as existing datasets are limited and tied to specific camera hardware. Generative models offer a promising way to address this scarcity -- however, existing diffusion frameworks are designed to synthesize photo-finished sRGB images rather than physically consistent linear representations. This paper presents RawGen, to our knowledge the first diffusion-based framework enabling text-to-raw generation for arbitrary target cameras, alongside sRGB-to-raw inversion. RawGen leverages the generative priors of large-scale sRGB diffusion models to synthesize physically meaningful linear outputs, such as CIE XYZ or camera-specific raw representations, via specialized processing in latent and pixel spaces. To handle unknown and diverse ISP pipelines and photo-finishing effects in diffusion-model training data, we build a many-to-one inverse-ISP dataset where multiple sRGB renditions of the same scene generated using diverse ISP parameters are anchored to a common scene-referred target. Fine-tuning a conditional denoiser and specialized decoder on this dataset allows RawGen to obtain camera-centric linear reconstructions that effectively invert the rendering pipeline. We demonstrate RawGen's superior performance over traditional inverse-ISP methods that assume a fixed ISP. Furthermore, we show that augmenting training pipelines with RawGen's scalable, text-driven synthetic data can benefit downstream low-level vision tasks.

---

## 论文详细总结（自动生成）

# RawGen：学习相机原始图像生成 - 论文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：大规模原始图像数据集收集困难，现有数据集有限且与特定相机硬件绑定，阻碍了低级视觉任务的发展。
- **背景**：相机捕获场景参考的线性原始图像，通过板载图像信号处理器(ISP)处理成显示参考的8位sRGB输出。虽然原始数据对底层视觉任务更忠实，但收集大规模原始数据集是主要瓶颈。
- **现有方法局限**：现有扩散框架旨在合成成品sRGB图像，而非物理上一致的线性表示；传统逆ISP方法假设固定ISP，无法处理扩散模型训练数据中未知且多样的ISP流程和照片处理效果。

## 2. 论文提出的方法论

- **核心思想**：RawGen是首个基于扩散的框架，支持任意目标相机的文本到原始图像生成及sRGB到原始图像转换，利用大规模sRGB扩散模型的生成先验，通过在潜空间和像素空间的专门处理合成物理一致的线性输出。
  
- **关键技术细节**：
  - **多对一逆ISP数据集构建**：将同一场景的多个sRGB渲染版本锚定到共同的场景参考目标(CIE XYZ)
  - **两个微调阶段**：
    1. **去噪器微调**：微调预训练的DiT模型，将sRGB条件输入映射到线性XYZ锚定潜在表示
    2. **解码器微调**：微调VAE解码器，从XYZ域潜在重建线性XYZ图像
  - **推理阶段**：统一推理管道，先合成场景参考的CIE XYZ图像，然后渲染到目标相机的线性原始空间

- **算法流程**：
  1. 数据构建：从原始DNG图像生成CIE XYZ锚定和多个sRGB变体
  2. 去噪器微调：使用多对一目标训练条件去噪器
  3. 解码器微调：训练VAE解码器重建XYZ图像
  4. 推理：通过sRGB图像或文本提示生成XYZ，然后映射到目标相机原始空间

## 3. 实验设计

- **数据集**：
  - MIT-Adobe FiveK：提供专家编辑的sRGB图像
  - RAISE：提供大规模原始DNG图像
  - S24 dataset：用于评估神经ISP模型
  - NUS-8：用于光照估计评估

- **对比方法**：
  - CIE XYZ Net [3]
  - InvISP [70]
  - Raw-Diffusion [55]
  - Graphics2RAW [62]
  - EnlightenGAN [35]
  - UPI [11]

- **评估场景**：
  1. 多对一sRGB到XYZ可逆性评估
  2. 设备特定原始合成评估
  3. 下游任务应用评估（光照估计、神经ISP学习、原始域去噪）

## 4. 资源与算力

- 论文中未明确提到使用的GPU型号、数量或训练时长等算力信息
- 仅提到使用FLUX.1-Kontext作为DiT骨干，通过LoRA进行微调，但未提供具体硬件资源信息

## 5. 实验数量与充分性

- **实验数量**：
  - 多对一重建实验：使用MIT-Adobe FiveK数据集，评估5种专家编辑风格
  - 文本增强合成变体实验：使用100种颜色分级提示
  - 设备特定原始合成实验：评估与真实原始数据的分布对齐
  - 下游任务实验：光照估计、神经ISP学习、原始域去噪

- **充分性评估**：
  - 实验覆盖了多个数据集和任务
  - 使用了定量(PSNR, SSIM)和定性评估
  - 与多种基线方法进行了比较
  - 进行了消融实验(如RawGen one-to-one对比)
  - 实验设计较为客观，使用了标准评估协议

## 6. 论文的主要结论与发现

- **主要结论**：
  - RawGen能够有效处理多样ISP流程，在传统逆ISP方法上表现更优
  - 生成的合成数据可有效增强下游低级视觉任务性能
  - RawGen支持文本驱动的原始图像生成，为数据稀缺问题提供解决方案

- **发现**：
  - 多对一训练策略能有效抑制ISP引起的变异性
  - RawGen生成的原始图像与真实原始数据分布对齐良好
  - 合成数据在光照估计、神经ISP学习和去噪任务上表现优于现有方法

## 7. 优点

- **方法亮点**：
  - 首个支持任意目标相机文本到原始图像生成的扩散框架
  - 多对一训练策略处理未知ISP流程
  - 解耦设计：相机无关的线性生成和设备特定的原始合成
  - 支持多种输入条件：sRGB图像或文本提示

- **实验设计亮点**：
  - 使用真实专家编辑数据集评估泛化能力
  - 多种下游任务验证实用性
  - 与多种基线方法进行全面比较
  - 定量和定性评估相结合

## 8. 不足与局限

- **方法局限**：
  - 准确的设备特定原始合成依赖于颜色映射以外的因素，如噪声特性和PSFs
  - 没有探索空间变化噪声、镜头渐变和光学模糊等更物理的传感器建模

- **实验覆盖不足**：
  - 实验主要集中在特定任务和数据集，可能存在覆盖不足
  - 可能存在合成数据与真实数据分布偏差的风险

- **应用限制**：
  - 应用上可能受限于预训练扩散模型的语义理解能力
  - 需要目标相机的DNG元数据才能进行特定相机渲染

（完）
