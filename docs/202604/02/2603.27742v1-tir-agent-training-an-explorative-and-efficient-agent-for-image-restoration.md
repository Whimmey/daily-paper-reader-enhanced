---
title: "TIR-Agent: Training an Explorative and Efficient Agent for Image Restoration"
title_zh: TIR-Agent：训练用于图像恢复的探索性和高效智能体
authors: "Yisheng Zhang, Guoli Jia, Haote Hu, Shanxu Zhao, Kaikai Zhao, Long Sun, Xinwei Long, Kai Tian, Che Jiang, Zhaoxiang Liu, Kai Wang, Shiguo Lian, Kaiyan Zhang, Bowen Zhou"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27742v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 可训练的图像修复智能体
tldr: TIR-Agent是一种可训练的图像恢复代理，解决了现有框架依赖启发式任务调度和全面工具遍历导致的次优恢复路径和高计算成本问题。通过监督微调和强化学习的两阶段训练，结合随机扰动策略和多维自适应奖励机制，TIR-Agent在多个基准测试中超越12个基线模型，实现2.5倍以上的推理加速。
selection_source: fresh_fetch
motivation: 现有图像恢复框架缺乏学习策略来决策，导致恢复路径次优且计算成本过高。
method: TIR-Agent采用监督微调和强化学习的两阶段训练流程，结合随机扰动策略和多维自适应奖励机制来优化工具调用决策。
result: TIR-Agent在多个基准测试中超越12个基线模型，包括全模型、无训练代理和专有模型，实现2.5倍以上的推理加速。
conclusion: TIR-Agent通过可训练的决策策略显著提升了图像恢复的效率和效果，为视觉语言代理在图像恢复领域的应用提供了新方向。
---

## 摘要
协调专门工具进行图像恢复(IR)的视觉语言智能体已成为一种有前景的方法，但大多数现有框架以无需训练的方式运行。它们依赖启发式任务调度和穷尽式工具遍历，导致次优的恢复路径和极高的计算成本。我们认为核心瓶颈在于缺乏经过学习的决策策略，因为视觉语言模型无法有效处理感知退化的任务排序和工具组合。为此，我们提出了TIR-Agent，一种可训练的图像恢复智能体，通过监督微调(SFT)和强化学习(RL)的两阶段训练流程执行直接工具调用策略。两个关键设计支撑了有效的RL训练：(i)应用于SFT数据的随机扰动策略，扩展了策略在任务调度和工具组合方面的探索范围；(ii)多维度自适应奖励机制，动态重新加权异构图像质量指标以减轻奖励黑客攻击。为支持训练期间高吞吐量的异步GPU工具调用，我们进一步开发了全局共享的模型调用池。在领域内和领域外退化实验中，TIR-Agent优于12个基线模型，包括6个一体化模型、3个无需训练的智能体和3个专有模型，并通过消除冗余工具执行实现了超过2.5倍的推理加速。

## Abstract
Vision-language agents that orchestrate specialized tools for image restoration (IR) have emerged as a promising method, yet most existing frameworks operate in a training-free manner. They rely on heuristic task scheduling and exhaustive tool traversal, resulting in sub-optimal restoration paths and prohibitive computational cost. We argue that the core bottleneck lies in the absence of a learned policy to make decision, as a vision-language model cannot efficiently handle degradation-aware task ordering and tool composition. To this end, we propose TIR-Agent, a trainable image restoration agent that performs a direct tool-calling policy through a two-stage training pipeline of supervised fine-tuning (SFT) followed by reinforcement learning (RL). Two key designs underpin effective RL training: (i) a random perturbation strategy applied to the SFT data, which broadens the policy's exploration over task schedules and tool compositions, and (ii) a multi-dimensional adaptive reward mechanism that dynamically re-weights heterogeneous image quality metrics to mitigate reward hacking. To support high-throughput, asynchronous GPU-based tool invocation during training, we further develop a globally shared model-call pool. Experiments on both in-domain and out-of-domain degradations show that TIR-Agent outperforms 12 baselines, including 6 all-in-one models, 3 training-free agents, and 3 proprietary models, and achieves over 2.5$\times$ inference speedup by eliminating redundant tool executions.

---

## 论文详细总结（自动生成）

# TIR-Agent 论文总结

## 1. 论文的核心问题与整体含义

论文针对图像恢复(IR)领域现有框架的局限性展开研究。当前大多数视觉语言智能体框架以无需训练的方式运行，依赖启发式任务调度和穷尽式工具遍历，导致次优的恢复路径和极高的计算成本。核心瓶颈在于缺乏经过学习的决策策略，因为视觉语言模型无法有效处理感知退化的任务排序和工具组合。研究背景是现实世界图像往往遭受复合退化（噪声、低光、压缩伪影等），而现有解决方案要么是全模型(all-in-one models)在处理多样化退化时性能受限，要么是基于智能体的框架在无需训练设置中效率低下。

## 2. 论文提出的方法论

### 核心思想
提出TIR-Agent，一种可训练的图像恢复智能体，通过直接工具调用策略优化恢复过程，采用监督微调(SFT)和强化学习(RL)的两阶段训练流程。

### 关键技术细节
- **探索驱动扰动(EDP)**：
  - 任务级扰动：随机排列任务顺序，增加任务调度多样性
  - 工具级扰动：混合原始工具选择分布和均匀分布，公式为 Pm(m|t) = (1−αm)P(m|t) + αmU(m|t)
  - 整体扰动：Snew = Fm(Ft(S))

- **多维自适应奖励(MAR)**：
  - 计算每个指标的相对性能变化并动态调整权重
  - 使用指数移动平均(EMA)跟踪指标性能
  - 解耦优势计算并加权组合，实现多指标平衡优化

- **全局共享模型调用池(MC-Pool)**：
  - 管理GPU资源集，支持高并发工具调用
  - 实现基于锁的分配和重试机制，提高训练稳定性

## 3. 实验设计

### 数据集/场景
- SFT阶段：39,827个训练样本，包含4KAgent生成的轨迹
- RL阶段：4,000个图像对，从ImageNet采样
- 评估数据集：MiO100数据集，包含三种分组(Group A, B, C)

### Benchmark
- 全参考(FR)指标：PSNR, SSIM, LPIPS
- 无参考(NR)指标：MANIQA, CLIPIQA, MUSIQ

### 对比方法
- 全模型方法：AirNet, PromptIR, MiOIR, DA-CLIP, InstructIR, AutoDIR
- 无需训练的智能体：AgenticIR, MAIR, 4KAgent
- 可训练的智能体：SimpleCall
- 专有模型：GPT-5.2, Claude Opus 4.5, Gemini 3.0 Pro

## 4. 资源与算力

- SFT阶段：8个NVIDIA A100 GPU，学习率1e-5，训练3个epoch
- RL阶段：8个NVIDIA A100 GPU，学习率5e-6，批量大小64
- MC-Pool：32个NVIDIA RTX 3090 GPU用于恢复模型推理
- 最大并行rollout数限制为128以控制内存开销

## 5. 实验数量与充分性

### 实验数量
- 主要比较实验：在MiO100数据集的三个分组上与12个基线模型比较
- 领域外退化组合验证：在未见过的退化组合上测试
- 消融实验：对训练算法和优化策略进行消融研究
- 超参数分析：分析EDP和MAR中超参数的影响

### 充分性与公平性
- 实验覆盖了领域内和领域外的退化组合
- 使用了多种评估指标(FR和NR)
- 与多种类型的基线模型进行了比较
- 消融实验验证了各个组件的有效性
- 超参数分析提供了对模型选择的见解

## 6. 论文的主要结论与发现

- TIR-Agent在多个基准测试中超越12个基线模型
- 实现了超过2.5倍的推理加速，通过消除冗余工具执行
- 在领域外退化组合上表现出强大的泛化能力
- 随着退化数量增加，TIR-Agent的优势更加明显，特别是在NR指标上
- 专有模型在图像恢复任务上存在任务偏差，倾向于选择某些特定任务

## 7. 优点

### 方法亮点
- 两阶段训练流程(SFT+RL)有效学习工具调用策略
- EDP策略显著增加了任务调度和工具选择的多样性
- MAR机制动态平衡异构图像质量指标，减轻奖励黑客问题
- 全局共享MC-Pool支持高并发、高频率的GPU工具调用

### 实验设计亮点
- 全面比较了多种类型的基线模型
- 使用了FR和NR多种评估指标
- 在领域内和领域外退化组合上进行了验证
- 详细的消融实验和超参数分析

## 8. 不足与局限

- 实验主要在MiO100数据集上进行，可能存在数据集偏差
- 未讨论模型在不同硬件平台上的部署效率和资源消耗
- 未探讨模型在极端退化情况下的鲁棒性
- 未分析模型在不同图像分辨率上的性能表现
- 未讨论模型的计算复杂度和内存需求对实际应用的影响
- 未探索模型在实时图像恢复场景中的适用性

（完）
