---
title: Latent Bias Alignment for High-Fidelity Diffusion Inversion in Real-World Image Reconstruction and Manipulation
title_zh: 用于真实世界图像重建与操作的高保真扩散逆变换的潜在偏差对齐
authors: "Weiming Chen, Qifan Liu, Siyi Liu, Yushun Tang, Yijia Wang, Zhihan Zhu, Zhihai He"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23903v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 高保真扩散逆变换用于图像重建
tldr: 针对扩散模型在真实世界图像重建中的低质量和鲁棒性问题，本文提出潜在偏差优化(LBO)和图像潜在提升(ILB)方法，通过引入潜在偏差向量和调整图像潜在表示，有效解决了逆变换与生成轨迹不匹配以及与VQAE重建不匹配的挑战，显著提升了图像重建质量和下游任务性能。
selection_source: fresh_fetch
motivation: 现有扩散逆变换方法存在重建质量低和鲁棒性弱的问题，主要源于逆变换与生成轨迹不匹配以及与VQAE重建不匹配。
method: 本文提出潜在偏差优化(LBO)和图像潜在提升(ILB)方法，通过引入潜在偏差向量和调整图像潜在表示来解决上述不匹配问题。
result: 实验结果表明，该方法显著提高了扩散模型的图像重建质量以及图像编辑和稀有概念生成等下游任务的性能。
conclusion: 通过潜在偏差对齐策略，本文有效解决了扩散逆变换中的关键挑战，为扩散模型在真实世界图像重建和操纵中的应用提供了新思路。
---

## 摘要
最近研究表明，文本到图像扩散模型能够根据文本提示生成高质量图像。但它们能否用于从种子噪声生成或近似真实世界图像？这被称为扩散逆变换问题，它是连接扩散模型和真实世界场景的基础构建块。然而，现有的扩散逆变换方法通常存在重建质量低或鲁棒性弱的问题。需要仔细解决两个主要挑战：(1) 扩散过程中逆变换和生成轨迹之间的不对齐，以及 (2) 扩散逆变换过程与VQ自编码器(VQAE)重建之间的不匹配。为解决这些挑战，我们在每个逆变换步骤引入一个潜在偏差向量，通过学习来减少逆变换和生成轨迹之间的不对齐。我们将此策略称为潜在偏差优化(LBO)。此外，我们通过学习调整图像潜在表示（作为两者之间的连接接口）来近似联合优化扩散逆变换和VQAE重建过程。我们将此技术称为图像潜在提升(ILB)。大量实验结果表明，所提出的方法显著提高了扩散模型的图像重建质量，以及下游任务（包括图像编辑和罕见概念生成）的性能。

## Abstract
Recent research has shown that text-to-image diffusion models are capable of generating high-quality images guided by text prompts. But can they be used to generate or approximate real-world images from the seed noise? This is known as the diffusion inversion problem, which serves as a fundamental building block for bridging diffusion models and real-world scenarios. However, existing diffusion inversion methods often suffer from low reconstruction quality or weak robustness. Two major challenges need to be carefully addressed: (1) the misalignment between the inversion and generation trajectories during the diffusion process, and (2) the mismatch between the diffusion inversion process and the VQ autoencoder (VQAE) reconstruction. To address these challenges, we introduce a latent bias vector at each inversion step, which is learned to reduce the misalignment between inversion and generation trajectories. We refer to this strategy as Latent Bias Optimization (LBO). Furthermore, we perform an approximate joint optimization of the diffusion inversion and VQAE reconstruction processes by learning to adjust the image latent representation, which serves as the connecting interface between them. We refer to this technique as Image Latent Boosting (ILB). Extensive experimental results demonstrate that the proposed method significantly improves the image reconstruction quality of the diffusion model, as well as the performance of downstream tasks, including image editing and rare concept generation.

---

## 论文详细总结（自动生成）

# 论文总结：用于真实世界图像重建与操作的高保真扩散逆变换的潜在偏差对齐

## 1. 论文的核心问题与整体含义

- **研究背景**：文本到图像扩散模型能够根据文本提示生成高质量图像，但如何从种子噪声生成或近似真实世界图像（即扩散逆变换问题）是连接扩散模型与真实世界场景的关键挑战。
- **核心问题**：现有扩散逆变换方法存在两个主要挑战：
  - 扩散过程中逆变换和生成轨迹之间的不对齐
  - 扩散逆变换过程与VQ自编码器(VQAE)重建之间的不匹配
- **研究动机**：解决这两个挑战可以提高扩散模型的图像重建质量和鲁棒性，从而更好地应用于真实世界图像重建和操作任务。

## 2. 论文提出的方法论

- **核心思想**：提出"潜在偏差逆变换"(Latent Bias Inversion, LBI)方法，包含两个核心技术：潜在偏差优化(LBO)和图像潜在提升(ILB)。
- **潜在偏差优化(LBO)**：
  - 在每个逆变换步骤引入潜在偏差向量，学习减少逆变换和生成轨迹之间的不对齐
  - 定义逆变换过程：zt = zt-1 + bt
  - 定义生成过程：zt-1 = zt - ˜bt
  - 提供三种优化方法：
    - 梯度优化：通过梯度下降最小化|bt - ˜bt|
    - 数值优化：使用数值方法更新bt，提高效率
    - 混合优化：结合梯度优化和数值优化的优势
- **图像潜在提升(ILB)**：
  - 通过调整图像潜在表示z0（作为VQAE和扩散UNet模型的连接接口）实现近似联合优化
  - 设计一致性损失函数Lcon，包括L1距离、SSIM和LPIPS感知相似性
  - 引入正则化项Lreg，确保优化后的z0与扩散潜在空间保持一致

## 3. 实验设计

- **数据集/场景**：
  - 图像重建任务：COCO 2017验证集（5,000个图像-文本对）
  - 图像编辑任务：PIE-Bench数据集（700张图像，10个编辑类别）
  - 稀有概念生成任务：ImageNet-1K数据集（20个稀有概念图像作为参考）
- **评估指标**：
  - 图像重建：PSNR、SSIM和LPIPS
  - 图像编辑：结构距离、PSNR、SSIM和CLIP相似性
  - 稀有概念生成：CLIP图像编码器相似性
- **对比方法**：
  - 图像重建：与DDIM Inversion、NPI、NTI、EDICT、AIDI、PTI、FPI、RIVAL、TIC、ReNoise、BDIA、BELM、GNRI和ExactDPM等方法比较
  - 图像编辑：与DDIM Inversion、FPI、AIDI、NTI和ExactDPM等方法比较
  - 稀有概念生成：与SD1和NAO-SeedSelect比较

## 4. 资源与算力

- **硬件配置**：单个NVIDIA RTX A6000 GPU
- **软件框架**：PyTorch
- **优化器设置**：
  - LBO学习率η：0.001
  - ILB学习率γ：0.1（SD1和SD2）和0.01（SDXL）
- **推理设置**：所有方法使用50步推理，图像分辨率根据模型设置（SD1: 512×512，SD2: 768×768，SDXL: 1024×1024）
- **训练时长**：论文中未明确提及训练时长

## 5. 实验数量与充分性

- **实验数量**：
  - 在COCO 2017验证集上全面评估图像重建性能
  - 对比15种不同的扩散逆变换方法
  - 进行消融研究评估ILB中不同损失组件的贡献
  - 在PIE-Bench和ImageNet-1K上评估下游任务性能
- **充分性**：
  - 覆盖三种不同的扩散模型变体（SD1、SD2和SDXL）
  - 评估指标全面，包括像素级和感知指标
  - 消融实验验证了各个组件的有效性
  - 在多个下游任务上验证了方法的泛化能力
- **公平性**：
  - 所有方法按照原始文献重新实现
  - 使用相同的推理步骤数和图像分辨率设置
  - 保持一致的评估标准

## 6. 论文的主要结论与发现

- **主要结论**：
  - 提出的LBI方法在三种扩散模型变体上都显著优于现有方法
  - LBI的三个变体（梯度、数值和混合优化）性能相当，但数值方法效率最高
  - ILB模块对重建质量有显著提升
  - 该方法在图像编辑和稀有概念生成等下游任务上也表现出色
- **关键发现**：
  - 逆变换和生成轨迹的不对齐是影响重建质量的关键因素
  - VQAE和扩散UNet模型之间的不匹配可以通过优化图像潜在表示z0来缓解
  - 数值优化方法在保持性能的同时显著提高了计算效率

## 7. 优点

- **方法亮点**：
  - 直接建模相邻步骤之间的潜在偏差表示，寻求最优逆变换轨迹
  - LBO支持多种优化策略，在准确性和计算效率之间取得平衡
  - ILB通过调整图像潜在表示实现扩散逆变换和VQAE重建的近似联合优化
  - 方法不需要引入大量额外参数，计算开销小
- **实验设计亮点**：
  - 在整个COCO验证集上进行评估，而非仅使用少量样本
  - 对比了多种类型的现有方法，覆盖全面
  - 进行了全面的消融研究，验证了各个组件的贡献
  - 在多个下游任务上验证了方法的泛化能力

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅在标准数据集上进行了评估，未在更多样化的真实世界图像上测试鲁棒性
  - 未探索不同文本提示对逆变换质量的影响
- **偏差风险**：
  - 实验主要基于预训练的扩散模型，可能存在训练数据偏差
  - 评估指标可能存在主观性，特别是感知指标
- **应用限制**：
  - 方法需要额外的优化步骤，增加了计算复杂度
  - 对于某些特殊类型的图像（如高分辨率图像或特定风格图像），性能可能受限
  - 未探索方法在实时应用中的可行性

（完）
