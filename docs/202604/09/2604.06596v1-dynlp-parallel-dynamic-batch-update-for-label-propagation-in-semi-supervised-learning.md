---
title: "DynLP: Parallel Dynamic Batch Update for Label Propagation in Semi-Supervised Learning"
title_zh: 动态LP：半监督学习中标签传播的并行动态批量更新
authors: "S M Shovan, Arindam Khanda, S M Ferdous, Sajal K. Das, Mahantesh Halappanavar"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06596v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 半监督学习的高效标签传播
tldr: DynLP是一种针对半监督学习中标签传播问题的高效算法，解决了传统方法在处理增量数据批次时需要完全重新计算的低效问题。该算法基于GPU架构优化，通过只更新相关子图而非全图重算，实现了显著的性能提升，在大规模数据集上平均加速13倍，最高可达102倍。
selection_source: fresh_fetch
motivation: 传统标签传播算法在处理增量数据批次时需要完全重新计算，导致计算资源浪费和效率低下。
method: DynLP是一种基于GPU的动态批量并行标签传播算法，只执行必要的更新并将变化传播到相关子图。
result: DynLP在大规模数据集上实现了平均13倍，最高102倍于现有方法的加速比。
conclusion: DynLP通过GPU架构优化和选择性更新策略，显著提高了半监督学习中标签传播的效率。
---

## 摘要
半监督学习旨在仅使用少量标记数据推断类别标签。在基于图的半监督学习中，这通常通过标签传播来实现，以预测未标记节点的标签。然而，在实际应用中，数据通常以批量方式增量到达。每次出现新批次时，重新应用传统标签传播算法重新计算所有标签是冗余的、计算密集且低效的。为解决高效标签传播更新方法的缺失问题，我们提出了DynLP，这是一种新颖的以GPU为中心的动态批量并行标签传播算法，它仅执行必要的更新，将变化传播到相关子图，而无需完全重新计算。通过利用GPU架构优化，与最先进的方法相比，我们的算法在大规模数据集上平均实现了13倍，最高达102倍的加速。

## Abstract
Semi-supervised learning aims to infer class labels using only a small fraction of labeled data. In graph-based semi-supervised learning, this is typically achieved through label propagation to predict labels of unlabeled nodes. However, in real-world applications, data often arrive incrementally in batches. Each time a new batch appears, reapplying the traditional label propagation algorithm to recompute all labels is redundant, computationally intensive, and inefficient. To address the absence of an efficient label propagation update method, we propose DynLP, a novel GPU-centric Dynamic Batched Parallel Label Propagation algorithm that performs only the necessary updates, propagating changes to the relevant subgraph without requiring full recalculation. By exploiting GPU architectural optimizations, our algorithm achieves on average 13x and upto 102x speedup on large-scale datasets compared to state-of-the-art approaches.

---

## 论文详细总结（自动生成）

# DynLP论文总结

## 1. 论文的核心问题与整体含义
论文针对半监督学习中标签传播算法在处理增量数据批次时的效率问题。在基于图的半监督学习中，标签传播是预测未标记节点标签的常用方法，但当数据以批量方式增量到达时，传统方法需要完全重新计算所有标签，导致计算资源浪费和效率低下。这种低效性限制了半监督学习在动态数据环境中的应用，特别是在大规模数据集上。

## 2. 论文提出的方法论
### 核心思想
DynLP（Dynamic Batched Parallel Label Propagation）是一种基于GPU的动态批量并行标签传播算法，核心思想是只执行必要的更新，将变化传播到相关子图，而无需完全重新计算整个图。

### 关键技术细节
- **变化调整和稀疏化**：识别受删除和插入操作影响的顶点，构建仅包含新到达顶点的紧凑图表示，通过阈值过滤相似度低的边
- **标签初始化**：利用连通分量和已标记的超级节点（L0和L1）为新顶点提供更好的初始标签，减少迭代次数
- **迭代传播**：更新受影响顶点的标签，如果标签变化超过阈值，则将其邻居标记为下一轮需要更新的顶点

### 算法流程
算法包含三个主要步骤：变化调整和稀疏化、标签初始化、迭代传播。通过并行处理和GPU优化，显著提高了效率，避免了传统方法中的冗余计算。

## 3. 实验设计
### 数据集/场景
- 合成数据集：使用Erdős–Rényi模型生成的随机图，平均度为3、5和7
- 真实数据集：IMDB评论、ImageNet图像、Yelp评论、Amazon家庭评论、Amazon图书评论

### 基准方法
- ItLP（迭代标签传播）
- StLP（流式标签传播）
- 近似StLP（使用近似逆矩阵）
- 机器学习方法：A2LP和CAGNN（图神经网络）

## 4. 资源与算力
实验在NVIDIA H100 GPU（80GB VRAM）上进行，主机使用AMD EPYC 7502 32核CPU和32GB RAM。论文未明确说明训练时长。

## 5. 实验数量与充分性
论文进行了多组实验：
- 不同数据集上的性能比较
- 不同平均度（3、5、7）的影响
- 不同阈值（δ）对精度和迭代次数的影响
- 不同批次大小的影响
- 与多种基准方法的比较

实验较为充分，涵盖了不同规模的数据集和参数设置，比较了多种方法，包括传统优化方法和基于学习的方法。实验设计客观，使用了相同的评估指标（准确率和执行时间）。

## 6. 论文的主要结论与发现
- DynLP在大规模数据集上平均实现了13倍，最高达102倍的加速比
- DynLP保持了接近最优的准确率（平均约99%）
- 连通分量辅助的标签初始化显著减少了所需的迭代次数
- GPU架构优化和选择性更新策略显著提高了效率

## 7. 优点
- 提出了创新的动态批量并行标签传播算法
- 利用GPU架构优化实现了高效计算
- 通过连通分量和超级节点技术减少了迭代次数
- 实验设计全面，涵盖了多种数据集和基准方法
- 在大规模数据集上展示了显著的性能提升

## 8. 不足与局限
- 目前仅适用于二分类问题，未扩展到多类场景
- 实验主要集中在静态图上的增量更新，未考虑更复杂的动态图变化
- 虽然在大规模数据集上进行了测试，但未探讨在超大规模图上的表现
- 与机器学习方法相比，可能需要更多的领域知识来设置参数
- 未探讨算法在不同类型图结构上的泛化能力

（完）
