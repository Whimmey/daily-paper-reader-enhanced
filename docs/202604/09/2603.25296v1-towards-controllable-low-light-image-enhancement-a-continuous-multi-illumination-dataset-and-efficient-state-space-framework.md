---
title: "Towards Controllable Low-Light Image Enhancement: A Continuous Multi-illumination Dataset and Efficient State Space Framework"
title_zh: 迈向可控的低光照图像增强：一个连续多光照数据集与高效状态空间框架
authors: "Hongru Han, Tingrui Guo, Liming Zhang, Yan Su, Qiwen Xu, Zhuohua Ye"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25296v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 可控低光图像增强框架
tldr: 本文针对传统低光照图像增强方法的不适定性问题和亮度差异问题，提出可控低光照增强(CLE)框架，引入Light100连续多光照数据集和CLE-RKWV架构，通过HVI色彩空间噪声解耦监督和Space-to-Depth策略实现光照调制与纹理分离，显著减少对后处理的依赖。
selection_source: carryover_cache
motivation: 传统低光照图像增强方法难以解决任务的不适定性，导致预测与标签间存在亮度差异，需要后处理对齐。
method: 提出CLE框架，结合Light100数据集、HVI色彩空间噪声解耦监督和Space-to-Depth策略，实现光照调制与纹理恢复的分离。
result: 在七个基准测试上取得具有竞争力的性能和强大的可控性，显著减少对gt-mean后处理的依赖。
conclusion: 通过将低光照增强重新表述为条件问题，解决了传统方法的基本局限性，提供了更好的可控性和性能。
---

## 摘要
低光照图像增强(LLIE)传统上被表述为确定性映射。然而，这种范式往往难以解决任务的病态本质问题，其中未知的周围环境和传感器参数创建了多模态解空间。因此，最先进的方法经常在预测与标签之间遇到亮度差异，通常需要"gt-mean"后处理来对齐输出亮度以进行评估。为解决这一根本局限性，我们提出向可控低光照增强(CLE)的转变，明确将任务重新表述为良态的条件问题。为此，我们引入了CLE-RWKV，这是一个由Light100支持的全面框架，Light100是一个具有连续真实世界光照过渡的新基准。为解决亮度控制与色彩保真度之间的冲突，我们采用了HVI色彩空间中的噪声解耦监督策略，有效分离了光照调制与纹理恢复。在架构上，为使高效状态空间模型(SSMs)适应密集预测，我们利用了空间到深度(S2D)策略。通过将空间邻域折叠到通道维度，这种设计使模型能够恢复局部归纳偏置，有效弥合扁平化视觉序列中固有的"扫描间隙"，同时不牺牲线性复杂度。在七个基准测试上的实验表明，我们的方法实现了具有竞争力的性能和稳健的可控性，提供了一个真实世界多光照替代方案，显著减少了对gt-mean后处理的依赖。

## Abstract
Low-light image enhancement (LLIE) has traditionally been formulated as a deterministic mapping. However, this paradigm often struggles to account for the ill-posed nature of the task, where unknown ambient conditions and sensor parameters create a multimodal solution space. Consequently, state-of-the-art methods frequently encounter luminance discrepancies between predictions and labels, often necessitating "gt-mean" post-processing to align output luminance for evaluation. To address this fundamental limitation, we propose a transition toward Controllable Low-light Enhancement (CLE), explicitly reformulating the task as a well-posed conditional problem. To this end, we introduce CLE-RWKV, a holistic framework supported by Light100, a new benchmark featuring continuous real-world illumination transitions. To resolve the conflict between luminance control and chromatic fidelity, a noise-decoupled supervision strategy in the HVI color space is employed, effectively separating illumination modulation from texture restoration. Architecturally, to adapt efficient State Space Models (SSMs) for dense prediction, we leverage a Space-to-Depth (S2D) strategy. By folding spatial neighborhoods into channel dimensions, this design allows the model to recover local inductive biases and effectively bridge the "scanning gap" inherent in flattened visual sequences without sacrificing linear complexity. Experiments across seven benchmarks demonstrate that our approach achieves competitive performance and robust controllability, providing a real-world multi-illumination alternative that significantly reduces the reliance on gt-mean post-processing.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义

传统低光照图像增强(LLIE)方法将任务表述为确定性映射，但这种范式难以解决任务的病态本质问题。由于未知的环境条件和传感器参数，单一输入可能对应多个有效的正常光照表示，导致预测与标签间存在系统性亮度差异。现有方法通常需要"gt-mean"后处理来对齐输出亮度，但这种后处理存在物理不准确性，包括噪声方差放大和非线性ISP失真。论文提出转向可控低光照增强(CLE)范式，将任务重新表述为条件问题，通过引入目标亮度作为条件，将不适定问题转化为良定问题，从而避免后处理需求。

## 2. 论文提出的方法论

### 核心思想
将低光照增强重新表述为条件生成任务，使用目标亮度β作为输入条件，构建从低光照图像到目标亮度图像的映射函数F(x,β)。

### 关键技术细节
- **Light100数据集**：包含275个场景，每个场景有100个渐进光照级别(1W-100W)，提供连续真实世界多光照参考。
- **HVI色彩空间**：分离强度(I)和色度(H,V)，用于噪声解耦监督。
- **噪声解耦监督策略**：结合目标亮度级别和干净纹理/色彩，创建混合目标，避免模型学习传感器噪声。
- **CLE-RWKV架构**：
  - 空间到深度(S2D)设计：将空间邻域折叠到通道维度，解决"扫描间隙"问题
  - 局部RWKV(L-RWKV)块：包含空间混合和通道混合两个子模块
  - 周期性洗牌策略：保留空间拓扑结构，恢复局部归纳偏置

### 算法流程
1. 输入sRGB图像转换为HVI域，分离强度图和颜色图
2. 将目标亮度β编码为高维光照嵌入
3. 通过MLP生成仿射参数，通过FiLM层调制特征
4. 使用S2D设计处理特征，应用L-RWKV块
5. 重建并转换回sRGB域

### 损失函数
Ltotal = Lbase(Î,Îβ^Hybrid) + λLbase(ÎHVI,ÎHybrid-HVI)
其中Lbase包含ℓ1损失、SSIM损失、边缘损失和LPIPS损失

## 3. 实验设计

### 数据集
- LOL：v1和v2版本，包含真实和合成子集
- SID & SMID：用于长曝光评估
- SDSD：静态视频数据集
- Light100：作者提出的新数据集，275个场景，每个场景100个光照级别

### Benchmark
- LOL-v1, LOL-v2-Real, LOL-v2-Syn
- SID, SMID
- SDSD-in, SDSD-out
- Light100

### 对比方法
- 传统LLIE方法：SID, DeepUPE, RetinexNet, EnGAN, RUAS, FIDE, KinD, SNR-Net
- 基于Transformer的方法：Retinexformer
- 基于Mamba的方法：RetinexMamba, MambaLLIE
- 其他SOTA方法：URWKV, CIDNet
- 可控增强方法：LLFlow, PyDiff, LLFormer, GSAD, CLE-Diff

## 4. 资源与算力

- 实现框架：PyTorch
- GPU型号：NVIDIA GeForce RTX4090（数量未明确说明）
- 优化器：AdamW，β1=0.9，β2=0.999，权重衰减10^-4
- 学习率：初始2×10^-4，使用余弦退火调度，1000个周期，3个周期预热
- 训练配置：3×256×256随机裁剪，批量大小为4，数据增强包括随机翻转和旋转
- 训练时长：论文中未明确说明

## 5. 实验数量与充分性

### 实验数量
- 在7个基准数据集上进行评估
- 进行了消融研究，验证S2D设计、HVI色彩空间、噪声解耦监督等组件贡献
- 效率分析比较不同架构类别的性能-速度权衡
- 可视化分析展示方法在不同亮度级别的控制能力

### 充分性与公平性
- 实验覆盖多种低光照增强场景（真实、合成、长曝光、视频）
- 所有基线方法在Light100数据集上重新训练以确保公平比较
- 使用标准指标（PSNR、SSIM、LPIPS）进行客观评估
- 消融实验充分验证了各设计选择的有效性

## 6. 论文的主要结论与发现

### 主要结论
- 可控低光照增强(CLE)范式比传统确定性映射更有效
- Light100数据集提供了真实世界的连续多光照参考
- CLE-RWKV架构在性能和效率之间取得了良好平衡
- 噪声解耦监督策略有效解决了亮度控制和色彩保真度的冲突

### 关键发现
- 传统方法存在"回归到中位数"问题，倾向于预测训练集的平均亮度
- gt-mean后处理方法存在物理不准确性，包括噪声方差放大和非线性ISP失真
- S2D设计有效解决了SSM在密集预测中的"扫描间隙"问题
- HVI色彩空间有助于分离光照调制和纹理恢复

## 7. 优点

### 方法亮点
- 提出CLE范式，将不适定问题转化为条件问题
- 创建Light100数据集，提供真实世界的连续多光照参考
- 设计噪声解耦监督策略，在HVI空间中分离光照和纹理
- 提出S2D设计，使SSM能够有效处理密集预测任务
- CLE-RWKV架构在保持线性复杂度的同时实现了高性能

### 实验设计亮点
- 在多个基准数据集上全面评估方法性能
- 进行详细的消融研究，验证各组件贡献
- 效率分析比较不同架构类别的性能-速度权衡
- 可视化分析展示方法在不同亮度级别的控制能力

## 8. 不足与局限

### 实验覆盖不足
- 主要在室内受控环境测试，缺乏复杂户外场景的评估
- 未探索不同相机传感器类型的影响
- 未测试在视频序列上的实时性能

### 偏差风险
- 数据集可能偏向特定光照条件
- 评估指标可能不完全反映视觉质量
- 主观偏好可能因文化背景而异

### 应用限制
- 需要目标亮度参数，可能需要用户交互
- 计算效率虽然高，但在移动设备上的实际性能未验证
- 未探索在极端低光照条件下的表现
- 未考虑动态光照变化场景

（完）
