---
title: "GraphER: An Efficient Graph-Based Enrichment and Reranking Method for Retrieval-Augmented Generation"
title_zh: GraphER：一种用于检索增强生成的高效基于图的丰富化和重排序方法
authors: "Ruizhong Miao, Yuying Wang, Rongguang Wang, Chenyang Li, Tao Sheng, Sujith Ravi, Dan Roth"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24925v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 基于图的RAG增强方法
tldr: GraphER针对检索增强生成系统中语义搜索的不足，提出了一种基于图的增强和重排序方法。该方法在离线索引阶段丰富数据对象，查询时进行图重排序，无需知识图谱即可与标准向量存储无缝集成，有效捕获多种邻近关系，同时保持低延迟开销。
selection_source: carryover_cache
motivation: 现有RAG系统的语义搜索无法满足复杂信息需求，代理检索策略和知识图谱方法分别存在效率低下和维护成本高的问题。
method: GraphER通过离线索引丰富数据对象和查询时图重排序，在不依赖知识图谱的情况下捕获多种邻近关系。
result: 多个检索基准实验证明GraphER方法能有效提升检索性能且引入的延迟开销可忽略。
conclusion: GraphER为RAG系统提供了一种高效、兼容性强的检索增强解决方案，能够更好地处理复杂信息需求。
---

## 摘要
检索增强生成(RAG)系统中的语义搜索通常难以满足复杂的信息需求，特别是当相关证据分散在多个来源中时。针对此问题的先前方法包括智能体检索策略，通过生成额外查询来扩展语义搜索空间。然而，这些方法未能充分利用数据的组织结构，而是依赖迭代探索，可能导致检索效率低下。另一类方法利用知识图谱通过图边建模非语义关系。尽管在捕获更丰富的邻近性方面有效，但这些方法会产生显著的维护成本，并且通常与大多数生产系统中使用的向量存储不兼容。为解决这些局限性，我们提出了GraphER，一种基于图的丰富化和重排序方法，能够捕获超越语义相似性的多种邻近性形式。GraphER在离线索引期间独立丰富数据对象，并在查询时对候选对象进行基于图的重排序。这种设计不需要知识图谱，使GraphER能够与标准向量存储无缝集成。此外，GraphER与检索器无关，且引入的延迟开销可忽略不计。在多个检索基准上的实验证明了所提出方法的有效性。

## Abstract
Semantic search in retrieval-augmented generation (RAG) systems is often insufficient for complex information needs, particularly when relevant evidence is scattered across multiple sources. Prior approaches to this problem include agentic retrieval strategies, which expand the semantic search space by generating additional queries. However, these methods do not fully leverage the organizational structure of the data and instead rely on iterative exploration, which can lead to inefficient retrieval. Another class of approaches employs knowledge graphs to model non-semantic relationships through graph edges. Although effective in capturing richer proximities, such methods incur significant maintenance costs and are often incompatible with the vector stores used in most production systems. To address these limitations, we propose GraphER, a graph-based enrichment and reranking method that captures multiple forms of proximity beyond semantic similarity. GraphER independently enriches data objects during offline indexing and performs graph-based reranking over candidate objects at query time. This design does not require a knowledge graph, allowing GraphER to integrate seamlessly with standard vector stores. In addition, GraphER is retriever-agnostic and introduces negligible latency overhead. Experiments on multiple retrieval benchmarks demonstrate the effectiveness of the proposed approach.

---

## 论文详细总结（自动生成）

# GraphER论文总结

## 1. 论文的核心问题与整体含义

GraphER针对检索增强生成(RAG)系统中语义搜索的不足，解决当相关证据分散在多个来源时，传统语义搜索无法满足复杂信息需求的问题。现有方法存在两大局限：代理检索策略虽能扩展语义搜索空间，但未能充分利用数据组织结构，依赖迭代探索导致效率低下；知识图谱方法虽能捕获更丰富邻近性，但维护成本高且与多数生产系统中的向量存储不兼容。GraphER旨在提供一种高效方法，在不依赖知识图谱的情况下捕获超越语义相似性的多种邻近性形式，同时保持与现有系统的兼容性。

## 2. 论文提出的方法论

### 核心思想
GraphER是一种基于图的丰富化和重排序方法，在离线索引期间独立丰富数据对象，在查询时对候选对象进行基于图的重排序。该方法无需知识图谱，可与标准向量存储无缝集成，且与检索器无关，引入的延迟开销可忽略。

### 关键技术细节
- **离线索引阶段**：为每个数据对象添加额外字段，捕获潜在图边关系
- **在线检索阶段**：包括初始检索、结果阈值处理、候选对象图构建、基于图的重排序和最终排序列表输出

### 三种邻近性类型
1. **结构邻近性**：通过预定义规则建立，如网页间超链接或数据库表间外键关系
2. **概念邻近性**：使用LLM提取命名实体列表，共享实体的对象被认为概念上接近
3. **上下文邻近性**：同一文档中相邻块在上下文上接近

### 图排序算法
- **个性化PageRank (PPR)**：但倾向于偏爱枢纽节点
- **图凝聚平滑 (GCS)**：提出的新算法，使用行归一化转换矩阵，返回p(t)和s的元素最大值
- **图注意力网络 (GAT)**：结合GCS分数、查询嵌入和数据对象嵌入，捕获非线性图效应

## 3. 实验设计

### 数据集/场景
1. **表格检索任务**：Spider 1.0、Bird、Beaver数据集
2. **多跳问答检索任务**：HotpotQA、2WikiMultihopQA、MuSiQue数据集
3. **分块文档检索任务**：BEIR-NQ数据集

### Benchmark
- 使用完美召回率@K (PR@K)作为主要评估指标
- 对于多跳问答任务，还使用GPT-5评估生成答案的正确性

### 对比方法
- **基线方法**：
  - Llama-Embed-Nemotron-8B+BM25
  - Cohere-Embed-4+BM25
  - Multilingual-E5-large+BM25（仅用于BEIR-NQ）
- **GraphER变体**：
  - GraphER-PPR（使用个性化PageRank）
  - GraphER-GCS（使用图凝聚平滑）
  - GraphER-GAT（使用图注意力网络）
  - GraphER-MLP（用于消融研究）

## 4. 资源与算力

实验在配备以下硬件的计算节点上进行：
- 两个AMD EPYC 7J13处理器
- 1TB RAM
- 单个NVIDIA A100-SXM4-40GB GPU

GraphER-GCS平均需要约0.49秒来排序200个对象，GraphER-GAT平均需要约0.55秒，与后续LLM推理时间相比可忽略不计。论文未明确提及训练时长或具体GPU使用数量。

## 5. 实验数量与充分性

### 实验数量
- 在3种任务类型（表格检索、多跳问答检索、分块文档检索）上进行了实验
- 使用了7个不同数据集
- 对比了多种基线方法和GraphER的不同变体
- 进行了消融研究验证各组件贡献

### 充分性与公平性
- 实验覆盖多种任务类型和数据集，表明方法具有良好泛化能力
- 使用不同嵌入模型测试，证明方法鲁棒性
- 进行超参数调优（在Bird训练集上调整阻尼因子α）
- 消融研究验证图结构重要性
- 使用标准评估指标和对比方法，实验设计客观

## 6. 论文的主要结论与发现

### 主要结论
- GraphER能有效提升检索性能，特别是在需要从多个来源获取信息的复杂查询场景中
- GraphER-GCS和GraphER-GAT在大多数情况下优于基线方法和GraphER-PPR
- 图结构能捕获超越语义相似性的多种邻近性形式，提高检索完整性
- GraphER与现有检索基础设施无缝集成，无需额外资源，且引入的延迟开销可忽略

### 主要发现
- 结构邻近性对表格检索任务特别有效
- 概念邻近性对多跳问答检索任务有益
- 上下文邻近性有助于分块文档检索
- GAT模型能捕获非线性图效应，在大多数情况下优于线性方法
- PPR倾向于偏爱枢纽节点，可能导致某些场景下的性能下降

## 7. 优点

### 方法亮点
- 无需维护知识图谱，降低系统复杂度和维护成本
- 与标准向量存储无缝集成，易于部署到现有RAG系统
- 离线处理LLM调用，在线查询阶段无需LLM，降低延迟
- 支持多种类型的邻近性关系，可根据不同场景灵活选择
- 提出新的图凝聚平滑算法，解决PPR的局限性

### 实验设计亮点
- 在多种任务类型和数据集上验证方法有效性
- 使用不同嵌入模型测试，证明方法鲁棒性
- 进行全面消融研究，验证各组件贡献
- 使用完美召回率作为评估指标，适合需要完整信息检索的场景
- 考虑实际应用中的延迟开销，证明其可忽略不计

## 8. 不足与局限

### 实验覆盖不足
- 主要集中在三种任务类型，可能无法覆盖所有RAG应用场景
- 对特定领域（如医疗、法律等）的验证不足
- 未探索更大规模检索语料库上的性能

### 偏差风险
- 实验中使用的数据集可能存在特定领域偏差
- 评估指标可能无法完全反映实际应用中的检索质量
- 使用GPT-5评估生成答案可能存在评估偏差

### 应用限制
- 需要额外离线处理步骤丰富数据对象
- 对于动态变化的数据，需定期重新执行离线处理
- 图构建和重排序的计算复杂度与候选对象数量呈二次关系，在大规模候选集上可能面临性能挑战
- 虽然延迟开销可忽略，但相比基线方法仍增加计算负担

（完）
