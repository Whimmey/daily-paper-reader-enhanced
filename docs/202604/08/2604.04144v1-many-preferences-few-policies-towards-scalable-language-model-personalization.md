---
title: "Many Preferences, Few Policies: Towards Scalable Language Model Personalization"
title_zh: 多种偏好，少数政策：迈向可扩展的语言模型个性化
authors: "Cheol Woo Kum, Jai Moondra, Roozbeh Nahavandi, Andrew Perrault, Milind Tambe, Swati Gupta"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04144v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 大模型个性化方法
tldr: 这篇论文针对为每个用户单独维护语言模型(LLM)的高成本问题，提出了一种可扩展的个性化解决方案。作者开发了PALM算法，通过构建一个小型LLM组合来代表多样化的用户偏好，实现了在计算资源有限的情况下提供个性化服务。该方法首次为LLM组合的大小和近似质量提供了理论保证，并展示了在输出多样性方面优于基线方法的优势。
selection_source: fresh_fetch
motivation: 为每个用户单独维护语言模型因计算、内存和系统复杂度限制而不切实际。
method: PALM算法通过多维权重向量建模用户偏好，生成小型LLM组合以覆盖多样化的用户需求。
result: 研究提供了LLM组合大小和近似质量的理论保证，并展示了在输出多样性方面优于基线方法的实证结果。
conclusion: 该方法实现了系统成本与个性化之间的权衡，并确定了覆盖用户偏好景观所需的LLM多样性。
---

## 摘要
LLM个性化的圣杯是为每个用户提供一个完全符合该用户偏好的单一LLM。然而，由于计算、内存和系统复杂度的限制，为每个用户维护一个独立的LLM是不切实际的。我们通过开发一种有原则的方法来解决这个问题，该方法选择一个小型LLM组合，以捕捉不同用户之间的代表性行为。我们通过多维权重向量对用户在多种特质（如安全性、幽默感、简洁性）上的偏好进行建模。给定这些维度上的奖励函数，我们的算法PALM（对齐的LLM组合）生成一个小型LLM组合，使得对于任何权重向量，该组合都包含对应标量化目标的近优LLM。据我们所知，这是第一个对LLM组合的大小和近似质量提供理论保证的个性化结果。它表征了系统成本与个性化之间的权衡，以及覆盖用户偏好景观所需的LLM多样性。我们提供了验证这些保证的经验结果，并展示了与常见基线相比更大的输出多样性。

## Abstract
The holy grail of LLM personalization is a single LLM for each user, perfectly aligned with that user's preferences. However, maintaining a separate LLM per user is impractical due to constraints on compute, memory, and system complexity. We address this challenge by developing a principled method for selecting a small portfolio of LLMs that captures representative behaviors across heterogeneous users. We model user preferences across multiple traits (e.g., safety, humor, brevity) through a multi-dimensional weight vector. Given reward functions across these dimensions, our algorithm PALM (Portfolio of Aligned LLMs) generates a small portfolio of LLMs such that, for any weight vector, the portfolio contains a near-optimal LLM for the corresponding scalarized objective. To the best of our knowledge, this is the first result that provides theoretical guarantees on both the size and approximation quality of LLM portfolios for personalization. It characterizes the trade-off between system cost and personalization, as well as the diversity of LLMs required to cover the landscape of user preferences. We provide empirical results that validate these guarantees and demonstrate greater output diversity over common baselines.

---

## 论文详细总结（自动生成）

# 论文总结：多种偏好，少数政策：迈向可扩展的语言模型个性化

## 1. 论文的核心问题与整体含义
- **研究动机**：LLM个性化理想是为每个用户提供完全符合其偏好的单一LLM，但为每个用户维护单独的LLM因计算、内存和系统复杂度的限制而不切实际。
- **核心问题**：如何在系统成本与个性化之间找到平衡，即如何选择小型LLM组合来捕捉多样化用户偏好，同时保持高效和可扩展性。
- **研究背景**：随着LLM在现实世界应用中的广泛部署，个性化需求增长，但当前系统倾向于使用小型固定LLM集合，难以捕捉用户偏好多样性。

## 2. 论文提出的方法论
- **核心思想**：通过构建小型LLM组合来覆盖整个用户偏好空间，而非为每个用户单独维护一个LLM。
- **关键技术**：
  - **PALM算法**：Portfolio of Aligned LLMs，通过多维权重向量建模用户偏好
  - **权重向量建模**：用户偏好通过多维权重向量表示，涵盖多种特质（如安全性、幽默感、简洁性）
  - **组合构建**：生成小型LLM组合，确保对于任何权重向量，组合中都包含对应标量化目标的近优LLM
- **算法流程**：
  1. 构建结构化权重网格，结合乘法和均匀网格
  2. 为每个权重向量训练/获取最优策略
  3. 使用剪枝算法移除冗余策略，形成最终组合
- **理论保证**：提供组合大小和近似质量的理论保证，表征系统成本与个性化之间的权衡

## 3. 实验设计
- **数据集/场景**：
  - **RLVR-GSM**：数学推理任务，两个目标（简洁性和有帮助性）
  - **Safety Alignment**：安全对齐任务，两个奖励模型（更好和安全）
  - **Helpful Assistants**：助手任务，三个奖励模型（有帮助性、无害性和幽默感）
- **基准方法**：
  - **均匀间隔权重向量**：多目标对齐文献中常用的方法
  - **随机采样权重向量**：从Dirichlet分布中采样，常用于训练可导向模型
- **评估指标**：
  - 近似质量：乘法间隙(ε)和加法间隙(δ)
  - 政策使用分布： perplexity（困惑度）衡量策略使用的平衡性
  - 定性分析：比较不同方法生成的响应多样性

## 4. 资源与算力
- **硬件配置**：
  - **RLVR-GSM任务**：4×NVIDIA H100 80GB GPU（3个用于训练，1个用于推理）
  - **其他任务**：使用MOD算法作为可扩展实验代理，基于ALPACA-7B和LLaMA-2 7B
- **训练设置**：
  - 训练40,000个episodes，约208个梯度步骤
  - 有效批大小为48（每设备批大小4×3 GPU×4梯度累积步骤）
  - 使用AdamW优化器，学习率5×10^-7，混合精度bf16
- **资源消耗**：论文明确提到了训练资源，但未详细说明总训练时间或总计算量

## 5. 实验数量与充分性
- **实验规模**：
  - 三个主要任务，每个任务测试不同组合大小（2-7个策略）
  - 每个组合大小进行多次参数配置实验
  - 进行消融研究，替换初始权重生成方案
- **充分性评估**：
  - **充分性**：实验覆盖了不同维度和场景，包括理论验证和实证测试
  - **客观性**：使用标准化指标（ε和δ）进行量化评估
  - **公平性**：与自然基线方法（均匀和随机）进行对比，确保公平比较
  - **局限性**：实验主要在特定任务上进行，可能存在领域偏差

## 6. 论文的主要结论与发现
- **核心结论**：
  - PALM算法在近似质量和行为多样性方面显著优于基线方法
  - 少于7个策略就能实现接近最优的性能，子最优性小于1.5%
  - PALM组合展现出更平衡的政策使用分布， perplexity更高
- **关键发现**：
  - 均匀间隔的权重可能产生近乎冗余的策略
  - PALM构建的组合基于实际策略输出而非权重向量距离，因此策略占据奖励空间的不同区域
  - 理论分析与实证结果一致，验证了算法的有效性

## 7. 优点
- **理论贡献**：首次为LLM组合的大小和近似质量提供理论保证
- **算法设计**：结合乘法和均匀网格，在边界附近保持稳定的乘法保证，同时控制组合大小
- **实验全面性**：涵盖多个任务和对比方法，包括定量和定性分析
- **实用性**：方法不依赖于特定的偏好获取协议，支持灵活的用户交互
- **效率**：组合大小与奖励函数数量相关，与用户数量无关，实现可扩展性

## 8. 不足与局限
- **实验覆盖局限**：
  - 实验主要在特定任务上进行，可能无法完全推广到所有应用场景
  - 仅测试了中小规模模型（3B-7B参数），未在大规模模型上验证
- **算法依赖**：
  - 需要访问优化器来计算最优策略，实际应用中可能受限
  - 依赖奖励函数的质量和多样性，可能无法捕捉所有用户偏好
- **应用限制**：
  - 未探讨大规模用户场景下的实际部署挑战
  - 未考虑动态用户偏好变化的适应性
- **潜在偏差**：
  - 实验可能存在领域偏差，特定任务上的结果可能不具普适性
  - 评估指标可能无法完全反映用户体验的多样性

（完）
