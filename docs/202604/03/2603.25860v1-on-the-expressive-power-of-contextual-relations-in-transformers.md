---
title: On the Expressive Power of Contextual Relations in Transformers
title_zh: 关于Transformer中上下文关系的表达能力
authors: Demián Fraiman
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25860v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: Transformer上下文关系的数学框架
tldr: 本研究针对Transformer架构在自然语言处理中表现优异但数学表征不完整的问题，提出了一种基于测度论的上下文表征框架，将文本建模为语义嵌入空间上的概率测度，并引入了Sinkhorn Transformer架构。研究证明了该架构可以一致近似任何连续耦合函数，为Transformer的表达能力提供了严格的数学基础。
selection_source: fresh_fetch
motivation: Transformer架构在自然语言处理中取得了显著成功，但其表达能力的精确数学表征仍不完善。
method: 提出了一种测度论框架，将文本建模为概率测度，上下文关系表示为耦合测度，并设计了Sinkhorn Transformer架构。
result: 证明了Sinkhorn Transformer可以一致近似任何编码语义关系的连续耦合函数，建立了通用近似定理。
conclusion: Sinkhorn Transformer及其数学框架为理解Transformer的表达能力提供了理论基础，扩展了我们对上下文关系建模的理解。
---

## 摘要
Transformer架构在建模自然语言中的上下文关系方面取得了显著的实证成功，但其表达能力的精确数学表征仍不完善。在这项工作中，我们引入了一个上下文表征的测度论框架，其中文本被建模为语义嵌入空间上的概率测度，词语之间的上下文关系被表示为它们之间的耦合测度。在此框架下，我们引入了Sinkhorn Transformer，一种类Transformer架构。我们的主要结果是一个通用近似定理：任何编码语义关系耦合测度的概率测度之间的连续耦合函数，都可以通过适当参数的Sinkhorn Transformer进行一致近似。

## Abstract
Transformer architectures have achieved remarkable empirical success in modeling contextual relationships in natural language, yet a precise mathematical characterization of their expressive power remains incomplete. In this work, we introduce a measure-theoretic framework for contextual representations in which texts are modeled as probability measures over a semantic embedding space, and contextual relations between words, are represented as coupling measures between them. Within this setting, we introduce Sinkhorn Transformer, a transformer-like architecture. Our main result is a universal approximation theorem: any continuous coupling function between probability measures, that encodes the semantic relation coupling measure, can be uniformly approximated by a Sinkhorn Transformer with appropriate parameters.

---

## 论文详细总结（自动生成）

# 关于Transformer中上下文关系的表达能力论文总结

## 1. 论文的核心问题与整体含义

论文的核心问题是：尽管Transformer架构在自然语言处理中取得了显著成功，但其表达能力的精确数学表征仍不完善。现有分析通常将注意力机制描述为表示上的启发式加权方案，而忽略了注意力层在根本上能够近似什么样的功能对象。

论文的整体含义是：研究Transformer是否能够学习任何可能的语义上下文关系系统，其中每个上下文系统是将文本序列映射到上下文语义图的分配，表示词语之间的语义关系。这项工作旨在为Transformer的表达能力提供严格的数学基础。

## 2. 论文提出的方法论

### 核心思想
- 引入一个测度论框架，将文本建模为语义嵌入空间上的概率测度
- 上下文关系表示为这些分布之间的耦合测度
- 注意力机制被解释为将输入测度对映射到具有规定边缘的联合分布的算子

### 关键技术细节
- **文本表示**：将文本表示为概率测度 μ = 1/n Σ δxi，其中xi是token的嵌入
- **Sinkhorn Transformer架构**：包含三个主要组件：
  1. 两个基于Transformer的编码器Q和K，用于将输入测度处理为查询和键嵌入
  2. 基于嵌入之间差异的成本函数：c(μ,ν)(x,y) = -⟨Q(μ,x), K(ν,y)⟩
  3. 应用Sinkhorn算子获得联合测度：T(μ,ν) = Sc(μ,ν)(μ,ν)

### 算法流程
1. 将文本序列表示为语义嵌入空间上的概率测度
2. 使用Transformer编码器处理输入测度，生成查询和键嵌入
3. 计算成本函数，衡量嵌入之间的相似性
4. 应用Sinkhorn算子获得联合测度，表示上下文关系

## 3. 实验设计

该论文主要是理论性研究，没有进行传统的实证实验。论文主要关注数学理论框架和证明，而不是在具体数据集上的性能评估。

论文与以下工作进行了对比：
- 现有注意力机制表达能力分析（Vaswani et al., 2017; Yun et al., 2020等）
- 最优传输在注意力和表示学习中的应用（Cuturi, 2013; Genevay et al., 2016等）
- 注意力机制的语义解释相关研究

## 4. 资源与算力

该论文主要是理论性研究，没有明确提到使用的算力资源（如GPU型号、数量、训练时长等）。论文专注于数学证明和理论框架，而不是实证实验和模型训练。

## 5. 实验数量与充分性

由于该论文主要是理论性研究，没有进行传统的实证实验，因此没有传统意义上的实验数量。论文的主要贡献是建立了Sinkhorn Transformer的通用近似定理，这是一个理论结果，而不是基于实验的发现。

论文提供了详细的数学证明，包括：
- 测度论框架的构建
- Sinkhorn算子的性质
- 通用近似定理的证明
- 各种引理和命题的证明

这些理论证明的充分性和严谨性可以通过数学界的同行评审来评估。

## 6. 论文的主要结论与发现

### 主要结论
- 提出了Sinkhorn Transformer架构，可以一致近似任何编码语义关系的连续耦合函数
- 建立了通用近似定理：任何连续耦合函数都可以通过适当参数的Sinkhorn Transformer进行一致近似
- 为Transformer的表达能力提供了严格的数学基础

### 主要发现
- 注意力机制可以被解释为将输入测度对映射到具有规定边缘的联合分布的算子
- 语义关系可以被建模为分布之间的耦合测度
- Sinkhorn算子提供了丰富的和稳定的耦合系统类

## 7. 优点

### 理论框架的优点
- 提供了一个严格的数学框架来理解Transformer的上下文关系建模能力
- 将注意力机制从点相似性分数提升到结构化概率关系的近似
- 使用测度论和最优传输理论为语义理解提供了新的视角

### Sinkhorn Transformer的优点
- 保留了标准Transformer的所有中间层注意力机制
- 仅修改最终归一化步骤，使架构与标准Transformer非常接近
- 提供了注意力矩阵作为耦合测度的结构化概率解释
- 在实际应用中可以高效实现，使用Sinkhorn算法计算

## 8. 不足与局限

### 理论局限性
- 论文专注于表示表达能力，没有解决学习动态、统计效率或泛化问题
- 没有探讨如何从有限数据中学习这类耦合值表示
- 没有考虑计算复杂度和实际应用中的效率问题

### 应用限制
- 理论框架假设语义空间是紧致的，可能不适用于所有实际应用
- 没有讨论如何处理大规模数据集和长序列
- 没有探索除了熵正则化最优传输之外的其他正则化方案

### 未来工作方向
- 需要推导耦合值注意力机制的定量近似率和样本复杂度界
- 需要将框架扩展到序列或动态设置
- 需要探索除熵正则化最优传输之外的其他正则化方案
- 需要研究这类表示如何从有限数据中学习

（完）
