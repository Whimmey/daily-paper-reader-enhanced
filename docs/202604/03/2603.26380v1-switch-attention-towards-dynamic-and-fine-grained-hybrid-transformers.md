---
title: "Switch Attention: Towards Dynamic and Fine-grained Hybrid Transformers"
title_zh: 切换注意力：迈向动态和细粒度混合Transformer
authors: "Yusheng Zhao, Hourun Li, Bohan Wu, Jingyang Yuan, Meng Zhang, Yichun Yin, Lifeng Shang, Ming Zhang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26380v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 混合Transformer注意力机制
tldr: Switch Attention (SwiAttn)是一种新型混合transformer架构，解决了全注意力计算随序列长度二次增长和滑动窗口注意力感受野有限的矛盾。该方法通过动态路由机制为每个token选择全注意力或滑动窗口注意力，结合自适应正则化和持续预训练策略，在保持全局信息捕获能力的同时提高了计算效率，在23个基准数据集上验证了其有效性。
selection_source: fresh_fetch
motivation: 标准全注意力计算随序列长度二次增长，而滑动窗口注意力感受野有限，现有混合模型采用静态交替模式限制了计算资源的高效分配。
method: Switch Attention通过动态路由机制为每个token在每个transformer层选择全注意力或滑动窗口注意力分支，并采用自适应正则化和持续预训练优化模型。
result: 在23个基准数据集上的实验表明，Switch Attention在保持全局信息捕获能力的同时提高了计算效率，在常规(4K)和长(32K)上下文长度下均有效。
conclusion: Switch Attention实现了动态和细粒度的混合注意力机制，为长上下文语言模型提供了高效且有效的解决方案。
---

## 摘要
注意力机制已成为现代Transformer架构的核心组件。然而，标准全注意力的计算量随序列长度呈二次方增长，成为长上下文语言建模的主要瓶颈。滑动窗口注意力通过限制上下文长度来提高效率，但代价是感受野变窄。尽管现有尝试通过构建混合模型来兼顾两者优势，但它们通常采用静态的、启发式设计的交替模式，限制了在不同场景下对计算的高效分配。在本文中，我们提出了切换注意力（SwiAttn），一种新型混合Transformer，能够在全注意力和滑动窗口注意力之间实现动态和细粒度的路由。对于每个Transformer层中的每个token，SwiAttn动态地将计算路由到全注意力分支以进行全局信息聚合，或路由到滑动窗口分支以实现高效局部模式匹配。我们设计了一种自适应正则化目标，以鼓励模型向高效方向发展。此外，我们采用持续预训练来优化模型，将全注意力架构转换为混合架构。我们在常规（4K）和长（32K）上下文长度的二十三个基准数据集上进行了广泛实验，证明了所提方法的有效性。

## Abstract
The attention mechanism has been the core component in modern transformer architectures. However, the computation of standard full attention scales quadratically with the sequence length, serving as a major bottleneck in long-context language modeling. Sliding window attention restricts the context length for better efficiency at the cost of narrower receptive fields. While existing efforts attempt to take the benefits from both sides by building hybrid models, they often resort to static, heuristically designed alternating patterns that limit efficient allocation of computation in various scenarios. In this paper, we propose Switch Attention (SwiAttn), a novel hybrid transformer that enables dynamic and fine-grained routing between full attention and sliding window attention. For each token at each transformer layer, SwiAttn dynamically routes the computation to either a full-attention branch for global information aggregation or a sliding-window branch for efficient local pattern matching. An adaptive regularization objective is designed to encourage the model towards efficiency. Moreover, we adopt continual pretraining to optimize the model, transferring the full attention architecture to the hybrid one. Extensive experiments are conducted on twenty-three benchmark datasets across both regular (4K) and long (32K) context lengths, demonstrating the effectiveness of the proposed method.

---

## 论文详细总结（自动生成）

# Switch Attention 论文总结

## 1. 论文的核心问题与整体含义

论文针对标准全注意力机制计算复杂度随序列长度呈二次方增长的问题，以及滑动窗口注意力感受野有限的矛盾，提出了一种动态和细粒度的混合Transformer架构。现有混合模型通常采用静态的、启发式设计的交替模式，限制了计算资源在不同场景下的高效分配。本文旨在通过动态路由机制，为每个token在每个transformer层智能选择全注意力或滑动窗口注意力，兼顾全局信息捕获和计算效率。

## 2. 论文提出的方法论

### 核心思想
Switch Attention (SwiAttn) 通过动态路由机制为每个token在每个transformer层选择全注意力或滑动窗口注意力分支，实现计算资源的智能分配。

### 关键技术细节
- **双分支路由机制**：包含全注意力分支（全局信息聚合）和滑动窗口分支（高效局部模式匹配）
- **路由器函数**：根据输入特征决定使用哪个分支
- **自适应正则化**：设计自适应正则化目标，鼓励模型使用更高效的滑动窗口分支
- **持续预训练**：从预训练的全注意力模型转换到混合架构

### 算法流程
1. 计算软门控：˜σ(l) = sigmoid(R(H(l)))
2. 获得硬门控：σ(l) = 1(˜σ(l) > τ) + ˜σ(l) - sg(˜σ(l))
3. 计算两个分支的输出：O(l)FULL和O(l)SWA
4. 使用门控选择输出：O(l) = σ(l) ⊙ O(l)FULL + (1 - σ(l)) ⊙ O(l)SWA
5. 应用自适应正则化：L(l)t = γ(l)t * sp(R(h(l)t))

## 3. 实验设计

### 数据集/场景
- **常识推理**：Lambada、Wikitext、BoolQ、PIQA、SIQA、HellaSwag、WinoGrande、ARC-easy、ARC-challenge、OpenBookQA
- **上下文检索**：SWDE、FDA、TriviaQA、NQ、DROP、SQuAD
- **长上下文理解**：MultiFieldQA、TRec、MultiNews、GovReport、Qasper、TriviaQA、SAMSum

### Benchmark
- 23个基准数据集
- 常规(4K)和长(32K)上下文长度

### 对比方法
- 滑动窗口注意力（SWA-ZS和SWA-CPT）
- 静态混合模型（StaticHybrid）
- 全注意力模型（FullAttn）

## 4. 资源与算力

- 模型规模：1.5B参数，22层，隐藏维度2048
- 注意力机制：组查询注意力（GQA），16个注意力头和8个键值头
- 训练框架：Megatron-LM
- 评估工具：LM-Eval-Harness
- 硬件：Ascend 910C NPUs
- 训算量：
  - 4K模型：预训练240B tokens，持续预训练20B tokens
  - 32K模型：预训练192B tokens（4K上下文），扩展到32K上下文后训练24B tokens，持续预训练8B tokens

## 5. 实验数量与充分性

### 实验数量
- 23个基准数据集上的主要实验
- 多种对比方法的性能比较
- 7种变体的消融研究（V1-V7）
- 针对路由动态性的进一步分析（Needle-in-a-Haystack、推理效率、各层全注意力比例、双分支路由动态性）

### 充分性评估
- 实验覆盖多种任务类型（常识推理、上下文检索、长上下文理解）
- 对比了多种基线方法，包括静态和动态方法
- 进行了详细的消融研究，验证了各组件的有效性
- 分析了模型的动态行为和效率特性
- 实验设计客观公平，使用相同的训练预算和评估标准

## 6. 论文的主要结论与发现

- SwiAttn能够在保持全局信息捕获能力的同时提高计算效率
- 在23个基准数据集上验证了方法的有效性，特别是在长上下文理解任务上表现最佳
- 动态路由机制能够根据上下文需求智能分配计算资源
- 不同transformer层表现出不同的路由模式，平均全注意力比例约为0.13，低于静态混合模型的0.25
- 在32K上下文长度下，SwiAttn实现了4倍以上的加速，在简单数据分布下甚至达到12倍加速

## 7. 优点

- **动态和细粒度的路由机制**：能够根据上下文需求智能分配计算资源
- **自适应正则化目标**：能够根据预测难度和分支分歧度调整正则化强度
- **持续预训练策略**：有效利用预训练知识，节省计算资源
- **全面的实验设计**：覆盖多种任务和数据集，验证了方法的泛化能力
- **深入的分析**：详细分析了模型的动态行为和效率特性，提供了有价值的见解

## 8. 不足与局限

- **硬件实现**：缺乏硬件高效实现，特别是在prefill阶段计算两个分支
- **模型规模**：由于计算资源限制，未验证方法在更大模型（1.5B以上）上的有效性
- **数据偏差**：实验主要在特定类型的数据集上进行，可能存在偏差
- **泛化能力**：未探讨方法在不同语言或领域上的泛化能力
- **应用场景**：实验主要关注语言建模任务，可能限制了方法在其他应用场景的评估
- **理论分析**：缺乏对路由机制的理论分析和保证

（完）
