---
title: "To Memorize or to Retrieve: Scaling Laws for RAG-Considerate Pretraining"
title_zh: 记忆还是检索：面向RAG的预训练缩放定律
authors: "Karan Singh, Michael Yu, Varun Gangal, Zhuofu Tao, Sachin Kumar, Emmy Liu, Steven Y. Feng"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00715v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 研究语言模型预训练中的RAG
tldr: 本研究探索了检索增强生成(RAG)与预训练之间的权衡关系，通过在不同模型规模和数据预算下训练语言模型，发现检索能持续提升性能，并提出了一个三维缩放框架来优化数据资源分配，为设计可扩展语言模型系统提供了定量基础。
selection_source: fresh_fetch
motivation: 理解参数化知识(预训练)与非参数化知识(检索)之间的关系，特别是在固定数据预算下的权衡。
method: 训练不同规模(30M-3B参数)的OLMo-2模型，系统变化预训练数据规模和检索存储大小，并在多样化基准上评估性能。
result: 检索在所有模型规模上均能提升性能，且检索的边际效用取决于模型规模、任务类型和预训练饱和度。
conclusion: 提出的三维缩放框架为理解何时以及如何检索应补充预训练提供了定量基础，指导可扩展语言建模系统的数据资源分配。
---

## 摘要
检索增强生成(RAG)通过在测试时为知识密集型场景提供相关上下文来提高语言模型(LM)的性能。然而，预训练期间获取的参数化知识与通过检索获取的非参数化知识之间的关系仍然难以理解，特别是在固定数据预算的情况下。在这项工作中，我们在广泛的模型和数据规模范围内系统地研究了预训练语料库大小与检索存储大小之间的权衡。我们在最多100B的DCLM数据上训练了基于OLMo-2的、参数量从30M到3B的LM，同时变化预训练数据规模(参数量的1-150倍)和检索存储大小(1-20倍)，并在涵盖推理、科学问答和开放域问答的多样化基准测试中评估性能。我们发现，检索在所有模型规模上都一致地优于仅使用参数化的基线，并引入了一个三维缩放框架，该框架将性能建模为模型大小、预训练标记和检索语料库大小的函数。这种缩放流形使我们能够估计在预训练和检索之间固定数据预算的最佳分配，揭示出检索的边际效用强烈依赖于模型规模、任务类型和预训练饱和度。我们的结果为理解检索何时以及如何补充预训练提供了定量基础，为可扩展语言建模系统设计中数据资源的分配提供了实践指导。

## Abstract
Retrieval-augmented generation (RAG) improves language model (LM) performance by providing relevant context at test time for knowledge-intensive situations. However, the relationship between parametric knowledge acquired during pretraining and non-parametric knowledge accessed via retrieval remains poorly understood, especially under fixed data budgets. In this work, we systematically study the trade-off between pretraining corpus size and retrieval store size across a wide range of model and data scales. We train OLMo-2-based LMs ranging from 30M to 3B parameters on up to 100B tokens of DCLM data, while varying both pretraining data scale (1-150x the number of parameters) and retrieval store size (1-20x), and evaluate performance across a diverse suite of benchmarks spanning reasoning, scientific QA, and open-domain QA. We find that retrieval consistently improves performance over parametric-only baselines across model scales and introduce a three-dimensional scaling framework that models performance as a function of model size, pretraining tokens, and retrieval corpus size. This scaling manifold enables us to estimate optimal allocations of a fixed data budget between pretraining and retrieval, revealing that the marginal utility of retrieval depends strongly on model scale, task type, and the degree of pretraining saturation. Our results provide a quantitative foundation for understanding when and how retrieval should complement pretraining, offering practical guidance for allocating data resources in the design of scalable language modeling systems.

---

## 论文详细总结（自动生成）

# 论文总结：记忆还是检索：面向RAG的预训练缩放定律

## 1. 论文的核心问题与整体含义

论文核心问题是在固定数据预算下，如何权衡分配资源用于预训练(参数化知识存储)和检索(非参数化知识存储)。研究动机源于RAG虽能提高语言模型性能，但预训练获取的参数化知识与检索获取的非参数化知识之间的关系仍不明确，特别是在有限数据资源条件下。论文试图回答：给定固定数据预算，如何最优分配数据用于预训练和检索，以最大化模型性能。

## 2. 论文提出的方法论

### 核心思想
将预训练和检索视为两种互补的知识存储机制，研究它们在固定数据预算下的权衡关系，提出一个三维缩放框架来优化数据资源分配。

### 关键技术细节
- 使用OLMo-2模型系列，参数量从30M到3B
- 使用DCLM数据集作为预训练语料库，最多使用100B标记
- 构建不同规模的检索索引(1B-20B标记)，使用FAISS进行索引
- 采用Qwen3-Embedding-8B作为嵌入模型，固定分块长度为900标记，重叠率为28%

### 公式表达
- 基础参数化缩放定律：L(N, D) = A(N/10^9)^(-α) + B(D/10^9)^(-β) + L0
- 扩展的三维缩放定律：L(N, D, R) = A(N/10^9)^(-α) + B(D/10^9)^(-β) - C log(1 + ηR/10^9) + L0
  其中N是模型参数量，D是预训练标记数，R是检索语料库大小

## 3. 实验设计

### 数据集/场景
- DCLM数据集作为预训练语料库
- 检索索引使用DCLM的保留部分构建

### Benchmark
- 推理类：PIQA, CommonsenseQA, HellaSwag, StrategyQA
- 科学/开放域问答：SciQ, OpenBookQA, AI2-ARC (Easy和Challenge), Natural Questions, SimpleQA

### 对比方法
- 仅参数化基线(无检索)
- 不同预训练数据规模和检索存储大小的组合
- 不同查询策略(仅问题、问题+答案选择、问题+黄金答案)

## 4. 资源与算力

论文提到使用NVIDIA H100 GPU，8个设备并行训练，采用FSDP数据并行和混合精度训练。但未明确说明训练时长或总计算量。

## 5. 实验数量与充分性

### 实验数量
- 6种模型规模(30M, 136M, 233M, 728M, 1B, 3B参数)
- 多种预训练数据规模(参数量的1-150倍)
- 多种检索存储大小(1-20倍)
- 9个不同的基准测试
- 27次运行(3个随机种子×3个模型家族)

### 充分性
- 实验覆盖了广泛的模型规模和数据分配比例
- 进行了稳定性分析，验证了结果对初始化和数据顺序的鲁棒性
- 使用交叉验证和留一法验证了缩放定律的预测能力
- 进行了消融研究，分析了不同查询策略的影响

## 6. 论文的主要结论与发现

- 检索在所有模型规模上都一致地提升性能
- 存在一个交叉点(D/N≈4.14)，超过后检索成为预训练的有效替代
- 检索的边际效用取决于模型规模、任务类型和预训练饱和度
- 小模型从检索中获益最多，大模型则表现出递减收益
- 检索对知识密集型任务(如CommonsenseQA)特别有效，但对推理密集型任务(如GSM8K)效果有限
- 检索质量是瓶颈，改进查询策略能带来额外收益

## 7. 优点

- 首次系统研究了预训练和检索作为相同数据预算的竞争接受者
- 提出了统一的三维缩放框架，量化了参数化和非参数化知识的权衡
- 实验设计全面，覆盖了广泛的模型规模和数据分配比例
- 使用困惑度(PPL)作为主要指标，避免了离散成功指标的阈值行为
- 提供了检索效率的定量指标(替换成本σ和边际收益κ)
- 进行了详细的稳定性分析，验证了结果的鲁棒性

## 8. 不足与局限

- 检索设置相对简单，使用单一检索器、固定分块策略和固定top-k协议
- 主要关注困惑度作为缩放分析的最稳定指标，可能无法完全捕捉所有下游行为
- 结论局限于所研究的规模、架构和语料库
- 检索质量可能成为瓶颈，特别是在查询策略不够理想的情况下
- 未探索更复杂的检索系统(如重排序、学习过滤、自适应分块等)如何影响最优预训练-检索分配
- 对推理密集型任务的检索效果研究有限

（完）
