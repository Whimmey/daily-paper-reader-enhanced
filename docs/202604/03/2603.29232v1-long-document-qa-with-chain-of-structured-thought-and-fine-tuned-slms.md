---
title: Long-Document QA with Chain-of-Structured-Thought and Fine-Tuned SLMs
title_zh: 基于结构化思维链和微调小型语言模型的长文档问答
authors: "Zhuowen Liang, Xiaotian Lin, Zhengxuan Zhang, Yuyu Luo, Haixun Wang, Nan Tang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29232v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 结构化思考的文档问答
tldr: 针对长文档问答中LLMs推理脆弱且延迟高的问题，本文提出LiteCoST框架，通过结构化思维链(CoST)生成结构化输出和SLM微调技术，使小型语言模型在保持与LLM相当质量的同时，实现2-4倍的延迟降低，为高效可靠的长文档问答提供了新方案。
selection_source: fresh_fetch
motivation: 大型语言模型在长文档直接推理中表现脆弱且延迟高，需要更可靠高效的文档问答方法。
method: 提出LiteCoST框架，结合结构化思维链引导和两阶段SLM微调，实现高质量低延迟的长文档问答。
result: 3B/7B SLMs在多领域长文档问答上达到LLM相当质量，同时比GPT-4o和DeepSeek-R1快2-4倍。
conclusion: 通过结构优先的行为蒸馏，LiteCoST框架实现了高效可靠的长文档问答，为小型模型在复杂任务中的应用提供了新思路。
---

## 摘要
大型语言模型(LLMs)被广泛应用于文档数据分析，然而在长文档、噪声文档上的直接推理仍然脆弱且容易出错。因此，我们研究文档问答(QA)，它将分散的证据整合为结构化输出(如表、图或块)，以支持可靠、可验证的问答。我们提出了一个双支柱框架LiteCoST，旨在使用小型语言模型(SLMs)实现高精度和低延迟。支柱1：结构化思维链(CoST)。我们引入了一个CoST模板，这是一个模式感知的指令，引导强大的LLM生成逐步的CoST轨迹和相应的结构化输出。该过程诱导最小结构，规范化实体/单位，对齐记录，序列化输出，并验证/完善它，产生可审计的监督。支柱2：SLM微调。紧凑模型在两个阶段使用LLM生成的CoST数据进行训练：用于结构对齐的监督微调，随后是结合答案/格式质量和过程一致性三重奖励的组相对策略优化(GRPO)。通过将结构优先行为蒸馏到SLMs中，该方法在使用3B/7B SLMs的多领域长文档问答上实现了与LLM相当的质量，同时比GPT-4o和DeepSeek-R1(671B)提供2-4倍的更低延迟。代码可在https://github.com/HKUSTDial/LiteCoST获取。

## Abstract
Large language models (LLMs) are widely applied to data analytics over documents, yet direct reasoning over long, noisy documents remains brittle and error-prone. Hence, we study document question answering (QA) that consolidates dispersed evidence into a structured output (e.g., a table, graph, or chunks) to support reliable, verifiable QA. We propose a two-pillar framework, LiteCoST, to achieve both high accuracy and low latency with small language models (SLMs). Pillar 1: Chain-of-Structured-Thought (CoST). We introduce a CoST template, a schema-aware instruction that guides a strong LLM to produce both a step-wise CoST trace and the corresponding structured output. The process induces a minimal structure, normalizes entities/units, aligns records, serializes the output, and verifies/refines it, yielding auditable supervision. Pillar 2: SLM fine-tuning. The compact models are trained on LLM-generated CoST data in two stages: Supervised Fine-Tuning for structural alignment, followed by Group Relative Policy Optimization (GRPO) incorporating triple rewards for answer/format quality and process consistency. By distilling structure-first behavior into SLMs, this approach achieves LLM-comparable quality on multi-domain long-document QA using 3B/7B SLMs, while delivering 2-4x lower latency than GPT-4o and DeepSeek-R1 (671B). The code is available at https://github.com/HKUSTDial/LiteCoST.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义

### 研究动机和背景
- **核心问题**：大型语言模型(LLMs)在长文档、噪声文档上的直接推理表现脆弱且容易出错，存在三个主要挑战：(1)证据分散在长文档中，导致遗漏或幻觉；(2)值以异构单位和格式出现，需要规范化；(3)长上下文推理必须在整个结构中保持一致性。
- **应用痛点**：直接使用强大LLM进行文档问答存在高延迟、高计算成本和隐私问题，而小型语言模型(SLMs)难以处理结构化提取所需的复杂技能。
- **整体含义**：研究一种能够将分散证据整合为结构化输出(如表、图或块)的文档问答方法，以支持可靠、可验证的问答，同时保持高精度和低延迟。

## 2. 论文提出的方法论

### 核心思想
- 提出LiteCoST框架，包含两个支柱：(1)结构化思维链(CoST)用于生成高质量结构化输出和可审计轨迹；(2)两阶段SLM微调将结构优先行为转移到紧凑模型。

### 关键技术细节
- **CoST模板**：模式感知指令，引导LLM生成逐步CoST轨迹和相应结构化输出
- **CoST过程四步骤**：
  - 结构分析：动态选择最适合的数据结构并实例化准确模式
  - CoST轨迹生成：采用基于指令的思维链范式，逐步生成轨迹引导模式对齐提取
  - 质量验证：采用LLM-as-Judge方法评估生成结构数据的质量
  - 迭代细化：使用迭代结构化器通过重新生成结构知识来改进低质量样本

- **SLM微调两阶段流程**：
  - 监督微调(SFT)：增强模型处理结构、格式和步骤推理的基本能力
  - 组相对策略优化(GRPO)：引入双重奖励机制，包括结果奖励和过程奖励

- **GRPO优化公式**：
  J_GRPO(θ) = E[v∼P(V), {oi}Gi=1∼πθ_old(O|v)] [1/G * Σ_i=1^G min(clip(ratio_i, 1-ε, 1+ε) * A_i, ratio_i * A_i) - βD_KL(πθ∥πref)]
  其中A_i = r_i - mean(r_1...r_G)/std(r_1...r_G)是组相对优势

- **整体奖励**：格式合规性、答案正确性和过程奖励的总和，并引入轨迹级系数调节过程奖励

## 3. 实验设计

### 数据集/场景
- **训练数据集**：从四个大规模多任务资源构建(FinQA、TAT-QA、SQuAD、LegalBench)，针对金融、法律和通用知识领域
- **评估数据集**：
  - Loong基准：1600个测试样本，跨越三个领域(金融、法律、论文)，四个任务类别(焦点定位、比较、聚类、推理链)，四个文档长度设置
  - LongBench：涵盖单文档和多文档QA任务，包括NarrativeQA、Qasper、HotpotQA和2WikiMultihopQA

### Benchmark
- Loong基准：使用GPT-4o作为自动评分员，从准确性、幻觉和完整性三个维度对模型响应进行0-100分评分
- LongBench：使用标准F1分数评估基于提取信息的推理质量

### 对比方法
- **大型语言模型**：GPT-4o、DeepSeek-R1、GPT-4o-mini、Qwen2.5-14B-Instruct等
- **微调信息提取模型**：ODIE、IEpile、Struc-bench
- **模块化提取框架**：StructRAG
- **提示方法**：Zero-shot、Chain-of-Thought(CoT)、DSPy

## 4. 资源与算力

- **模型部署**：在配备八个NVIDIA RTX 4090 GPU(每个24GB VRAM)的集群上部署和运行3B到14B的模型
- **计算成本**：
  - 数据生成：使用GPT-4o进行结构分析、CoST轨迹生成、质量验证和迭代细化，每个领域约30美元
  - GRPO微调：使用GPT-4o-mini评估结构对齐、格式合规性和答案正确性，每次训练运行额外10美元
  - 总成本约40美元，但一旦训练完成，下游推理仅依赖紧凑的SLMs，消除了重复的LLM调用

## 5. 实验数量与充分性

### 实验数量
- 在Loong基准的金融子集上进行了全面评估，包括不同模型大小、不同文档长度设置
- 进行了消融研究，评估不同奖励设计的影响
- 在法律领域和开放域QA(LongBench)上进行了泛化实验
- 进行了结构数据对LLM推理影响的实验

### 充分性与公平性
- **全面性**：实验涵盖了多个领域(金融、法律、科学)和多种任务类型
- **公平性**：使用了相同的提示模板确保公平比较，包含多种类型的基线模型
- **客观性**：实验结果客观呈现，包括准确率和完美率指标
- **验证充分**：包含详细的消融研究，验证了不同组件的贡献

## 6. 论文的主要结论与发现

### 主要结论
- LiteCoST框架使3B/7B SLMs在多领域长文档问答上实现了与LLM相当的质量
- SLM推理速度比GPT-4o和DeepSeek-R1快2-4倍
- 结构化数据显著提高了LLM在知识密集型推理任务上的性能
- Qwen-LiteCoST在多个任务上超过了GPT-4o，实现了最佳整体性能

### 主要发现
- CoST提示方法显著优于Zero-shot和CoT，特别是在复杂推理任务上
- 过程奖励和结果奖励的结合对模型性能提升至关重要
- 迭代细化模块有效改进了低质量样本
- LiteCoST在长文档处理中表现出强大的鲁棒性，随着文档长度增加，性能下降幅度较小

## 7. 优点：方法或实验设计上有哪些亮点

### 方法亮点
- **结构化思维链(CoST)**：提供了可审计的监督信号，增强了模型的可解释性
- **两阶段微调流程**：有效将结构优先行为转移到SLMs
- **双重奖励机制**：同时优化结果质量和过程一致性
- **动态结构选择**：适应不同查询需求
- **迭代细化模块**：重用低质量样本，提供更丰富的监督

### 实验设计亮点
- **多领域验证**：在金融、法律和科学领域验证了方法的泛化能力
- **全面评估指标**：包括准确率和完美率
- **详细消融研究**：验证了不同组件的贡献
- **多样化基线比较**：从大型语言模型到专门微调的模型
- **文档长度分析**：评估了不同文档长度对性能的影响

## 8. 不足与局限

### 实验覆盖局限
- **领域限制**：主要集中在金融、法律和科学文献领域，在其他领域的泛化能力需要进一步验证
- **文档类型**：虽然测试了不同文档长度，但可能未覆盖所有可能的文档类型和结构
- **语言限制**：对比实验主要集中在英文任务，多语言能力未充分评估

### 偏差风险
- **评估偏差**：依赖GPT-4o作为评估者可能引入评估偏差
- **数据不平衡**：训练数据可能存在领域不平衡，影响模型在特定领域的表现
- **信息损失**：结构化输出可能简化了某些复杂问题，导致信息损失

### 应用限制
- **领域适应**：需要预先训练数据来构建CoST监督信号，对于新领域可能需要额外工作
- **任务适用性**：结构化输出可能不适合开放式叙事性问题
- **资源需求**：计算资源需求虽然比纯LLM方法低，但仍需要一定的计算资源进行训练
- **性能权衡**：在某些任务中(如焦点定位和推理链)，结构化数据可能导致性能轻微下降

（完）
