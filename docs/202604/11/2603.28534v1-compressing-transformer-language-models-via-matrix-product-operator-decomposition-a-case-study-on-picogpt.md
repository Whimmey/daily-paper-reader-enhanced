---
title: "Compressing Transformer Language Models via Matrix Product Operator Decomposition: A Case Study on PicoGPT"
title_zh: 基于矩阵积算子分解的Transformer语言模型压缩：PicoGPT案例研究
authors: "Younes Javanmard, Tanmoy Pandit, Masoud Mardani"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28534v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 通过矩阵算子分解压缩Transformer模型
tldr: "本研究提出使用矩阵算子分解(MPO)方法压缩Transformer语言模型，通过将权重矩阵分解为低秩核心链实现高效压缩。在PicoGPT模型上的实验表明，MPO压缩可实现高达13倍的压缩率，在chi=16时仅使用191,872参数(原1,022,224)即可保留97.7%的基线准确率，为资源受限设备部署大型语言模型提供了实用解决方案。"
selection_source: carryover_cache
motivation: Transformer语言模型的二次参数增长使其在资源受限硬件上的部署成本高昂，需要有效的压缩方法。
method: 使用矩阵算子分解(MPO)将PicoGPT模型中的所有线性层替换为MPOLinear模块，通过键维度控制压缩率和模型性能。
result: "MPO压缩实现了高达13倍的压缩率，在chi=16时模型参数减少81%的同时保留了97.7%的基线准确率，chi=8模型在参数效率上表现最佳。"
conclusion: MPO参数化为Transformer模型提供了一种理论上合理且实用的压缩替代方案，优于传统的低秩方法和非结构化剪枝。
---

## 摘要
基于Transformer的语言模型在NLP任务上表现出色，但其参数随隐藏维度呈二次方增长，使得在资源受限硬件上的部署成本高昂。我们研究矩阵积算子(MPO)分解作为Transformer的系统性压缩方法。MPO将权重矩阵分解为低秩核心链，近似质量由键维度χ控制。我们将PicoGPT（一个约100万参数的GPT-2风格字符级语言模型）中的每个nn.Linear层替换为参数化为MPO链的MPOLinear模块。核心通过预训练密集权重的TT-SVD或随机初始化进行初始化，并使用标准PyTorch autograd进行训练，无需自定义反向传播。我们为PicoGPT中五种不同的权重形状推导了平衡分解方案，并在Tiny Shakespeare上评估了键维度χ∈{4,8,16,32}。在χ=4时，MPO压缩每个Transformer块可实现高达13倍的压缩。在χ=16时，模型使用191,872个参数而非1,020,224个，同时保留了97.7%的基线标记准确率（51.6%对比52.8%）。重构误差遵循预期趋势，在相同键维度下，三站点分解比两站点分解的误差更低。χ=8的模型在每参数准确率方面表现最佳，在该指标上超过密集基线2.7倍。这些结果表明，MPO参数化是Transformer压缩中低秩方法和非结构化剪枝的一种实用且理论充分的替代方案。

## Abstract
Transformer-based language models achieve strong performance across NLP tasks, but their quadratic parameter scaling with hidden dimension makes deployment on resource-constrained hardware expensive. We study Matrix Product Operator (MPO) decomposition as a principled compression method for transformers. MPO factorises weight matrices into chains of low-rank cores, with approximation quality controlled by the bond dimension chi. We replace every nn.Linear layer in PicoGPT, a GPT-2-style character-level language model with about 1M parameters, with an MPOLinear module parameterised as an MPO chain. Cores are initialised either by TT-SVD from pretrained dense weights or from random initialisation, and trained using standard PyTorch autograd without a custom backward pass. We derive balanced factorisation schemes for the five distinct weight shapes in PicoGPT and evaluate bond dimensions chi in {4, 8, 16, 32} on Tiny Shakespeare. MPO compression achieves up to 13x compression per transformer block at chi = 4. At chi = 16, the model uses 191,872 parameters instead of 1,020,224 while retaining 97.7% of baseline token accuracy (51.6% vs 52.8%). Reconstruction error follows the expected trend and is lower for three-site than two-site factorisations at the same bond dimension. The chi = 8 model gives the best accuracy per parameter, exceeding the dense baseline by 2.7x on this metric. These results show that MPO parameterisation is a practical and theoretically grounded alternative to low-rank methods and unstructured pruning for transformer compression.

---

## 论文详细总结（自动生成）

# 论文总结：基于矩阵积算子分解的Transformer语言模型压缩

## 1. 论文的核心问题与整体含义

- **核心问题**：Transformer语言模型的参数量随隐藏维度呈二次方增长，导致在资源受限硬件上的部署成本高昂。
- **研究背景**：现有压缩方法（如剪枝、量化和低秩分解）对所有权重结构进行均匀处理，对近似误差的控制有限。
- **整体含义**：论文提出使用矩阵积算子(MPO)分解作为Transformer的系统性压缩方法，这是一种源自量子物理学的张量网络技术，能够提供理论上有依据且可解释的压缩方案。

## 2. 论文提出的方法论

- **核心思想**：将PicoGPT模型中的每个nn.Linear层替换为MPOLinear模块，将权重矩阵参数化为MPO链，通过键维度χ控制压缩率和模型性能。
- **关键技术细节**：
  - 使用TT-SVD算法将密集权重转换为MPO核心
  - 为PicoGPT中五种不同的权重形状推导了平衡分解方案
  - 核心作为标准nn.Parameter存储，使用PyTorch autograd自动处理梯度流
  - 初始化采用启发式尺度σ = N^(-1/4)_in χ^(-(L-1)/(2L))，确保重构权重与密集基线初始化具有相同量级的方差
- **算法流程**：
  1. 将权重矩阵重塑为交错张量
  2. 通过顺序SVD扫描转换为MPO
  3. 在前向传播中通过张量收缩重构完整权重矩阵
  4. 反向传播通过自动微分处理梯度流

## 3. 实验设计

- **数据集**：Tiny Shakespeare字符级语言数据集，约110万个字符，词汇量V=65
- **评估指标**：参数压缩比、重构误差(Frobenius范数)、标记准确率
- **对比方法**：
  - 密集基线模型（1,020,224参数）
  - 不同键维度χ∈{4,8,16,32}的MPO模型
  - 两站点(L=2)与三站点(L=3)分解方案对比
- **训练设置**：AdamW优化器，余弦学习率调度，批量大小32，序列长度256，训练2000步

## 4. 资源与算力

- 论文中未明确提及使用的GPU型号、数量或训练时长等算力信息
- 仅提到使用PyTorch框架实现，但未提供具体的硬件配置信息

## 5. 实验数量与充分性

- **实验数量**：进行了多组实验，包括不同键维度的比较、训练动态分析、重构误差分析等
- **充分性评估**：
  - ✅ 实验设计较为全面，覆盖了不同压缩率和分解方案
  - ❌ 缺乏多种子平均和标准差等稳定性评估
  - ❌ 仅在小规模PicoGPT模型上验证，未在大型模型上测试
  - ⚠️ 实验对比主要与自身不同设置对比，缺乏与其他压缩方法的直接对比

## 6. 论文的主要结论与发现

- **压缩效果**：MPO压缩可实现高达13倍的压缩率，在χ=4时每个Transformer块压缩13倍
- **性能保持**：在χ=16时，模型使用191,872参数（减少81%）的同时保留了97.7%的基线标记准确率（51.6%对比52.8%）
- **误差特性**：重构误差随键维度增加而系统性降低，相同键维度下三站点分解比两站点分解误差更低
- **参数效率**：χ=8的模型在每参数准确率上表现最佳，超过密集基线2.7倍
- **训练动态**：较大键维度的模型收敛更快，达到更高的最终验证准确率

## 7. 优点

- **理论依据**：MPO分解提供了理论上合理的压缩框架，具有明确的误差控制机制
- **实现简单**：与标准PyTorch训练流程完全兼容，无需自定义反向传播
- **可解释性**：键维度提供了直观的精度-压缩权衡控制
- **开源实现**：提供了完整的PyTorch实现和训练代码
- **灵活性**：支持从随机初始化开始训练，也支持先训练密集模型再压缩微调

## 8. 不足与局限

- **模型规模限制**：仅在小型PicoGPT模型上验证，未在大型模型如GPT-2或LLaMA上测试
- **实现局限**：当前实现主要展示参数压缩，未实现推理时的内存和FLOP节省
- **实验覆盖不足**：缺乏与其他压缩方法（如LoRA、剪枝）的直接对比
- **稳定性评估不足**：未提供多种子平均和标准差等稳定性评估
- **压缩范围有限**：仅压缩了线性层，未压缩嵌入表、层归一化参数和偏置项
- **计算效率问题**：前向传播中重构完整权重矩阵，未利用MPO结构的计算优势

（完）
