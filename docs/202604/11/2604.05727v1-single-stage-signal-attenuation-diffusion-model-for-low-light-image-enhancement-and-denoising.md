---
title: Single-Stage Signal Attenuation Diffusion Model for Low-Light Image Enhancement and Denoising
title_zh: 单阶段信号衰减扩散模型用于低光照图像增强与去噪
authors: "Ying Liu, Junchao Zhang, Caiyun Wu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05727v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 图像修复扩散
tldr: 扩散模型在低光照图像增强方面表现优异，但现有方法采用两阶段流程或辅助校正网络，导致增强与去噪分离。本文提出信号衰减扩散模型(SADM)，将信号衰减机制整合到扩散流程中，实现单阶段同时进行亮度调整和噪声抑制，编码低光照退化物理先验，无需额外校正模块，平衡了可解释性、恢复质量和计算效率。
selection_source: fresh_fetch
motivation: 现有基于扩散的低光照图像增强方法采用两阶段流程或辅助校正网络，导致增强与去噪分离，优化目标不一致，性能受限。
method: 提出信号衰减扩散模型(SADM)，将信号衰减机制整合到扩散流程中，模拟低光照退化固有信号衰减，实现单阶段同时进行亮度调整和噪声抑制。
result: SADM通过多尺度金字塔采样验证了与DDIM的一致性，平衡了可解释性、恢复质量和计算效率，消除了对额外校正模块或分阶段训练的需求。
conclusion: 信号衰减扩散模型通过整合信号衰减机制，实现了低光照图像增强与去噪的单阶段统一处理，提高了恢复质量并简化了流程。
---

## 摘要
扩散模型通过前向噪声添加和反向去噪的概率建模在图像恢复方面表现出色，它们在处理复杂噪声的同时保留细节的能力使其非常适合低光照图像增强(LLIE)。主流的基于扩散模型的低光照图像增强方法要么采用两阶段流程，要么使用辅助校正网络来优化U-Net的输出，这切断了增强与去噪之间的内在联系，并由于优化目标不一致导致次优性能。为解决这些问题，我们提出了信号衰减扩散模型(SADM)，这是一种将信号衰减机制整合到扩散流程中的新型扩散过程，能够在单阶段内实现亮度调整和噪声抑制的同步进行。具体而言，信号衰减系数在前向噪声添加过程中模拟了低光照退化的固有信号衰减，编码了低光照退化的物理先验，以明确引导反向去噪朝向亮度恢复和噪声抑制的并发优化，从而消除了现有方法所依赖的额外校正模块或分阶段训练的需求。我们通过多尺度金字塔采样验证了我们的设计与去噪扩散隐模型(DDIM)的一致性，平衡了可解释性、恢复质量和计算效率。

## Abstract
Diffusion models excel at image restoration via probabilistic modeling of forward noise addition and reverse denoising, and their ability to handle complex noise while preserving fine details makes them well-suited for Low-Light Image Enhancement (LLIE). Mainstream diffusion based LLIE methods either adopt a two-stage pipeline or an auxiliary correction network to refine U-Net outputs, which severs the intrinsic link between enhancement and denoising and leads to suboptimal performance owing to inconsistent optimization objectives. To address these issues, we propose the Signal Attenuation Diffusion Model (SADM), a novel diffusion process that integrates the signal attenuation mechanism into the diffusion pipeline, enabling simultaneous brightness adjustment and noise suppression in a single stage.   Specifically, the signal attenuation coefficient simulates the inherent signal attenuation of low-light degradation in the forward noise addition process, encoding the physical priors of low-light degradation to explicitly guide reverse denoising toward the concurrent optimization of brightness recovery and noise suppression, thereby eliminating the need for extra correction modules or staged training relied on by existing methods. We validate that our design maintains consistency with Denoising Diffusion Implicit Models(DDIM) via multi-scale pyramid sampling, balancing interpretability, restoration quality, and computational efficiency.

---

## 论文详细总结（自动生成）

# 论文总结：单阶段信号衰减扩散模型用于低光照图像增强与去噪

## 1. 论文的核心问题与整体含义

### 研究动机和背景
- **核心问题**：现有基于扩散模型(low-light image enhancement, LLIE)的方法采用两阶段流程或辅助校正网络，导致增强与去噪分离，切断了两者间的内在联系，由于优化目标不一致导致次优性能。
- **应用背景**：低光照图像增强在夜间监控、自动驾驶和遥感等关键应用中至关重要，但面临亮度不足、噪声明显和细节丢失的挑战，严重影响下游计算机视觉任务性能。
- **技术现状**：传统基于Retinex理论的方法常在噪声区域引入视觉伪影和色彩失真；深度学习方法虽取得进展，但大多采用多阶段设计，将亮度和去噪分解为顺序子任务，增加了模型复杂度并阻碍了协同优化。

## 2. 论文提出的方法论

### 核心思想
- 提出信号衰减扩散模型(SADM)，将信号衰减机制整合到扩散流程中，实现单阶段同时进行亮度调整和噪声抑制。
- 通过编码低光照退化的物理先验，明确引导反向去噪朝向亮度恢复和噪声抑制的并发优化，消除对额外校正模块或分阶段训练的需求。

### 关键技术细节
- **前向扩散过程**：引入衰减系数kt，设计为xt = kt(atxt-1 + btεt)，其中εt ~ N(0, I)，kt = 0.999 × kt-1(k0 = 1)，模拟低光照退化的固有信号衰减。
- **数学框架**：通过严格数学推导建立前向和反向过程的统一框架，确保亮度和去噪任务在单阶段内协同优化。
- **网络架构**：采用SR3Unet作为基础网络，输入包含低光照图像、位置编码和去雾先验条件，输出预测噪声。
- **采样策略**：结合多尺度金字塔采样和DDIM采样提高推理效率，采用差异化迭代策略处理跨尺度恢复。
- **损失函数**：设计包含GTmeanLoss和PerceptionLoss的复合损失函数，并引入亮度对齐损失和感知损失确保视觉一致性。

### 算法流程
- **训练阶段**：根据训练时间步自适应选择图像尺度，生成对应尺度的噪声图像，将噪声图像、时间步信息和图像条件输入SR3Unet网络，通过复合损失函数进行约束。
- **采样阶段**：从标准高斯分布生成噪声图，应用DDIM采样算法迭代恢复图像，总迭代步数为10，步跨不同尺度分配为[1,1,1,2,2,2,4,4,4,4]。

## 3. 实验设计

### 数据集与场景
- **主要数据集**：
  - LOLv1 (485训练/15测试对)
  - LOLv2-Real (689/100)
  - LOLv2-Synthetic (900/100)
- **泛化测试数据集**：DICM, NPE, MEF等其他公共低光照数据集
- **场景覆盖**：包含室内、室外、不同光照条件和噪声水平的低光照图像

### 评估基准
- **定量指标**：PSNR(峰值信噪比)和SSIM(结构相似性指数)
- **定性评估**：视觉效果对比，包括局部放大视图展示细节恢复和噪声抑制效果

### 对比方法
- **传统方法**：Zero-DCE
- **基于Retinex的方法**：URetinexNet (UR-Net), Retinexformer (RF)
- **基于深度学习的方法**：LLFormer (LLF)
- **基于扩散模型的方法**：PyDiff, GSAD, Wavelet-Based Diffusion Models (WBDM), FourierDiff (FD), LightenDiffusion (LightenD), AnlightenDiff (AnlightenD), CLODE, Diff-Retinex-Plus (DRP)

## 4. 资源与算力

- **硬件配置**：NVIDIA GeForce RTX 4090 D GPU (24.5 GB VRAM)
- **训练设置**：
  - 单GPU训练
  - 批处理大小：8
  - 数据加载工作线程：4
  - 总训练迭代次数：1,000,000次
- **优化器**：Adam优化器
  - 生成器学习率：1e-4
  - 判别器和辅助组件学习率：0.002
  - 学习率衰减策略：在50k、75k、100k、150k和200k次迭代时衰减0.5倍
- **训练时长**：完整训练运行约需3天完成

## 5. 实验数量与充分性

### 实验数量
- **主要数据集测试**：在LOLv1、LOLv2-Real和LOLv2-Syn三个主要数据集上进行全面评估
- **泛化能力测试**：在多个其他公共低光照数据集上评估模型泛化能力
- **消融实验**：进行5组消融实验验证各组件有效性
- **对比实验**：与12种前沿方法进行定量和定性对比

### 充分性与客观性
- **优势**：
  - 实验覆盖多种类型的低光照图像增强方法
  - 使用标准评估指标(PSNR和SSIM)和视觉质量评估
  - 进行详细的消融研究验证各组件贡献
  - 提供丰富的可视化结果对比，包括局部放大视图
- **局限性**：
  - 可能缺乏对极端低光照场景的专门测试
  - 未充分探索模型在不同传感器类型下的表现
  - 对不同噪声类型的鲁棒性测试可能不够全面

## 6. 论文的主要结论与发现

### 主要结论
- SADM在PSNR和SSIM指标上显著优于所有比较方法
- 在LOLv1上达到最高PSNR(28.0987)和SSIM(0.8770)
- 在LOLv2-Real上PSNR(28.9342)排名第一，SSIM(0.8797)略低于GSAD
- 在LOLv2-Syn上，SADM仍具有竞争力的PSNR(26.7045)和SSIM(0.9224)
- SADM在保持高质量恢复的同时，实现了单阶段处理，简化了模型结构

### 主要发现
- 信号衰减机制有效模拟了低光照退化过程，使扩散过程更符合物理实际
- 单阶段框架能够同时优化亮度和去噪任务，避免了多阶段方法的目标不一致问题
- 多尺度金字塔采样与DDIM采样结合显著提高了推理效率
- 去雾预处理作为先验条件有助于提高模型性能，特别是在暗区特征提取和色彩保真方面

## 7. 优点

### 方法设计优点
- **创新性**：首次将信号衰减机制整合到扩散模型中，实现单阶段低光照增强与去噪
- **理论严谨性**：数学推导严谨，建立了前向和反向过程的统一框架
- **效率优化**：多尺度金字塔采样与DDIM采样结合，在保持质量的同时提高推理效率
- **先验设计**：去雾预处理作为先验条件有效提高了模型性能，特别是在暗区特征提取方面

### 实验设计优点
- **全面性**：在多个标准数据集上进行了全面评估，覆盖不同类型的低光照场景
- **多维度评估**：同时使用定量指标(PSNR、SSIM)和定性评估(视觉效果对比)
- **可复现性**：提供了详细的训练配置和参数设置，便于结果复现
- **对比充分**：与多种前沿方法进行对比，证明方法优越性

## 8. 不足与局限

### 实验覆盖局限
- **场景覆盖**：可能缺乏对极端低光照场景的专门测试
- **传感器多样性**：未充分探索模型在不同传感器类型下的表现
- **噪声类型**：对不同噪声类型的鲁棒性测试可能不够全面

### 偏差风险
- **数据偏差**：训练数据可能存在特定场景偏差，影响模型泛化能力
- **评估指标**：PSNR和SSIM等指标可能不完全反映视觉质量
- **文化地域**：未充分探索模型在不同文化、地域场景下的表现

### 应用限制
- **模型复杂度**：参数量较大(97.8M)，限制在资源受限设备上的部署
- **训练成本**：训练时间较长(约3天)，增加了应用门槛
- **推理速度**：虽然采用DDIM加速，但推理速度可能仍不如轻量级方法
- **特殊场景**：对某些特殊低光照场景(如强光源存在)的处理效果可能有限

（完）
