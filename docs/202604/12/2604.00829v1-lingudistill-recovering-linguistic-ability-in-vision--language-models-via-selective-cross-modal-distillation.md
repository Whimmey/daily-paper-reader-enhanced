---
title: "LinguDistill: Recovering Linguistic Ability in Vision- Language Models via Selective Cross-Modal Distillation"
title_zh: "LinguDistill: 通过选择性跨模态蒸馏恢复视觉-语言模型中的语言能力"
authors: "Patrick Amadeus Irawan, Erland Hilman Fuadi, Shanu Kumar, Alham Fikri Aji, Yova Kementchedjhieva"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00829v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 视觉语言模型语言能力恢复方法
tldr: "视觉语言模型(VLM)的适配会导致预训练语言模型(LM)的原生语言能力下降。LinguDistill提出了一种无适配器的蒸馏方法，通过层级KV缓存共享利用冻结LM作为教师，选择性蒸馏语言信号，恢复约10%的语言性能，同时保持视觉任务表现，为多模态模型中的模态退化提供高效解决方案。"
selection_source: carryover_cache
motivation: 将预训练语言模型适配为视觉语言模型会导致原生语言能力下降，且难以通过标准方法恢复。
method: LinguDistill是一种无适配器的蒸馏方法，通过层级KV缓存共享利用冻结LM作为教师，选择性蒸馏语言信号以恢复语言能力。
result: "LinguDistill在语言和知识基准上恢复了约10%的损失性能，同时在视觉密集型任务上保持了可比的性能。"
conclusion: 语言能力可以在不添加额外模块的情况下恢复，为多模态模型中的模态特定退化提供了高效且实用的解决方案。
---

## 摘要
将预训练语言模型(LMs)适配为视觉-语言模型(VLMs)可能会由于其原生语言能力在多模态适应过程中因表示转换和跨模态干扰而退化。即使使用标准目标进行有针对性的任务特定微调，这种损失也难以恢复。先前的恢复方法通常引入额外的模块作为中间对齐层，以维持或隔离模态特定的子空间，这增加了架构复杂性，在推理时增加了参数，并限制了跨模型和设置的灵活性。我们提出了LinguDistill，一种无适配器的蒸馏方法，通过利用原始冻结的LM作为教师来恢复语言能力。我们通过引入逐层KV缓存共享来克服实现视觉条件教师监督的关键挑战，这使教师能够接触到学生的多模态表示，而无需修改任一模型的架构。然后，我们在语言密集型数据上有选择地蒸馏教师的强语言信号以恢复语言能力，同时保持学生在多模态任务上的视觉基础。因此，LinguDistill恢复了语言和知识基准测试中约10%的性能损失，同时在视觉密集型任务上保持了可比的性能。我们的研究结果表明，语言能力可以在不增加额外模块的情况下得到恢复，为多模态模型中的模态特定退化提供了一种高效且实用的解决方案。

## Abstract
Adapting pretrained language models (LMs) into vision-language models (VLMs) can degrade their native linguistic capability due to representation shift and cross-modal interference introduced during multimodal adaptation. Such loss is difficult to recover, even with targeted task-specific fine-tuning using standard objectives. Prior recovery approaches typically introduce additional modules that act as intermediate alignment layers to maintain or isolate modality-specific subspaces, which increases architectural complexity, adds parameters at inference time, and limits flexibility across models and settings. We propose LinguDistill, an adapter-free distillation method that restores linguistic capability by utilizing the original frozen LM as a teacher. We overcome the key challenge of enabling vision-conditioned teacher supervision by introducing layer-wise KV-cache sharing, which exposes the teacher to the student's multimodal representations without modifying the architecture of either model. We then selectively distill the teacher's strong linguistic signal on language-intensive data to recover language capability, while preserving the student's visual grounding on multimodal tasks. As a result, LinguDistill recovers $\sim$10% of the performance lost on language and knowledge benchmarks, while maintaining comparable performance on vision-heavy tasks. Our findings demonstrate that linguistic capability can be recovered without additional modules, providing an efficient and practical solution to modality-specific degradation in multimodal models.

---

## 论文详细总结（自动生成）

# LinguDistill 论文总结

## 1. 论文的核心问题与整体含义
- **核心问题**：将预训练语言模型(LMs)适配为视觉-语言模型(VLMs)会导致原生语言能力下降，这种下降是由于多模态适应过程中的表示转换和跨模态干扰造成的。
- **研究背景**：即使使用标准目标进行有针对性的任务特定微调，这种损失也难以恢复。先前的恢复方法通常引入额外的模块作为中间对齐层，增加了架构复杂性，在推理时增加了参数，限制了跨模型和设置的灵活性。
- **整体含义**：研究如何在不增加额外模块的情况下恢复VLMs的语言能力，解决多模态模型中的模态特定退化问题。

## 2. 论文提出的方法论
- **核心思想**：LinguDistill是一种无适配器的蒸馏方法，利用原始冻结的LM作为教师来恢复语言能力。
- **关键技术细节**：
  - **层级KV缓存共享机制**：使教师能够接触到学生的多模态表示，无需修改任一模型的架构
  - **选择性蒸馏**：在语言密集型数据上有选择地蒸馏教师的强语言信号，同时保持学生在多模态任务上的视觉基础
- **算法流程**：
  1. 学生解码器处理完整的多模态序列并生成KV缓存
  2. 教师解码器重新处理相同的文本提示以形成查询状态
  3. 教师直接重用学生每一层的KV缓存
  4. 教师使用其计算的查询在传输的学生记忆上关注
  5. 使用选择性蒸馏目标，根据数据源应用不同的权重

## 3. 实验设计
- **数据集/场景**：
  - 使用The Cauldron数据集，包含50个VL指令调整数据集
  - 保留17个核心来源，涵盖VQA、OCR/文档、知识和领域特定任务的平衡混合
  - 定义8个语言密集型VL数据集的子集用于数据消融
- **Benchmark**：
  - 语言和知识密集型基准：ARC Easy和Challenge、HellaSwag、ScienceQA、COCO 2017 captioning
  - 文档、OCR和视觉特定基准：DocVQA、InfographicVQA、OCRBench、RealWorldQA
  - 一般多模态基准：MMMU、MMStar、MME感知和认知、AI2D
- **对比方法**：
  - Baselines：nanoVLM（预训练检查点）、nanoVLM-full（所有源微调）、nanoVLM-lang（语言子集微调）
  - Uniform KD：distill-full（所有源KD）、distill-lang（语言子集KD）
  - Selective KD：LinguDistill（选择性KD）、LinguDistill-highKD（更高教师参与度）、LinguDistill-lowKD（更低教师参与度）

## 4. 资源与算力
- 使用RTX A6000 PRO (96 GB) GPU
- 有效批大小为128
- 使用BF16精度
- 训练约380M token到4000步，使用余弦LR调度器
- 所有实验在单个RTX A6000 PRO GPU上完成

## 5. 实验数量与充分性
- **实验数量**：设计了10个运行变体，作为基线、方法验证和进一步的数据和超参数消融
- **充分性**：
  - 实验覆盖了多种任务类型（语言密集型、视觉密集型、一般多模态）
  - 进行了详细的消融研究，包括数据子集、选择性蒸馏和蒸馏超参数
  - 对比了多种基线方法，包括标准微调和均匀蒸馏
  - 实验设计客观公平，所有变体共享相同的基线模型和训练设置

## 6. 论文的主要结论与发现
- **主要结论**：
  - LinguDistill在语言和知识基准上恢复了约10%的性能损失
  - 在视觉密集型任务上保持了可比的性能
  - 语言能力可以在不增加额外模块的情况下恢复
- **主要发现**：
  - 标准微调会导致语言能力显著下降（COCO captioning下降15.9%，MME认知下降24.2%）
  - 仅在语言密集型数据上训练不能完全恢复语言能力
  - 均匀蒸馏会损害视觉任务（OCRBench下降40.5%）
  - 选择性蒸馏实现了最佳的语言恢复，同时限制了视觉退化
  - 教师实际上转移了知识和推理能力，而不仅仅是语言模式

## 7. 优点
- **方法亮点**：
  - 无适配器设计，不增加额外参数或推理成本
  - 层级KV缓存共享机制，使冻结LM能够访问多模态上下文
  - 选择性蒸馏策略，根据任务类型调整蒸馏信号
  - 简单而有效，训练后教师被移除，只留下改进的VLM
- **实验设计亮点**：
  - 全面的评估框架，涵盖多种任务类型
  - 详细的消融研究，验证了各个组件的重要性
  - 对失败模式的深入分析，提供了对模型行为的见解
  - 公平的对比实验，所有变体共享相同的基线和训练设置

## 8. 不足与局限
- **实验覆盖**：
  - 仅使用单一图像和1024个token的序列长度限制，可能影响长文档理解性能
  - 实验规模相对较小，仅在一个RTX A6000 PRO GPU上进行
- **偏差风险**：
  - 可能存在数据集偏差，主要使用英文数据集
  - 仅评估了特定模型架构（nanoVLM-460M-8k）
- **应用限制**：
  - OCR任务中存在性能下降，特别是在关键信息提取和文档导向VQA任务中
  - 模型在某些情况下会将文本修正为真实单词，即使原始文本不是真实单词
  - 模型有时会从错误的位置读取文本或进行释义而非精确复制
  - MMMU基准测试表现下降，归因于单图像、1024-token训练约束而非蒸馏目标本身

（完）
