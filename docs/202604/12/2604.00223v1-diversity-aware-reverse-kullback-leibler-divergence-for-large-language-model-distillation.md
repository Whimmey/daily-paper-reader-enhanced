---
title: Diversity-Aware Reverse Kullback-Leibler Divergence for Large Language Model Distillation
title_zh: 面向大语言模型蒸馏的多样性感知反向Kullback-Leibler散度
authors: "Hoang-Chau Luong, Dat Ba Tran, Lingwei Chen"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00223v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 新颖的大模型蒸馏方法
tldr: 本研究针对大型语言模型蒸馏中反向Kullback-Leibler(RKL)散度导致的过度自信预测和输出多样性不足问题，提出了一种多样性感知RKL(DRKL)方法。通过分析RKL梯度组成，DRKL消除了非目标梯度对目标logit的持续推动，同时加强非目标监督，保留了RKL的优化优势。实验证明DRKL在各种场景下均优于现有方法，实现了更好的性能和保真度-多样性权衡。
selection_source: carryover_cache
motivation: RKL散度在LLM蒸馏中存在导致过度自信预测和输出多样性不足的问题。
method: 提出多样性感知RKL(DRKL)，通过消除非目标梯度效应并加强非目标监督来解决RKL的局限性。
result: DRKL在各种数据集和模型家族上均优于现有蒸馏方法，实现了更好的性能和保真度-多样性权衡。
conclusion: DRKL通过改进RKL的梯度机制，有效提升了大型语言模型蒸馏的效果和多样性。
---

## 摘要
反向Kullback-Leibler (RKL)散度最近已成为大语言模型(LLM)蒸馏的首选目标，持续优于前向KL(FKL)，特别是在具有大词汇量和显著师生能力差距的情况下，其中RKL专注于主导模式的学习而非强制密集对齐。然而，RKL引入了一个结构性限制，使学生模型倾向于过度自信的预测。我们首先通过将RKL的梯度分解为目标和非目标组件来分析RKL，并表明即使当学生模型已经匹配教师模型时，非目标梯度也会持续推动目标logit上升，从而降低输出多样性。此外，RKL对非目标类别提供弱监督，导致尾部对齐不佳。为解决这些问题，我们提出了多样性感知RKL(DRKL)，它消除了这种梯度效应，同时加强了非目标监督并保留了RKL的优化优势。跨数据集和模型族的广泛实验表明，DRKL持续优于FKL、RKL和其他最先进的蒸馏目标，实现了更好的性能和优越的保真度-多样性权衡。

## Abstract
Reverse Kullback-Leibler (RKL) divergence has recently emerged as the preferred objective for large language model (LLM) distillation, consistently outperforming forward KL (FKL), particularly in regimes with large vocabularies and significant teacher-student capacity mismatch, where RKL focuses learning on dominant modes rather than enforcing dense alignment. However, RKL introduces a structural limitation that drives the student toward overconfident predictions. We first provide an analysis of RKL by decomposing its gradients into target and non-target components, and show that non-target gradients consistently push the target logit upward even when the student already matches the teacher, thereby reducing output diversity. In addition, RKL provides weak supervision over non-target classes, leading to poor tail alignment. To address these issues, we propose Diversity-aware RKL (DRKL), which removes this gradient effect and strengthens non-target supervision while preserving the optimization benefits of RKL. Extensive experiments across datasets and model families demonstrate that DRKL consistently outperforms FKL, RKL, and other state-of-the-art distillation objectives, achieving better performance and a superior fidelity-diversity trade-off.

---

## 论文详细总结（自动生成）

# 论文总结：面向大语言模型蒸馏的多样性感知反向Kullback-Leibler散度

## 1. 论文的核心问题与整体含义

论文针对大语言模型(LLM)蒸馏中反向Kullback-Leibler (RKL)散度存在的结构性限制问题展开研究。RKL散度虽已成为LLM蒸馏的首选目标，在大词汇量和显著师生能力差距情况下优于前向KL(FKL)，但它会导致学生模型产生过度自信的预测和输出多样性不足。具体表现为：非目标梯度持续推动目标logit上升，即使学生已匹配教师也会降低输出多样性；同时RKL对非目标类别提供弱监督，导致尾部对齐不佳。研究动机是解决这些局限性，同时保留RKL的优化优势。

## 2. 论文提出的方法论

### 核心思想
通过分析RKL梯度组成，发现非目标梯度对目标logit有持续向上的推动力，导致过度自信。DRKL方法消除这种梯度效应，同时加强非目标监督。

### 关键技术细节
- 将RKL分解为目标项(TRKL)和非目标项(NRKL)
- DRKL保留RKL在目标类别的二元概率上的公式，但在非目标项中用固定超参数γ替换(1-qm)权重
- 公式：L_DRKL = DKL(˜q_m∥˜p_m) + γ DKL(ˆq∥ˆp)
- 其中m表示目标token，ˆp和ˆq表示归一化的非目标分布
- 通过去除非目标梯度对目标logit的影响，避免过度自信同时保留RKL的优化优势

## 3. 实验设计

### 数据集/场景
- 训练数据集：databricks-dolly-15k（14k训练样本，500验证，500测试）
- 评估基准：Dolly验证集、Self-Instruct、Vicuna Eval、Super-Natural Instructions (Super-NI)和Unnatural Instructions (UnNI)

### 对比方法
- 基线方法：FKL（前向KL）、RKL（反向KL）、Sym-KL（对称KL）
- 最先进方法：DKD（解耦知识蒸馏）、Jensen-Shannon散度(JS)、倾斜FKL(SFKL)、倾斜RKL(SRKL)、自适应KL(AKL)、α-β散度(AB)

## 4. 资源与算力

论文未明确提及使用的GPU型号、数量或训练时长等算力信息。实验设置中提到使用batch size为32，训练20个epoch，学习率设置（GPT-2为5×10^-4，OPT为5×10^-5），但未提供具体硬件资源信息。

## 5. 实验数量与充分性

### 实验数量
- 多个模型对实验：GPT-2 XL(1.5B)→GPT-2 Base(0.1B)、GPT-2 XL(1.5B)→GPT-2 Medium(0.3B)、OPT-6.7B→OPT-1.3B
- 5个评估数据集，每个实验使用5个随机种子计算平均值和标准差
- 消融研究：分析超参数γ∈{0.5,1.0,...,4.0}的影响
- 效率分析：比较不同方法的计算时间和性能权衡

### 充分性评估
- 实验覆盖多种模型规模和数据集，具有较好的多样性
- 使用多个评估指标：ROUGE-L（保真度）、Distinct-2、Negative Self-BLEU（多样性）
- 提供详细的消融研究和超参数分析
- 包含定性案例分析，直观展示方法优势
- 实验设计公平，所有方法使用相同训练设置、超参数和初始化

## 6. 论文的主要结论与发现

### 主要结论
- DRKL在各种数据集和模型家族上均优于现有蒸馏方法
- DRKL实现了更好的性能和保真度-多样性权衡
- DRKL能够减少过度自信的预测，同时保持或提高性能

### 主要发现
- RKL在大词汇量和师生能力差距情况下优于FKL，是因为它简化了对齐问题，专注于主导模式
- RKL的非目标梯度持续推动目标logit上升，导致过度自信和多样性降低
- DRKL通过消除这种梯度效应，同时加强非目标监督，有效解决了RKL的局限性
- DRKL与SRKL等RKL变体结合使用可进一步提升性能

## 7. 优点

### 方法亮点
- 理论分析深入，通过梯度分解揭示了RKL的结构性限制
- 方法设计简洁有效，只需修改RKL中的一个权重因子
- 保留了RKL的优化优势，同时解决了其局限性
- 可与现有RKL变体结合使用，进一步提升性能

### 实验设计亮点
- 使用多个基准数据集进行评估，确保结果的鲁棒性
- 同时考虑性能和多样性指标，全面评估方法效果
- 提供了详细的消融研究和超参数分析
- 包含定性案例分析，直观展示方法优势
- 验证了DRKL的效率和计算成本优势

## 8. 不足与局限

### 实验覆盖局限
- 实验主要集中在指令跟随任务，可能无法完全推广到其他类型的NLP任务
- 未探索DRKL在不同规模模型蒸馏中的表现极限
- 缺乏对多语言或跨语言蒸馏效果的评估

### 应用限制
- 未研究DRKL在特定领域或专业领域蒸馏中的效果
- 缺乏对计算资源消耗的详细分析，可能影响实际应用评估
- 虽然DRKL改善了多样性，但与教师模型相比仍有差距，可能需要进一步改进

### 偏差风险
- 实验主要基于英文数据集，可能存在语言偏差
- 评估指标主要集中在ROUGE-L等，可能无法完全反映生成质量
- 案例分析数量有限，可能无法全面覆盖各种指令类型

（完）
