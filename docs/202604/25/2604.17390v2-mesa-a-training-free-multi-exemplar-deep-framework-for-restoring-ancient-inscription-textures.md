---
title: "MESA: A Training-Free Multi-Exemplar Deep Framework for Restoring Ancient Inscription Textures"
title_zh: MESA：一种用于修复古代铭文纹理的免训练多示例深度框架
authors: "Vasileios Toulatzis, Sofia Theodoridou, Ioannis Fudos"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17390v2"
tags: ["query:dl-cv"]
score: 9.0
evidence: 图像修复与重建
tldr: 针对古代铭文受损阻碍分析的问题，本文提出了MESA，一种无需训练的多样本深度修复框架。该方法利用保存完好的铭文样本，通过VGG19特征和Gram矩阵捕捉纹理风格，结合OCR估计的字符宽度优化权重，指导受损区域重建。实验证明了其优势，并提供了修复策略选择指南。
selection_source: fresh_fetch
motivation: 古代铭文常因碎片化或侵蚀受损，阻碍了阅读与分析，而现有图像修复方法在铭文恢复应用上存在局限。
method: MESA通过VGG19特征编码和Gram矩阵选择最佳样本，结合OCR估计的字符宽度调整层权重，并利用掩码限制合成区域，实现无需训练的纹理修复。
result: 比较实验证明了MESA在修复效果上优于GAN及单图像合成等方法。
conclusion: 本文不仅提供了有效的修复框架，还给出了根据可用样本和元数据选择修复策略的实用路线图。
---

## 摘要
古代铭文常因破碎、风化或其他损坏而出现缺失或受损区域，从而阻碍了阅读和分析。我们回顾了先前的图像修复方法及其在铭文图像恢复中的适用性，然后介绍了MESA（多示例、风格感知）——这是一种图像级修复方法，利用保存完好的示例铭文（来自同一铭文古迹、材料或相似字形）来指导受损文本的重建。MESA将VGG19卷积特征编码为格拉姆矩阵，以捕捉示例的纹理、风格和笔画结构；对于每个神经网络层，它选择与受损输入之间均方位移（MSD）最小的示例。逐层贡献权重源自示例集中光学字符识别（OCR）估算的字符宽度，以使滤波器偏向于匹配字母几何形状的尺度，同时训练掩码保留完整区域，从而使合成仅限于受损区域。我们还总结了先前的网络架构以及示例和单图像合成、修复和生成对抗网络（GAN）方法，强调了MESA所解决的局限性。对比实验证明了MESA的优势。最后，我们提供了一份实用的路线图，用于根据可用的示例和元数据选择修复策略。

## Abstract
Ancient inscriptions frequently suffer missing or corrupted regions from fragmentation, erosion, or other damage, hindering reading, and analysis. We review prior image restoration methods and their applicability to inscription image recovery, then introduce MESA (Multi-Exemplar, Style-Aware) -an image-level restoration method that uses well-preserved exemplar inscriptions (from the same epigraphic monument, material, or similar letterforms) to guide reconstruction of damaged text. MESA encodes VGG19 convolutional features as Gram matrices to capture exemplar texture, style, and stroke structure; for each neural network layer it selects the exemplar minimizing Mean-Squared Displacement (MSD) to the damaged input. Layer-wise contribution weights are derived from Optical Character Recognition-estimated character widths in the exemplar set to bias filters toward scales matching letter geometry, and a training mask preserves intact regions so synthesis is restricted to damaged areas. We also summarize prior network architectures and exemplar and single-image synthesis, inpainting, and Generative Adversarial Network (GAN) approaches, highlighting limitations that MESA addresses. Comparative experiments demonstrate the advantages of MESA. Finally, we provide a practical roadmap for choosing restoration strategies given available exemplars and metadata.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义

*   **研究背景与动机**：古代铭文常因风化、侵蚀、破碎或人为破坏（如划痕）导致纹理缺失或损坏，严重阻碍了历史学家的阅读与分析。
*   **现有方法的局限**：现有的文本修复方法多侧重于字符级别的预测（如基于LLM或Transformer的方法），往往忽略了铭文的视觉纹理和结构；而通用的图像修复方法（如GAN）通常需要大量成对的“损坏-完好”训练数据，这在文化遗产领域难以获取，且容易产生不符合历史风格的伪影。
*   **整体含义**：本文提出的 MESA 框架旨在解决上述问题，通过一种无需训练、基于多示例的风格感知方法，利用保存完好的铭文样本来指导受损区域的图像级纹理重建，既恢复了文本内容，又保留了原始的视觉风格和材质结构。

### 论文提出的方法论

*   **核心思想**：MESA（Multi-Exemplar, Style-Aware）是一种免训练的图像修复方法。它利用同一古迹、材质或相似字形的完好铭文作为示例，通过深度特征匹配来指导受损图像的重建。
*   **关键技术细节**：
    *   **特征提取与风格匹配**：使用预训练的 VGG19 网络提取特征，并计算格拉姆矩阵来捕捉示例的纹理、风格和笔画结构。这种方法关注空间不变性的统计特征，而非精确的空间对应。
    *   **多示例选择策略**：对于神经网络的每一层，计算受损输入与所有示例图像特征之间的均方位移（MSD），并选择该层上 MSD 最小的示例作为该层的指导样本（即 $L_{Min}^l$）。
    *   **字符尺度加权**：提出了一种数据驱动的层权重计算方法。利用 OCR（Tesseract）估算示例集中字符的宽度分布，根据网络层滤波器大小与字符宽度分布的匹配程度（概率密度函数）来分配权重，使网络更关注与字母几何形状匹配的尺度。
    *   **掩码机制**：在优化过程中应用空间掩码，强制合成过程仅限于受损区域，完好区域的像素值在每一步迭代中被钳制，从而保持原始图像的对比度和结构。
*   **算法流程**：
    1.  输入受损铭文图像及一组完好示例图像。
    2.  通过 VGG19 提取多层特征图并计算 Gram 矩阵。
    3.  对每一层，筛选出与输入图像风格损失最小的示例。
    4.  结合 OCR 导出的层权重，计算总损失函数（加权求和）。
    5.  使用 L-BFGS 优化算法更新输入图像（仅更新掩码区域），直至收敛。

### 实验设计

*   **数据集与场景**：
    *   使用了三个不同的铭文数据集（Dataset A, B, C），每个数据集包含划痕和噪声两种类型的受损子集。
    *   场景涵盖环境因素导致的噪声退化（如湿度）和人为因素导致的划痕损伤。
*   **Benchmark 与对比方法**：
    *   **免训练基线**：Deep Image Prior (DIP)。
    *   **监督学习基线**：DnCNN（去噪）、DRUNET（通用修复）、ESRGAN（超分辨率/感知增强）。这些方法使用了合成生成的受损/完好图像对进行训练。
*   **评估指标**：
    *   **传统图像指标**：PSNR、SSIM、LPIPS（基于 AlexNet 和 VGG）。
    *   **文本恢复指标**：Levenshtein 距离。
    *   **新提出的指标**：文本恢复分数（TRS，基于固定分母的归一化编辑距离）和对数缩放的 Levenshtein 相似度（LLS），旨在更公平地评估文本恢复质量。

### 资源与算力

*   **硬件配置**：实验使用了一块 NVIDIA GeForce RTX 2080 Ti GPU（配备 11GB GDDR6 显存，基础时钟频率 1350 MHz）。
*   **软件环境**：Python，基于 TensorFlow 框架实现。
*   **训练/优化设置**：使用 L-BFGS 优化算法，最大迭代次数设置为 5000 个 epoch。
*   **算力说明**：文中未明确提及具体的总训练时长或 wall-clock 时间，仅给出了硬件配置和迭代次数上限。

### 实验数量与充分性

*   **实验数量**：
    *   在 3 个不同的数据集上进行了全面测试。
    *   对比了 5 种主要方法（DIP-Denoise, DIP-Inpainting, DnCNN, DRUNET, ESRGAN）及其在不同训练数据（噪声训练 vs 划痕训练）下的变体。
    *   包含消融实验（附录 A），探讨了不同网络层组合、损失函数（最小 MSD vs 平均 MSD）及初始化策略对结果的影响。
*   **充分性与客观性**：
    *   实验设计较为充分，涵盖了不同类型的损伤和多个数据集。
    *   评估指标结合了感知质量（LPIPS）和文本准确性（TRS, LLS），比单纯的像素级指标（PSNR）更符合铭文修复的实际需求。
    *   **公平性考量**：监督方法使用了合成数据进行训练，而 MESA 使用真实的完好示例。虽然数据来源不同，但这恰恰反映了 MESA 在缺乏成对训练数据场景下的优势，符合实际应用中的数据稀缺情况。

### 论文的主要结论与发现

*   MESA 在文本恢复指标（TRS, LLS）和感知指标（LPIPS）上均优于免训练基线 DIP，并且在多数情况下匹配或超越了需要大量训练数据的监督方法（DnCNN, DRUNET, ESRGAN）。
*   虽然在像素级指标（PSNR, SSIM）上 MESA 可能略逊于专门针对像素重建优化的监督方法，但其在视觉上能更好地恢复铭文的纹理和笔画结构。
*   提出的新指标（TRS 和 LLS）比原始的 Levenshtein 距离更能反映文本恢复的实际效果，避免了长文本或严重损坏导致的数值偏差。
*   论文总结了一份实用路线图，指导用户根据可用的示例数据和元数据选择最合适的修复策略（例如，当有风格一致的示例时首选 MESA，仅有单张图像时考虑 DIP）。

### 优点

*   **免训练与低数据依赖**：不需要昂贵的成对“损坏-完好”数据集进行预训练，仅需风格相似的完好示例即可，非常适合文化遗产保护领域。
*   **风格感知与结构保持**：通过 Gram 捕捉纹理风格，并结合掩码机制，能有效保持完好区域不变，仅修复受损部分，避免了风格突变。
*   **创新的加权机制**：利用 OCR 估算的字符宽度分布来动态调整网络层权重，将先验知识（字母几何形状）引入深度特征优化过程。
*   **全面的评估体系**：引入了针对文本恢复任务的新指标，填补了传统图像指标无法准确衡量文字可读性的空白。

### 不足与局限

*   **对示例的依赖**：MESA 的性能高度依赖于是否存在风格相关且保存完好的示例铭文。如果缺乏此类示例，方法的效果会大打折扣。
*   **文字系统的局限性**：基于 OCR（Tesseract）的字符宽度分析机制主要针对拉丁文和希腊文等字母语言优化。对于象形文字（如圣书体）、楔形文字或表意文字，该加权机制可能失效或效果不佳，限制了其在非字母文字系统上的泛化能力。
*   **计算效率**：虽然免去了大规模预训练，但针对每张输入图像都需要进行 5000 次迭代的 L-BFGS 优化，相比单次前向传播的监督网络，处理速度可能较慢（文中也指出未来需研究更高效的优化方案）。

（完）
