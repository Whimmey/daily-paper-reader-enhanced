---
title: "MESA: A Training-Free Multi-Exemplar Deep Framework for Restoring Ancient Inscription Textures"
title_zh: MESA：一种用于恢复古代铭文纹理的免训练多示例深度框架
authors: "Vasileios Toulatzis, Ioannis Fudos"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17390v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 用于重建受损古代铭文的图像修复框架
tldr: 针对古代铭文受损阻碍分析的问题，本文提出MESA，一种无需训练的多示例深度修复框架。该方法利用保存完好的示例铭文，通过VGG19特征和Gram矩阵捕捉纹理风格，结合OCR估计的字符宽度加权，指导受损区域的纹理重建。实验表明MESA优于现有方法，并提供了修复策略选择指南。
selection_source: fresh_fetch
motivation: 古代铭文常因碎片化或侵蚀导致缺失损坏，阻碍了阅读分析，而现有图像修复方法在铭文恢复上存在局限性。
method: MESA利用保存完好的示例铭文，通过VGG19卷积特征和Gram矩阵捕捉纹理风格，并结合OCR估计的字符宽度进行逐层加权，在无需训练的情况下指导受损区域重建。
result: 比较实验证明了MESA在铭文纹理修复方面优于现有的GAN和单图像合成方法。
conclusion: 本文不仅提供了有效的MESA修复框架，还总结了现有方法并给出了根据可用示例和元数据选择修复策略的实用路线图。
---

## 摘要
古代铭文常因破碎、侵蚀或其他损坏而出现缺失或受损区域，从而阻碍了阅读和分析。我们回顾了先前的图像修复方法及其在铭文图像恢复中的适用性，然后介绍了MESA（多示例、风格感知）——这是一种图像级修复方法，利用保存完好的示例铭文（来自同一铭文碑刻、材料或相似字形）来指导受损文本的重建。MESA将VGG19卷积特征编码为格拉姆矩阵，以捕捉示例的纹理、风格和笔画结构；对于每个神经网络层，它选择与受损输入之间均方位移（MSD）最小的示例。逐层贡献权重源于示例集中光学字符识别（OCR）估计的字符宽度，使滤波器偏向于匹配字母几何形状的尺度，同时训练掩码保留完整区域，从而使合成仅限于受损区域。我们还总结了先前的网络架构以及示例和单图像合成、修复和生成对抗网络（GAN）方法，强调了MESA所解决的局限性。对比实验证明了MESA的优势。最后，我们提供了一份实用的路线图，用于根据可用的示例和元数据选择修复策略。

## Abstract
Ancient inscriptions frequently suffer missing or corrupted regions from fragmentation, erosion, or other damage, hindering reading, and analysis. We review prior image restoration methods and their applicability to inscription image recovery, then introduce MESA (Multi-Exemplar, Style-Aware) -an image-level restoration method that uses well-preserved exemplar inscriptions (from the same epigraphic monument, material, or similar letterforms) to guide reconstruction of damaged text. MESA encodes VGG19 convolutional features as Gram matrices to capture exemplar texture, style, and stroke structure; for each neural network layer it selects the exemplar minimizing Mean-Squared Displacement (MSD) to the damaged input. Layer-wise contribution weights are derived from Optical Character Recognition-estimated character widths in the exemplar set to bias filters toward scales matching letter geometry, and a training mask preserves intact regions so synthesis is restricted to damaged areas. We also summarize prior network architectures and exemplar and single-image synthesis, inpainting, and Generative Adversarial Network (GAN) approaches, highlighting limitations that MESA addresses. Comparative experiments demonstrate the advantages of MESA. Finally, we provide a practical roadmap for choosing restoration strategies given available exemplars and metadata.

---

## 论文详细总结（自动生成）

### 论文总结：MESA：一种用于恢复古代铭文纹理的免训练多示例深度框架

#### 1. 论文的核心问题与整体含义
*   **核心问题**：古代铭文常因风化、侵蚀或人为破坏（如划痕）导致纹理缺失或损坏，这不仅降低了视觉质量，还使得文本难以辨认，阻碍了历史学家和考古学家的分析工作。
*   **研究动机**：现有的文本恢复方法（如基于LLM或Transformer的模型）主要侧重于字符级别的预测，往往忽略了铭文的视觉结构和纹理细节；而现有的图像修复方法（如GAN）通常需要大量的成对训练数据（损坏图像与清晰图像），这在文化遗产领域往往难以获取或成本极高。
*   **整体含义**：本文旨在填补这一空白，提出一种无需针对特定数据集进行训练的图像级修复方法，利用保存完好的示例铭文来指导受损区域的纹理重建，从而在恢复文本内容的同时保留铭文的艺术风格和笔画结构。

#### 2. 论文提出的方法论
*   **核心思想**：提出 MESA（Multi-Exemplar, Style-Aware）框架，这是一种基于优化的免训练方法。它利用预训练的 VGG19 网络提取特征，通过计算格拉姆矩阵来捕捉示例图像的纹理和风格，并引导受损图像的重建。
*   **关键技术细节**：
    *   **多示例匹配**：对于 VGG19 的每一层，方法会计算受损输入图像与多个示例图像特征图之间的格拉姆矩阵距离（均方位移 MSD），并选择距离最小的那个示例作为该层的指导。
    *   **字符尺度加权**：创新性地提出了一种数据驱动的层级权重计算方法。利用 OCR（Tesseract）分析示例集中字符的宽度分布，拟合概率密度函数（PDF）。网络层的权重由该层感受野大小与字符宽度分布的匹配程度决定（即字符宽度落在该层尺度区间的概率）。
    *   **掩码机制**：在优化过程中使用二进制掩码，强制仅更新受损区域的像素，保留完好区域的颜色、亮度和结构不变。
    *   **优化算法**：使用 L-BFGS 算法对输入图像进行迭代优化，最小化加权后的风格损失函数。
*   **公式流程**：
    *   损失函数定义为各层最小示例损失的加权和：$L_{total} = \sum_{l} \hat{weight}_l L_{Min}^l$。
    *   其中 $L_{Min}^l$ 是第 $l$ 层输入与最佳示例之间的格拉姆矩阵均方误差。
    *   $\hat{weight}_l$ 是基于字符宽度分布归一化后的权重。

#### 3. 实验设计
*   **数据集**：使用了三个不同的铭文数据集（Dataset A, B, C），每个数据集包含“划痕”和“噪声”两种类型的受损子集。这些数据集既作为 MESA 的示例源，也作为监督学习基线方法的 Ground Truth。
*   **Benchmark 与对比方法**：
    *   **免训练基线**：Deep Image Prior (DIP)。
    *   **监督学习基线**：DnCNN（去噪）、DRUNET（通用修复）、ESRGAN（超分辨率/感知增强）。这些方法使用了合成生成的受损/清晰图像对进行训练。
*   **评估指标**：
    *   **图像质量指标**：PSNR、SSIM、LPIPS（基于 AlexNet 和 VGG）。
    *   **文本恢复指标**：Levenshtein 距离，以及本文新提出的 **Text Recovery Score (TRS)** 和 **Log-scaled Levenshtein Similarity (LLS)**。

#### 4. 资源与算力
*   **硬件环境**：实验在一块 **NVIDIA GeForce RTX 2080 Ti** GPU（11GB GDDR6 显存，基础时钟频率 1350 MHz）上进行。
*   **软件环境**：使用 Python 和 TensorFlow 框架实现。
*   **计算配置**：优化器设置为 L-BFGS，最大迭代次数为 5000 次。文中未明确提及具体的训练或推理总时长，但指出了硬件规格。

#### 5. 实验数量与充分性
*   **实验数量**：在三个不同的数据集上进行了广泛的测试，涵盖了两种主要的退化类型（噪声和划痕）。对比了 4 种主流方法及其不同变体（如针对噪声训练 vs 针对划痕训练），并使用了 7 种不同的评估指标。
*   **充分性与客观性**：
    *   **充分**：实验不仅展示了视觉效果的对比，还提供了详尽的定量数据。新提出的文本恢复指标（TRS, LLS）弥补了传统图像指标在文本可读性评估上的不足。
    *   **客观性**：对于监督学习方法，作者采用了跨数据集训练的策略（即在一个数据集上训练，在另一个上测试），以模拟真实世界中缺乏成对数据的场景，这比同数据集训练更具挑战性和说服力。同时，作者也报告了理想情况下的同数据集训练结果作为参考。

#### 6. 论文的主要结论与发现
*   MESA 在文本恢复指标（Levenshtein 距离、TRS、LLS）上显著优于所有对比方法，表明其在恢复铭文可读性方面效果最佳。
*   在感知指标（LPIPS）上，MESA 表现出色，与 ESRGAN 持平或更好，说明其生成的纹理在视觉上更接近真实。
*   在像素级指标（PSNR, SSIM）上，MESA 略低于部分监督学习方法（如 ESRGAN），这是因为 MESA 侧重于结构和文本的恢复，而非像素级的精确重建。
*   实验证明，基于 OCR 字符宽度分布的层级加权策略是有效的，优于任意分配权重。

#### 7. 优点
*   **免训练与低数据依赖**：不需要昂贵的成对损坏/清晰数据集进行网络训练，仅需一些风格相似的完好示例即可，非常适合文化遗产保护领域。
*   **结构保持能力**：通过多示例和掩码机制，能够有效保留铭文的原始结构和未受损部分，避免过度平滑或产生幻觉纹理。
*   **创新的加权机制**：利用 OCR 分析字符几何特征来指导网络层权重，将领域知识（铭文字符宽度）融入深度学习特征提取中。
*   **新评估指标**：提出的 TRS 和 LLS 指标为评估文本修复任务提供了更合理、归一化的量化标准。

#### 8. 不足与局限
*   **示例依赖性**：MESA 的性能高度依赖于是否存在风格一致且保存完好的示例铭文。如果找不到相似的示例，或者受损铭文是孤立的，方法效果会大打折扣。
*   **文字系统局限性**：基于 Tesseract OCR 的字符宽度分析主要针对拉丁文和希腊文等字母文字优化。对于象形文字（如圣书体）、楔形文字或结构复杂的表意文字，该加权策略可能失效或效果不佳。
*   **计算成本**：虽然不需要训练网络，但针对每张图像都需要运行 L-BFGS 优化（5000次迭代），相比前馈神经网络（如训练好的 ESRGAN），其单张图像的处理时间可能较长。
*   **纹理细节**：方法主要关注全局结构和笔画恢复，对于极低层次的纹理细节（如石材的微小颗粒感）可能不如专门针对像素优化的监督方法精确。

（完）
