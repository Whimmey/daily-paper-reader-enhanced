---
title: Latent Denoising Improves Visual Alignment in Large Multimodal Models
title_zh: 潜在去噪提升大型多模态模型中的视觉对齐
authors: "Dhruv Parikh, Jacob Fein-Ashley, Rajgopal Kannan, Viktor Prasanna"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21343v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 针对LMM的潜在去噪与教师块恢复
tldr: 针对大型多模态模型（LMM）视觉表征弱的问题，本文提出了一种潜在去噪框架。该方法通过破坏视觉token并训练模型恢复特征，结合对比蒸馏防止表征坍塌。实验表明，该方法在多项基准测试中提升了视觉理解和推理能力，且增强了模型对图像损坏的鲁棒性，推理无额外开销。
selection_source: fresh_fetch
motivation: 现有LMM仅通过语言建模目标间接监督视觉token，导致内部视觉表征较弱且在分布偏移下表现脆弱。
method: 提出一种潜在去噪框架，通过显著性感知的混合噪声破坏视觉token，并利用解码器从中间层恢复教师特征，同时结合对比块蒸馏防止表征坍塌。
result: 该方法在多项标准多模态基准测试中提升了视觉理解与推理能力，并在组合鲁棒性及图像损坏测试中表现出更强的抗干扰性。
conclusion: 该框架在不增加推理开销的前提下，有效改善了LMM的内部视觉特征对齐和鲁棒性。
---

## 摘要
大型多模态模型（如 LLaVA）通常采用自回归语言建模目标进行训练，仅对视觉标记提供间接监督。这往往导致内部视觉表征较弱，且在分布偏移下表现脆弱。受利用潜在去噪学习高质量视觉分词器的最新进展启发，我们表明同一原理为改善 LMM 内部视觉特征对齐和多模态理解提供了一种有效的视觉监督形式。我们提出了一个潜在去噪框架，利用感知显著性的掩码和高斯噪声混合来破坏投影后的视觉标记。LMM 被训练通过解码器从选定中间 LLM 层的隐藏状态中恢复干净的教师块特征，从而对这些受损标记进行去噪。为了防止表征坍塌，我们的框架还保留了教师的图像内相似性结构，并应用了图像内对比块蒸馏。在推理过程中，破坏和辅助头被禁用，不引入额外的推理开销。在广泛的标准多模态基准测试中，我们的方法始终优于强基线，改善了视觉理解和推理，并在组合鲁棒性基准测试（如 NaturalBench）上取得了显著收益。此外，在应用于基准图像的 ImageNet-C 风格非对抗性常见损坏下，我们的方法保持了更高的准确率，并在中度和严重损坏水平下表现出更小的性能下降。我们的代码可在 https://github.com/dhruvashp/latent-denoising-for-lmms 获取。

## Abstract
Large Multimodal Models (LMMs) such as LLaVA are typically trained with an autoregressive language modeling objective, providing only indirect supervision to visual tokens. This often yields weak internal visual representations and brittle behavior under distribution shift. Inspired by recent progress on latent denoising for learning high-quality visual tokenizers, we show that the same principle provides an effective form of visual supervision for improving internal visual feature alignment and multimodal understanding in LMMs. We propose a latent denoising framework that corrupts projected visual tokens using a saliency-aware mixture of masking and Gaussian noising. The LMM is trained to denoise these corrupted tokens by recovering clean teacher patch features from hidden states at a selected intermediate LLM layer using a decoder. To prevent representation collapse, our framework also preserves the teacher's intra-image similarity structure and applies intra-image contrastive patch distillation. During inference, corruption and auxiliary heads are disabled, introducing no additional inference-time overhead. Across a broad suite of standard multimodal benchmarks, our method consistently improves visual understanding and reasoning over strong baselines, and yields clear gains on compositional robustness benchmarks (e.g., NaturalBench). Moreover, under ImageNet-C-style non-adversarial common corruptions applied to benchmark images, our method maintains higher accuracy and exhibits reduced degradation at both moderate and severe corruption levels. Our code is available at https://github.com/dhruvashp/latent-denoising-for-lmms.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义

*   **核心问题**：现有的大型多模态模型（如 LLaVA）通常仅通过自回归语言建模目标进行训练，这导致视觉标记仅受到间接的监督。这种训练方式使得模型内部的视觉表征较弱，容易在分布偏移（如输入图像质量下降）时表现脆弱，且容易出现过度依赖文本提示、物体幻觉以及视觉定位能力不足等问题。
*   **研究动机**：受近期利用“潜在去噪”学习高质量视觉分词器进展的启发，作者提出将这一原理应用于多模态理解任务。核心动机是探究通过让模型从受损的潜在视觉标记中恢复干净特征，是否能作为一种有效的视觉监督形式，从而强化 LMM 内部的视觉表征，提升其对齐能力和鲁棒性。

### 论文提出的方法论

*   **核心思想**：提出一种训练时的“潜在去噪”框架。该方法不依赖纯文本监督，而是将投影后的视觉标记视为潜在空间，对其进行破坏，并训练 LMM 从中间层的隐藏状态中恢复对齐的教师特征。
*   **关键技术细节**：
    *   **显著性感知的混合破坏**：结合了高斯噪声（软破坏）和掩码（硬破坏）。利用冻结的视觉编码器提取的显著性图，对高显著性区域优先应用高斯噪声（保留部分信号），对低显著性区域优先应用掩码（完全移除信息）。
    *   **特征恢复**：在 LLM 的选定中间层（通常为网络中部，如 32 层中的第 16 层）附加一个轻量级解码器。模型被训练去解码被破坏标记的隐藏状态，以恢复干净的教师特征（来自冻结的视觉编码器）。
    *   **多目标训练函数**：
        1.  **重建损失 ($L_{rec}$)**：归一化的 L2 损失，对齐预测特征与教师特征。
        2.  **关系对齐损失 ($L_{rel}$)**：最小化学生特征与教师特征之间相似度矩阵的 KL 散度，以保持全局几何结构。
        3.  **对比损失 ($L_{con}$)**：图像内对比块蒸馏，防止表征坍塌，增强判别性。
    *   **推理阶段**：破坏过程和辅助解码器在推理时被禁用，因此不引入任何额外的推理开销。

### 实验设计

*   **模型架构**：在三种不同的 LMM 架构上进行了验证，包括 LLaVA-1.5（分别搭配 CLIP ViT-L/14 和 SigLIP-SO400M 视觉编码器）以及 Qwen-2.5-VL-7B-Instruct。
*   **数据集与基准**：涵盖了 18 个多模态基准测试，分为以下几类：
    *   **视觉问答 (VQA)**：VQAv2, GQA。
    *   **幻觉检测**：POPE, HallBench。
    *   **通用推理**：MME, MMBench, MMStar, MMMU。
    *   **科学与数学**：SQA-IMG, MathVista。
    *   **文本/OCR**：TextVQA, OCRBench。
    *   **图表理解**：ChartQA, AI2D。
    *   **鲁棒性**：NaturalBench, RealWorldQA, VizWiz, Q-Bench。
*   **损坏评估协议**：受 ImageNet-C 启发，设计了一种确定性的非对抗性损坏协议，对基准图像应用噪声、模糊、天气和数字四大类损坏，严重程度分为 3 级（中度）和 5 级（重度）。
*   **对比方法**：主要对比了在相同条件下训练但不使用潜在去噪的匹配基线模型。此外，在附录中与现有的视觉监督方法（如 LocalAlign, BASIC, ROSS 等）进行了定性对比。

### 资源与算力

*   **硬件配置**：论文中明确提到使用了 **8 个 GPU** 进行训练，并采用了 **DeepSpeed ZeRO-2** 优化策略。
*   **具体型号与时长**：文中未明确说明使用的具体 GPU 型号（如 A100 或 H100）以及具体的训练总时长（小时数或天数），仅提到了训练轮数（Stage 1 和 Stage 2 各 1 个 epoch）。

### 实验数量与充分性

*   **实验数量**：实验非常充分，涵盖了 3 种架构、18 个标准基准测试、4 种损坏类型 × 2 种严重程度的鲁棒性测试。此外，还进行了详细的消融实验，包括损失组件分析、损坏率调整、监督层选择、噪声条件参数以及高斯噪声参数的扫描。
*   **充分性与客观性**：实验设计非常全面且客观。不仅验证了标准数据集上的性能提升，还专门设计了针对分布偏移的损坏测试。内部表征分析（CKA、kNN 分类、有效秩、注意力热力图）从多个角度验证了方法的有效性。消融实验系统地验证了各个设计选择的必要性。

### 论文的主要结论与发现

*   **性能提升**：潜在去噪在所有三种架构的绝大多数基准测试中（15/18, 18/18, 17/18）均带来了一致的性能提升，特别是在需要细粒度视觉推理的任务（如 GQA, MMStar）和组合鲁棒性基准（如 NaturalBench）上收益显著。
*   **鲁棒性增强**：在图像受到损坏（噪声、模糊等）的情况下，该方法保持了更高的准确率，且随着损坏严重程度的增加，性能下降幅度明显小于基线模型。
*   **内部表征改善**：分析表明，该方法显著提高了 LLM 内部视觉特征与视觉基础模型（如 CLIP 和 DINOv2）的对齐度，提高了特征的判别性（kNN 准确率提升）和内在维度，并使得视觉注意力更加聚焦于空间相关的区域。

### 优点

*   **零推理开销**：所有的破坏操作和辅助解码器仅在训练时使用，推理时完全移除，不影响部署速度。
*   **统一且高效的框架**：将连续（高斯）和离散（掩码）的破坏方式与关系及对比损失相结合，提供了一个统一的视觉监督视角。
*   **自包含性**：直接利用 LMM 自带的冻结视觉编码器作为教师，无需引入额外的重型外部模型（如某些基于扩散的方法），降低了训练复杂度。
*   **广泛的适用性**：该方法在不同家族的模型（LLaVA, Qwen）和不同的视觉编码器（CLIP, SigLIP）上均表现出良好的泛化能力。

### 不足与局限

*   **MME 指标异常**：在两个架构上，MME（感知+认知）的综合得分略有下降。作者推测这可能是因为 MME 的二元判断格式对细粒度视觉对齐的改进不够敏感，但这仍表明该方法在某些特定评估指标上可能存在权衡。
*   **训练复杂度**：虽然推理无开销，但训练过程引入了额外的逻辑（显著性计算、破坏策略、多损失函数优化），增加了训练流程的复杂性。
*   **教师依赖**：方法的有效性在一定程度上依赖于冻结的视觉编码器作为教师提供高质量的特征。如果视觉编码器本身存在偏差或缺陷，可能会限制监督的上限（尽管实验显示对 DINOv2 的跨模型对齐也有所改善）。
*   **超参数敏感性**：虽然消融实验显示方法对参数有一定容忍度，但仍需要针对不同模型调整破坏率、噪声参数和监督层位置，以获得最佳性能。

（完）
