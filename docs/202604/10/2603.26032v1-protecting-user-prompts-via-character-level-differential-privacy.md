---
title: Protecting User Prompts Via Character-Level Differential Privacy
title_zh: 基于字符级差分隐私的用户提示保护
authors: "Shashie Dilhara Batan Arachchige, Hassan Jameel Asghar, Benjamin Zi Hao Zhao, Dinusha Vatsalan, Dali Kaafar"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26032v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 使用差分隐私保护大语言模型用户提示
tldr: 本研究针对大语言模型用户提示中敏感信息泄露问题，提出了一种基于字符级差分隐私的提示清理方法。该方法通过随机扰动每个字符保护敏感信息，同时利用上下文和词频特性恢复非敏感内容。实验表明，该方法能有效保护敏感PII，同时保持下游任务的良好性能，无需预先识别敏感信息。
selection_source: carryover_cache
motivation: 用户提示中的敏感个人信息可能暴露给托管大语言模型的第三方，需要有效的保护机制。
method: 使用字符级差分隐私随机独立地扰动每个字符，利用上下文和词频特性在远程LLM中恢复非敏感词，同时保护敏感信息。
result: 敏感PII的重建率接近随机词的理论重建率，而非敏感词重建率显著更高，方法在保护隐私的同时保持了下游任务的效用。
conclusion: 该方法无需明确识别敏感信息即可有效保护用户提示，在隐私保护和任务效用之间取得了良好平衡。
---

## 摘要
大型语言模型(LLMs)根据用户提示生成响应。通常，这些提示可能包含高度敏感的信息，包括个人身份信息(PII)，这些信息可能会暴露给托管这些模型的第三方。在这项工作中，我们提出了一种新的方法来清理用户提示。我们的机制使用差分隐私的随机响应机制，随机且独立地扰动单词中的每个字符。然后，将扰动后的文本发送到远程LLM，该模型首先执行提示恢复，随后执行预期的下游任务。其理念是，即使由于上下文线索以及这些词通常非常常见而导致它们被扰动，恢复仍能够重建非敏感词。另一方面，扰动会使敏感词的重建变得困难，因为它们很少见。我们使用两个LLM(Llama-3.1 8B Instruct和GPT-4o mini)在两个数据集(i2b2/UTHealth和Enron)上实验验证了我们的方法。我们还使用统一的隐私-效用评估，将我们的方法与词级差分隐私机制以及基于规则的PII删除基线进行了比较。我们的结果表明，在这些数据集中标记的敏感PII的重建率接近完全随机词的理论重建率，而非敏感词的重建率则高得多。我们的方法的优势在于，它可以在不明确识别提示中敏感信息的情况下应用，同时为下游任务展示了良好的隐私-效用权衡。

## Abstract
Large Language Models (LLMs) generate responses based on user prompts. Often, these prompts may contain highly sensitive information, including personally identifiable information (PII), which could be exposed to third parties hosting these models. In this work, we propose a new method to sanitize user prompts. Our mechanism uses the randomized response mechanism of differential privacy to randomly and independently perturb each character in a word. The perturbed text is then sent to a remote LLM, which first performs a prompt restoration and subsequently performs the intended downstream task. The idea is that the restoration will be able to reconstruct non-sensitive words even when they are perturbed due to cues from the context, as well as the fact that these words are often very common. On the other hand, perturbation would make reconstruction of sensitive words difficult because they are rare. We experimentally validate our method on two datasets, i2b2/UTHealth and Enron, using two LLMs: Llama-3.1 8B Instruct and GPT-4o mini. We also compare our approach with a word-level differentially private mechanism, and with a rule-based PII redaction baseline, using a unified privacy-utility evaluation. Our results show that sensitive PII tagged in these datasets are reconstructed at a rate close to the theoretical rate of reconstructing completely random words, whereas non-sensitive words are reconstructed at a much higher rate. Our method has the advantage that it can be applied without explicitly identifying sensitive pieces of information in the prompt, while showing a good privacy-utility tradeoff for downstream tasks.

---

## 论文详细总结（自动生成）

# 论文总结：基于字符级差分隐私的用户提示保护

## 1. 论文的核心问题与整体含义

论文针对大型语言模型(LLMs)用户提示中敏感信息泄露的问题。随着LLMs的广泛应用，用户经常在提示中包含个人身份信息(PII)，如姓名、地址、电话号码等，这些信息可能被托管LLMs的第三方获取和泄露，导致严重的隐私风险。现有保护方法各有局限：基于命名实体识别(NER)和规则的方法依赖正确识别敏感信息，易产生误报和漏报；同态加密和安全多方计算(SMC)需要改变LLM架构，计算开销大；自定义混淆技术缺乏正式的隐私保证。论文旨在提供一种无需预先识别敏感信息、计算高效且具有形式化隐私保证的解决方案。

## 2. 论文提出的方法论

### 核心思想
- 使用字符级差分隐私(k-ary Randomized Response, k-RR)机制在用户端对提示中的每个字符进行随机扰动
- 利用LLM的上下文恢复能力重建常见非敏感词汇，同时使敏感词汇(通常罕见)难以恢复
- 提供自动化的隐私保护，无需预先识别或标记敏感信息

### 关键技术细节
- **k-RR算法**：对每个字符，以概率(1-γ)保持原字符，以概率γ替换为其他93个可打印ASCII字符中的一个
- **隐私参数**：γ = (k-1)/(k-1+e^ε)，其中k=94(可打印ASCII字符数)，ε是隐私参数
- **处理流程**：
  1. 用户端对提示中的每个字符应用k-RR算法
  2. 将扰动后的文本发送到远程LLM
  3. LLM首先尝试恢复文本，然后执行下游任务
- **恢复机制**：LLM利用上下文线索和词频特性恢复常见词汇，而敏感词汇难以恢复

## 3. 实验设计

### 数据集/场景
- **i2b2/UTHealth医疗数据集**：1,304份纵向医疗记录，描述296名患者，平均每条记录有22.14个PHI实例
- **Enron数据集**：约60万封邮件，随机抽取10,000条记录，平均每条记录有8.02个PI实例

### Benchmark
- **理论基线**：随机字符重建率(Pr[T0])，作为敏感词汇重建率的参考
- **隐私指标**：敏感词重建率(隐私保护得分的补数)
- **效用指标**：非敏感词重建率、语义相似度(余弦相似度)

### 对比方法
- **词级差分隐私方法**：在嵌入空间中添加校准噪声并解码回离散标记
- **基于规则的PII删除基线**：使用Microsoft Presidio框架检测和匿名化PII

## 4. 资源与算力

论文中未明确提到使用的GPU型号、数量或训练时长等算力信息。实验使用了两个LLM：GPT-4o mini和Llama-3.1 8B Instruct，但没有详细说明运行这些模型的硬件配置。

## 5. 实验数量与充分性

### 实验数量
- 在两个数据集(i2b2/UTHealth和Enron)上进行了实验
- 使用两个LLM(GPT-4o mini和Llama-3.1 8B)
- 测试了不同ε值(1到10)下的隐私-效用权衡
- 进行了迭代恢复实验，评估第二次恢复对敏感信息重建的影响
- 分析了不同类型敏感实体(姓名、位置)的重建情况
- 与两种基线方法(词级DP和Presidio)进行了比较

### 实验充分性与公平性
- 实验设计较为全面，覆盖了不同数据域和模型
- 使用统一的隐私-效用评估框架，确保公平比较
- 理论分析与实验结果相结合，验证了方法的有效性
- 实验考虑了不同类型的敏感信息(姓名、地址等)，验证了方法的泛化能力

## 6. 论文的主要结论与发现

### 主要结论
- 字符级差分隐私能有效保护敏感PII，其重建率接近随机词的理论重建率
- 非敏感词的重建率显著高于敏感词，表明方法在保护隐私的同时保持了效用
- 推荐的ε值：GPT-4o mini为5.5，Llama-3.1 8B为6.0，在这些值下能实现良好的隐私-效用平衡
- 完整实体(如全名、完整地址)的重建率低于单个词的重建率，增强了隐私保护

### 主要发现
- LLM能够有效恢复常见的非敏感词汇，即使部分字符被扰动
- 敏感词汇(通常罕见)难以被LLM恢复，即使有上下文线索
- 街道地址等高熵字符串比城市/州和设施名称更难恢复
- 迭代恢复不会显著增加敏感信息的重建率

## 7. 优点

### 方法亮点
- 无需预先识别敏感信息，避免了基于NER和规则方法的局限性
- 在字符级别应用差分隐私，保护粒度更细
- 利用LLM的上下文恢复能力，在保护隐私的同时保持文本可读性
- 提供可调节的隐私参数ε，允许根据需求调整隐私-效用权衡

### 实验设计亮点
- 使用统一的隐私-效用评估框架，确保不同方法间的公平比较
- 结合理论分析和实验验证，增强结果的可信度
- 考虑不同类型的敏感实体，全面评估方法性能
- 分析迭代恢复的影响，评估方法的鲁棒性

## 8. 不足与局限

### 实验覆盖局限
- 实验主要在医疗和邮件数据集上进行，可能无法完全覆盖所有应用场景
- 未考虑对抗性攻击或更复杂的威胁模型
- 方法假设敏感词汇是罕见的，但某些常见词汇(如"John")也可能是敏感的

### 应用限制
- 未评估方法在低资源环境下的性能
- 字符级扰动可能影响文本的语义结构，特别是在非英语语言中
- 未考虑多语言支持
- 实验主要关注文本恢复和摘要任务，可能不适用于所有LLM应用
- 未评估方法在实时应用中的延迟和效率

（完）
