---
title: "VOSR: A Vision-Only Generative Model for Image Super-Resolution"
title_zh: VOSR：一种仅基于视觉的图像超分辨率生成模型
authors: "Rongyuan Wu, Lingchen Sun, Zhengqiang Zhang, Xiangtao Kong, Jixin Zhao, Shihao Wang, Lei Zhang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03225v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 仅视觉的图像修复生成模型
tldr: VOSR是一种纯视觉生成模型，用于图像超分辨率任务。它通过预训练视觉编码器提取低分辨率输入的语义特征，并采用恢复导向的引导策略替代无条件分支。VOSR训练成本仅为T2I-based方法的十分之一，却能实现更高质量的超分辨率结果，首次证明无需多模态预训练也能实现高质量生成式超分辨率。
selection_source: fresh_fetch
motivation: 现有生成式超分辨率方法依赖文本到图像扩散模型，而超分辨率本质上是低分辨率输入条件化的图像恢复任务，需要更适合的模型设计。
method: VOSR使用预训练视觉编码器提取低分辨率输入的语义特征，采用恢复导向的引导策略，并从多步模型蒸馏为单步模型以提高效率。
result: VOSR训练成本仅为T2I-based方法的十分之一，但在多步和单步设置中都能达到具有竞争力的感知质量和效率，且产生更忠实结构且更少幻觉的结果。
conclusion: 高质量生成式超分辨率可以通过纯视觉模型实现，无需依赖多模态预训练。
---

## 摘要
最近的大多数生成式图像超分辨率(SR)方法依赖于适应在网页规模文本-图像数据上预训练的大型文本到图像(T2I)扩散模型。虽然这些方法有效，但这种范式从通用的T2I生成器开始，而SR本质上是一个低分辨率(LR)输入条件下的图像恢复任务。在这项工作中，我们研究纯粹在视觉数据上训练的SR模型是否可以与基于T2I的模型相媲美。为此，我们提出了VOSR，一种仅基于视觉的SR生成框架。我们首先使用预训练的视觉编码器作为视觉语义指导，从LR输入中提取语义丰富且空间定位的特征。然后，我们重新审视了用于训练生成模型的分类器自由指导，并表明标准的无条件分支不适合从头开始训练的恢复模型。因此，我们用一种面向恢复的指导策略取代它，该策略保留了弱LR锚点。基于这些设计，我们首先从头开始训练一个多步VOSR模型，然后将其提炼为一个单步模型以实现高效推理。VOSR所需的训练成本不到代表性基于T2I的SR方法的十分之一，但在多步和单步设置中，它都能达到具有竞争力的甚至更好的感知质量和效率，同时在合成和真实世界基准上产生更忠实结构且幻觉更少。我们的结果首次表明，无需多模态预训练即可实现高质量的生成式SR。代码和模型可在https://github.com/cswry/VOSR找到。

## Abstract
Most of the recent generative image super-resolution (SR) methods rely on adapting large text-to-image (T2I) diffusion models pretrained on web-scale text-image data. While effective, this paradigm starts from a generic T2I generator, despite that SR is fundamentally a low-resolution (LR) input-conditioned image restoration task. In this work, we investigate whether an SR model trained purely on visual data can rival T2I-based ones. To this end, we propose VOSR, a Vision-Only generative framework for SR. We first extract semantically rich and spatially grounded features from the LR input using a pretrained vision encoder as visual semantic guidance. We then revisit classifier-free guidance for training generative models and show that the standard unconditional branch is ill-suited to restoration models trained from scratch. We therefore replace it with a restoration-oriented guidance strategy that preserves weak LR anchors. Built upon these designs, we first train a multi-step VOSR model from scratch and then distill it into a one-step model for efficient inference. VOSR requires less than one-tenth of the training cost of representative T2I-based SR methods, yet in both multi-step and one-step settings, it achieves competitive or even better perceptual quality and efficiency, while producing more faithful structures with fewer hallucinations on both synthetic and real-world benchmarks. Our results, for the first time, show that high-quality generative SR can be achieved without multimodal pretraining. The code and models can be found at https://github.com/cswry/VOSR.

---

## 论文详细总结（自动生成）

# VOSR: 一种仅基于视觉的图像超分辨率生成模型总结

## 1. 论文的核心问题与整体含义

论文针对现有生成式图像超分辨率(SR)方法过度依赖文本到图像(T2I)扩散模型的问题展开研究。当前主流方法虽然有效，但存在根本性矛盾：超分辨率本质上是低分辨率(LR)输入条件下的图像恢复任务，而非自由形式的图像合成。现有方法从通用T2I生成器出发，通过提示、适配器或控制机制强制模型符合LR输入，这种范式引入了通过文本或文本对齐表示的语义，增加了细节幻觉风险，且训练成本高昂。研究动机是探索纯视觉数据训练的SR模型能否达到与基于T2I的方法相当的性能，同时降低训练成本并提高结构保真度。

## 2. 论文提出的方法论

### 核心思想
VOSR是一种纯视觉生成框架，专为图像超分辨率任务设计，不依赖多模态预训练。它通过预训练视觉编码器提取LR输入的语义特征，并采用恢复导向的引导策略替代无条件分支，实现高质量且忠实于输入的超分辨率重建。

### 关键技术细节
- **视觉语义条件**：结合两种条件分支：
  - 结构条件：cstr = E(ILR)，使用VAE编码器E将LR图像编码到与HR图像相同的潜在空间
  - 语义条件：csem = V(ILR)，使用预训练视觉编码器(如DINO)提取语义特征
- **恢复导向的引导**：替代标准分类器自由指导(CFG)中的无条件分支，采用部分条件分支：
  - 完全条件分支：vcond = vθ(zt, t, cstr, csem)
  - 部分条件分支：vpcond = vθ(zt, t, αcstr, ∅)，其中α∈(0,1)是结构保留因子
  - 推理时：vcfg = vpcond + s(vcond - vpcond)，s是引导尺度
- **多步到单步蒸馏**：先训练多步VOSR模型，然后通过一致性蒸馏压缩为单步模型，保持高质量的同时提高推理效率

### 算法流程
1. 给定LR图像ILR，提取结构条件cstr和语义条件csem
2. 在扩散过程中，将结构条件作为空间对齐潜在条件，语义条件通过交叉注意力注入
3. 训练时随机选择完全条件或部分条件模式
4. 推理时应用恢复导向引导，平衡生成能力和输入保真度
5. 通过蒸馏将多步模型压缩为单步模型以实现高效部署

## 3. 实验设计

### 数据集
- **训练数据**：约100M张网络图像，经质量和多样性过滤后，使用Real-ESRGAN退化流程合成LR-HR对
- **测试数据集**：
  - LSDIR：250张图像的验证集，标准×4 SR协议
  - RealSR：真实世界SR基准
  - ScreenSR：新构建的屏幕重摄影基准，提供更高质量参考图像

### Benchmark
- 合成测试：LSDIR验证集
- 真实世界测试：RealSR和ScreenSR基准
- 评估指标：PSNR、SSIM(失真保真度)；LPIPS、DISTS、AFINE-FR(基于参考的感知质量)；NIQE、MUSIQ、MANIQA等(无参考感知质量)

### 对比方法
- **多步设置**：ResShift(纯视觉)、StableSR、PASD、SeeSR、DiT4SR(基于T2I)
- **单步设置**：SinSR(纯视觉)、OSEDiff、PiSA-SR(基于T2I)

## 4. 资源与算力

论文未明确提及具体的GPU型号、数量或训练时长等算力信息。仅提到训练了两个VOSR变体(VOSR-0.5B和VOSR-1.4B)，使用LightningDiT作为骨干网络，但没有提供具体的训练资源消耗数据，这限制了对其效率的全面评估。

## 5. 实验数量与充分性

### 实验数量
- 在三个数据集(LSDIR、ScreenSR、RealSR)上进行定量比较
- 消融研究：视觉语义条件和部分条件的影响
- 用户研究：30张图像，10名参与者评估人类感知偏好
- 引导尺度行为分析
- 不同蒸馏方法比较(基于捷径和基于递归一致性)

### 充分性与公平性
- 实验覆盖合成和真实世界场景
- 对比多种代表性方法，包括纯视觉和基于T2I的方法
- 使用多种评估指标全面评估性能
- 消融研究验证关键组件有效性
- 用户研究提供人类感知验证

实验设计充分且公平，但缺乏具体训练资源消耗信息可能影响对方法效率的全面评估。

## 6. 论文的主要结论与发现

### 主要结论
- 高质量生成式超分辨率可通过纯视觉模型实现，无需多模态预训练
- VOSR训练成本不到基于T2I方法的十分之一
- 在多步和单步设置中均达到具有竞争力的感知质量和效率
- 在合成和真实世界基准上产生更忠实结构且幻觉更少的结果

### 主要发现
- 视觉语义条件对解决严重退化下的语义模糊至关重要
- 恢复导向引导比标准无条件分支更适合SR任务
- 引导尺度影响与T2I-based方法相反：大尺度有利于保真度，小尺度允许更强生成
- 纯视觉框架能平衡感知质量和输入保真度，优于基于T2I的方法

## 7. 优点

### 方法亮点
- **纯视觉框架**：专门为恢复任务设计，不依赖多模态预训练
- **双分支条件设计**：结合结构条件和语义条件，平衡保真度和生成能力
- **恢复导向引导**：保留弱LR锚点，更适合恢复任务
- **高效蒸馏**：从多步模型蒸馏为单步模型，保持高质量同时提高效率
- **视觉语义提取**：使用预训练视觉编码器，避免文本表示的空间粗糙问题

### 实验设计亮点
- 构建新ScreenSR基准，提供更高质量参考图像
- 使用多种评估指标全面评估方法性能
- 详细消融研究验证关键组件有效性
- 包含用户研究提供人类感知验证
- 引导尺度行为分析提供方法机制深入理解

## 8. 不足与局限

- **规模限制**：训练数据规模和模型大小仍落后于构建在10B+规模T2I基础模型上的方法
- **算力信息缺失**：未提供具体训练资源消耗信息，难以全面评估效率
- **任务范围有限**：主要集中在图像超分辨率，未探索更广泛图像恢复任务
- **退化类型覆盖**：可能未充分评估在各种退化类型上的泛化能力
- **应用场景限制**：虽在真实世界基准评估，但可能未覆盖所有实际应用场景
- **内容偏差风险**：模型可能对特定类型图像内容表现更好，存在潜在偏差风险

（完）
