---
title: Your Pre-trained Diffusion Model Secretly Knows Restoration
title_zh: 你的预训练扩散模型秘密地知道修复
authors: "Sudarshan Rajagopalan, Vishal M. Patel"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04924v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 预训练扩散模型用于修复
tldr: 预训练扩散模型在图像恢复方面展现出固有潜力，但传统方法依赖微调或特定控制模块。本研究发现通过直接学习文本编码器输出的提示嵌入可解锁模型的恢复能力，并采用扩散桥框架解决训练不稳定性问题。该方法在多种退化任务上取得竞争性性能，无需额外微调或专用控制模块。
selection_source: fresh_fetch
motivation: 传统扩散模型恢复方法依赖微调或特定控制模块，未能充分利用预训练模型内在的恢复能力。
method: 通过学习文本编码器输出的提示嵌入并采用扩散桥框架对齐训练和推理动态，解锁预训练扩散模型的固有恢复能力。
result: 在WAN视频模型和FLUX图像模型上实现的轻量级提示学习方法在各种退化任务上取得竞争性性能，无需微调或专用控制模块。
conclusion: 预训练扩散模型本身具有恢复行为，可通过直接学习提示嵌入有效利用，为轻量级、通用图像恢复提供了新途径。
---

## 摘要
预训练扩散模型在一体化修复(AiOR)领域取得了显著进展，提供了更好的感知质量和泛化能力。然而，基于扩散的修复方法主要依赖于微调或ControlNet风格的模块，以利用预训练扩散模型的先验知识进行AiOR。在这项工作中，我们表明这些预训练扩散模型本质上具有修复行为，可以通过直接学习文本编码器输出的提示嵌入来解锁。有趣的是，这种行为在很大程度上无法通过文本提示和文本标记嵌入优化来访问。此外，我们观察到简单的提示学习是不稳定的，因为使用退化图像的前向噪声过程与反向采样轨迹不匹配。为了解决这个问题，我们在扩散桥接框架内训练提示，该框架对齐训练和推理动态，强制从嘈杂的退化状态到清晰图像的连贯去噪路径。基于这些见解，我们在预训练的WAN视频模型和FLUX图像模型上引入了我们轻量级学习的提示，将它们转换为高性能的修复模型。大量实验表明，我们的方法在各种退化情况下实现了具有竞争力的性能和泛化能力，同时避免了微调和特定的修复控制模块。

## Abstract
Pre-trained diffusion models have enabled significant advancements in All-in-One Restoration (AiOR), offering improved perceptual quality and generalization. However, diffusion-based restoration methods primarily rely on fine-tuning or Control-Net style modules to leverage the pre-trained diffusion model's priors for AiOR. In this work, we show that these pre-trained diffusion models inherently possess restoration behavior, which can be unlocked by directly learning prompt embeddings at the output of the text encoder. Interestingly, this behavior is largely inaccessible through text prompts and text-token embedding optimization. Furthermore, we observe that naive prompt learning is unstable because the forward noising process using degraded images is misaligned with the reverse sampling trajectory. To resolve this, we train prompts within a diffusion bridge formulation that aligns training and inference dynamics, enforcing a coherent denoising path from noisy degraded states to clean images. Building on these insights, we introduce our lightweight learned prompts on the pre-trained WAN video model and FLUX image models, converting them into high-performing restoration models. Extensive experiments demonstrate that our approach achieves competitive performance and generalization across diverse degradations, while avoiding fine-tuning and restoration-specific control modules.

---

## 论文详细总结（自动生成）

# 论文总结：Your Pre-trained Diffusion Model Secretly Knows Restoration

## 1. 论文的核心问题与整体含义

论文核心问题：预训练扩散模型在图像/视频修复任务中具有内在潜力，但传统方法（如微调或ControlNet风格模块）未能充分利用这种潜力。作者发现预训练扩散模型本质上具有修复行为，但这种行为无法通过传统的文本提示或文本标记嵌入优化来访问。

研究背景：All-in-One Restoration (AiOR) 旨在使用单一统一模型恢复受各种退化（如雾霾、雨、雪、运动模糊、低光等）影响的图像或视频。大型预训练文本到图像/视频扩散模型（如Stable Diffusion、FLUX）因其强大的自然图像分布先验而成为有吸引力的修复骨干网络。然而，现有方法主要通过微调或引入特定控制模块来利用这些先验，这些方法通常需要大量训练，且可能对训练分布过于敏感，从而削弱了强大的预训练先验。

## 2. 论文提出的方法论

### 核心思想
- 预训练扩散模型本身具有修复行为，可通过直接学习文本编码器输出的提示嵌入来解锁
- 解决训练-推理轨迹不匹配问题，通过扩散桥框架对齐训练和推理动态

### 关键技术细节
- **直接优化上下文嵌入空间**：优化文本编码器输出的上下文嵌入p，而非标记空间，以解锁修复行为
- **扩散桥框架**：采用扩散桥接框架对齐训练和推理动态，解决训练-推理轨迹不匹配问题
- **能量导向扩散桥(EBR)**：构建中间状态，确保从嘈杂退化状态到清晰图像的连贯去噪路径

### 算法流程
1. 给定退化图像x_deg和清洁图像x_clean，通过VAE编码器得到潜在表示z_deg和z_clean
2. 构建嘈杂退化状态：z_t^deg = (1-t)z_deg + tε，其中ε~N(0,I)
3. 优化文本编码器输出的上下文嵌入p，使模型从嘈杂状态预测清洁潜在表示
4. 使用扩散桥框架解决训练-推理不匹配：
   - z_t^ebr = (1-t)[(1-t/T0)z_clean + (t/T0)z_deg] + tε
   - 优化p使模型从z_t^ebr预测清洁潜在表示
5. 推理时使用确定性采样器与相同桥接计划，确保中间状态与训练时一致

## 3. 实验设计

### 数据集/场景
- **图像修复(FLUX模型)**：
  - 训练数据：RESIDE(去雾)、Snow100K(去雪)、Rain13K(去雨)、LOLv1(低光)、GoPro(去模糊)
  - OOD测试：HazeRD、LHP、WeatherBench、4KRD、SICE
  - 混合和未见退化：TOLED、POLED、CDD、LOLBlur

- **视频修复(WAN模型)**：
  - 训练数据：REVIDE、NTU-Rain、RVSD、SDSD、GoPro
  - OOD测试：RHVD、NTU-Rain、LasVR、AAU-RainSnow、Lol-iPhone、4KRD

### Benchmark
- 图像质量指标：PSNR、SSIM、LPIPS、DISTS、CLIPIQA、MUSIQ
- 视频质量指标：DOVER

### 对比方法
- 图像修复：DCPT、DFPIR、AutoDIR、PixWizard、FoundIR
- 视频修复：ViWS-Net、AverNet

## 4. 资源与算力

- 使用2× NVIDIA H100 GPU（80GB）
- 训练参数：WAN模型训练20k迭代，FLUX模型训练40k迭代
- 批量大小：2

## 5. 实验数量与充分性

### 实验数量
- 多个数据集上的实验（图像和视频修复）
- 五种退化类型（去雾、去雨、去雪、低光增强、去模糊）
- OOD、混合和未见退化测试
- 消融实验（桥接方法比较、T0参数选择、提示注入方式等）

### 充分性
- 实验覆盖多种退化类型和数据分布
- 包含定量和定性评估
- 进行了消融实验验证关键组件
- 与多种SOTA方法进行了比较
- 实验设计客观公平，使用标准指标和评估协议

## 6. 论文的主要结论与发现

### 主要结论
- 预训练扩散模型本身具有修复行为，可通过直接学习文本编码器输出的提示嵌入来解锁
- 文本标记空间优化无法有效访问修复行为，而上下文嵌入空间优化可以
- 训练-推理轨迹不匹配是简单提示学习不稳定的主要原因
- 扩散桥框架可以解决轨迹不匹配问题，实现有效的提示学习

### 主要发现
- 修复知识存在于预训练扩散先验中，但无法通过文本标记接口访问
- 能量导向扩散桥(EBR)比传统扩散桥(DDBM)更适合修复任务
- 轻量级提示学习可以在不微调主干网络的情况下实现高性能修复

## 7. 优点

### 方法亮点
- 发现并解锁了预训练扩散模型的内在修复能力
- 提出轻量级提示学习方法，无需微调或专用控制模块
- 解决了训练-推理轨迹不匹配的关键问题
- 适用于多种预训练模型（WAN和FLUX）

### 实验设计亮点
- 全面评估方法在多种退化类型和数据分布上的性能
- 包含OOD、混合和未见退化测试，验证泛化能力
- 进行详细消融实验，验证关键组件有效性
- 使用多种评估指标，包括感知质量指标

## 8. 不足与局限

- 实验主要集中在常见图像/视频退化类型，可能未覆盖所有可能的退化场景
- 训练数据可能存在偏差，特别是视频修复任务中训练数据量相对较小
- 提示学习需要为每种退化类型单独训练，对于未知退化类型可能需要额外训练
- 方法依赖于预训练扩散模型，可能受限于这些模型的架构和训练数据
- 计算资源需求较高，特别是对于大型模型如FLUX（120亿参数）
- 未充分探索方法在极端退化情况下的性能
- 未研究方法在实时应用中的计算效率

（完）
