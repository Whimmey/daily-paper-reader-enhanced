---
title: Lightweight LLM Agent Memory with Small Language Models
title_zh: 基于小型语言模型的轻量级LLM智能体记忆
authors: "Jiaquan Zhang, Chaoning Zhang, Shuxu Chen, Zhenzhen Huang, Pengcheng Zheng, Zhicheng Wang, Ping Guo, Fan Mo, Sung-Ho Bae, Jie Zou, Jiwei Wei, Yang Yang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07798v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 轻量级大模型记忆系统
tldr: 本文提出LightMem，一种由小型语言模型驱动的轻量级LLM代理内存系统，通过模块化内存管理、两阶段检索流程和离线整合机制，在保持低延迟的同时提高了内存准确性，解决了传统检索系统不稳定性和大型模型调用延迟的问题。
selection_source: fresh_fetch
motivation: 现有LLM代理内存系统面临检索式内存准确性不稳定和大型模型调用延迟过高的双重挑战。
method: LightMem将内存分为短期、中期和长期记忆，采用两阶段检索流程并分离在线处理与离线整合，使用小型语言模型驱动内存操作。
result: 实验显示LightMem在LoCoMo数据集上平均F1提升约2.5，检索延迟仅83ms，端到端延迟581ms，实现了高效准确的内存管理。
conclusion: LightMem通过小型语言模型和模块化设计，为LLM代理提供了一种平衡准确性和计算效率的轻量级内存解决方案。
---

## 摘要
尽管LLM智能体可以利用工具处理复杂任务，但它们仍需要记忆来保持跨轮次一致性并在长程交互中积累可重用信息。然而，基于检索的外部记忆系统虽然在线开销低，但由于查询构建和候选筛选有限，准确性不稳定。相比之下，许多系统使用重复的大模型调用来进行在线记忆操作，提高了准确性但在长交互中累积了延迟。我们提出了LightMem，一个由小型语言模型(SLMs)驱动的轻量级记忆系统，用于改善智能体记忆。LightMem模块化了记忆检索、写入和长期整合，并将在线处理与离线整合分离，以在有限计算下实现高效记忆调用。我们将记忆组织为短期记忆(STM)用于即时对话上下文，中期记忆(MTM)用于可重用交互摘要，以及长期记忆(LTM)用于整合知识，并使用用户标识符支持多用户环境下的独立检索和增量维护。在线操作中，LightMem在固定检索预算下运行，并通过两阶段过程选择记忆：基于向量的粗略检索后跟语义一致性重排序。离线操作中，它抽象可重用交互证据并将其增量整合到LTM中。实验显示在不同模型规模上都有提升，在LoCoMo上平均F1改进约2.5，更加有效且具有低中位延迟(检索83毫秒；端到端581毫秒)。

## Abstract
Although LLM agents can leverage tools for complex tasks, they still need memory to maintain cross-turn consistency and accumulate reusable information in long-horizon interactions. However, retrieval-based external memory systems incur low online overhead but suffer from unstable accuracy due to limited query construction and candidate filtering. In contrast, many systems use repeated large-model calls for online memory operations, improving accuracy but accumulating latency over long interactions. We propose LightMem, a lightweight memory system for better agent memory driven by Small Language Models (SLMs). LightMem modularizes memory retrieval, writing, and long-term consolidation, and separates online processing from offline consolidation to enable efficient memory invocation under bounded compute. We organize memory into short-term memory (STM) for immediate conversational context, mid-term memory (MTM) for reusable interaction summaries, and long-term memory (LTM) for consolidated knowledge, and uses user identifiers to support independent retrieval and incremental maintenance in multi-user settings. Online, LightMem operates under a fixed retrieval budget and selects memories via a two-stage procedure: vector-based coarse retrieval followed by semantic consistency re-ranking. Offline, it abstracts reusable interaction evidence and incrementally integrates it into LTM. Experiments show gains across model scales, with an average F1 improvement of about 2.5 on LoCoMo, more effective and low median latency (83 ms retrieval; 581 ms end-to-end).

---

## 论文详细总结（自动生成）

# 论文总结：基于小型语言模型的轻量级LLM智能体记忆系统

## 1. 论文的核心问题与整体含义

论文针对LLM智能体记忆系统的效率与准确性之间的权衡问题展开研究。现有系统主要分为两类：基于检索的外部记忆系统虽然在线开销低，但由于查询构建和候选筛选有限，准确性不稳定；而使用大模型调用的记忆系统虽然提高了准确性，但在长交互中累积了显著延迟。LLM智能体需要记忆来保持跨轮次一致性并在长程交互中积累可重用信息，但现有系统无法同时满足高效性和准确性要求。LightMem旨在解决这一矛盾，提供一种轻量级记忆解决方案。

## 2. 论文提出的方法论

### 核心思想
LightMem是一种由小型语言模型(SLMs)驱动的轻量级记忆系统，通过模块化内存管理、两阶段检索流程和离线整合机制，在保持低延迟的同时提高内存准确性。

### 关键技术细节
- **三层记忆结构**：
  - 短期记忆(STM)：即时对话上下文，作为工作内存
  - 中期记忆(MTM)：可重用交互摘要，存储用户特定信息
  - 长期记忆(LTM)：整合知识，存储去标识化的通用语义知识

- **小型语言模型驱动**：
  - SLM-1：意图和查询规划，将用户输入转换为结构化检索计划
  - SLM-2：候选验证和压缩，执行语义一致性检查
  - SLM-3：增量记忆写入，提取可重用信息并压缩为MTM条目

- **两阶段检索流程**：
  - 第一阶段：基于元数据约束的粗略检索，返回2K个候选
  - 第二阶段：语义一致性的重排序和压缩，返回最终K个记忆

- **离线整合**：将MTM中的高价值证据抽象为去标识化的LTM知识，增量整合到图结构中

## 3. 实验设计

### 数据集/场景
- **LoCoMo**：评估逻辑推理的基准，包含长对话(平均9K token)，覆盖五个核心任务类别：单跳、多跳、时间、开放领域和对抗性
- **DialSim**：从电视节目衍生的对话模拟数据集，包含1,300个多方会话，跨越多年

### Benchmark
- **F1和BLEU-1**：测量答案正确性和与参考响应的token级重叠
- **ROUGE-L和METEOR**：捕获序列级重叠和软token匹配
- **SBERT相似度**：量化生成响应与参考之间的语义一致性

### 对比方法
- **LoCoMo**：直接依赖长上下文重放
- **ReadAgent**：依赖外部缓存检索
- **MemoryBank**：存储用户事件摘要并检索相关条目
- **MemGPT**：将上下文窗口视为虚拟内存
- **A-MEM**：通过LLM驱动的笔记记录和自动链接构建自组织记忆网络

## 4. 资源与算力

- **推理设备**：NVIDIA RTX 4090 (24GB) GPU
- **小型语言模型**：
  - SLM-1：用于HQ生成和检索控制
  - SLM-2：用于语义重排序和压缩
  - SLM-3：用于在线写作和MTM维护
- **模型部署**：
  - 默认使用量化Llama-3.2-1B-Instruct模型，通过Ollama实现
  - 还评估了Qwen2.5-1.5B-Instruct模型以验证鲁棒性
- **离线整合**：由大上下文LLM处理，与在线路径解耦

论文未明确提到训练时长或具体的训练资源使用情况。

## 5. 实验数量与充分性

### 实验数量
- 在两个数据集(LoCoMo和DialSim)上进行了实验
- 使用了多种骨干模型：GPT-4o、GPT-4o-mini、Qwen2.5(1.5B和3B)、Llama 3.2(1B和3B)
- 进行了5组消融研究，评估各个组件的贡献：
  - 移除语义重排序
  - 移除HQ和检索路由
  - 移除MTM
  - 移除离线整合
  - 移除图结构
- 进行了统计显著性分析，使用配对bootstrap重采样

### 充分性评估
- **充分性**：实验覆盖了不同模型规模和不同类型的任务，消融研究验证了各个组件的必要性，使用多种评估指标全面评估性能
- **客观性**：对比了多种基线方法，覆盖了不同的记忆系统设计，统计显著性分析增强了结果的可信度
- **公平性**：使用GPT-4o-mini作为固定响应生成器进行公平比较，隔离了记忆系统的影响

## 6. 论文的主要结论与发现

- LightMem在不同模型规模上都有性能提升
- 在LoCoMo上平均F1改进约2.5
- 实现了低中位延迟(检索83毫秒；端到端581毫秒)
- 在DialSim上提高了语义一致性，SBERT相似度从19.51(A-MEM)提高到23.40
- 两阶段检索流程显著提高了检索准确性
- 小型语言模型能够有效处理高频率、结构化的记忆决策任务
- 在线处理与离线整合的分离设计实现了高效记忆使用

## 7. 优点

### 方法亮点
- **模块化设计**：将内存检索、写入和长期整合模块化，实现独立优化和弹性扩展
- **两阶段检索流程**：结合向量相似性和语义一致性，提高检索准确性
- **小型语言模型驱动**：使用SLMs处理高频率在线记忆操作，降低延迟
- **在线-离线解耦**：将轻量级在线处理与重计算离线整合分离
- **用户级隔离**：通过用户标识符支持多用户环境下的独立检索和增量维护

### 实验设计亮点
- 使用多种骨干模型评估泛化能力
- 采用多种评估指标全面评估性能
- 消融研究验证各组件贡献
- 统计显著性分析增强结果可信度
- 延迟测量评估实际应用可行性

## 8. 不足与局限

### 实验覆盖
- 仅在两个数据集上进行了评估，可能无法覆盖所有应用场景
- 主要关注对话场景，可能不适用于其他类型的任务
- 对比方法的选择可能存在偏差，没有包含所有可能的基线

### 偏差风险
- 使用GPT-4o-mini作为固定响应生成器可能引入模型特定的偏差
- 实验结果可能依赖于特定的提示设计

### 应用限制
- 离线整合需要额外的计算资源，可能不适合资源受限环境
- 用户标识符的使用可能带来隐私问题
- 在复杂的多用户交互场景中，用户级隔离可能导致信息共享受限

### 方法局限
- 当SLM-1为模糊查询生成多个HQ时，固定Top-K预算可能导致焦点分散
- 小型语言模型的容量和表示能力有限，可能影响复杂记忆操作
- 离线整合的频率和策略可能影响长期记忆的质量

（完）
