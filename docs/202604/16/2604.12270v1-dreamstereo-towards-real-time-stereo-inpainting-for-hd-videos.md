---
title: "DreamStereo: Towards Real-Time Stereo Inpainting for HD Videos"
title_zh: "DreamStereo: 面向高清视频的实时立体修复"
authors: "Yuan Huang, Sijie Zhao, Jing Cheng, Hao Xu, Shaohui Jiao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12270v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 立体视频修复方法
tldr: 立体视频修复面临数据稀缺和计算冗余两大挑战。本文提出DreamStereo方法，包含梯度感知视差扭曲、基于视差的双投影和稀疏感知立体修复三个组件，显著减少冗余计算，实现HD视频的实时立体修复处理，在保持高质量的同时提升10.7倍速度。
selection_source: fresh_fetch
motivation: 高质量立体修复数据集的缺乏和现有方法对全帧的冗余处理限制了立体视频修复的效率和效果。
method: DreamStereo通过梯度感知视差扭曲、基于视差的双投影和稀疏感知立体修复三个组件解决数据稀缺和计算冗余问题。
result: "SASI方法减少70%以上冗余标记，实现10.7倍加速，可在单个A100 GPU上实时处理HD视频(25 FPS)。"
conclusion: DreamStereo实现了高效高质量的立体视频修复，为实时处理HD视频提供了有效解决方案。
---

## 摘要
立体视频修复旨在用视觉连贯的内容填充扭曲视频中的遮挡区域，同时保持时间一致性，这仍然是一个具有挑战性的开放问题。待填充区域沿物体边界分布，仅占据每帧的一小部分，导致两个关键挑战。首先，由于高质量立体修复数据集的稀缺，现有方法在此类任务上表现不佳，这限制了它们学习有效修复先验的能力。其次，这些方法对帧的所有区域应用相同的处理，尽管大多数像素不需要修改，导致大量冗余计算。为解决这些问题，我们引入了三个相互关联的组件。我们首先提出了梯度感知视差扭曲(GAPW)，它利用反向扭曲和坐标映射函数的梯度来获得连续的边缘和平滑的遮挡区域。然后，引入了一种基于视差的双投影(PBDP)策略，该策略结合GAPW来生成几何一致的立体修复对和准确的遮挡掩码，无需立体视频。最后，我们提出了稀疏感知立体修复(SASI)，它减少了70%以上的冗余标记，在扩散推理过程中实现了10.7倍的加速，并提供了与全计算相当的结果，使单个A100 GPU能够实时处理高清(768 x 1280)视频，帧率为25 FPS。

## Abstract
Stereo video inpainting, which aims to fill the occluded regions of warped videos with visually coherent content while maintaining temporal consistency, remains a challenging open problem. The regions to be filled are scattered along object boundaries and occupy only a small fraction of each frame, leading to two key challenges. First, existing approaches perform poorly on such tasks due to the scarcity of high-quality stereo inpainting datasets, which limits their ability to learn effective inpainting priors. Second, these methods apply equal processing to all regions of the frame, even though most pixels require no modification, resulting in substantial redundant computation. To address these issues, we introduce three interconnected components. We first propose Gradient-Aware Parallax Warping (GAPW), which leverages backward warping and the gradient of the coordinate mapping function to obtain continuous edges and smooth occlusion regions. Then, a Parallax-Based Dual Projection (PBDP) strategy is introduced, which incorporates GAPW to produce geometrically consistent stereo inpainting pairs and accurate occlusion masks without requiring stereo videos. Finally, we present Sparsity-Aware Stereo Inpainting (SASI), which reduces over 70% of redundant tokens, achieving a 10.7x speedup during diffusion inference and delivering results comparable to its full-computation counterpart, enabling real-time processing of HD (768 x 1280) videos at 25 FPS on a single A100 GPU.

---

## 论文详细总结（自动生成）

# DreamStereo 论文总结

## 1. 论文的核心问题与整体含义

立体视频修复(stereo video inpainting)旨在用视觉连贯的内容填充扭曲视频中的遮挡区域，同时保持时间一致性，这是一个具有挑战性的开放问题。该问题主要源于两个关键挑战：
- **数据稀缺**：高质量立体修复数据集的稀缺限制了现有方法学习有效修复先验的能力
- **计算冗余**：待填充区域仅占每帧的一小部分，但现有方法对所有像素进行相同处理，导致大量不必要的计算

随着AR/VR设备的普及，立体数字媒体需求增长，但多相机视觉捕获设备的高成本限制了立体视觉内容的广泛发展。因此，如何将大量现有的单目视觉内容转换为立体内容成为一个重要问题。

## 2. 论文提出的方法论

论文提出DreamStereo方法，包含三个相互关联的核心组件：

### 2.1 梯度感知视差扭曲(GAPW)
- **核心思想**：解决传统前向扭曲(forward warping)导致的分散像素问题
- **技术细节**：结合反向扭曲(backward warping)和坐标映射函数的梯度计算
- **算法流程**：
  1. 计算坐标映射函数的雅可比矩阵：JT(x′, y′) = [∂x′/∂x, ∂x′/∂y; ∂y′/∂x, ∂y′/∂y]
  2. 通过阈值δ确定遮挡掩码：M(x′, y′) = ||JT(x′, y′)||₂ > δ
  3. 在双目系统中简化为：M(x′, y′) = ∂x′/∂x > δ

### 2.2 基于视差的双投影(PBDP)
- **核心思想**：从单目视频生成高质量立体修复数据对，无需真实立体视频
- **技术细节**：
  1. 使用深度估计器预测单目视频的视差D₁
  2. 通过GAPW将视频V₁从视图view₁扭曲到视图view₂，得到V₂和D₂
  3. 再次应用GAPW将V₂从视图view₂扭曲回视图view₁，得到V′₁和遮挡掩码M₁
  4. 将(V₁, M₁)作为训练对加入数据集

### 2.3 稀疏感知立体修复(SASI)
- **核心思想**：减少视觉标记的冗余计算，提高推理速度
- **技术细节**：
  1. 使用3D变分自编码器(3D VAE)压缩输入视频到潜在空间
  2. 基于GAPW生成的掩码进行稀疏标记选择，仅保留遮挡区域及其周围区域的标记
  3. 在扩散模型推理过程中，仅处理选中的稀疏标记
  4. 通过膨胀函数Φ(·)和选择函数S(·)实现稀疏处理

## 3. 实验设计

### 3.1 数据集
- **HD-100**：从互联网4K视频收集的真实世界数据集(100个样本)
- **Dynamic Replica**：合成立体数据集(20个验证样本)，具有真实视差
- **SVD**：从Apple Vision Pro子集选择的空间视频数据集(20个样本)

### 3.2 评估指标
- **PSNR**：峰值信噪比，衡量修复质量
- **SSIM**：结构相似性指数，评估结构保持能力
- **LPIPS**：感知相似性，衡量感知质量

### 3.3 对比方法
- **立体修复方法**：StereoCrafter、ZeroStereo
- **通用视频修复模型**：VACE-1.3B、ProPainter
- **2D转3D方法**：Deep3D、Depthify.ai、Owl3D

## 4. 资源与算力

- **GPU型号**：NVIDIA A100
- **训练配置**：使用4个A100 GPU
- **训练时长**：
  - Stage 1：10k步，批量大小12
  - Stage 2：2.5k步，批量大小2
- **学习率**：4×10⁻⁵
- **推理性能**：在单个A100 GPU上实现25 FPS的HD视频(768×1280)实时处理

## 5. 实验数量与充分性

论文进行了多组充分且客观的实验：

- **跨数据集评估**：在HD-100、Dynamic Replica和SVD三个不同类型的数据集上进行了测试
- **消融研究**：
  - 数据构建方法对比(GAPW vs TrajectoryCrafter)
  - 稀疏感知加速效果(不同保留率下的性能与速度权衡)
  - 宽基线鲁棒性测试(不同最大视差设置下的表现)
- **定量与定性评估**：同时提供数值指标和视觉结果对比
- **公平比较**：使用统一的输入和计时设置，确保比较的公平性

实验设计全面覆盖了方法的不同组件和参数设置，验证了各组件的有效性和方法的鲁棒性。

## 6. 论文的主要结论与发现

- **GAPW有效性**：相比传统前向扭曲，GAPW能生成更平滑的遮挡区域和更少的分散像素，提高数据质量
- **PBDP价值**：基于单目视频的立体数据构建方法能有效生成高质量的立体修复对，无需真实立体视频
- **SASI效率**：通过减少70%以上的冗余标记，实现10.7倍推理加速，同时保持与全计算相当的质量
- **实时性能**：在单个A100 GPU上实现HD视频(768×1280)的25 FPS实时处理
- **宽基线鲁棒性**：方法在大视差设置下仍能保持良好的几何一致性和视觉质量

## 7. 优点

- **创新性**：首次提出针对立体视频修复的稀疏感知处理框架
- **高效性**：显著减少计算冗余，实现实时处理HD视频的能力
- **数据效率**：通过PBDP策略，无需真实立体视频即可构建高质量训练数据
- **质量保证**：在保持高质量修复的同时实现速度提升
- **全面评估**：在多个数据集上进行了充分验证，包括真实世界和合成数据

## 8. 不足与局限

- **深度估计依赖**：方法性能依赖于深度估计的准确性，深度估计误差会直接影响修复质量
- **特定场景挑战**：
  - 透明物体处理存在困难
  - 反射表面的几何一致性难以保证
  - 高频细节的保真度有限
- **应用限制**：
  - 仅在特定分辨率(768×1280)和帧率(25 FPS)下进行了测试
  - 实验主要针对一般场景，对特殊场景的泛化能力有待验证
- **计算资源**：虽然实现了实时处理，但仍需要高端GPU(A100)支持
- **潜在偏差**：训练数据可能存在分布偏差，影响在未见场景上的表现

（完）
