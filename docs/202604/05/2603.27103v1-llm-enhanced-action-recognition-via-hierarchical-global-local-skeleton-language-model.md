---
title: LLM Enhanced Action Recognition via Hierarchical Global-Local Skeleton-Language Model
title_zh: 基于层次化全局-局部骨架-语言模型的大语言模型增强动作识别
authors: "Ruosi Wang, Fangwei Zuo, Lei Li, Zhaoqiang Xia"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27103v1"
tags: ["query:dl-cv"]
score: 8.0
evidence: 大模型增强的骨架动作识别
tldr: 针对基于骨架的动作识别中现有方法难以捕捉长距离关节依赖和复杂时间动态的问题，本文提出HocSLM模型。该方法结合层次化全局-局部网络(HGLNet)和大型视觉语言模型(VLM)，通过骨架-语言融合模块实现跨模态语义对齐，在多个基准数据集上达到最先进性能。
selection_source: carryover_cache
motivation: 现有GCN方法依赖短距离运动拓扑，难以捕捉长距离关节依赖和复杂时间动态，且缺乏足够的动作语义建模。
method: 提出层次化全局-局部骨架-语言模型(HocSLM)，包含HGLNet骨架网络、VLM文本生成和骨架-语言融合模块。
result: 在NTU RGB+D 60、NTU RGB+D 120和Northwestern-UCLA三个基准数据集上达到最先进性能。
conclusion: 通过结合层次化骨架网络和大型语言模型，HocSLM显著提升了动作识别的语义表征能力和跨模态理解能力。
---

## 摘要
基于骨架的人体动作识别近年来取得了显著进展。然而，大多数现有的基于GCN的方法依赖于短程运动拓扑结构，这不仅难以捕捉长程关节依赖关系和复杂的时间动态，还由于对动作语义建模不足而限制了跨模态语义对齐和理解。为解决这些挑战，我们提出了一种层次化全局-局部骨架-语言模型(HocSLM)，使大型动作模型能更好地代表动作语义。首先，我们设计了一个层次化全局-局部网络(HGLNet)，该网络由复合拓扑空间模块和双路径层次时间模块组成。通过协同整合多级全局和局部模块，HGLNet在保持人体结构先验知识的同时，实现了全局和局部尺度的动态协同建模，显著增强了模型对复杂时空关系的表征能力。然后，采用大型视觉-语言模型(VLM)通过将原始RGB视频序列输入该模型来生成文本描述，为骨架-语言模型的进一步训练提供丰富的动作语义。此外，我们通过结合HGLNet和生成的描述特征，引入了一个骨架-语言序列融合模块，该模块利用骨架-语言模型(SLM)在统一的语义空间内精确对齐骨架时空特征和文本动作描述。SLM模型能够显著增强HGLNet的语义辨别能力和跨模态理解能力。大量实验表明，所提出的HocSLM在三个主流基准数据集上达到了最先进的性能：NTU RGB+D 60、NTU RGB+D 120和Northwestern-UCLA。

## Abstract
Skeleton-based human action recognition has achieved remarkable progress in recent years. However, most existing GCN-based methods rely on short-range motion topologies, which not only struggle to capture long-range joint dependencies and complex temporal dynamics but also limit cross-modal semantic alignment and understanding due to insufficient modeling of action semantics. To address these challenges, we propose a hierarchical global-local skeleton-language model (HocSLM), enabling the large action model be more representative of action semantics. First, we design a hierarchical global-local network (HGLNet) that consists of a composite-topology spatial module and a dual-path hierarchical temporal module. By synergistically integrating multi-level global and local modules, HGLNet achieves dynamically collaborative modeling at both global and local scales while preserving prior knowledge of human physical structure, significantly enhancing the model's representation of complex spatio-temporal relationships. Then, a large vision-language model (VLM) is employed to generate textual descriptions by passing the original RGB video sequences to this model, providing the rich action semantics for further training the skeleton-language model. Furthermore, we introduce a skeleton-language sequential fusion module by combining the features from HGLNet and the generated descriptions, which utilizes a skeleton-language model (SLM) for aligning skeletal spatio-temporal features and textual action descriptions precisely within a unified semantic space. The SLM model could significantly enhance the HGLNet's semantic discrimination capabilities and cross-modal understanding abilities. Extensive experiments demonstrate that the proposed HocSLM achieves the state-of-the-art performance on three mainstream benchmark datasets: NTU RGB+D 60, NTU RGB+D 120, and Northwestern-UCLA.

---

## 论文详细总结（自动生成）

# 论文总结：基于层次化全局-局部骨架-语言模型的大语言模型增强动作识别

## 1. 论文的核心问题与整体含义

论文针对基于骨架的人体动作识别领域中现有方法的局限性展开研究。核心问题在于：大多数基于GCN的方法依赖于短程运动拓扑结构，难以捕捉长程关节依赖关系和复杂的时间动态，同时由于对动作语义建模不足，限制了跨模态语义对齐和理解能力。研究背景是骨架数据相比RGB图像和光流特征具有更强的视角不变性和抗遮挡能力，但传统方法如ST-GCN、MS-G3DNet等在建模复杂动作时存在明显局限性，特别是在需要精细语义理解的场景中表现不佳。

## 2. 论文提出的方法论

### 核心思想
提出层次化全局-局部骨架-语言模型(HocSLM)，结合层次化全局-局部网络(HGLNet)和大型视觉语言模型(VLM)，通过骨架-语言融合模块实现跨模态语义对齐，增强模型对动作语义的理解能力。

### 关键技术细节
- **层次化全局-局部网络(HGLNet)**：
  - 复合拓扑空间模块(CTS)：包含全局空间调制块(GSM)和局部空间增强块(LSE)
    - GSM基于自注意力机制引入相似性-差异性调制块，增强空间表征能力
    - LSE基于物理连接先验，通过动态门控机制自适应增强判别性局部关节关系
  - 双路径层次时间模块(DHT)：包含全局时间注意力模块(GTA)和局部时间卷积模块(LTC)
    - GTA利用自注意力机制灵活捕获任意帧间的长程语义依赖
    - LTC通过四并行分支(标准卷积、扩张卷积、池化和1×1卷积)高效提取短程时序模式

- **骨架-语言序列融合模块(SSF)**：
  - 骨架语义理解块：通过全局平均池化和双层MLP将骨架特征映射到语言模型语义空间
  - 条件文本生成块：将视觉token与文本提示和描述拼接，输入Qwen3-4B模型进行自回归文本生成
  - 协同优化块：联合优化生成损失和分类损失，实现跨模态语义对齐

### 算法流程
输入骨架序列和RGB视频，HGLNet提取骨架特征，VLM生成文本描述，SSF模块对齐骨架特征和文本描述，最后联合优化分类和生成任务。

## 3. 实验设计

### 数据集
- **NTU RGB+D**：56,880个样本，60个动作类别，采用Cross-Subject(X-Sub)和Cross-View(X-View)评估协议
- **NTU RGB+D 120**：113,000+样本，120个动作类别，采用Cross-Subject(X-Sub)和Cross-Setup(X-Setup)评估协议
- **Northwestern-UCLA**：1,494个样本，10个动作类别，采用标准跨视角评估协议

### 评估指标
- Top-1 Accuracy：直接反映预测最高置信度类别与真实标签匹配的比例

### 对比方法
与多种最先进方法进行对比，包括：
- 传统方法：ST-GCN, AS-GCN, 2s-AGCN, SGN, AGC-LSTM, DGNN
- 改进方法：ST-TR-agcn, Shift-GCN, DC-GCN + ADG, Dynamic-GCN
- 多尺度方法：MS-G3D, MST-GCN
- 最新方法：CTR-GCN, Info-GCN, GAP, LA-GCN等

## 4. 资源与算力

- **硬件**：NVIDIA GeForce RTX Pro 6000 GPU
- **软件平台**：PyTorch
- **训练参数**：
  - 批处理大小：64
  - 训练轮数：85个epoch
  - 初始学习率：1×10^-4
  - 权重衰减系数：0.01
  - 优化器：AdamW

## 5. 实验数量与充分性

### 实验数量
- 在三个主流数据集上进行全面评估
- 进行详细的消融实验，验证各模块贡献
- 进行多流融合实验(6种不同数据流)
- 提供可视化分析，包括时空注意力分布和文本生成示例

### 充分性评估
实验设计较为全面，覆盖了不同数据集和评估协议；消融实验充分验证了各个模块的有效性；与多种最先进方法进行了公平对比；提供了可视化结果增强结果解释性。实验结果表明方法在不同场景下均具有稳定性能。

## 6. 论文的主要结论与发现

- HocSLM在三个基准数据集上达到最先进性能，在NTU RGB+D 60上达到93.6%(X-Sub)和97.1%(X-View)，在NTU RGB+D 120上达到90.9%(X-Sub)和91.8%(X-Setup)，在Northwestern-UCLA上达到97.4%
- 层次化全局-局部网络能有效捕捉长程关节依赖和复杂时间动态，全局和局部模块的协同建模对性能提升至关重要
- 骨架-语言融合模块显著提升了模型的语义辨别能力和跨模态理解能力
- 多流融合策略进一步提升了模型性能，六流融合达到93.61%的准确率

## 7. 优点

### 方法亮点
- 创新性地将大型语言模型引入骨架动作识别任务，实现跨模态语义对齐
- 设计层次化全局-局部网络，有效平衡全局和局部特征建模
- 提出骨架-语言序列融合模块，实现骨架时空特征与文本描述的精确对齐
- 联合优化分类和生成任务，增强模型语义理解能力

### 实验设计亮点
- 在多个主流数据集上进行全面评估，验证方法泛化能力
- 进行详细的消融实验，验证各模块贡献
- 提供可视化分析，增强结果解释性
- 与多种最先进方法进行公平对比

## 8. 不足与局限

- 实验主要在标准数据集上进行，缺乏在真实场景中的验证
- 模型计算复杂度较高，可能影响实时应用性能
- 依赖大型预训练语言模型，增加了部署成本
- 未充分探索模型在不同文化和动作类别上的泛化能力
- 对遮挡和噪声环境的鲁棒性未充分验证
- 模型在资源受限设备上的部署可行性未进行探讨

（完）
