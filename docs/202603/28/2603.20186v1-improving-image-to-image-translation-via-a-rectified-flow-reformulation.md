---
title: Improving Image-to-Image Translation via a Rectified Flow Reformulation
title_zh: 通过修正流重构改进图像到图像翻译
authors: "Satoshi Iizuka, Shun Okamoto, Kazuhiro Fukui"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20186v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 图像到图像翻译方法
tldr: 该研究提出I2I-RFR方法，将标准图像到图像回归网络重新表述为连续时间传输模型。通过噪声损坏目标与输入的通道级联和t加权像素损失优化，实现ODE推理时的渐进式细化。实验表明该方法在多种任务中提升性能，特别是感知质量和细节保留，同时保持轻量级和简单训练流程。
selection_source: fresh_fetch
motivation: 现有像素级图像到图像回归方法常导致过度平滑，而生成式方法又需要额外组件和复杂训练流程。
method: I2I-RFR通过将噪声损坏的地面真值目标与主干输入通道级联，优化t加权像素损失，实现连续时间传输模型。
result: 在多种图像到图像翻译和视频恢复任务中，I2I-RFR显著提升了性能，特别是在感知质量和细节保留方面。
conclusion: I2I-RFR为传统图像到图像模型提供了一种轻量级融入连续时间细化的方法，无需复杂生成式流程。
---

## 摘要
在这项工作中，我们提出了图像到图像修正流重构(I2I-RFR)，这是一种实用的即插即用重构方法，将标准的I2I回归网络重新表述为连续时间传输模型。虽然逐像素的I2I回归简单、稳定且易于跨任务适应，但它通常会过度平滑病态和多模态目标，而生成式替代方案通常需要额外组件、任务特定调整以及更复杂的训练和推理流程。我们的方法通过将真实目标的噪声损坏版本按通道拼接来增强主干输入，并优化一个简单的t加权像素损失。通过诱导的速度场，该目标可以接受修正流的解释，从而能够在推理时实现基于ODE的渐进式细化，同时 largely 保留了标准的监督训练流程。在大多数情况下，采用I2I-RFR只需要扩展输入通道，并且可以通过几个显式求解器步骤(例如3步)进行推理，无需蒸馏。在多个图像到图像翻译和视频恢复任务上的广泛实验表明，I2I-RFR通常在各种任务和主干网络上提高了性能，特别是在感知质量和细节保留方面有显著提升。总体而言，I2I-RFR提供了一种轻量级的方法，将连续时间细化纳入传统I2I模型，而无需复杂的生成式流程。

## Abstract
In this work, we propose Image-to-Image Rectified Flow Reformulation (I2I-RFR), a practical plug-in reformulation that recasts standard I2I regression networks as continuous-time transport models. While pixel-wise I2I regression is simple, stable, and easy to adapt across tasks, it often over-smooths ill-posed and multimodal targets, whereas generative alternatives often require additional components, task-specific tuning, and more complex training and inference pipelines. Our method augments the backbone input by channel-wise concatenation with a noise-corrupted version of the ground-truth target and optimizes a simple t-reweighted pixel loss. This objective admits a rectified-flow interpretation via an induced velocity field, enabling ODE-based progressive refinement at inference time while largely preserving the standard supervised training pipeline. In most cases, adopting I2I-RFR requires only expanding the input channels, and inference can be performed with a few explicit solver steps (e.g., 3 steps) without distillation. Extensive experiments across multiple image-to-image translation and video restoration tasks show that I2I-RFR generally improves performance across a wide range of tasks and backbones, with particularly clear gains in perceptual quality and detail preservation. Overall, I2I-RFR provides a lightweight way to incorporate continuous-time refinement into conventional I2I models without requiring a heavy generative pipeline.

---

## 论文详细总结（自动生成）

# 论文总结：通过修正流重构改进图像到图像翻译

## 1. 论文的核心问题与整体含义

**研究动机和背景**：
图像到图像(I2I)翻译是计算机视觉的基础问题，涵盖图像超分辨率、修复、低光增强等多种任务。现有方法存在两难困境：像素级回归方法简单稳定但会过度平滑病态和多模态目标；而生成式方法(如GAN、扩散模型)虽能处理多模态但需要额外组件、任务特定调整和复杂训练流程。论文旨在弥合这一差距，提出一种即插即用的方法，结合回归方法的简单性和生成方法处理多模态的能力。

## 2. 论文提出的方法论

**核心思想**：
将标准I2I回归网络重新表述为连续时间传输模型，通过噪声损坏的地面真值目标与输入图像的通道级联来增强主干输入，实现渐进式细化。

**关键技术细节**：
- 构建噪声混合目标：$y_t = (1-t)y + t\varepsilon$，其中$\varepsilon \sim N(0, I)$，$t \in (0,1]$
- 网络输入为$[x; y_t]$，其中$x$是输入图像，$y_t$是噪声损坏的目标
- 使用$t^{-1}$加权的$\ell_1$损失：$E[\|y - f_{\theta'}([x; y_t])\|/t]$
- 使用Beta(2,1)采样策略进行$t$的非均匀采样，避免小$t$值时的梯度方差
- 推理时使用显式Euler求解器，从$t=1$到$t=0$进行$N$步(默认3步)迭代

**算法流程**：
- 训练：采样$(x,y)$，生成$y_t$，计算$t$加权损失，更新网络参数
- 推理：初始化$y_t \sim N(0,I)$，通过$N$步Euler方法求解ODE，得到最终输出

## 3. 实验设计

**数据集/场景**：
- 图像超分辨率：DF2K训练集，Set5、Set14、BSD100、Urban100测试集
- 图像去模糊：GoPro训练集，RealBlur-J测试集
- 低光模糊增强：LOLBlur训练和测试集
- 水下图像增强：LSUI训练集，LSUI和UIEB测试集
- 视频恢复：REDS训练集，DAVIS测试集

**Benchmark**：
- 超分辨率：PSNR、SSIM、LPIPS
- 去模糊：PSNR、SSIM、LPIPS
- 低光增强：PSNR、SSIM、LPIPS
- 水下增强：PSNR、SSIM、LPIPS
- 视频恢复：PSNR、SSIM、LPIPS

**对比方法**：
- 基线回归方法：NLSN、SwinIR、NAFNet、Restormer、FFTFormer、LEDNet、DarkIR、U-Net等
- 扩散模型方法：Palette、Tang et al.的扩散方法
- 复合损失变体：I2I-RFR-CL(保留原始复合损失设计)

## 4. 资源与算力

论文中提到了部分计算资源信息：
- 使用NVIDIA A6000 GPU进行评估
- 训练使用NVIDIA RTX 3090、RTX A6000和H100 GPU
- 视频恢复模型更计算密集，尽可能使用H100 GPU
- 批量大小根据任务和GPU内存从8到32不等
- 训练迭代次数从200K到4M不等，取决于模型

但未明确说明总的训练时长或GPU使用小时数。

## 5. 实验数量与充分性

**实验数量**：
- 在5个主要任务上进行了实验：超分辨率、去模糊、低光增强、水下增强、视频恢复
- 每个任务测试了多个骨干网络
- 进行了消融研究：参数化研究(目标预测vs速度预测)、时间嵌入影响、推理步数研究、t采样策略比较
- 进行了复合损失变体实验(I2I-RFR-CL)

**充分性与公平性**：
- 实验设计较为全面，覆盖了多种I2I任务
- 使用了标准评估指标和基准数据集
- 对比了多种类型的基线方法(回归方法和生成方法)
- 消融研究验证了方法中各组件的重要性
- 实验较为客观，使用相同的训练和评估协议

## 6. 论文的主要结论与发现

**主要结论**：
- I2I-RFR在多种I2I任务上提升了性能，特别是在感知质量和细节保留方面
- 该方法保持了回归模型的简单性和稳定性，同时获得了生成方法处理多模态的能力
- 仅需扩展输入通道和少量推理步骤(3步)，无需额外组件或蒸馏

**主要发现**：
- 直接目标预测比速度预测更适合I2I任务
- Beta(2,1)采样策略比均匀采样更稳定
- 显式时间嵌入在I2I-RFR中不是必需的
- 复合损失变体(I2I-RFR-CL)在某些任务上能进一步提升性能

## 7. 优点

**方法亮点**：
- 即插即用设计，可轻松集成到现有I2I模型中
- 仅需最小架构修改(扩展输入通道)
- 训练流程简单，保持标准监督学习
- 推理高效，仅需少量ODE求解步骤
- 不需要GAN判别器、时间嵌入模块或潜在编码器

**实验设计亮点**：
- 全面覆盖多种I2I任务和骨干网络
- 消融研究深入分析各组件影响
- 与多种类型基线方法进行公平比较
- 提供详细的计算开销分析

## 8. 不足与局限

**实验覆盖**：
- 实验主要集中在RGB图像处理，对非RGB图像(如医学图像)的适用性未充分验证
- 对视频处理任务的研究相对有限，仅测试了老电影修复
- 未探索模型在不同计算资源限制下的表现

**偏差风险**：
- 对极端退化情况下的鲁棒性研究不足
- 未研究模型在真实世界噪声分布下的泛化能力
- 对不同图像分辨率和尺寸的适应性未充分探索

**应用限制**：
- 方法在某些特定任务(如水下增强的SFGNet)上表现不稳定
- 未探索模型在实时应用中的性能
- 对计算资源的要求虽低于扩散模型，但仍高于标准回归方法

（完）
