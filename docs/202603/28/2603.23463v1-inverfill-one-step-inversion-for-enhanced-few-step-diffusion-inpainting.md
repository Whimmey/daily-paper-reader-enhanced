---
title: "InverFill: One-Step Inversion for Enhanced Few-Step Diffusion Inpainting"
title_zh: "InverFill: 一步反演用于增强少步扩散图像修复"
authors: "Duc Vu, Kien Nguyen, Trong-Tung Nguyen, Ngan Nguyen, Phong Nguyen, Khoi Nguyen, Cuong Pham, Anh Tran"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23463v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 基于扩散的图像修复方法
tldr: 本文针对现有扩散模型在图像修复中需要大量采样步骤的问题，提出InverFill方法，通过将输入掩码图像的语义信息注入初始噪声，实现高保真度的少步骤图像修复。该方法无需重新训练模型，仅增加最小推理开销，显著提升了图像质量和文本一致性，在低NFE下甚至匹配专门修复模型。
selection_source: fresh_fetch
motivation: 现有少步骤文本到图像模型直接应用于图像修复会导致背景与修复区域协调性差和伪影问题。
method: InverFill是一种一步反演方法，将输入掩码图像的语义信息注入初始噪声，实现语义对齐的少步骤图像修复。
result: InverFill显著提升了基线少步骤模型的图像质量和文本一致性，在低NFE下匹配专门修复模型，且无需真实图像监督。
conclusion: InverFill通过语义对齐的噪声初始化，实现了高效高质量的少步骤图像修复，为实际应用提供了可行方案。
---

## 摘要
最近的基于扩散的模型在图像修复中实现了照片级真实感，但需要许多采样步骤，限制了实际应用。少步文本到图像模型提供更快的生成速度，但简单地将它们应用于图像修复会导致背景与修复区域之间的协调性差和伪影。我们将这一原因追溯到随机高斯噪声初始化，在低函数评估次数下会导致语义错位和保真度降低。为了解决这个问题，我们提出了InverFill，这是一种专为图像修复设计的一步反演方法，它将输入掩码图像的语义信息注入初始噪声，实现高保真度的少步图像修复。InverFill不训练图像修复模型，而是利用少步文本到图像模型，在混合采样管道中使用语义对齐的噪声作为输入，显著改进了普通混合采样，甚至在低NFE下匹配专门的图像修复模型。此外，InverFill不需要真实图像监督，仅增加最小的推理开销。大量实验表明，InverFill持续提升基线少步模型，在不进行昂贵重新训练或大量迭代优化的情况下提高了图像质量和文本一致性。

## Abstract
Recent diffusion-based models achieve photorealism in image inpainting but require many sampling steps, limiting practical use. Few-step text-to-image models offer faster generation, but naively applying them to inpainting yields poor harmonization and artifacts between the background and inpainted region. We trace this cause to random Gaussian noise initialization, which under low function evaluations causes semantic misalignment and reduced fidelity. To overcome this, we propose InverFill, a one-step inversion method tailored for inpainting that injects semantic information from the input masked image into the initial noise, enabling high-fidelity few-step inpainting. Instead of training inpainting models, InverFill leverages few-step text-to-image models in a blended sampling pipeline with semantically aligned noise as input, significantly improving vanilla blended sampling and even matching specialized inpainting models at low NFEs. Moreover, InverFill does not require real-image supervision and only adds minimal inference overhead. Extensive experiments show that InverFill consistently boosts baseline few-step models, improving image quality and text coherence without costly retraining or heavy iterative optimization.

---

## 论文详细总结（自动生成）

# InverFill: 一步反演用于增强少步扩散图像修复 - 论文总结

## 1. 论文的核心问题与整体含义

- **核心问题**：现有基于扩散的图像修复模型需要大量采样步骤，限制了实际应用；而少步文本到图像模型直接应用于图像修复会导致背景与修复区域协调性差和伪影问题。
- **研究动机**：随机高斯噪声初始化在低函数评估次数下会导致语义错位和保真度降低，这是导致少步图像修复质量差的主要原因。
- **整体含义**：图像修复是计算机视觉中的重要任务，需要生成与周围背景协调且符合文本提示的内容，而现有方法在效率和效果之间存在权衡。

## 2. 论文提出的方法论

- **核心思想**：提出InverFill，一种专为图像修复设计的一步反演方法，将输入掩码图像的语义信息注入初始噪声，实现语义对齐的少步骤图像修复。
- **关键技术细节**：
  - **一步反演网络**：将掩码图像映射为语义对齐的反演噪声潜变量
  - **Re-Blending操作**：防止训练过程中的信息泄漏，同时保留关键语义信息
  - **高斯正则化损失**：确保反演噪声潜变量与期望的高斯分布对齐
  - **无图像训练流程**：不需要图像-掩码-文本三元组和多阶段训练过程
- **算法流程**：
  1. 训练阶段：使用无图像训练流程，生成图像-掩码-提示三元组
  2. 反演网络输入：掩码图像的潜在表示和文本提示
  3. 应用Re-Blending操作：将预测噪声潜变量的掩码区域替换为随机高斯噪声
  4. 高斯正则化：确保混合潜变量符合高斯分布
  5. 推理阶段：使用训练好的反演网络生成语义对齐的噪声潜变量，然后进行少步混合采样

## 3. 实验设计

- **数据集/场景**：
  - BrushBench [16]：600张图像和标注掩码
  - MagicBrush [61]：535张测试图像，包含多种分割和随机掩码
  - FFHQ [18]：10K图像
  - DIV2K [1]：900张训练和验证集图像
- **Benchmark**：
  - 图像生成质量：ImageReward (IR)、HPS v2 (HPS)、Aesthetic Score (AS)
  - 文本对齐：CLIP Similarity (CLIP)
  - 一致性：LPIPS、SSIM
- **对比方法**：
  - 少步模型：SANA-Sprint 0.6B、SDXL-Turbo
  - 专门修复模型：BrushNet、SDXL-Inpainting
  - 多步模型：Sana 0.6B、HD-Painter
  - 其他方法：DDIM Inversion

## 4. 资源与算力

- **计算设备**：四个NVIDIA A100 40GB GPU
- **训练时长**：8-10小时
- **批处理大小**：32
- **优化器**：AdamW
- **学习率**：1×10^-5
- **推理开销**：仅增加0.04-0.06秒（在NVIDIA A100 40GB GPU上）

## 5. 实验数量与充分性

- **实验数量**：
  - 在BrushBench和MagicBrush上进行了定量评估
  - 在FFHQ和DIV2K上进行了额外评估
  - 进行了消融实验，验证各个组件的贡献
  - 进行了损失权重消融实验
  - 与多种基线方法进行了比较
- **充分性**：
  - 实验覆盖了多种数据集和掩码类型
  - 使用了多种评估指标，包括人类偏好指标
  - 进行了详细的消融实验，验证了各个组件的有效性
  - 与多种先进方法进行了比较，包括少步模型和专门修复模型
  - 实验设计客观公平，所有方法在相同条件下评估

## 6. 论文的主要结论与发现

- **主要结论**：
  - InverFill显著提升了基线少步骤模型的图像质量和文本一致性
  - 在低NFE下匹配专门修复模型
  - 无需真实图像监督，仅增加最小推理开销
  - 方法可以与现有少步修复模型结合使用，进一步提升性能
- **发现**：
  - 随机高斯噪声初始化是导致少步图像修复质量差的主要原因
  - 语义对齐的噪声初始化可以显著改善修复质量
  - Re-Blending操作和高斯正则化对于防止信息泄漏和确保分布对齐至关重要

## 7. 优点

- **方法亮点**：
  - 一步反演网络设计，高效且无需迭代优化
  - Re-Blending操作防止信息泄漏
  - 高斯正则化确保噪声潜变量符合期望分布
  - 无图像训练流程，简化训练过程
  - 最小推理开销，适合实际应用
- **实验设计亮点**：
  - 使用多种数据集和评估指标进行全面评估
  - 详细的消融实验验证各组件贡献
  - 与多种先进方法进行公平比较
  - 考虑了不同NFE设置下的性能

## 8. 不足与局限

- **方法局限**：
  - 主要局限是修复区域与背景之间可能存在颜色不一致
  - 未充分探索不同类型掩码的影响
  - 未评估方法在极端大掩码情况下的表现
- **实验覆盖局限**：
  - 实验主要在标准数据集上进行，可能无法完全覆盖真实场景的多样性
  - 可能存在对特定类型图像的偏好偏差
- **应用限制**：
  - 未讨论计算资源受限环境下的性能
  - 未探讨方法在实时应用中的可行性
  - 未考虑伦理问题和社会影响（仅在论文末尾简单提及）

（完）
