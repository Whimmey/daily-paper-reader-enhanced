---
title: "MFSR: MeanFlow Distillation for One Step Real-World Image Super Resolution"
title_zh: MFSR：用于单步真实世界图像超分辨率的MeanFlow蒸馏
authors: "Ruiqing Wang, Kai Zhang, Yuanzhi Zhu, Hanshu Yan, Shilin Lu, Jian Yang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20690v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 新颖的单步图像超分辨率技术
tldr: MFSR是一种新的图像超分辨率蒸馏框架，解决了扩散模型和流模型在真实世界图像超分辨率中多步采样导致的推理速度慢问题。该方法使用MeanFlow作为学习目标，使学生模型能够近似概率流ODE中任意状态之间的平均速度，实现单步高质量恢复，同时保留少量步骤进一步优化的灵活性，实验证明其效果与多步教师相当且计算成本更低。
selection_source: fresh_fetch
motivation: 扩散模型和流模型在真实世界图像超分辨率中虽效果好但多步采样导致推理速度慢且难以部署。
method: MFSR使用MeanFlow作为学习目标，使学生模型近似概率流ODE中任意状态之间的平均速度，并通过改进的无分类器引导策略增强恢复能力。
result: MFSR在合成和真实世界基准测试上实现了高效、灵活且高质量的图像超分辨率，效果与多步教师相当甚至更好，同时计算成本大大降低。
conclusion: MFSR通过MeanFlow蒸馏框架实现了单步高质量图像超分辨率，同时保留了多步优化的灵活性，为实际应用提供了高效解决方案。
---

## 摘要
扩散和流模型推动了真实世界图像超分辨率(Real-ISR)的发展，但它们的多步采样使得推理速度缓慢且难以部署。单步蒸馏降低了成本，但通常会降低恢复质量，并失去了通过更多步骤进行优化的选择。我们提出了用于超分辨率的Mean流(MFSR)，这是一种新的蒸馏框架，能够在单步中生成逼真的结果，同时仍允许选择少量步骤的路径进行进一步改进。我们的方法使用MeanFlow作为学习目标，使学生能够近似概率流ODE(PF-ODE)任意状态之间的平均速度，并在不显式展开的情况下有效捕捉教师模型的动态特性。为了更好地利用预训练的生成先验，我们通过教师CFG蒸馏策略改进了原始MeanFlow的无分类器引导(CFG)公式，从而增强了恢复能力并保留了精细细节。在合成和真实世界基准上的实验表明，MFSR实现了高效、灵活且高质量的图像超分辨率，其结果与多步教师模型相当甚至更好，同时需要低得多的计算成本。

## Abstract
Diffusion- and flow-based models have advanced Real-world Image Super-Resolution (Real-ISR), but their multi-step sampling makes inference slow and hard to deploy. One-step distillation alleviates the cost, yet often degrades restoration quality and removes the option to refine with more steps. We present Mean Flows for Super-Resolution (MFSR), a new distillation framework that produces photorealistic results in a single step while still allowing an optional few-step path for further improvement. Our approach uses MeanFlow as the learning target, enabling the student to approximate the average velocity between arbitrary states of the Probability Flow ODE (PF-ODE) and effectively capture the teacher's dynamics without explicit rollouts. To better leverage pretrained generative priors, we additionally improve original MeanFlow's Classifier-Free Guidance (CFG) formulation with teacher CFG distillation strategy, which enhances restoration capability and preserves fine details. Experiments on both synthetic and real-world benchmarks demonstrate that MFSR achieves efficient, flexible, and high-quality super-resolution, delivering results on par with or even better than multi-step teachers while requiring much lower computational cost.

---

## 论文详细总结（自动生成）

# MFSR论文总结

## 1. 论文的核心问题与整体含义

**研究动机和背景**：
- 扩散模型和流模型在真实世界图像超分辨率(Real-ISR)任务中表现出色，但它们的多步采样机制导致推理速度慢，难以实际部署。
- 现有单步蒸馏方法虽降低了计算成本，但通常会降低恢复质量，并失去了通过更多步骤进行优化的灵活性。
- 传统ISR方法在处理真实世界图像时表现不佳，因为这些图像经历了复杂且未知的退化过程。

## 2. 论文提出的方法论

**核心思想**：
- 提出MFSR（Mean Flows for Super-Resolution），一种新的蒸馏框架，能够在单步中生成逼真的结果，同时保留少量步骤进一步优化的灵活性。
- 使用MeanFlow作为学习目标，使学生模型能够近似概率流ODE(PF-ODE)中任意状态之间的平均速度。

**关键技术细节**：
- **模型初始化**：从预训练的DiT4SR教师模型初始化学生模型，添加额外的时间嵌入分支以适应两个时间步输入。
- **改进的瞬时速度**：使用教师模型的CFG预测构建瞬时速度，包括负提示增强的CFG策略：`v_neg_inst = v(zt,t|zLR,c) + w[v(zt,t|zLR,c) - v(zt,t|zLR,c_neg)]`
- **MeanFlow蒸馏损失**：在潜在空间中计算，使用教师引导的瞬时速度和Jacobian-Vector Product (JVP)项。
- **时间嵌入稳定**：采用`c_noise(t) = t`而非原始的`1000/t`，避免梯度范数过度放大。
- **采样时间步**：从联合分布`p(t,s) = p(t)p(s|t)`中采样，其中`p(t) = U[0,1]`，`p(s|t) = U[t,1]`。
- **损失函数**：使用Pseudo-Huber损失而非L2损失，减少训练过程中的损失方差。

**算法流程**：
1. 初始化学生模型从教师模型复制权重，并添加第二个时间嵌入器
2. 重复采样噪声、图像对、时间步，计算潜在表示和文本提示
3. 计算瞬时速度和MeanFlow蒸馏损失
4. 更新学生模型直至收敛
5. 推理时使用N步采样，每步更新：`zτn+1 = zτn + (τn+1-τn)u(zτn,τn,τn+1|zLR,c)`

## 3. 实验设计

**数据集/场景**：
- **训练数据集**：DIV2K、DIV8K、Flickr2K、LSDIR、NKUSR8K和FFHQ的前10K张人脸图像，使用Real-ESRGAN退化管道生成配对数据(128×128 LR和512×512 HR)
- **测试数据集**：
  - 合成数据集：DIV2K验证集的100张随机裁剪图像
  - 真实世界数据集：RealSR、DRealSR、RealLR200、RealLQ250

**Benchmark**：
- **参考指标**：PSNR、LPIPS、FID、NIQE、MUSIQ、MANIQA、CLIPIQA
- **无参考指标**：LIQE（用于无GT图像的数据集）

**对比方法**：
- 单步扩散方法：SinSR、CTMSR、OSEDiff、AddSR、S3Diff、TSDSR
- 多步扩散方法：DiT4SR（作为教师模型）

## 4. 资源与算力

- **硬件配置**：8个NVIDIA H200 GPU
- **训练设置**：批处理大小80，训练约19小时，总共12K次迭代
- **优化器**：Adam，学习率5e-5
- **模型架构**：在SD3.5的transformer块中融入LoRA，LoRA秩为64

## 5. 实验数量与充分性

**实验数量**：
- 在多个数据集上进行定量比较：RealSR、DRealSR、DIV2K-Val、RealLQ250、RealLR200
- 用户研究：75名志愿者参与，比较1步MFSR与4种其他方法
- 消融研究：包括CFG策略、CFG尺度w、t≠s比例等
- 不同采样步数比较：1/2/3/4/5/8步效果分析

**充分性与公平性**：
- 实验覆盖合成和真实世界场景
- 使用多种评估指标，包括参考和无参考指标
- 与多种最新方法进行了比较
- 消融实验验证了各个组件的有效性
- 用户研究提供了主观质量评估
- 实验设计较为全面，但特定场景覆盖可能不足

## 6. 论文的主要结论与发现

**主要结论**：
- MFSR能够在单步中生成高质量的超分辨率结果，同时保留少量步骤进一步优化的灵活性
- MFSR的效果与多步教师模型相当甚至更好，同时计算成本大大降低
- 改进的CFG策略（特别是负提示增强）显著提升了恢复能力和细节保留

**主要发现**：
- 从噪声初始化而非LR初始化可以获得更好的生成能力和退化去除效果
- t≠s的比例为0.5时效果最佳，低于原始MeanFlow的0.75
- CFG尺度w=6时效果最佳，提供了良好的平衡
- 增加采样步数可以进一步提升质量，但收益递减

## 7. 优点

**方法亮点**：
- 首次将MeanFlow框架应用于Real-ISR任务，实现了单步和少量步图像恢复
- 提出了改进的CFG蒸馏策略，利用教师模型的CFG预测作为瞬时速度，提供更强的监督
- 仅使用MeanFlow蒸馏损失，在潜在空间中计算，无需通过编码器或解码器反向传播，提高了训练效率
- 保留了多步优化的灵活性，允许在效率和效果之间进行权衡

**实验设计亮点**：
- 在多个数据集上进行全面评估，包括合成和真实世界场景
- 使用多种评估指标，包括参考和无参考指标
- 进行了详细的消融研究，验证了各个组件的有效性
- 通过用户研究提供了主观质量评估，弥补了客观指标的局限性

## 8. 不足与局限

- 实验主要集中在×4放大倍率，对于其他放大倍率的效果可能不同
- 依赖预训练的教师模型(DiT4SR)，可能限制了方法在资源有限环境中的应用
- 训练过程需要大量计算资源（8个H200 GPU，19小时）
- 提示提取过程可能引入额外的复杂性和潜在偏差
- 对于某些特定类型退化（如严重模糊、噪声）的效果可能需要进一步验证
- 实验主要集中在自然图像，对于医学图像、卫星图像等特殊领域的适用性未充分探索
- 虽然进行了用户研究，但样本量相对较小（25张图像，75名参与者）
- 方法在计算效率上优于多步模型，但与真正的单步传统方法相比可能仍有差距

（完）
