---
title: Set-Valued Prediction for Large Language Models with Feasibility-Aware Coverage Guarantees
title_zh: 具有可行性感知覆盖保证的大语言模型集值预测
authors: "Ye Li, Anqi Hu, Yuanchang Ye, Shiyan Tong, Zhiyuan Wang, Bo Fu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22966v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: LLM的集合值预测
tldr: 这篇论文针对大型语言模型(LLMs)的预测局限性，提出了一种集合值预测框架，提供可行性感知的覆盖保证。研究指出传统点预测低估了模型能力，通过建立最小可实现风险水平(MRL)和数据驱动校准程序，确保预测集包含正确答案的概率。实验验证了该框架在多个任务和模型上的统计有效性和预测效率。
selection_source: fresh_fetch
motivation: 传统点预测低估了大型语言模型的能力，忽略了可能存在于更广泛输出空间中的有效答案。
method: 提出基于原则的集合值预测框架，通过建立最小可实现风险水平和数据驱动校准程序构建预测集。
result: 在六个语言生成任务和五个大型语言模型上的实验验证了该框架的统计有效性和预测效率。
conclusion: 集合值预测框架能够提供可行性感知的覆盖保证，有效提升大型语言模型的预测可靠性。
---

## 摘要
大语言模型(LLMs)本质上在大生成空间上运行，但传统用法通常将最可能的生成(MLG)作为点预测报告，这低估了模型的能力：尽管排名最高的响应可能不正确，但有效答案可能仍然存在于更广泛的输出空间中，并且可以通过重复采样潜在地被发现。这一观察促使我们从点预测转向集值预测，其中模型生成一组候选响应，而不是单个MLG。在本文中，我们提出了一个集值预测的原则性框架，该框架提供可行性感知的覆盖保证。我们表明，考虑到LLM生成的有限采样特性，覆盖并不总是可实现的：即使进行多次采样，LLMs也可能无法在采样的候选集中为某些问题产生可接受的响应。为了解决这个问题，我们建立了一个最小可实现风险水平(MRL)，低于该水平就无法满足统计覆盖保证。基于这一见解，我们随后开发了一种数据驱动的校准程序，该程序通过估计严格的阈值从采样响应中构建预测集，确保在目标风险水平可行时，所得集合以期望的概率包含正确答案。在六种语言生成任务上使用五种LLMs进行的广泛实验证明了我们框架的统计有效性和预测效率。

## Abstract
Large language models (LLMs) inherently operate over a large generation space, yet conventional usage typically reports the most likely generation (MLG) as a point prediction, which underestimates the model's capability: although the top-ranked response can be incorrect, valid answers may still exist within the broader output space and can potentially be discovered through repeated sampling. This observation motivates moving from point prediction to set-valued prediction, where the model produces a set of candidate responses rather than a single MLG. In this paper, we propose a principled framework for set-valued prediction, which provides feasibility-aware coverage guarantees. We show that, given the finite-sampling nature of LLM generation, coverage is not always achievable: even with multiple samplings, LLMs may fail to yield an acceptable response for certain questions within the sampled candidate set. To address this, we establish a minimum achievable risk level (MRL), below which statistical coverage guarantees cannot be satisfied. Building on this insight, we then develop a data-driven calibration procedure that constructs prediction sets from sampled responses by estimating a rigorous threshold, ensuring that the resulting set contains a correct answer with a desired probability whenever the target risk level is feasible. Extensive experiments on six language generation tasks with five LLMs demonstrate both the statistical validity and the predictive efficiency of our framework.

---

## 论文详细总结（自动生成）

# 论文总结：具有可行性感知覆盖保证的大语言模型集值预测

## 1. 论文的核心问题与整体含义

- **核心问题**：传统大型语言模型(LLMs)使用点预测(MLG)方式低估了模型能力，忽略了可能存在于更广泛输出空间中的有效答案。
- **研究动机**：虽然排名最高的响应可能不正确，但有效答案可能仍然存在于更广泛的输出空间中，可以通过重复采样被发现。
- **背景**：LLMs本质上在大生成空间上运行，但传统用法通常只报告最可能的生成作为点预测，这限制了模型能力的充分发挥，特别是在开放生成场景中。

## 2. 论文提出的方法论

- **核心思想**：从点预测转向集值预测，模型生成一组候选响应而不是单个MLG，提供可行性感知的覆盖保证。
- **关键技术**：
  - **最小可实现风险水平(MRL)**：建立低于该水平就无法满足统计覆盖保证的风险水平，表征有限采样 imposed 的可行性边界
  - **数据驱动校准程序**：通过估计严格阈值从采样响应中构建预测集
  - **可靠性评分函数**：结合自不确定性(TokenSAR)、跨样本一致性和语义共识，评估每个候选响应的可信度
  - **集合级错覆盖损失**：定义预测集是否包含可接受答案的损失函数
- **算法流程**：
  1. 计算最小可实现风险水平(MRL)：αₗ = (1/(n+1))∑ᵢ₌₁ⁿ lᵢ，其中lᵢ是指示有限采样失败的二元变量
  2. 构建可靠性评分函数F：结合不确定性、一致性和语义共识
  3. 定义集合级错覆盖损失ℓᵢ(λ)：指示预测集是否包含可接受答案
  4. 校准阈值：选择λ̂ = inf{λ: n̂ℒₙ(λ) + 1/(n+1) ≤ α}
  5. 应用阈值到新测试数据构建预测集

## 3. 实验设计

- **数据集/场景**：
  - 六种语言生成任务：CoQA、TriviaQA、SciQA、MedQA、MedMCQA和Financial-QA-10K
  - 多种应用场景：开放域、科学、医疗和金融任务
- **基准**：点预测(MLG)作为基准方法
- **对比方法**：
  - 五种LLMs：Qwen2.5-3B-Instruct、Qwen2.5-7B-Instruct、LLaMA-3.1-8B-Instruct、Vicuna-13b-v1.5和Qwen3-14B
- **评估指标**：
  - 覆盖率：验证预测集包含正确答案的概率
  - 平均预测集大小(APSS)：评估预测效率和不确定性

## 4. 资源与算力

- 论文中未明确提及使用的GPU型号、数量或训练时长等算力信息
- 实验配置参数：num-beams=5，K=20（采样数量），temperature=1.0和top-p=0.9
- 随机种子固定为10，校准-测试分割比例为0.5（默认）

## 5. 实验数量与充分性

- **实验数量**：
  - 六个数据集 × 五个模型 = 30种模型-数据集组合
  - 三种语义匹配阈值（0.5、0.6、0.7）的对比实验
  - 多种采样预算（K=5到20）的影响实验
  - 语义去重、可靠性评分组件、校准比例等消融实验
- **充分性与客观性**：
  - 实验覆盖了多种任务类型和应用场景
  - 考虑了不同模型规模和性能的影响
  - 进行了全面的参数敏感性分析
  - 实验设计较为客观，使用相同的评估标准和随机种子

## 6. 论文的主要结论与发现

- **主要结论**：集值预测框架能够提供可行性感知的覆盖保证，有效提升大型语言模型的预测可靠性
- **关键发现**：
  - 点预测在开放生成中存在固有局限性，即使采样候选集中存在有效答案，点预测也无法充分利用
  - 可行性感知的覆盖保证只在可行区域内（α ≥ αₗ）实现，低于MRL时无法满足统计保证
  - 采样预算直接影响可行性边界，增加采样数量可降低MRL，但存在边际效益递减
  - 平均预测集大小(APSS)揭示了覆盖-效率权衡，可作为模型不确定性的有用指标
  - 语义去重提高了实际效率而不损害效用，减少了预测集大小
  - 框架对可靠性评分的具体构建和校准比例具有鲁棒性

## 7. 优点

- **方法亮点**：
  - 首次提出可行性感知的集值预测框架，解决了开放生成中有限采样导致的覆盖保证失效问题
  - 创新性地建立了最小可实现风险水平(MRL)概念，明确表征了有限采样 imposed 的可行性边界
  - 开发了数据驱动的校准程序，确保在可行区域内提供严格的统计覆盖保证
  - 设计了多维度可靠性评分机制，结合自不确定性、跨样本一致性和语义共识
- **实验设计亮点**：
  - 在多种任务和模型上全面验证方法有效性
  - 系统性地分析了不同参数设置的影响
  - 考虑了实际应用中的效率问题（如语义去重）
  - 评估了框架的鲁棒性和实用性

## 8. 不足与局限

- **实验覆盖局限**：
  - 主要关注问答任务，可能不适用于其他类型的生成任务（如摘要、翻译）
  - 评估主要基于文本相似度，可能无法完全捕捉语义等价性
- **偏差风险**：
  - 依赖语义相似度评估，可能存在评估标准的主观性
  - 实验主要在英文数据集上进行，可能存在语言偏差
- **应用限制**：
  - 需要额外的校准数据，增加了部署成本
  - 计算成本较高，需要多次采样（K=20）
  - 预测集大小可能较大（APSS可达10-17），影响实用性
  - 在高风险应用中可能需要更严格的评估标准
  - 未探索交互式或代理式LLM系统的应用

（完）
