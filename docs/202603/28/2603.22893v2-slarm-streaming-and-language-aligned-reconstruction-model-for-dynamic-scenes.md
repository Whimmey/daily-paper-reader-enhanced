---
title: "SLARM: Streaming and Language-Aligned Reconstruction Model for Dynamic Scenes"
title_zh: SLARM：面向动态场景的流式与语言对齐重建模型
authors: "Zhicheng Qiu, Jiarui Meng, Tong-an Luo, Yican Huang, Xuan Feng, Xuanfu Li, ZHan Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22893v2"
tags: ["query:dl-cv"]
score: 9.0
evidence: 结合动态场景重建与语言对齐表示
tldr: SLARM是一种统一动态场景重建、语义理解和实时流式推理的前馈模型。通过高阶运动建模捕捉复杂运动，无需流监督；从LSeg提取语义特征实现语言对齐表示；使用窗口因果注意力实现低延迟流式推理。该方法在动态估计、渲染质量和场景解析方面达到最先进水平，显著提升运动精度、重建质量和分割性能。
selection_source: fresh_fetch
motivation: 现有方法难以同时处理动态场景重建、语义理解和实时流式推理。
method: SLARM采用高阶运动建模、语义特征提取和窗口因果注意力机制实现统一框架下的动态场景重建。
result: "SLARM在动态估计、渲染质量和场景解析方面达到最先进水平，比现有方法提高运动精度21%，重建PSNR提高1.6dB，分割mIoU提高20%。"
conclusion: SLARM通过统一动态场景重建、语义理解和实时流式推理，为动态场景处理提供了高效解决方案。
---

## 摘要
我们提出了SLARM，一个前馈模型，统一了动态场景重建、语义理解和实时流式推理。SLARM通过高阶运动建模捕捉复杂、非均匀运动，仅通过可微渲染进行训练，无需任何流监督。此外，SLARM从LSeg中提取语义特征，获得语言对齐的表示。这种设计支持通过自然语言进行语义查询，语义与几何之间的紧密耦合进一步提高了动态重建的准确性和鲁棒性。此外，SLARM使用基于窗口的因果注意力处理图像序列，实现了稳定、低延迟的流式推理，且不会累积内存成本。在这个统一框架下，SLARM在动态估计、渲染质量和场景解析方面取得了最先进的结果，相比现有方法，运动准确率提高了21%，重建PSNR提高了1.6 dB，分割mIoU提高了20%。

## Abstract
We propose SLARM, a feed-forward model that unifies dynamic scene reconstruction, semantic understanding, and real-time streaming inference. SLARM captures complex, non-uniform motion through higher-order motion modeling, trained solely on differentiable renderings without any flow supervision. Besides, SLARM distills semantic features from LSeg to obtain language-aligned representations. This design enables semantic querying via natural language, and the tight coupling between semantics and geometry further enhances the accuracy and robustness of dynamic reconstruction. Moreover, SLARM processes image sequences using window-based causal attention, achieving stable, low-latency streaming inference without accumulating memory cost. Within this unified framework, SLARM achieves state-of-the-art results in dynamic estimation, rendering quality, and scene parsing, improving motion accuracy by 21%, reconstruction PSNR by 1.6 dB, and segmentation mIoU by 20% over existing methods.

---

## 论文详细总结（自动生成）

# SLARM论文总结

## 1. 论文的核心问题与整体含义

SLARM针对动态场景重建领域的三大核心挑战：
- **运动建模局限**：现有方法(如STORM)假设恒定速度运动，无法捕捉真实世界中的复杂非线性动态
- **功能单一**：多数方法仅关注几何重建，缺乏高级语义理解能力，限制了下游感知和推理任务
- **推理效率低**：需要批量处理输入帧，缺乏增量、流式推理能力，难以满足实时应用需求

研究背景基于神经辐射场(NeRF)和3D高斯溅射(3DGS)的进展，以及动态场景重建在沉浸式交互和体验中的潜力。现有优化方法训练时间长且泛化能力有限，而前馈模型主要局限于静态场景，动态环境的前馈重建仍处于探索阶段。

## 2. 论文提出的方法论

### 核心思想
SLARM是一个统一的4D高斯推理框架，同时实现动态场景重建、语义理解和实时流式推理，通过单次前向传播联合优化几何、运动和语义。

### 关键技术细节

**高阶运动建模**：
- 使用多阶泰勒展开建模位移作为时间的可微分函数
- 公式：Γ(Δt) = Σ(l=0到L-1) [ml · (Δt)^(l+1) / (l+1)!]
- 其中ml = sl · vl/||vl||2，sl是标量速度，vl是3D方向向量
- 采用L=3，对应3阶展开，建模位置的前三个时间导数—速度、加速度和加加速度

**语言对齐的4D语义**：
- 从2D基础模型LSeg中提取语言对齐语义知识
- 每个高斯基元增强有高维语义特征向量f_sem^j ∈ R^d
- 渲染时通过alpha混合时间变形高斯的语义属性合成语义特征图
- 使用MSE损失监督4D语义场：L_sem = ||~F_{t+Δt} - ^F'_{t+Δt}||^2

**流式4D场景重建**：
- 处理每帧独立，结合窗口注意力实现恒定低延迟流式推理
- 遵循严格因果性：推理时只有当前和过去观测可用
- 通过向后传播高斯基元回溯细化动态内容，同时保持静态元素
- 根据运动幅度将高斯分为静态和动态子集，使用运动阈值τ_m进行划分

### 算法流程
1. 使用ViT提取视觉特征，将每帧划分为非重叠块
2. 注入几何先验：将每个像素的视线编码为6D Plücker坐标
3. 添加时间戳嵌入和两个特殊令牌(Sky token和Affine token)
4. 通过交替注意力Transformer骨干处理令牌序列
5. 高斯解码器回归像素对齐的4DGS表示
6. 两个辅助头预测动态属性向量和语义特征向量

## 3. 实验设计

### 数据集/场景
- **Waymo Open Dataset (WOD)**：大规模自动驾驶基准，包含丰富的动态和同步多摄像头视频序列
  - 1000个驾驶序列：798个训练，202个验证
  - 每个序列约20秒长，10 fps（约200帧）
  - 输入图像下采样至160×240，特征提取使用320×480
- **额外数据集**：NuScenes和Argoverse2，用于验证泛化能力

### Benchmark
- **动态重建**：PSNR、SSIM、深度RMSE(D-RMSE)
- **流估计**：3D端点误差(EPE3D)、Acc 5、Acc 10、角误差θerr
- **语义分割**：mIoU、准确率(Acc)

### 对比方法
- **动态重建**：LGM、LGM*、GS-LRM、MapAnything、STORM
- **语义分割**：EfficientViT-Seg、Mask2Former-R50、SegMAN、SegFormer、OffSeg系列、LSeg、Mask2Former-Swin
- **流估计**：NSFP、NSFP++、STORM

## 4. 资源与算力

- **训练时长**：4天
- **硬件**：64个华为昇腾910B NPU
- **批处理大小**：64
- **优化器**：AdamW
- **训练迭代次数**：200k

## 5. 实验数量与充分性

### 实验数量
- 主要实验在Waymo Open Dataset上进行
- 在NuScenes和Argoverse2数据集上进行了额外验证
- 进行了多组消融研究，包括：
  - 高阶运动表示的影响（不同阶数比较）
  - 语义引导对动态重建的增强效果
  - 不同语义损失项的贡献
  - 不同运动水平下的模型性能
  - 在线与离线推理模式的比较

### 充分性与公平性
- 实验设计全面，涵盖多个数据集和评估指标
- 与多种最先进方法进行多维度比较
- 消融实验验证了各组件的有效性
- 在流估计评估中采用更全面策略，考虑整个视频序列
- 定性和定量结果相结合展示方法有效性

## 6. 论文的主要结论与发现

### 主要结论
- SLARM在动态估计、渲染质量和场景解析方面取得了最先进的结果
- 相比现有方法，运动准确率提高了21%，重建PSNR提高了1.6 dB，分割mIoU提高了20%
- 高阶运动建模能有效捕捉复杂、非均匀运动
- 语义与几何的紧密耦合提高了动态重建的准确性和鲁棒性
- 窗口因果注意力实现了稳定、低延迟的流式推理，不会累积内存成本

### 主要发现
- 3阶运动模型足以获得最佳性能：在短时间内，真实世界运动可以用加加速度级动力学很好地近似
- 语义一致性作为时间正则化器有益于动态重建
- 流式推理模式(SLARM-W)在推理速度和内存消耗方面优于离线模式(SLARM-F)，但在运动估计精度上略低

## 7. 优点

### 方法亮点
- **统一框架**：同时实现动态场景重建、语义理解和实时流式推理
- **高阶运动建模**：通过多阶泰勒展开捕捉复杂非线性动态，无需显式监督
- **语言对齐语义**：从LSeg提取语义特征，支持自然语言查询和LLM集成
- **流式推理架构**：使用窗口因果注意力实现恒定低延迟推理，支持长时程部署
- **多任务联合优化**：在单次前向传播中联合优化几何、运动和语义，实现任务相互增强

### 实验设计亮点
- 在大规模自动驾驶数据集上进行全面评估
- 与多种最先进方法进行多维度比较
- 消融实验验证各组件贡献
- 定性和定量结果相结合展示方法有效性
- 在多个数据集上验证方法泛化能力

## 8. 不足与局限

### 实验覆盖
- 主要集中在自动驾驶场景，可能无法完全代表所有动态环境
- 对复杂材质(如玻璃或镜子)的处理能力有限
- 语义分割评估仅限于特定类别，可能缺乏对罕见类别的评估

### 偏差风险
- 依赖准确的相机姿态，姿态误差可能影响重建质量
- 从Waymo数据集训练，可能存在领域偏差
- 语义特征从LSeg蒸馏，可能继承其局限性

### 应用限制
- 需要已知相机内参和外参
- 对计算资源要求较高(64个NPU训练4天)
- 流式推理模式在运动估计精度上略低于离线模式
- 目前不支持自校准，需要外部提供相机参数
- 依赖光度一致性，在光照变化大的场景可能表现不佳

（完）
