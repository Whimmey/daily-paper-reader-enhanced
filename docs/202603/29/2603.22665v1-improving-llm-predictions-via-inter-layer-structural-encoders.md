---
title: Improving LLM Predictions via Inter-Layer Structural Encoders
title_zh: 通过层间结构编码器改进大语言模型预测
authors: "Tom Ulanovski, Eyal Blyachman, Maya Bechler-Speicher"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22665v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: LLM的层间结构编码器
tldr: "本研究提出了一种名为ILSE的新型结构化方法，通过Cayley-Encoder利用expander Cayley图实现LLM各层间的高效信息传播，从内部层表示中学习有效表示。在13个分类和语义相似性任务上，ILSE超越了基线和现有方法，准确性和相似性指标分别提升高达44%和25%，同时展示了出色的数据效率，使小型LLM能够与大型模型竞争。"
selection_source: fresh_fetch
motivation: 现有LLM仅依赖最终层表示进行预测，但研究表明中间层可能包含更多任务相关特征，且不同任务可能需要不同层的最优表示。
method: ILSE引入了基于expander Cayley图的Cayley-Encoder几何编码器，从LLM所有内部层表示中学习一个有效表示。
result: "在13个分类和语义相似性任务上，ILSE超越了基线和现有方法，准确性和相似性指标分别提升高达44%和25%，并且在少样本场景下数据效率高。"
conclusion: ILSE通过整合LLM各层信息，显著提升了模型性能，使小型LLM能够与大型模型竞争，为LLM预测提供了新的有效方法。
---

## 摘要
大语言模型(LLMs)的标准做法是基于最终层的token表示进行预测。然而，最近的研究表明，中间层编码了大量信息，这些信息可能比仅使用最终层表示包含更多任务相关特征。重要的是，研究表明对于不同任务，不同的层可能是最优的。在这项工作中，我们引入了层间结构编码器(ILSE)，这是一种强大的结构化方法，可以从LLM的内部层表示中共同学习一个有效表示。ILSE的核心是Cayley编码器，这是一个基于数学的几何编码器，它利用扩展Cayley图实现高效的层间信息传播。我们在13个分类和语义相似性任务上评估了ILSE，使用了9个预训练的LLM，参数范围从1400万到80亿。ILSE始终优于基线和现有方法，在准确率上提高了最多44%，在相似性指标上提高了25%。我们进一步证明，ILSE在少样本情况下具有数据效率，并且可以使小型LLM与显著更大的模型相媲美。

## Abstract
The standard practice in Large Language Models (LLMs) is to base predictions on the final-layer token representations. Recent studies, however, show that intermediate layers encode substantial information, which may contain more task-relevant features than the final-layer representations alone. Importantly, it was shown that for different tasks, different layers may be optimal. In this work we introduce Inter-Layer Structural Encoders (ILSE), a powerful structural approach to learn one effective representation from the LLM's internal layer representations all together. Central to ILSE is Cayley-Encoder, a mathematically grounded geometric encoder that leverages expander Cayley graphs for efficient inter-layer information propagation. We evaluate ILSE across 13 classification and semantic similarity tasks with 9 pre-trained LLMs ranging from 14 million to 8 billion parameters. ILSE consistently outperforms baselines and existing approaches, achieving up to 44% improvement in accuracy and 25% in similarity metrics. We further show that ILSE is data-efficient in few-shot regimes and can make small LLMs competitive with substantially larger models.

---

## 论文详细总结（自动生成）

# 论文总结：通过层间结构编码器改进大语言模型预测

## 1. 论文的核心问题与整体含义

大语言模型(LLMs)的标准做法是基于最终层的token表示进行预测，但这种做法存在明显局限。研究表明，不同层捕获不同语言特征：低层捕获表面特征和词性，中层编码句法结构，高层关注语义关系。更重要的是，中间层可能包含更多任务相关特征，且不同任务可能需要不同层的最优表示。这种对单一层的依赖限制了模型性能的发挥，因此如何有效整合多层信息成为一个关键问题。

## 2. 论文提出的方法论

### 核心思想
提出层间结构编码器(ILSE)，一种结构化方法，从LLM所有内部层表示中学习一个有效表示，利用expander Cayley图实现高效的层间信息传播。

### 关键技术细节
- **ILSE三种结构编码器**：
  - **Cayley-Encoder**：将层表示映射到特殊线性群SL(2, Zn)上的Cayley图节点，应用GNN学习最终表示
  - **Set-Encoder**：使用DeepSets架构学习排列不变函数，公式为f(Z) = ρ(PI-Pool(∑_{ℓ=1}^L ϕ(zℓ)))
  - **Fully-Connected(FC) Encoder**：构建完全连接图，每层与其他所有层连接，应用GNN学习最终表示

- **Cayley-Encoder实现流程**：
  1. 选择最小n使得|Vn|≥L(层数)
  2. 将L层表示随机映射到Cayley图的L个节点
  3. 当|Vn|>L时，剩余节点初始化为零向量的虚拟节点
  4. 应用GNN学习最终表示，仅使用非虚拟节点的表示

## 3. 实验设计

### 数据集与场景
- **分类任务**：Banking77(77类意图)、Emotion(6类情感)、MTOP Domain(领域分类)、MTOP Intent(意图分类)、Poem Sentiment(5类情感)
- **语义文本相似性(STS)任务**：STSBenchmark、STS12、STS13、STS14、STS15、STS16、BIOSSES、SICK-R
- **实验场景**：全数据训练、零样本STS迁移学习、少样本训练(每类1-1024个样本)、不同LLM规模评估(1400万到80亿参数)

### 对比方法
- **基线方法**：Last-Layer、Best-Layer、Weighted、MLP Last-Layer、MLP Best-Layer、DWAtt
- **评估基准**：MTEB基准

## 4. 资源与算力

论文中提到使用了A6000、A100、V100和GeForce RTX 3090 GPU，但未明确说明具体GPU数量和训练时长。附录中提供了不同方法在不同任务上的大致训练时间，但这些时间因GPU硬件不同而有所差异。

## 5. 实验数量与充分性

### 实验数量
- 13个分类和语义相似性任务
- 9个预训练LLM，参数范围从1400万到80亿
- 多种ILSE变体评估(Cayley-Encoder、Set-Encoder、FC-Encoder)
- 不同GNN架构(GIN和GCN)
- 不同训练场景(全数据、零样本、少样本)

### 实验充分性
- 实验覆盖了多种任务类型和模型规模
- 对比了多种基线方法，包括简单方法和先进方法
- 进行了消融实验，比较了不同ILSE变体的性能
- 实验设计客观，所有基线方法使用相同的层表示作为输入
- 公平比较，所有方法在相同条件下评估

## 6. 论文的主要结论与发现

- ILSE在所有15个分类配置、3个监督STS配置和17/21个零样本STS迁移设置中表现最佳
- ILSE在分类任务上平均比Last-Layer基线提高30%，比Best-Layer基线提高25%
- 在Emotion任务上，ILSE比Last-Layer和Best-Layer分别提高高达44%和40%
- Cayley-Encoder在STS任务上表现最佳，在24/24个STS任务配置中17个获得最高分
- ILSE在少样本场景下表现优异，仅用32个样本每类就能超越所有基线
- ILSE使小型LLM能够与大型模型竞争，例如Pythia-14M使用ILSE可以达到与Pythia-2.8B相当的性能

## 7. 优点

### 方法亮点
- **数学基础**：Cayley图具有理论保证的扩展性质，确保高效信息传播
- **结构化方法**：三种不同的结构编码器提供了灵活的层间信息整合方式
- **高效性**：仅需添加少量可训练参数(相对于基础模型约0.1%)
- **通用性**：适用于任何预训练LLM，无需修改原始架构
- **数据效率**：在少样本场景下表现优异

### 实验设计亮点
- **全面的评估**：覆盖多种任务类型和模型规模
- **多样化的基线**：包括简单方法和先进方法
- **实际应用场景**：包括少样本学习和零样本迁移
- **可复现性**：提供了详细的实验设置和超参数选择

## 8. 不足与局限

- **实验覆盖**：主要关注分类和STS任务，未探索生成任务或其他NLP任务
- **模型规模**：最大测试模型为8B参数，未测试更大模型(如70B)
- **计算效率**：Cayley-Encoder训练时间较长，特别是对于深层模型
- **理论分析**：缺乏对为什么Cayley结构特别有效的深入理论分析
- **实际应用**：未探讨在真实生产环境中的部署挑战
- **偏差风险**：未评估方法在不同语言或文化背景下的表现
- **可扩展性**：未研究ILSE在超大规模模型上的适用性

（完）
