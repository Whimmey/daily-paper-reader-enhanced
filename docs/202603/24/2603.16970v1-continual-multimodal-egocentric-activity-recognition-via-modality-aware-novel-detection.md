---
title: Continual Multimodal Egocentric Activity Recognition via Modality-Aware Novel Detection
title_zh: 基于模态感知新颖检测的持续多模态第一视角动作识别
authors: "Wonseon Lim, Hyejeong Im, Dae-Won Kim"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16970v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 结合视觉线索和深度学习的多模态第一视角动作识别
tldr: 本研究针对多模态第一视角动作识别在开放世界中的持续学习挑战，提出了MAND框架。针对现有方法过度依赖RGB模态且易受灾难性遗忘影响的问题，MAND通过模态感知自适应评分（MoAS）在推理时融合多模态证据以检测新类别，并利用模态表征稳定训练（MoRST）在持续学习中保持各模态的判别力。实验证明，该方法显著提升了新类检测性能和已知类分类精度。
selection_source: fresh_fetch
motivation: 现有方法在开放世界持续学习中过度依赖RGB模态，忽视了IMU等模态的互补信息，且在持续学习过程中面临严重的模态不平衡和遗忘问题。
method: 提出了MAND框架，包含利用能量分数自适应集成多模态逻辑值的MoAS推理策略，以及通过辅助头和逻辑值蒸馏保持模态判别力的MoRST训练方法。
result: "在多模态第一视角基准测试中，新活动检测的AUC提升了高达10%，已知类分类准确率提升了2.8%。"
conclusion: MAND通过充分挖掘和稳定多模态特征，有效解决了开放世界持续学习中的新类检测与知识保留难题。
---

## 摘要
多模态第一视角动作识别整合了视觉和惯性线索，以实现稳健的第一人称行为理解。然而，在开放世界环境中部署此类系统需要能够检测新颖活动，同时从非平稳数据流中持续学习。现有方法依赖主逻辑值（logits）进行新颖性评分，未能充分利用来自各个模态的互补证据。由于这些逻辑值通常由 RGB 模态主导，来自其他模态（尤其是 IMU）的线索仍未得到充分利用，且这种不平衡在灾难性遗忘的影响下会随时间推移而加剧。为此，我们提出了 MAND，这是一个用于多模态第一视角开放世界持续学习的模态感知框架。在推理阶段，模态感知自适应评分（MoAS）根据能量分数估计样本级的模态可靠性，并自适应地整合模态逻辑值，以更好地利用互补模态线索进行新颖检测。在训练阶段，模态表征稳定训练（MoRST）通过辅助头和模态级逻辑值蒸馏，在不同任务间保持特定模态的可区分性。在公开多模态第一视角基准测试上的实验表明，与最先进的基准方法相比，MAND 将新颖活动检测的 AUC 提升了高达 10%，已知类分类准确率提升了高达 2.8%。

## Abstract
Multimodal egocentric activity recognition integrates visual and inertial cues for robust first-person behavior understanding. However, deploying such systems in open-world environments requires detecting novel activities while continuously learning from non-stationary streams. Existing methods rely on the main logits for novelty scoring, without fully exploiting the complementary evidence available from individual modalities. Because these logits are often dominated by RGB, cues from other modalities, particularly IMU, remain underutilized, and this imbalance worsens over time under catastrophic forgetting. To address this, we propose MAND, a modality-aware framework for multimodal egocentric open-world continual learning. At inference, Modality-aware Adaptive Scoring (MoAS) estimates sample-wise modality reliability from energy scores and adaptively integrates modality logits to better exploit complementary modality cues for novelty detection. During training, Modality-wise Representation Stabilization Training (MoRST) preserves modality-specific discriminability across tasks via auxiliary heads and modality-wise logit distillation. Experiments on a public multimodal egocentric benchmark show that MAND improves novel activity detection AUC by up to 10\% and known-class classification accuracy by up to 2.8\% over state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### 论文总结：基于模态感知新颖检测的持续多模态第一视角动作识别 (MAND)

#### 1. 核心问题与整体含义
本文研究的是**多模态第一视角动作识别 (MMEA)** 在**开放世界持续学习 (OWCL)** 场景下的挑战。
*   **背景：** 第一视角识别（如 AR/VR、穿戴式设备）需要整合视觉 (RGB) 和运动 (IMU) 信号。在现实中，模型必须在不断学习新任务的同时，识别出从未见过的“新颖”动作（新类检测），并克服对旧知识的“灾难性遗忘”。
*   **核心问题：** 现有方法在进行新颖性检测时，主要依赖融合后的主逻辑值 (Main Logits)。由于 RGB 模态通常占据主导地位，IMU 等互补信息被忽视；且在持续学习过程中，IMU 模态比 RGB 更容易遭受遗忘，导致模型无法有效区分已知类和新颖类。

#### 2. 方法论
论文提出了 **MAND** 框架，包含两个核心组件：

*   **模态感知自适应评分 (MoAS - 推理阶段)：**
    *   **核心思想：** 不再只看融合后的结果，而是根据每个模态的“可靠性”自适应地整合信息。
    *   **技术细节：** 
        1.  计算各模态的**能量分数 (Energy Score)**。
        2.  利用重放缓冲区 (Replay Buffer) 的统计数据（均值和标准差）对能量分数进行归一化，得到样本级的**可靠性权重 ($\alpha$)**。
        3.  将主逻辑值与加权后的各模态逻辑值相加，生成最终的 $z_{MoAS}$，并以其最大逻辑值作为新颖性评分。
*   **模态表征稳定训练 (MoRST - 训练阶段)：**
    *   **核心思想：** 在学习新任务时，显式地保护每个独立模态的判别能力。
    *   **技术细节：**
        1.  为每个模态编码器添加轻量级的**辅助分类头**。
        2.  **多目标损失函数：** 结合主分类器的交叉熵损失、辅助头的交叉熵损失，以及**模态级逻辑值蒸馏 (Logit Distillation)**。
        3.  通过蒸馏旧任务中各模态的逻辑值，防止特定模态（如 IMU）的决策边界发生剧烈偏移。

#### 3. 实验设计
*   **数据集：** 使用了 **UESTC-MMEA-CL** 基准数据集，包含 RGB、陀螺仪 (Gyro) 和加速度计 (Acce) 三种流，共 32 类动作。
*   **实验设置：** 模拟了三种持续学习序列：短序列（每次增加 8 类）、中序列（每次 4 类）、长序列（每次 2 类）。
*   **对比方法：** 
    *   **持续学习算法：** iCaRL, ER, DER++, Foster, CMR-MFN, MONET。
    *   **新颖性评分函数：** MSP, MaxLogit, Entropy, Energy。
*   **评价指标：** AUC、FPR95（针对新颖检测）；平均准确率 (ACC)、遗忘率 (FGT)（针对已知类识别）。

#### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA RTX 3090 GPU**。
*   **软件：** 基于 PyTorch 和 PyCIL 持续学习工具箱实现。
*   **训练细节：** 每个任务训练 50 个 epoch，RGB 使用 SGD 优化器，IMU 使用 RMSProp 优化器。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在三种不同的任务增量设置下进行了详尽对比，涵盖了主流的持续学习与新颖检测组合。
*   **消融实验：** 验证了 MoAS 和 MoRST 各自的贡献，并对比了不同的评分策略（如固定权重 vs. 自适应权重）。
*   **客观性：** 实验结果给出了 5 次运行的均值和标准差，并进行了 **Wilcoxon 符号秩检验** 以验证统计显著性。整体实验设计严谨，对比基准全面。

#### 6 主要结论与发现
*   **性能提升：** MAND 在新颖活动检测的 AUC 上比最强基准提升了高达 **10%**，在已知类分类准确率上提升了 **2.8%**。
*   **模态协同：** 实验证明，单纯增加模态并不一定能提升新颖检测性能（因为 RGB 主导），而 MAND 的自适应加权能有效挖掘 IMU 的互补价值。
*   **稳定性：** MoRST 显著降低了非主导模态的遗忘率，为推理阶段的可靠评分提供了基础。

#### 7. 优点
*   **视角独特：** 敏锐捕捉到了多模态持续学习中“模态主导”和“差异化遗忘”对新颖检测的负面影响。
*   **即插即用：** MoAS 模块在推理时不需要复杂的重新训练，具有较强的实用性。
*   **双管齐下：** 通过训练时的稳定化和推理时的自适应化，系统性地解决了开放世界下的持续学习问题。

#### 8. 不足与局限
*   **存储开销：** 为了进行模态级蒸馏，需要在重放缓冲区中额外存储每个模态的逻辑值和统计信息，这增加了内存负担。
*   **模态限制：** 实验主要集中在 RGB 和 IMU，对于更多样化或存在严重缺失（Sensor Missing）的模态场景，其鲁棒性尚待验证。
*   **超参数敏感性：** 损失函数中的权重参数（$\lambda, \beta$）可能需要针对不同数据集进行精细调优。

（完）
