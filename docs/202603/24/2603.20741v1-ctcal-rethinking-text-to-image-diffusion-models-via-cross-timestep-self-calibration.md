---
title: "CTCal: Rethinking Text-to-Image Diffusion Models via Cross-Timestep Self-Calibration"
title_zh: CTCal：通过跨时间步自校准重新思考文本到图像扩散模型
authors: "Xiefan Guo, Xinzhu Ma, Haiyu Zhang, Di Huang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20741v1"
tags: ["query:dl-cv"]
score: 10.0
evidence: 文本到图像扩散模型与计算机视觉对齐
tldr: 针对文本生成图像模型中对齐不精确的问题，本文提出了一种跨时间步自校准（CTCal）方法。研究发现，扩散模型在噪声较小时更容易建立准确的文本-图像对应关系。CTCal利用小时间步生成的可靠交叉注意力图作为显式监督，校准大时间步的学习过程，并引入时间步感知自适应权重进行优化。该方法具有模型无关性，在SD 2.1和SD 3等模型上显著提升了对齐性能。
selection_source: fresh_fetch
motivation: 传统的扩散损失仅提供隐式监督，导致模型在处理高噪声时间步时难以建立精细的文本-图像对齐关系。
method: 利用小时间步中更准确的交叉注意力图来校准大时间步的表示学习，并结合时间步感知自适应权重进行训练。
result: 在T2I-Compbench++和GenEval基准测试中，CTCal显著提升了多种扩散模型和流匹配模型的文本-图像对齐效果。
conclusion: CTCal通过跨时间步的显式自校准，为提升文本生成图像模型的语义一致性提供了一种简单且通用的有效方案。
---

## 摘要
文本到图像合成的最新进展主要由基于扩散的模型推动，但实现文本提示词与生成图像之间的精确对齐仍然是一个持续的挑战。我们发现，这一困难主要源于传统扩散损失的局限性，它仅为建模细粒度的文本-图像对应关系提供隐式监督。在本文中，我们提出了跨时间步自校准（CTCal），其基础是观察到在扩散模型中建立准确的文本-图像对齐随着时间步的增加而变得愈发困难。CTCal 利用在噪声较小的较小时间步形成的可靠文本-图像对齐（即交叉注意力图），来校准噪声较大的较大时间步的表示学习，从而在训练期间提供显式监督。我们进一步提出了一种时间步感知的自适应加权，以实现 CTCal 与扩散损失的和谐集成。CTCal 与模型无关，可以无缝集成到现有的文本到图像扩散模型中，涵盖了基于扩散（如 SD 2.1）和基于流（如 SD 3）的方法。在 T2I-Compbench++ 和 GenEval 基准测试上的广泛实验证明了所提 CTCal 的有效性和泛化能力。我们的代码可在 https://github.com/xiefan-guo/ctcal 获取。

## Abstract
Recent advancements in text-to-image synthesis have been largely propelled by diffusion-based models, yet achieving precise alignment between text prompts and generated images remains a persistent challenge. We find that this difficulty arises primarily from the limitations of conventional diffusion loss, which provides only implicit supervision for modeling fine-grained text-image correspondence. In this paper, we introduce Cross-Timestep Self-Calibration (CTCal), founded on the supporting observation that establishing accurate text-image alignment within diffusion models becomes progressively more difficult as the timestep increases. CTCal leverages the reliable text-image alignment (i.e., cross-attention maps) formed at smaller timesteps with less noise to calibrate the representation learning at larger timesteps with more noise, thereby providing explicit supervision during training. We further propose a timestep-aware adaptive weighting to achieve a harmonious integration of CTCal and diffusion loss. CTCal is model-agnostic and can be seamlessly integrated into existing text-to-image diffusion models, encompassing both diffusion-based (e.g., SD 2.1) and flow-based approaches (e.g., SD 3). Extensive experiments on T2I-Compbench++ and GenEval benchmarks demonstrate the effectiveness and generalizability of the proposed CTCal. Our code is available at https://github.com/xiefan-guo/ctcal.

---

## 论文详细总结（自动生成）

以下是对论文《CTCal: Rethinking Text-to-Image Diffusion Models via Cross-Timestep Self-Calibration》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的文本到图像（T2I）扩散模型在处理复杂提示词时，往往难以实现精确的语义对齐（如属性错位、物体缺失等）。
*   **研究动机**：作者发现传统的扩散损失（Diffusion Loss）仅对文本-图像的对应关系提供**隐式监督**。通过观察发现，扩散模型在**小时间步（低噪声）**下生成的交叉注意力图（Cross-Attention Maps）比在**大时间步（高噪声）**下更准确。因此，研究如何利用模型自身的这种“跨时间步”一致性来提供显式监督，是提升对齐性能的关键。

### 2. 方法论
核心思想是**跨时间步自校准（CTCal）**，即利用模型在低噪声阶段学习到的可靠对齐知识来校准高噪声阶段的学习。
*   **核心流程**：在训练时，对同一张图像采样两个时间步 $t_{stu}$（大时间步，学生）和 $t_{tea}$（小时间步，教师，通常设为0）。通过最小化两者交叉注意力图之间的差异，实现显式对齐监督。
*   **关键技术细节**：
    *   **词性筛选策略**：仅选择名词（Noun）对应的注意力图进行校准，因为名词直接关联空间语义，避免了虚词（如“and”, “the”）引入的噪声。
    *   **像素-语义联合优化**：不仅在像素级对齐注意力图，还引入了一个轻量级的**自编码器（Autoencoder）**，在语义特征空间进行对齐，并辅以重构任务防止模式崩塌。
    *   **主体响应对齐正则化**：通过 ReLU 函数约束不同主体（名词）之间的注意力响应强度，防止某些显著物体掩盖微小物体。
    *   **时间步感知自适应权重**：引入权重 $\lambda_t = t_{stu} / T_{train}$，使得在噪声更大的阶段，CTCal 损失占据更高比例，符合“噪声越大越需要校准”的直觉。

### 3. 实验设计
*   **数据集**：
    *   **T2I-Compbench++**：涵盖颜色绑定、形状、纹理、空间关系、计数、复杂组合等多个维度。
    *   **GenEval**：用于跨基准验证，评估物体聚焦的对齐能力。
*   **基准模型（Backbones）**：Stable Diffusion 2.1（基于 UNet 的扩散模型）和 Stable Diffusion 3 (2B)（基于 Transformer 的流匹配模型）。
*   **对比方法**：
    *   基础模型：SD 1.4, SD 2.1, SD XL, PixArt-α, DALL-E 3, FLUX.1。
    *   优化方法：推理侧优化（Attend-and-Excite）、监督微调方法（GORS）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练配置**：提到使用了 **LoRA** 技术对文本编码器的自注意力层和去噪网络的注意力层进行微调。在数据集构建阶段，SD 2.1 采样了 10,000 个提示词，每个提示词生成 100 张候选图进行筛选。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准上进行了详尽的定量评估。
    *   进行了**消融实验**，逐一验证了词性筛选、联合优化、正则化和自适应权重的贡献。
    *   包含**用户研究**（12名志愿者，120组对比），增加了主观评价的客观性。
    *   补充了关于 $t_{tea}$ 取值、形容词加入效果、生成多样性（LPIPS）和图像质量（Aesthetic Score）的额外实验。
*   **充分性评价**：实验设计非常全面，不仅验证了对齐性能的提升，还证明了该方法不会损害图像质量和生成多样性，具有较强的说服力。

### 6. 主要结论与发现
*   **CTCal 的有效性**：在所有评估维度上，CTCal 均显著优于基准模型和现有的微调方法，尤其在空间关系和复杂组合方面提升巨大。
*   **模型无关性**：该方法在基于扩散的 SD 2.1 和基于流匹配的 SD 3 上均表现良好，证明了其广泛的适用性。
*   **注意力图的价值**：实验证实，通过显式约束注意力图的演化，可以有效纠正语义混淆，从而间接提升图像的整体生成质量。

### 7. 优点
*   **自监督思想**：无需额外的预训练教师模型，利用模型自身在不同噪声水平下的表现差异进行“自我进化”。
*   **即插即用**：作为一种训练策略，它可以无缝集成到现有的各种扩散/流模型架构中。
*   **显式且精细**：相比于只看最终生成图的 Reward 模型，CTCal 深入到注意力机制层面进行干预，监督更加直接。

### 8. 不足与局限
*   **训练开销**：由于每次训练迭代需要计算两个时间步的前向传播（虽然 $t_{tea}$ 梯度被截断），其计算成本会高于标准扩散训练。
*   **词性依赖**：虽然论文讨论了形容词，但核心逻辑仍高度依赖于名词提取的准确性，对于动词（动作对齐）或更抽象的逻辑语义（如否定词）的校准能力尚未深入探讨。
*   **算力透明度**：缺乏具体的算力消耗数据，对于开发者评估复现成本不够友好。

（完）
