---
title: Joint Degradation-Aware Arbitrary-Scale Super-Resolution for Variable-Rate Extreme Image Compression
title_zh: 面向可变速率极端图像压缩的联合退化感知任意尺度超分辨率
authors: "Xinning Chai, Zhengxue Cheng, Xin Li, Rong Xie, Li Song"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17408v1"
tags: ["query:dl-cv"]
score: 10.0
evidence: 基于扩散模型的图像压缩与超分辨率
tldr: 本文针对极端图像压缩中多码率模型训练开销大及固定缩放倍率灵活性差的问题，提出了ASSR-EIC框架。该框架在编码端引入任意尺度下采样实现可控码率降低，并在解码端利用基于扩散模型的退化感知任意尺度超分辨率技术，在单一模型内实现了码率自适应的高保真重建。通过全局适配器和局部调制器平衡生成性与保真度，显著提升了超低码率下的图像恢复质量。
selection_source: fresh_fetch
motivation: 现有的极端图像压缩方法通常需要为不同码率训练独立模型，且缺乏灵活的超分辨率缩放能力以应对严重的信息丢失。
method: 提出一种结合任意尺度下采样与扩散驱动的退化感知超分辨率解码框架，利用全局适配器和局部调制器实现精细化的码率自适应重建。
result: 实验表明，该方法在极端图像压缩任务中达到了最先进的性能，并同时实现了灵活的码率控制和高质量的图像恢复。
conclusion: ASSR-EIC通过将任意尺度超分辨率与扩散模型深度结合，为变码率极端图像压缩提供了一种高效且灵活的解决方案。
---

## 摘要
最近基于扩散模型的极端图像压缩方法在超低比特率下表现出了卓越的性能。然而，大多数方法需要为每个目标比特率训练单独的扩散模型，导致了巨大的计算开销并阻碍了实际部署。同时，最近的研究表明，联合超分辨率可以作为增强低比特率重建的有效方法。然而，在迈向超低比特率区间时，由于严重的信息丢失，这些方法面临困难，且其对固定超分辨率尺度的依赖阻止了在不同比特率下的灵活适配。为了解决这些局限性，我们提出了 ASSR-EIC，这是一种利用任意尺度超分辨率（ASSR）来支持可变速率极端图像压缩（EIC）的新型图像压缩框架。在编码器端引入了任意尺度下采样模块以提供可控的速率降低，而基于扩散模型的联合退化感知 ASSR 解码器则能够在单个模型中实现速率自适应重建。我们利用压缩和缩放感知的扩散先验来引导重建，在不同的压缩和缩放设置下实现了高保真度和高真实感的修复。具体而言，我们设计了一个全局压缩-缩放适配器，为速率自适应提供整体引导；以及一个局部压缩-缩放调制器，动态平衡生成式和保真度导向的行为，以实现细粒度的、比特率自适应的细节修复。为了进一步提高重建质量，我们引入了双重语义增强设计。大量实验表明，ASSR-EIC 在极端图像压缩中提供了最先进的性能，同时支持灵活的比特率控制和自适应的速率相关重建。

## Abstract
Recent diffusion-based extreme image compression methods have demonstrated remarkable performance at ultra-low bitrates. However, most approaches require training separate diffusion models for each target bitrate, resulting in substantial computational overhead and hindering practical deployment. Meanwhile, recent studies have shown that joint super-resolution can serve as an effective approach for enhancing low-bitrate reconstruction. However, when moving toward ultra-low bitrate regimes, these methods struggle due to severe information loss, and their reliance on fixed super-resolution scales prevents flexible adaptation across diverse bitrates.   To address these limitations, we propose ASSR-EIC, a novel image compression framework that leverages arbitrary-scale super-resolution (ASSR) to support variable-rate extreme image compression (EIC). An arbitrary-scale downsampling module is introduced at the encoder side to provide controllable rate reduction, while a diffusion-based, joint degradation-aware ASSR decoder enables rate-adaptive reconstruction within a single model. We exploit the compression- and rescaling-aware diffusion prior to guide the reconstruction, yielding high fidelity and high realism restoration across diverse compression and rescaling settings. Specifically, we design a global compression-rescaling adaptor that offers holistic guidance for rate adaptation, and a local compression-rescaling modulator that dynamically balances generative and fidelity-oriented behaviors to achieve fine-grained, bitrate-adaptive detail restoration. To further enhance reconstruction quality, we introduce a dual semantic-enhanced design.   Extensive experiments demonstrate that ASSR-EIC delivers state-of-the-art performance in extreme image compression while simultaneously supporting flexible bitrate control and adaptive rate-dependent reconstruction.

---

## 论文详细总结（自动生成）

以下是对论文《Joint Degradation-Aware Arbitrary-Scale Super-Resolution for Variable-Rate Extreme Image Compression》（面向可变速率极端图像压缩的联合退化感知任意尺度超分辨率）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在带宽极度受限的场景（如无人机、深海探测）中，极端图像压缩（码率低于 0.1 bpp）至关重要。
*   **核心问题**：
    1.  **训练开销大**：现有的基于扩散模型的压缩方法通常需要为每个目标码率单独训练模型，导致部署成本极高。
    2.  **灵活性差**：传统的联合超分辨率（SR）增强压缩通常采用固定倍率（如 2x 或 4x），无法根据带宽需求进行连续、灵活的码率调节。
    3.  **信息丢失严重**：在超低码率下，图像结构和纹理损失严重，传统回归模型难以恢复真实细节。
*   **研究意义**：提出一种名为 **ASSR-EIC** 的框架，通过在单一模型中集成“任意尺度下采样”与“退化感知扩散重建”，实现了高性能、可变码率的极端图像压缩。

### 2. 论文提出的方法论
*   **核心思想**：将变码率压缩建模为一个“退化感知”的生成式问题。在编码端通过调整下采样比例 $s$ 来控制码率，在解码端利用扩散模型根据压缩强度和缩放因子进行自适应重建。
*   **关键技术细节**：
    *   **任意尺度下采样模块**：在编码前对图像进行空间缩放，通过改变数据维度实现信号层面的码率控制，且兼容 VVC、MS-ILLM 等现有编解码器。
    *   **联合退化感知 ASSR 解码器**：基于 Stable Diffusion 架构，引入压缩感知（Codec 类型、质量参数）和缩放感知（因子 $s$）信号。
    *   **全局压缩-缩放适配器 (Global Adaptor)**：将编码参数转化为嵌入向量，注入 U-Net 的多尺度特征中，引导全局重建方向。
    *   **局部压缩-缩放调制器 (Local Modulator)**：通过 Gain-Bias 机制动态调整特征，在生成细节与保持保真度之间取得平衡。
    *   **双重语义增强设计**：结合文本提示（Caption）提供全局语义，以及利用 SAM-Tiny 提取的软语义提示（Soft Semantic Prompt）增强结构一致性。
*   **算法流程**：输入图像 $\rightarrow$ 任意尺度下采样 $\rightarrow$ 基础编解码器传输 $\rightarrow$ 扩散模型接收编码参数与初步解码图 $\rightarrow$ 迭代去噪生成高分辨率原图。

### 3. 实验设计
*   **数据集**：Kodak（经典测试集）、CLIC2020（专业压缩挑战集）、MS-COCO（大规模通用场景）。
*   **Benchmark（基准）**：
    *   **传统标准**：VVC (VTM), HEVC (HM)。
    *   **学习型压缩**：ELIC, MS-ILLM。
    *   **极端压缩 SOTA**：Text+Sketch, PerCo, DiffEIC。
*   **评估指标**：MS-SSIM（保真度）、LPIPS/DISTS/FID（感知质量）、CLIPScore（语义一致性）。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 GPU** 进行训练。
*   **训练时长**：在 OpenImagesV6 数据集（10万张图）上训练 10万次迭代，耗时约 **2 天**。
*   **推理效率**：文中对比了 20 步和 50 步扩散推理的耗时，虽然编码速度与 SOTA 相当，但解码由于扩散迭代存在明显延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流数据集上进行了全面的定量对比。
    *   **消融实验**：分别验证了全局适配器、局部调制器、文本引导和语义注意力的有效性。
    *   **泛化性实验**：将模型直接应用于训练时未见过的编解码器（如 HEVC, ELIC），验证了框架的通用性。
    *   **对比实验**：与通用的超分辨率模型（如 RealESRGAN, SeeSR）作为后处理方案进行了对比。
*   **评价**：实验设计客观、充分，通过跨编解码器的验证证明了该方法作为“插件”的鲁棒性。

### 6. 论文的主要结论与发现
*   **性能领先**：ASSR-EIC 在超低码率下显著优于 PerCo 和 DiffEIC，尤其在 FID 和 CLIPScore 上表现卓越。
*   **码率节省**：相比 VVC 锚点模型，在相同感知质量下可节省超过 80% 的码率。
*   **单模型多码率**：通过调节缩放因子 $s$，单个模型即可覆盖广泛的超低码率区间，无需重复训练。
*   **保真度平衡**：在提升视觉真实感的同时，其 MS-SSIM 指标并未像其他生成式方法那样大幅下降，保持了较好的像素级一致性。

### 7. 优点（亮点）
*   **高兼容性**：采用插件式设计，不改变现有编解码器结构，易于集成到现有标准中。
*   **连续可调性**：支持任意尺度的缩放，提供了比固定倍率 SR 更精细的码率控制手段。
*   **退化感知机制**：通过显式建模压缩伪影和缩放损失，使扩散模型能根据退化程度“对症下药”，减少了生成式伪影。

### 8. 不足与局限
*   **解码延迟**：作为基于扩散模型的方法，解码过程需要多次迭代，难以满足实时通信需求。
*   **计算复杂度**：MACs（计算量）远高于非扩散模型，对移动端设备不友好。
*   **极端缩放下的偏差**：在极大的缩放因子（如 $s > 2$）下，虽然视觉上依然真实，但可能会出现与原图细节不符的“过度锐化”或纹理改变。

（完）
