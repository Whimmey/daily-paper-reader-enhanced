---
title: "LPNSR: Prior-Enhanced Diffusion Image Super-Resolution via LR-Guided Noise Prediction"
title_zh: LPNSR：通过 LR 引导噪声预测的先验增强扩散图像超分辨率
authors: "Shuwei Huang, Shizhuo Liu, Zijun Wei"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21045v1"
tags: ["query:dl-cv"]
score: 10.0
evidence: 计算机视觉中基于扩散模型的图像超分辨率
tldr: 针对扩散模型超分辨率在高效推理时存在的重建质量下降问题，本文提出了LPNSR框架。该方法通过数学推导得出残差转移扩散范式下的最优中间噪声解析解，并设计了受低分辨率（LR）引导的多输入感知噪声预测器，以取代随机高斯噪声，增强结构先验引导。同时，引入高质量预上采样网络优化初始点。实验证明，LPNSR在仅需4步推理的情况下，在合成和真实数据集上均达到了最先进的感知性能。
selection_source: fresh_fetch
motivation: 现有的高效残差转移扩散框架在极短采样步数下，因随机噪声导致的误差累积和初始化偏差，面临严重的性能退化。
method: 提出一种由LR引导的噪声预测器来替代随机高斯噪声，并结合高质量预上采样网络来优化扩散起点。
result: 在合成和真实世界数据集上，LPNSR仅需4步推理即可实现最先进的感知重建效果，且无需依赖大规模文本生成图像先验。
conclusion: LPNSR通过引入结构先验和优化噪声预测，成功解决了高效扩散超分辨率模型在有限步数下的质量与效率平衡问题。
---

## 摘要
基于扩散的图像超分辨率（SR）旨在从相应的低分辨率（LR）观测值中重建高分辨率（HR）图像，但在推理效率与重建质量之间面临着基本权衡。最先进的残差偏移（residual-shifting）扩散框架实现了高效的 4 步推理，但在紧凑的采样轨迹中性能下降严重。这主要归因于两个核心局限性：一是中间步骤中无约束随机高斯噪声固有的次优性，导致了误差累积和 LR 先验引导不足；二是朴素双三次上采样引起的初始化偏差。在本文中，我们提出了 LPNSR，一种先验增强的高效扩散框架来解决这些问题。我们首先从数学上推导了残差偏移扩散范式下最优中间噪声的闭式解析解，并据此设计了一个 LR 引导的多输入感知噪声预测器来替代随机高斯噪声，在将 LR 结构先验嵌入反向过程的同时，充分保留了框架核心的高效残差偏移机制。我们进一步利用高质量的预上采样网络来减轻初始偏差，以优化扩散起点。通过紧凑的 4 步轨迹，LPNSR 可以进行端到端优化。广泛的实验表明，LPNSR 在合成数据集和真实世界数据集上均实现了最先进的感知性能，且不依赖于任何大规模文本到图像先验。该方法的源代码可在 https://github.com/Faze-Hsw/LPNSR 找到。

## Abstract
Diffusion-based image super-resolution (SR), which aims to reconstruct high-resolution (HR) images from corresponding low-resolution (LR) observations, faces a fundamental trade-off between inference efficiency and reconstruction quality. The state-of-the-art residual-shifting diffusion framework achieves efficient 4-step inference, yet suffers from severe performance degradation in compact sampling trajectories. This is mainly attributed to two core limitations: the inherent suboptimality of unconstrained random Gaussian noise in intermediate steps, which leads to error accumulation and insufficient LR prior guidance, and the initialization bias caused by naive bicubic upsampling. In this paper, we propose LPNSR, a prior-enhanced efficient diffusion framework to address these issues. We first mathematically derive the closed-form analytical solution of the optimal intermediate noise for the residual-shifting diffusion paradigm, and accordingly design an LR-guided multi-input-aware noise predictor to replace random Gaussian noise, embedding LR structural priors into the reverse process while fully preserving the framework's core efficient residual-shifting mechanism. We further mitigate initial bias with a high-quality pre-upsampling network to optimize the diffusion starting point. With a compact 4-step trajectory, LPNSR can be optimized in an end-to-end manner. Extensive experiments demonstrate that LPNSR achieves state-of-the-art perceptual performance on both synthetic and real-world datasets, without relying on any large-scale text-to-image priors. The source code of our method can be found at https://github.com/Faze-Hsw/LPNSR.

---

## 论文详细总结（自动生成）

### LPNSR：通过 LR 引导噪声预测的先验增强扩散图像超分辨率论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
图像超分辨率（SR）在扩散模型（Diffusion Models）的加持下取得了显著进展，但面临**推理效率与重建质量**之间的严峻权衡。
*   **背景**：现有的高效框架（如 ResShift）虽能实现 4 步快速推理，但在极短的采样路径下，生成质量较原版（如 15 步）有明显退化。
*   **核心问题**：作者指出退化源于两个瓶颈：
    1.  **噪声次优性**：中间采样步骤使用无约束的随机高斯噪声，缺乏任务特定的先验引导，导致误差累积。
    2.  **初始化偏差**：初始阶段采用简单的双三次（Bicubic）上采样，导致扩散起点远离目标流形，模型难以在极短步数内纠正偏差。

#### 2. 方法论：核心思想与关键技术
LPNSR 提出了一种先验增强的扩散框架，核心思想是将随机过程转化为受引导的确定性映射：
*   **最优噪声的数学推导**：论文从数学上证明了在残差偏移扩散范式下，存在一个能使地面真值（GT）似然最大化的“最优中间噪声”闭式解。该解显示最优噪声与当前状态 $x_t$、预测的清晰图像 $x'_0$、低分辨率图像 $y_0$ 及时间步 $t$ 存在显式条件依赖。
*   **LR 引导的噪声预测器**：设计了一个多输入感知的 UNet 预测器 $g_\omega$，取代传统的随机高斯采样。它在推理的每一步动态生成与 LR 结构一致的噪声图，将结构先验嵌入反向扩散过程。
*   **高质量预上采样**：引入预训练的回归网络（如 SwinIR-GAN）代替双三次插值，为扩散过程提供更高质量的起点，从而在 1-2 步极短推理时也能保持较好效果。
*   **端到端优化**：利用 4 步推理的紧凑性，对噪声预测器进行全路径端到端训练，确保训练与推理阶段的数学一致性。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：LSDIR（大规模图像恢复数据集）+ FFHQ（人脸数据集前 10k 张）。
    *   **测试集**：合成数据集 ImageNet-Test；真实世界数据集 RealSR 和 RealSet80。
*   **Benchmark 与对比方法**：
    *   对比了 8 种主流方法，包括 GAN 类（BSRGAN, RealESRGAN）和扩散类（DiffBIR, SeeSR, ResShift, SinSR, OSEDiff, InvSR）。
*   **评价指标**：涵盖全参考指标（PSNR, SSIM, LPIPS）和无参考感知指标（NIQE, PI, CLIPIQA, MUSIQ）。

#### 4. 资源与算力
*   **训练配置**：进行了 200k 次迭代，Batch Size 为 16，采用 AdamW 优化器和余弦退火学习率调度。
*   **硬件设备**：推理性能测试在单张 **NVIDIA RTX 3090 Ti GPU** 上完成。
*   **训练时长**：文中未明确给出总训练小时数，但给出了迭代次数和规模。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度对比**：在合成与真实两个场景下进行了定量分析，证明了模型在感知指标上的领先地位。
*   **消融实验**：
    1.  **步数消融**：测试了 1 到 4 步的表现，验证了预上采样对极短步数的贡献。
    2.  **模块消融**：逐一替换 $t=4, 3, 2$ 步的预测器为随机噪声，验证了每一步预测器的独立贡献。
    3.  **损失函数消融**：分析了 L1、LPIPS 和 GAN 损失对最终质量的影响。
    4.  **骨干网络消融**：对比了不同预上采样模型（BSRGAN, RealESRGAN, SwinIR-GAN）的兼容性。

#### 6. 主要结论与发现
*   **性能突破**：LPNSR 在 4 步推理下，感知质量（如 MUSIQ, CLIPIQA）达到了 SOTA 水平，甚至优于依赖大规模文本-图像（T2I）先验的模型（如 InvSR）。
*   **噪声非随机性**：证明了在高效扩散 SR 中，使用预测的确定性噪声图远优于随机高斯噪声，能有效抑制误差累积。
*   **初始化至关重要**：在极少步数（1-2 步）下，初始点的质量对最终结果起决定性作用；随着步数增加（3-4 步），噪声预测器的引导作用变得更加显著。

#### 7. 优点（亮点）
*   **理论严谨**：不仅提出了模型，还给出了最优噪声的数学闭式解，为扩散反向过程的改进提供了理论支撑。
*   **效率与质量兼顾**：在保持 ResShift 极速推理优势的同时，显著提升了图像的边缘锐度和结构一致性。
*   **无需外部大模型先验**：不依赖 Stable Diffusion 等重型 T2I 模型，模型更轻量且避免了生成无关内容的风险。
*   **灵活性**：支持 1-4 步的任意步数推理，且无需针对不同步数重新训练。

#### 8. 不足与局限
*   **推理开销增加**：虽然噪声预测器在潜空间运行开销极小，但引入高质量预上采样网络（如 SwinIR）会增加额外的计算耗时（如从 0.81s 增加到 1.09s）。
*   **长路径适用性**：该端到端训练策略依赖于“短路径（4步）”，若推广到 50 步以上的传统 DDPM 框架，计算成本将变得不可接受。
*   **应用限制**：目前主要针对 ×4 超分辨率任务，对于更高倍率或其他退化类型的泛化性仍需进一步验证。

（完）
