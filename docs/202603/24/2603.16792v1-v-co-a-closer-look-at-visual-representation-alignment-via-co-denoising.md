---
title: "V-Co: A Closer Look at Visual Representation Alignment via Co-Denoising"
title_zh: V-Co：深入探究通过协同去噪实现的视觉表示对齐
authors: "Han Lin, Xichen Pan, Zun Wang, Yue Zhang, Chu Wang, Jaemin Cho, Mohit Bansal"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16792v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 视觉表示对齐与像素级扩散模型
tldr: 本研究针对像素空间扩散模型语义监督不足的问题，提出了V-Co框架，系统性地探究了视觉协同去噪的关键设计。通过统一的JiT框架，研究确定了双流架构、结构化无条件预测、感知漂移混合损失和RMS特征缩放四个核心要素。实验证明，V-Co在ImageNet-256上以更少的训练周期实现了优于现有像素扩散模型的性能，为表征对齐生成模型提供了实践指导。
selection_source: fresh_fetch
motivation: 像素空间扩散模型缺乏高层语义监督，且现有协同去噪方法的设计选择复杂且缺乏系统性分析。
method: 提出V-Co框架，通过双流架构、结构化CFG、感知漂移损失和RMS特征缩放来优化视觉协同去噪过程。
result: 在ImageNet-256实验中，V-Co在模型规模相当的情况下，以更短的训练时间超越了基线和先前的像素扩散方法。
conclusion: 系统的架构设计和特征校准是提升视觉协同去噪效果的关键，为未来表征对齐的生成模型提供了高效的配方。
---

## 摘要
像素空间扩散模型最近重新崛起，成为潜空间扩散模型（latent diffusion）的有力替代方案，能够在无需预训练自编码器的情况下实现高质量生成。然而，标准的像素空间扩散模型接收到的语义监督相对较弱，且并非专门为捕获高层视觉结构而设计。最近的表示对齐方法（如 REPA）表明，预训练的视觉特征可以显著改善扩散训练，而视觉协同去噪（visual co-denoising）已成为将此类特征融入生成过程的一个极具前景的方向。然而，现有的协同去噪方法往往将多种设计选择交织在一起，导致不清楚哪些设计选择才是真正关键的。因此，我们提出了 V-Co，在一个统一的基于 JiT 的框架下对视觉协同去噪进行了系统研究。这种受控设置使我们能够分离出使视觉协同去噪有效的关键要素。我们的研究揭示了有效视觉协同去噪的四个关键要素：第一，在保持特定特征计算的同时实现灵活的跨流交互，这促使了全双流架构的采用；第二，有效的无分类器指导（CFG）需要结构化定义的无条件预测；第三，更强的语义监督最好通过感知漂移混合损失（perceptual-drifting hybrid loss）来提供；第四，稳定的协同去噪还需要适当的跨流校准，我们通过基于 RMS 的特征重缩放来实现。总之，这些发现为视觉协同去噪提供了一个简单的方案。在 ImageNet-256 上的实验表明，在模型大小相当的情况下，V-Co 在使用更少训练轮数的同时，性能优于基础像素空间扩散基线和强大的先前像素扩散方法，为未来的表示对齐生成模型提供了实践指导。

## Abstract
Pixel-space diffusion has recently re-emerged as a strong alternative to latent diffusion, enabling high-quality generation without pretrained autoencoders. However, standard pixel-space diffusion models receive relatively weak semantic supervision and are not explicitly designed to capture high-level visual structure. Recent representation-alignment methods (e.g., REPA) suggest that pretrained visual features can substantially improve diffusion training, and visual co-denoising has emerged as a promising direction for incorporating such features into the generative process. However, existing co-denoising approaches often entangle multiple design choices, making it unclear which design choices are truly essential. Therefore, we present V-Co, a systematic study of visual co-denoising in a unified JiT-based framework. This controlled setting allows us to isolate the ingredients that make visual co-denoising effective. Our study reveals four key ingredients for effective visual co-denoising. First, preserving feature-specific computation while enabling flexible cross-stream interaction motivates a fully dual-stream architecture. Second, effective classifier-free guidance (CFG) requires a structurally defined unconditional prediction. Third, stronger semantic supervision is best provided by a perceptual-drifting hybrid loss. Fourth, stable co-denoising further requires proper cross-stream calibration, which we realize through RMS-based feature rescaling. Together, these findings yield a simple recipe for visual co-denoising. Experiments on ImageNet-256 show that, at comparable model sizes, V-Co outperforms the underlying pixel-space diffusion baseline and strong prior pixel-diffusion methods while using fewer training epochs, offering practical guidance for future representation-aligned generative models.

---

## 论文详细总结（自动生成）

以下是对论文《V-Co: A Closer Look at Visual Representation Alignment via Co-Denoising》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：像素空间扩散模型（Pixel-space Diffusion）因无需预训练自编码器（VAE）而备受关注，但其面临语义监督弱、难以捕获高层视觉结构的问题。
*   **核心挑战**：虽然“协同去噪”（Co-Denoising，即同时对像素和语义特征进行去噪）被认为是引入外部视觉知识的有效途径，但现有方法的设计选择（架构、引导策略、损失函数等）过于杂乱且缺乏系统性研究，导致难以确定哪些因素真正起作用。
*   **论文目标**：在统一的 JiT（Joint-in-Time）框架下，系统性地拆解并优化视觉协同去噪的各个环节，提炼出一套高效、可扩展的“配方”（Recipe）。

### 2. 方法论：V-Co 的四个关键要素
V-Co 提出了一套包含四个核心组件的协同去噪方案：
*   **全双流架构（Fully Dual-Stream Architecture）**：
    *   **核心思想**：为了平衡特征特定计算与跨流交互，采用双流设计。
    *   **细节**：像素流和语义流（如 DINOv2 特征）拥有各自独立的归一化层、MLP 和注意力投影（Q/K/V），但在自注意力层中进行联合交互。实验证明，这种设计优于简单的单流特征拼接。
*   **结构化无条件预测（Structural Masking for CFG）**：
    *   **核心思想**：改进无分类器指导（CFG）的实现方式。
    *   **细节**：在训练无条件分支时，不只是简单地丢弃输入，而是通过**语义到像素的注意力掩码（Semantic-to-Pixel Masking）**，在结构上阻断语义流对像素流的影响。
*   **感知漂移混合损失（Perceptual-Drifting Hybrid Loss）**：
    *   **核心思想**：结合实例级对齐与分布级正则化。
    *   **细节**：公式结合了感知损失（将预测图像拉向目标特征）和漂移损失（通过排斥机制防止模式崩溃）。引入相似度门控机制（Gating），当预测接近目标时侧重吸引，远离时侧重排斥。
*   **基于 RMS 的特征校准（RMS-based Feature Rescaling）**：
    *   **核心思想**：匹配两个流的去噪难度。
    *   **细节**：通过计算像素和语义特征的均方根（RMS）并进行缩放，使两者在扩散过程中具有等效的信噪比（SNR），从而稳定训练。

### 3. 实验设计
*   **数据集**：主要在 **ImageNet-256 × 256** 上进行验证。
*   **Benchmark 与对比方法**：
    *   **基线**：JiT（基础像素扩散模型）。
    *   **对比方法**：包括潜空间扩散模型（DiT, SiT, REPA）、像素空间扩散模型（ADM, RIN, Simple Diffusion, PixelGen, DeCo, ReDi）以及协同去噪方法（Latent Forcing）。
*   **评估指标**：FID（生成质量）和 IS（多样性）。

### 4. 资源与算力
*   **训练配置**：Batch Size 为 1024，使用 Adam 优化器。
*   **训练时长**：消融实验通常运行 200 个 epoch；最终模型（V-Co-L/H）运行 300 到 600 个 epoch。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体数量，但给出了不同规模模型（Base, Large, Huge）的参数量（260M 到 1.9B）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的消融实验（共计 13 张表格），涵盖了架构变体（单流 vs 双流）、CFG 策略（独立丢弃 vs 联合丢弃 vs 掩码）、损失函数组合、特征缩放比例、DINOv2 不同尺寸的影响等。
*   **充分性与公平性**：实验设计非常严谨。在对比不同架构时，通过调整隐藏维度确保了参数量的一致性；在对比损失函数时，统一了训练周期。这种受控变量法使得结论具有很高的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：V-Co-B/16（260M 参数）的性能即可匹配参数量近两倍的 JiT-L/16。
*   **超越巨型模型**：经过 300-500 epoch 训练的 V-Co-L 和 V-Co-H 在 FID 指标上超越了拥有 2B 参数的 JiT-G 模型。
*   **配方有效性**：证明了全双流架构、结构化掩码、混合损失和 RMS 校准是实现高效视觉表示对齐的四个支柱。

### 7. 优点
*   **系统性强**：不仅提出了新方法，更重要的是对现有协同去噪的混乱设计进行了“大扫除”，提供了清晰的理论解释（如 RMS 缩放与 SNR 匹配的等效性）。
*   **效率高**：相比于传统的像素扩散模型，V-Co 显著提升了收敛速度和最终生成的语义准确性。
*   **模块化设计**：提出的四个组件可以灵活组合或集成到其他扩散框架中。

### 8. 不足与局限
*   **场景局限**：实验主要集中在 ImageNet 类条件生成上，尚未在文生图（Text-to-Image）等更广泛、更复杂的开放域任务上验证。
*   **教师模型依赖**：高度依赖预训练的 DINOv2 特征，如果教师模型存在偏差或空间分辨率不足，可能会限制生成效果。
*   **计算开销**：虽然参数效率高，但双流架构在推理时相比单流模型会增加一定的计算延迟和内存占用。

（完）
