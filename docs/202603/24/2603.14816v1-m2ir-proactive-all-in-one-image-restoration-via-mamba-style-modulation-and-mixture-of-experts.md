---
title: "M2IR: Proactive All-in-One Image Restoration via Mamba-style Modulation and Mixture-of-Experts"
title_zh: M2IR：通过 Mamba 风格调制与混合专家实现的主动式全能图像修复
authors: "Shiwei Wang, Yongzhen Wang, Bingwen Hu, Liyan Zhang, Xiao-Ping Zhang, Mingqiang Wei"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14816v1"
tags: ["query:dl-cv"]
score: 10.0
evidence: 基于Mamba风格调制的通用图像修复
tldr: 针对现有全能图像修复模型被动传播退化信号导致特征学习受干扰的问题，本文提出M2IR框架。该框架引入Mamba风格Transformer（MST）块在编码阶段主动抑制退化，并结合自适应退化专家协作（ADEC）模块在解码阶段利用混合专家模型精准消除残余退化。M2IR实现了从被动修复到主动退化控制的转变，在多个基准测试中展现了卓越的泛化能力和细节恢复效果。
selection_source: fresh_fetch
motivation: 传统Transformer架构在修复过程中往往被动传播退化信号，导致解码器难以平衡伪影去除与细节保留。
method: 提出结合Mamba风格像素级调制的主动抑制机制，并利用DA-CLIP引导的混合专家协作模块进行针对性修复。
result: 实验证明M2IR在多种全能图像修复任务中均取得了优于现有技术的泛化性能和精细的细节恢复质量。
conclusion: 通过将修复过程从被动反应转变为主动退化控制，M2IR显著提升了全能图像修复的效率与适应性。
---

## 摘要
虽然基于 Transformer 的架构在全能图像修复的最新进展中占据主导地位，但它们在本质上仍然是反应性的：即传播退化信号而非主动抑制。在缺乏显式抑制机制的情况下，退化信号会干扰特征学习，迫使解码器在伪影去除和细节保留之间进行权衡，从而增加了模型复杂度并限制了适应性。为了应对这些挑战，我们提出了 M2IR，这是一种新型修复框架，它在编码阶段主动调节退化传播，并在解码阶段高效消除残余退化。具体而言，Mamba 风格 Transformer (MST) 模块执行像素级选择性状态调制，在减轻退化的同时保持结构完整性。与此同时，自适应退化专家协作 (ADEC) 模块利用由 DA-CLIP 驱动的路由引导的退化特定专家，并辅以共享专家，通过有针对性的协作修复来消除残余退化。通过集成 MST 模块和 ADEC 模块，M2IR 从被动反应转变为主动退化控制，有效地利用学习到的表示，在各种全能图像修复基准测试中实现了卓越的泛化能力、增强的适应性以及精细细节的精准恢复。我们的源代码可在 https://github.com/Im34v/M2IR 获取。

## Abstract
While Transformer-based architectures have dominated recent advances in all-in-one image restoration, they remain fundamentally reactive: propagating degradations rather than proactively suppressing them. In the absence of explicit suppression mechanisms, degraded signals interfere with feature learning, compelling the decoder to balance artifact removal and detail preservation, thereby increasing model complexity and limiting adaptability. To address these challenges, we propose M2IR, a novel restoration framework that proactively regulates degradation propagation during the encoding stage and efficiently eliminates residual degradations during decoding. Specifically, the Mamba-Style Transformer (MST) block performs pixel-wise selective state modulation to mitigate degradations while preserving structural integrity. In parallel, the Adaptive Degradation Expert Collaboration (ADEC) module utilizes degradation-specific experts guided by a DA-CLIP-driven router and complemented by a shared expert to eliminate residual degradations through targeted and cooperative restoration. By integrating the MST block and ADEC module, M2IR transitions from passive reaction to active degradation control, effectively harnessing learned representations to achieve superior generalization, enhanced adaptability, and refined recovery of fine-grained details across diverse all-in-one image restoration benchmarks. Our source codes are available at https://github.com/Im34v/M2IR.

---

## 论文详细总结（自动生成）

### M2IR：通过 Mamba 风格调制与混合专家实现的主动式全能图像修复

#### 1. 论文的核心问题与整体含义（研究动机和背景）
现有的全能（All-in-One）图像修复方法（如基于 Transformer 的架构）大多是**被动反应式**的。它们在编码阶段不加区分地处理清晰内容和退化信号，导致退化信息在网络中传播并与结构特征深度耦合。这迫使解码器必须花费大量计算资源去解耦特征，难以平衡“去除伪影”与“保留细节”。
**研究动机：** 改变这种被动局面，提出一种**主动式退化治理**框架。在编码阶段主动抑制退化传播，在解码阶段通过专家协作精准消除残余退化。

#### 2. 论文提出的方法论
M2IR 采用 U 型架构，核心包含两个关键组件：
*   **Mamba-Style Transformer (MST) 块：**
    *   **核心思想：** 将 Mamba 的选择性状态空间模型（SSM）中的“选择性门控机制”引入 Transformer。
    *   **技术细节：** 在多转置注意力（MDTA）分支中加入像素级选择性调制。通过深度卷积和 Sigmoid 激活生成软掩码（Soft Mask），主动抑制退化区域的激活，使模型在编码阶段就聚焦于结构和语义信息。
*   **Adaptive Degradation Expert Collaboration (ADEC) 模块：**
    *   **退化感知上下文先验 (DACP)：** 利用预训练的 **DA-CLIP** 提取图像退化特征和文本描述（如“多雨”、“模糊”）的相似度，生成空间感知的退化先验。
    *   **退化引导专家聚合 (DGEA)：** 采用混合专家（MoE）架构。路由（Router）根据 DACP 提供的先验，为每个像素动态选择 **Top-K 个专业专家** 和 **1 个共享专家**。专业专家负责特定退化修复，共享专家负责维持基础结构。
*   **损失函数：** 组合了 Charbonnier 损失（像素精度）、负载均衡损失（防止专家过度集中）和 FFT 损失（频域结构一致性）。

#### 3. 实验设计
*   **数据集与场景：**
    *   **合成退化：** 3D 设置（雾、雨、噪声）、5D 设置（增加模糊、低光照）。
    *   **复合退化：** CDD11 数据集（包含 11 种单一及组合退化类型）。
    *   **真实世界：** WeatherBench 数据集（真实的雾、雨、雪场景）。
*   **Benchmark 与对比方法：**
    *   对比了 Restormer、NAFNet 等通用修复方法，以及 AirNet、PromptIR、AdaIR、MIRAGE 等最先进的全能修复方法。
    *   评价指标包括 PSNR、SSIM、LPIPS 和 FID。

#### 4. 资源与算力
*   **硬件：** 使用了 **2 张 NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节：** 采用 AdamW 优化器，初始学习率 $2 \times 10^{-4}$，使用线性预热和余弦退火调度。输入图像裁剪为 $128 \times 128$ 的 Patch。
*   **效率分析：** 虽然引入 DA-CLIP 使参数量增加（从 39.1M 增至 285.3M），但计算量（FLOPs）仅微增（961G 到 975G），推理延迟仅增加 9ms（330ms 到 339ms），证明了其在实际应用中的可行性。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从 3D 到 5D 的多任务实验、复合退化实验、真实世界数据集验证、单任务独立训练实验以及泛化性（未见过的去雪任务）实验。
*   **消融实验：** 针对 MST、DACP、DGEA 各组件、专家数量 $N$、Top-K 选择数量、以及各项损失函数的权重进行了详尽的消融研究。
*   **客观性：** 通过 **t-SNE 可视化** 证明了 MST 编码器能有效使不同退化的特征分布重叠（即去除了退化特异性），通过**门控图可视化**展示了模型对退化区域的主动抑制，实验设计科学且论证充分。

#### 6. 论文的主要结论与发现
*   **主动抑制有效：** MST 能够有效阻止退化信号在编码阶段的扩散，减轻了解码器的负担。
*   **专家协作优越：** ADEC 模块通过共享专家与专业专家的配合，在处理复杂复合退化时表现出极强的鲁棒性。
*   **性能领先：** M2IR 在所有主流全能修复基准测试中均达到了 SOTA 性能，尤其在复合退化数据集 CDD11 上，PSNR 提升显著（较 MIRAGE 提升约 0.78dB）。

#### 7. 优点
*   **范式创新：** 提出了从“被动补偿”到“主动治理”的修复范式转变。
*   **架构融合：** 巧妙地结合了 Mamba 的选择性调制能力与 Transformer 的全局建模能力。
*   **泛化力强：** 在真实世界场景和未见过的退化类型上表现出卓越的鲁棒性。
*   **效率平衡：** 成功利用大规模预训练模型（DA-CLIP）的先验知识，且未显著增加推理负担。

#### 8. 不足与局限
*   **参数量较大：** 尽管推理速度尚可，但 285M 的参数量对于移动端或嵌入式设备的存储空间仍具挑战。
*   **特定任务微弱差距：** 在去模糊（Deblurring）任务上，虽然平均性能领先，但在个别指标上与某些专门针对模糊任务的模型相比优势不明显。
*   **训练复杂性：** 涉及 MoE 路由、多项损失函数平衡以及冻结/训练权重的配合，训练调优过程可能较为复杂。

（完）
