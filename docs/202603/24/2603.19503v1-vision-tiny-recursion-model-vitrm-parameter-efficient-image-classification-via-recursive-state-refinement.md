---
title: "Vision Tiny Recursion Model (ViTRM): Parameter-Efficient Image Classification via Recursive State Refinement"
title_zh: 视觉微型递归模型 (ViTRM)：通过递归状态细化实现参数高效的图像分类
authors: "Ange-Clément Akazan, Abdoulaye Koroko, Verlon Roel Mbingui, Choukouriyah Arinloye, Hassan Fifen, Rose Bandolo"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19503v1"
tags: ["query:dl-cv"]
score: 10.0
evidence: 用于计算机视觉图像分类的深度学习
tldr: 针对深度学习模型参数量巨大、难以在资源受限环境下部署的问题，本文受微型递归模型启发，提出了Vision Tiny Recursion Model (ViTRM)。该模型通过将传统的L层ViT编码器替换为仅包含3层的微型块并进行多次递归迭代，实现了极高的参数效率。实验表明，ViTRM在参数量比ViT少84倍的情况下，仍能在CIFAR数据集上保持竞争力，证明了递归计算是替代增加模型深度的一种高效方案。
selection_source: fresh_fetch
motivation: 现有的CNN和ViT模型参数密集且计算资源需求高，限制了其在资源受限环境中的应用。
method: 提出ViTRM架构，使用一个仅有3层的微型块通过N次递归迭代来替代深层的ViT编码器。
result: 在CIFAR-10/100上，ViTRM以比CNN少6倍、比ViT少84倍的参数量实现了具有竞争力的分类性能。
conclusion: 递归状态细化是视觉领域中一种可行的、能够有效替代增加架构深度的参数高效方案。
---

## 摘要
深度学习在计算机视觉领域的成功是由不断扩大的模型规模驱动的，从深层卷积神经网络 (CNN) 到大型视觉 Transformer (ViT)。尽管这些架构非常有效，但它们是参数密集型的，且需要大量的计算资源，限制了其在资源受限环境中的部署。受微型递归模型 (TRM) 的启发（该模型表明小型递归网络可以通过迭代状态细化来解决复杂的推理任务），我们提出了视觉微型递归模型 (ViTRM)：这是一种参数高效的架构，它将 $L$ 层 ViT 编码器替换为单个微小的 $k$ 层块（$k=3$），并递归应用 $N$ 次。尽管其参数量分别比基于 CNN 的模型和 ViT 少多达 6 倍和 84 倍，ViTRM 在 CIFAR-10 和 CIFAR-100 数据集上仍保持了具有竞争力的性能。这表明递归计算是视觉领域中替代架构深度的一种可行且参数高效的方案。

## Abstract
The success of deep learning in computer vision has been driven by models of increasing scale, from deep Convolutional Neural Networks (CNN) to large Vision Transformers (ViT). While effective, these architectures are parameter-intensive and demand significant computational resources, limiting deployment in resource-constrained environments. Inspired by Tiny Recursive Models (TRM), which show that small recursive networks can solve complex reasoning tasks through iterative state refinement, we introduce the \textbf{Vision Tiny Recursion Model (ViTRM)}: a parameter-efficient architecture that replaces the $L$-layer ViT encoder with a single tiny $k$-layer block ($k{=}3$) applied recursively $N$ times. Despite using up to $6 \times $ and $84 \times$ fewer parameters than CNN based models and ViT respectively, ViTRM maintains competitive performance on CIFAR-10 and CIFAR-100. This demonstrates that recursive computation is a viable, parameter-efficient alternative to architectural depth in vision.

---

## 论文详细总结（自动生成）

以下是对论文《Vision Tiny Recursion Model (ViTRM): Parameter-Efficient Image Classification via Recursive State Refinement》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保持计算机视觉模型性能的同时，极大地减少参数量，以适应资源受限的部署环境？
*   **研究背景**：当前的视觉模型（如 CNN 和 ViT）遵循“更大、更深”的范式，虽然效果显著，但参数动辄数千万甚至数十亿，计算成本极高。
*   **研究动机**：受算法推理领域“微型递归模型（TRM）”的启发，作者思考是否可以通过**时间上的递归计算**（多次调用同一组参数）来替代**空间上的架构深度**（堆叠不同的层），从而在视觉感知任务中实现极高的参数效率。

### 2. 论文提出的方法论：ViTRM 架构
*   **核心思想**：将传统的 $L$ 层 ViT 编码器替换为一个极小的、权重共享的 $k$ 层（实验中 $k=3$）Transformer 块，通过多次递归迭代（$N$ 次）来逐步细化图像表示和分类预测。
*   **关键技术细节**：
    *   **双状态递归**：模型维护两个动态状态：**潜在大脑/存储器 ($z$)**（捕捉空间信息）和**预测标记 ($y$)**（类似于 [CLS] token，用于最终分类）。
    *   **递归更新流程**：
        1.  **细化存储阶段**：使用共享 Transformer 块处理图像 patch ($x$)、预测标记 ($y$) 和存储器 ($z$) 的拼接，更新 $z$。
        2.  **更新预测阶段**：仅使用更新后的 $z$ 和当前的 $y$ 再次通过共享块，更新 $y$。
    *   **深度监督与停止机制**：在训练的每个递归步骤都施加监督损失（分类损失 + 停止损失）。引入一个“停止头（Halting Head）”来预测当前结果是否足够准确，从而支持推理时的自适应早期停止。
    *   **权重共享**：所有递归步骤共用同一组 Transformer 参数，极大地压缩了模型体积。

### 3. 实验设计
*   **数据集**：CIFAR-10 和 CIFAR-100（32x32 分辨率）。
*   **Benchmark（基准）**：
    *   **ViT 系列**：ViT-Small (16.9M), ViT-Base (85.1M), ViT-Large (302M)。
    *   **ResNet 系列**：ResNet-18, ResNet-34, ResNet-50 (11M-23M)。
*   **对比维度**：在 5 种不同的 Batch Size（64 到 1024）下测试 Top-1 准确率，以评估模型对优化超参数的鲁棒性。

### 4. 资源与算力
*   **硬件设备**：单台 **NVIDIA GH200 GPU (120GB)**。
*   **训练细节**：
    *   使用 AdamW 优化器。
    *   ViT 和 ResNet 基准训练 300 个 epoch。
    *   **ViTRM 训练多达 1000 个 epoch**（作者指出虽然 epoch 多，但由于参数极少，单次 epoch 的墙钟时间远短于大型 ViT）。
    *   使用了指数移动平均（EMA）来稳定递归训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流基准数据集，并针对 7 种基准模型在 5 种不同批次规模下进行了详尽对比。
*   **消融实验**：
    *   **监督深度 ($N_{supervision}$)**：研究了监督频率对性能的影响，发现过深的监督反而会导致过拟合。
    *   **递归深度 ($n_{latent\_steps}$)**：研究了迭代次数的影响，发现 3 次左右的递归是性能与成本的平衡点。
*   **充分性评价**：实验设计较为客观、公平，通过多批次规模的对比展示了递归模型在小数据集上比超大型模型（如 ViT-Large）更难过拟合的优势。

### 6. 主要结论与发现
*   **参数效率极高**：ViTRM 仅需 **3.6M** 参数，比 ViT-Base 少 **23 倍**，比 ViT-Large 少 **84 倍**。
*   **性能强劲**：在 CIFAR-10 上达到 93.1% 的准确率，与参数量大得多的 ViT-Small (94.0%) 接近，且全面超越了 ResNet-50。
*   **鲁棒性**：相比 ResNet，ViTRM 对 Batch Size 的变化更不敏感，表现出更稳定的优化特性。
*   **递归深度收益递减**：增加递归次数并不总是提升精度，适度的递归（如 3-6 次）即可达到最优效果。

### 7. 优点（亮点）
*   **范式创新**：成功将推理领域的递归思想引入视觉感知，挑战了“深度即性能”的传统观念。
*   **极低部署门槛**：3.6M 的参数量极易在嵌入式设备或移动端部署。
*   **自适应推理**：通过停止机制，模型可以根据输入图像的难度动态调整计算量（简单的图少递归，难的图多递归）。

### 8. 不足与局限
*   **数据集局限性**：实验仅限于低分辨率（32x32）的 CIFAR 数据集，尚未在 ImageNet-1K 或更高分辨率的大规模数据集上验证其扩展性。
*   **训练时长**：虽然单次迭代快，但为了收敛，ViTRM 需要比传统模型更多的训练 epoch（1000 vs 300）。
*   **任务覆盖**：目前仅验证了图像分类任务，对于目标检测、语义分割等需要高分辨率特征图的密集预测任务，其递归机制的有效性尚待证明。

（完）
