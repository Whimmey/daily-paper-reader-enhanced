---
title: "λ-GELU: Learning Gating Hardness for Controlled ReLU-ization in Deep Networks"
title_zh: λ-GELU：学习深度网络中受控ReLU化的门控硬度
authors: "Cristian Pérez-Corral, Alberto Fernández-Hernández, Jose I. Mestre, Manuel F. Dolz, Enrique S. Quintana-Ortí"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21991v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 用于深度网络的参数化GELU激活函数
tldr: 本研究提出了一种参数化的GELU激活函数λ-GELU，通过引入硬度参数λ控制门控尖锐程度，实现从平滑训练到ReLU兼容模型的受控转换。作者设计了约束重参数化和优化器感知的更新方案，解决了学习λ时的不稳定问题。实验表明该方法在多种架构中呈现结构化分层硬度分布，并提供了确定性ReLU化策略，有效桥接了平滑训练与ReLU为中心的下游处理流程。
selection_source: fresh_fetch
motivation: 现有GELU激活函数虽广泛使用，但部署、压缩和分析工具链更适合处理分段线性网络，需要一种可控的ReLU转换方法。
method: 提出λ-GELU激活函数f(x;λ)=xΦ(λx)，结合约束重参数化和优化器感知的更新方案来学习硬度参数λ。
result: 在MLPs、CNNs和Transformers等多种模型中观察到结构化的分层硬度分布，且该分布在不同初始化下具有鲁棒性。
conclusion: λ-GELU提供了一个最小且可解释的硬度控制旋钮，有效桥接了平滑训练与ReLU为中心的下游处理流程。
---

## 摘要
高斯误差线性单元(GELU)是作为修正线性单元(ReLU)的广泛使用的平滑替代方案，然而许多部署、压缩和分析工具链最自然地表达为针对分段线性(ReLU型)网络。我们研究了一种硬度参数化的GELU公式，f(x;λ)=xΦ(λ x)，其中Φ是高斯累积分布函数，λ∈[1,∞)控制门控锐度，目标是将平滑门控训练转变为通向ReLU兼容模型的受控路径。学习λ参数并非易事：简单的更新会导致不稳定动态和有效梯度衰减，因此我们引入了约束重参数化和感知优化器的更新方案。经验上，跨越涵盖多层感知机(MLP)、卷积神经网络(CNN)和Transformer的多样化模型-数据集对，我们观察到结构化的逐层硬度分布，并评估了它们在不同初始化下的鲁棒性。我们进一步研究了一种确定性ReLU化策略，其中学习的门控逐渐硬化至一个有原则的目标，使得能够在训练后用ReLU替代λ-GELU，同时减少干扰。总体而言，λ-GELU提供了一个最小且可解释的控制旋钮来分析和控制门控硬度，连接了平滑训练与以ReLU为中心的下游流程。

## Abstract
Gaussian Error Linear Unit (GELU) is a widely used smooth alternative to Rectifier Linear Unit (ReLU), yet many deployment, compression, and analysis toolchains are most naturally expressed for piecewise-linear (ReLU-type) networks. We study a hardness-parameterized formulation of GELU, f(x;λ)=xΦ(λ x), where Φ is the Gaussian CDF and λ \in [1, infty) controls gate sharpness, with the goal of turning smooth gated training into a controlled path toward ReLU-compatible models. Learning λ is non-trivial: naive updates yield unstable dynamics and effective gradient attenuation, so we introduce a constrained reparameterization and an optimizer-aware update scheme.   Empirically, across a diverse set of model--dataset pairs spanning MLPs, CNNs, and Transformers, we observe structured layerwise hardness profiles and assess their robustness under different initializations. We further study a deterministic ReLU-ization strategy in which the learned gates are progressively hardened toward a principled target, enabling a post-training substitution of λ-GELU by ReLU with reduced disruption. Overall, λ-GELU provides a minimal and interpretable knob to profile and control gating hardness, bridging smooth training with ReLU-centric downstream pipelines.

---

## 论文详细总结（自动生成）

# λ-GELU论文总结

## 1. 论文的核心问题与整体含义

论文针对深度学习中激活函数选择的两难困境：GELU作为平滑激活函数在训练时表现良好，但许多部署、压缩和分析工具链更适合处理分段线性(ReLU型)网络。研究目标是构建一种桥梁，既能利用GELU在训练时的优化优势，又能使模型最终能够与ReLU为中心的下游处理流程兼容，实现从平滑训练到ReLU兼容模型的受控转换。

## 2. 论文提出的方法论

### 核心思想
引入一个硬度参数λ来控制GELU门控的尖锐程度，实现从平滑GELU到硬ReLU的连续过渡。当λ=1时得到标准GELU，当λ→∞时函数趋近于ReLU。

### 关键技术细节
- **λ-GELU激活函数**：f(x;λ) = x·Φ(λx)，其中Φ是高斯累积分布函数，λ∈[1,∞)
- **约束重参数化**：使用softplus函数将无约束参数s映射到λ>1：λ(s) = 1 + log(1 + e^{st})，其中t>0是温度参数
- **优化器感知更新**：为硬度参数s设置单独的学习率ηs = c·ηw，其中c是控制λ适应速度的超参数
- **确定性ReLU化策略**：训练25%个epoch后，线性增加λ至目标值λtarget，然后评估用ReLU替换λ-GELU的效果

## 3. 实验设计

### 数据集/场景
- MLP/Adult（表格数据）
- MLP/FMNIST（图像数据）
- ResNet-18/CIFAR-10和CIFAR-100（图像分类）
- DeiT/TinyImageNet（视觉Transformer）
- GPT-2/WikiText-2（语言模型）

### Benchmark
- 验证准确率（表格和视觉任务）
- 验证困惑度（语言模型任务）

### 对比方法
- 标准GELU
- ReLU
- 不同初始化模式的λ-GELU（uniform、increasing、decreasing）

## 4. 资源与算力

论文中未明确提到使用的GPU型号、数量或训练时长等算力信息。

## 5. 实验数量与充分性

### 实验数量
- 超参数搜索：在MLP/FMNIST上对(t,c)参数进行网格搜索
- 多架构评估：在6种模型-数据集组合上评估方法
- 初始化鲁棒性测试：使用3种不同的初始化模式
- 硬度分析：评估不同初始化下的层间硬度相关性
- ReLU替换实验：评估渐进硬化和直接替换的效果

### 充分性评估
实验覆盖了多种架构类型（MLP、CNN、Transformer），使用了多个标准数据集，进行了消融实验（不同初始化模式）。实验设计客观，与基线方法进行了公平比较，但未探索更细粒度的参数控制（如通道级或神经元级）。

## 6. 论文的主要结论与发现

- λ-GELU能够有效控制门控硬度，实现从平滑到硬化的过渡
- 在多种架构中观察到结构化的层间硬度分布
- 硬度分布在不同初始化下具有鲁棒性（特别是在视觉和表格任务中）
- 渐进硬化策略使得ReLU替换的性能损失显著小于直接替换
- 通常使用ReLU的架构（如MLP和CNN）倾向于学习更大的λ值
- 语言模型（GPT-2）对激活函数替换更为敏感

## 7. 优点

- 提供了一个最小且可解释的控制旋钮来分析和控制门控硬度
- 约束重参数化确保了λ≥1，避免了不稳定更新
- 优化器感知的更新方案解决了梯度衰减问题
- 渐进硬化策略实现了平滑过渡到ReLU兼容模型
- 使用Spearman秩相关系数客观评估不同初始化下的硬度分布一致性
- 提出了基于ℓ1近似界的 principled 目标λtarget选择方法

## 8. 不足与局限

- 仅评估了层级共享的λ参数，未探索更细粒度的控制（如通道级或神经元级）
- 语言模型中的激活替换效果不佳，表明方法可能不适用于所有架构
- λtarget的选择是分布无关的，可能对归一化架构过于保守
- 未评估方法在实际下游应用（如量化、压缩）中的具体收益
- 实验主要关注性能指标，缺乏对计算开销的详细分析
- 未探索不同优化器对λ学习的影响

（完）
