---
title: "DSS-GAN: Directional State Space GAN with Mamba backbone for Class-Conditional Image Synthesis"
title_zh: DSS-GAN：基于Mamba主干的方向性状态空间GAN用于类别条件图像合成
authors: "Aleksander Ogonowski, Konrad Klimaszewski, Przemysław Rokita"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17637v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 新型GAN架构
tldr: DSS-GAN是首个采用Mamba作为分层生成器主干的生成对抗网络，用于条件图像合成。其核心创新是方向性潜在路由(DLR)机制，将潜在向量分解为方向特定子向量，与类别嵌入共同投影产生特征仿射调制。与传统全局条件不同，DLR沿特征图不同空间轴耦合类别和潜在结构，在多个数据集上超越StyleGAN2-ADA的性能，且潜在空间分析显示方向性子向量具有专业化特性。
selection_source: fresh_fetch
motivation: 现有生成对抗网络在条件图像合成中缺乏对类别信息和潜在结构沿不同空间轴的有效耦合机制。
method: DSS-GAN引入方向性潜在路由(DLR)机制，将潜在向量分解为方向特定子向量，与类别嵌入共同投影产生特征仿射调制，应用于Mamba生成器的所有尺度。
result: DSS-GAN在多个测试数据集上实现了优于StyleGAN2-ADA的FID、KID和精确度-召回率分数，且潜在空间分析显示方向性子向量具有可测量的专业化特性。
conclusion: 方向性潜在路由机制有效提升了生成对抗网络在条件图像合成任务中的性能，并揭示了潜在空间中方向性子向量的专业化特性。
---

## 摘要
我们提出了DSS-GAN，这是第一个采用Mamba作为分层生成器主干用于噪声到图像生成的生成对抗网络。主要贡献是方向性潜在路由(DLR)，一种新颖的条件机制，它将潜在向量分解为方向特定的子向量，每个子向量与类别嵌入共同投影，以产生相应Mamba扫描的特征级仿射调制。与注入全局信号的传统类别条件不同，DLR沿着特征图的不同空间轴耦合类别身份和潜在结构，并在所有生成尺度上一致应用。在多个测试数据集上，DSS-GAN相比StyleGAN2-ADA实现了更好的FID、KID和精确度-召回率分数。潜在空间分析表明，方向性子向量表现出可测量的专业化：沿单个分量的扰动会在合成的图像中产生结构化的、方向相关的变化。

## Abstract
We present DSS-GAN, the first generative adversarial network to employ Mamba as a hierarchical generator backbone for noise-to-image synthesis. The central contribution is Directional Latent Routing (DLR), a novel conditioning mechanism that decomposes the latent vector into direction-specific subvectors, each jointly projected with a class embedding to produce a feature-wise affine modulation of the corresponding Mamba scan. Unlike conventional class conditioning that injects a global signal, DLR couples class identity and latent structure along distinct spatial axes of the feature map, applied consistently across all generative scales. DSS-GAN achieves improved FID, KID, and precision-recall scores compared to StyleGAN2-ADA across multiple tested datasets. Analysis of the latent space reveals that directional subvectors exhibit measurable specialization: perturbations along individual components produce structured, direction-correlated changes in the synthesized image.

---

## 论文详细总结（自动生成）

# DSS-GAN论文总结

## 1. 论文的核心问题与整体含义

**研究动机与背景**：
- 现有生成对抗网络(GAN)在条件图像合成中缺乏对类别信息和潜在结构沿不同空间轴的有效耦合机制
- 传统条件GAN方法（如StyleGAN2-ADA）使用全局条件信号，无法有效捕捉图像中不同方向的空间结构
- 状态空间模型(SSM)特别是Mamba在视觉任务中的应用主要局限于判别性任务和图像到图像转换，尚未探索作为GAN生成器主干用于噪声到图像合成

**整体含义**：
- DSS-GAN是首个采用Mamba作为分层生成器主干用于条件图像生成的GAN
- 通过方向性潜在路由(DLR)机制，实现了类别信息和潜在结构沿不同空间轴的有效耦合
- 在保持生成质量的同时，显著减少了参数量（比StyleGAN2-ADA减少82%）

## 2. 论文提出的方法论

**核心思想**：
- 将潜在向量分解为方向特定的子向量，每个子向量与类别嵌入共同投影，产生特征级仿射调制
- 沿特征图的不同空间轴（行、列、对角线等）耦合类别身份和潜在结构
- 在所有生成尺度上一致应用这种方向性条件机制

**关键技术细节**：
- **方向性潜在路由(DLR)**：将潜在向量z分解为基潜在zbase和方向路由向量zdir
- zdir进一步分为K个等长子向量，每个子向量对应一个扫描方向
- 每个扫描方向k有独立的类嵌入表Ek，产生方向特定的类嵌入ek
- 方向特定的MLP将[zk_dir || ek]投影为仿射参数(γk, βk)
- 应用仿射调制到Mamba扫描的token序列：ŝk = γk ⊙ sk + βk
- **方向加权网络**：计算各方向的权重w = σS((b + Wz zdir + Wy ey)/τ)
- **180度旋转机制**：改善梯度稳定性，不改变输出空间布局

## 3. 实验设计

**数据集/场景**：
- AFHQ（3个类别：猫、狗、野生动物，每类约5k样本）
- FFHQ（无条件，70k样本）
- LSUN（室内场景：卧室、厨房，每类15k样本，128×128；室外建筑场景：桥梁、教堂、塔楼，每类15k样本，256×256）
- CelebA（10个属性类别，每类10k样本）

**Benchmark**：
- StyleGAN2-ADA作为主要对比方法
- 在相同分辨率和训练配置下进行比较

**对比方法**：
- StyleGAN2-ADA（主要基准）
- 不同扫描方向数量的DSS-GAN变体（1方向、2方向、3方向）
- 不同CNN边界分辨率的消融实验

## 4. 资源与算力

- **GPU型号**：NVIDIA H100
- **参数量**：3方向DSS-GAN模型7.3M，1方向4.4M，StyleGAN2-ADA 25.0M
- **推理速度**：1方向DSS-GAN在batch size 1时延迟8.1ms（约122 FPS）
- **训练配置**：128×128和256×256分辨率下batch size为96，512×512下为48
- **训练时长**：论文中未明确提及具体训练时长

## 5. 实验数量与充分性

**实验数量**：
- 5个数据集配置（AFHQ、FFHQ、LSUN室内、LSUN室外、CelebA）
- 多种分辨率（128×128、256×512、512×512）
- 多种扫描方向配置（1方向、2方向、3方向）
- 多种消融实验（CNN边界分辨率、旋转机制、方向多样性等）
- 潜在空间分析（类覆盖实验、方向潜在交换、噪声扰动等）

**充分性与公平性**：
- 实验设计较为充分，覆盖了多种数据集和配置
- 使用了标准评估指标（FID、KID、Precision、Recall、Density、Coverage）
- 在相同训练配置下与StyleGAN2-ADA进行对比
- 消融实验验证了各组件的贡献
- 潜在空间分析提供了对模型行为的深入理解

## 6. 论文的主要结论与发现

**主要结论**：
- DSS-GAN在多个数据集上实现了与StyleGAN2-ADA相当或更好的性能
- 方向性潜在路由机制有效提升了生成质量，特别是在具有方向性纹理结构的数据集上
- 模型参数量减少82%，同时保持或提高生成质量
- 潜在空间分析显示方向性子向量具有专业化特性

**主要发现**：
- 最优扫描方向数量取决于数据集几何结构而非简单最大化
- 类别变化在低分辨率阶段（8×8、16×16）影响最大
- 方向性子向量控制图像的不同空间特征（行、列、对角线）
- 180度旋转机制改善了梯度稳定性
- CNN边界分辨率对生成质量和多样性有显著影响

## 7. 优点

**方法亮点**：
- 首次将Mamba作为GAN生成器主干用于噪声到图像合成
- 方向性潜在路由(DLR)机制创新性地耦合了类别信息和潜在结构
- 方向特定的类嵌入和调制实现了空间感知的条件生成
- 180度旋转机制改善了训练稳定性
- 混合架构（Mamba+CNN）平衡了全局一致性和局部细节

**实验设计亮点**：
- 全面的消融实验验证了各组件的贡献
- 潜在空间分析提供了对模型行为的深入理解
- 多种数据集和分辨率下的广泛评估
- 与StyleGAN2-ADA在相同配置下的公平对比
- 方向数量与数据集几何结构的关联分析

## 8. 不足与局限

**方法局限**：
- 顺序性质限制了大批量推理吞吐量
- 高分辨率下Mamba块容量逐渐减少，可能影响性能
- CNN判别器 tied 到规则2D网格，限制了架构灵活性
- 未探索Mamba判别器的可能性

**实验局限**：
- 未应用自适应判别器增强(ADA)以在低数据 regime 下进一步改进
- 扫描方向选择基于先验知识而非端到端学习
- 在某些数据集（如LSUN室外）上性能不如StyleGAN2-ADA
- 训练超参数未进行系统网格搜索

**应用限制**：
- 未探索三维数据的扩展应用
- 模型在具有强方向性纹理的数据集上表现更好，对 isotropic 数据集优势相对较小
- 计算资源需求较高，特别是高分辨率生成时

（完）
