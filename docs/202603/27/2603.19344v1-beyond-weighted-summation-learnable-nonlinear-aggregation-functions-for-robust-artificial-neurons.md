---
title: "Beyond Weighted Summation: Learnable Nonlinear Aggregation Functions for Robust Artificial Neurons"
title_zh: 超越加权求和：用于鲁棒人工神经元的可学习非线性聚合函数
authors: Berke Deniz Bozyigit
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19344v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 新型神经网络设计
tldr: 传统人工神经元使用加权求和聚合输入，虽高效但对噪声敏感。本研究提出可学习的非线性聚合函数（F-Mean和Gaussian Support神经元）及混合神经元，在保持可训练性的同时提高神经网络鲁棒性。实验表明，混合神经元在噪声数据上鲁棒性显著提升，达到0.991的分数，远高于标准基线的0.890，证明神经元级聚合是提升网络鲁棒性的有效设计维度。
selection_source: fresh_fetch
motivation: 加权求和作为传统神经元的默认聚合机制，基于均值的估计使其对噪声或极端输入敏感，限制了神经网络在噪声环境下的鲁棒性。
method: 提出两种可微分聚合机制（F-Mean神经元和Gaussian Support神经元）及通过可学习混合参数在线性和非线性聚合之间插值的混合神经元，以在保持优化稳定性的同时提高鲁棒性。
result: 混合神经元在噪声CIFAR-10上达到0.991的鲁棒性分数，显著高于标准基线的0.890，且学习参数收敛到次线性聚合和高新颖性利用率。
conclusion: 神经元级聚合是构建更具噪声容忍度神经网络的有意义且未被充分探索的设计维度，可学习非线性聚合能有效提升网络鲁棒性。
---

## 摘要
加权求和自最早的神经网络模型以来一直是人工神经元的默认输入聚合机制。虽然计算效率高，但这种设计隐含地表现为基于均值的估计器，因此对噪声或极端输入敏感。本文研究了用可学习的非线性替代方案替代固定线性聚合是否可以在不牺牲可训练性的情况下提高神经网络的鲁棒性。介绍了两种可微分的聚合机制：一种基于可学习幂加权聚合规则的F-Mean神经元，以及一种基于距离感知亲和性权重的Gaussian Support神经元。为了保持标准神经元的优化稳定性，提出了混合神经元，通过可学习的混合参数在线性和非线性聚合之间进行插值。在CIFAR-10和带有附加高斯噪声的噪声CIFAR-10变体上的多层感知机和卷积神经网络中进行了评估，混合神经元在噪声下一致地提高了鲁棒性，而F-Mean混合体在干净数据上也产生了适度的改进。三向混合体实现了高达0.991的鲁棒性分数，而标准基线为0.890，并且学习到的参数一致收敛到次线性聚合（p ≈ 0.43--0.50）和高新颖性利用率（α ≈ 0.69--0.79）。这些发现表明，神经元级别的聚合是构建更具噪声容忍能力的神经网络的一个有意义且未被充分探索的设计维度。

## Abstract
Weighted summation has remained the default input aggregation mechanism in artificial neurons since the earliest neural network models. While computationally efficient, this design implicitly behaves like a mean-based estimator and is therefore sensitive to noisy or extreme inputs. This paper investigates whether replacing fixed linear aggregation with learnable nonlinear alternatives can improve neural network robustness without sacrificing trainability. Two differentiable aggregation mechanisms are introduced: an F-Mean neuron based on a learnable power-weighted aggregation rule, and a Gaussian Support neuron based on distance-aware affinity weighting. To preserve the optimisation stability of standard neurons, hybrid neurons are proposed that interpolate between linear and nonlinear aggregation through a learnable blending parameter. Evaluated in multilayer perceptrons and convolutional neural networks on CIFAR-10 and a noisy CIFAR-10 variant with additive Gaussian corruption, hybrid neurons consistently improve robustness under noise while F-Mean hybrids also yield modest gains on clean data. The three-way hybrid achieves robustness scores of up to 0.991 compared to 0.890 for the standard baseline, and learned parameters converge consistently to sub-linear aggregation (p $\approx$ 0.43--0.50) and high novelty utilisation ($α$ $\approx$ 0.69--0.79). These findings suggest that neuron-level aggregation is a meaningful and underexplored design dimension for building more noise-tolerant neural networks.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义
- **核心问题**：传统人工神经元使用加权求和作为输入聚合机制，这种设计虽计算高效但隐含表现为基于均值的估计器，对噪声或极端输入敏感。
- **研究动机**：探索用可学习的非线性替代方案替代固定线性聚合，以提高神经网络鲁棒性，同时不牺牲可训练性。
- **背景**：加权求和作为神经元的默认聚合机制已有70多年历史几乎未改变，但这种设计并非中性选择，它继承了均值对异常值的敏感性，限制了神经网络在噪声环境下的表现。

## 2. 论文提出的方法论
- **核心思想**：提出两种可学习的非线性聚合机制，并引入混合神经元来保持优化稳定性。
- **关键技术细节**：
  - **F-Mean神经元**：基于可学习的幂加权聚合规则，使用softplus变换确保正值，通过参数p控制聚合行为(p=1时接近标准线性聚合，p→0时接近调和平均，p→∞时接近最大值)。
  - **Gaussian Support神经元**：基于距离感知的亲和性权重，计算输入之间的两两相似度，通过σ参数控制权重分布。
  - **混合神经元**：通过可学习的混合参数α在线性和非线性聚合之间插值，包括两路混合和三路混合(线性、F-Mean和Gaussian)。

## 3. 实验设计
- **数据集/场景**：
  - CIFAR-10：50,000训练和10,000测试图像
  - 噪声CIFAR-10：测试时添加标准差σ=0.15的高斯噪声
- **架构**：
  - 多层感知机(MLP)：投影层(3072→128)→隐藏层→线性分类器
  - 卷积神经网络(CNN)：标准卷积特征提取→混合神经元分类器头(8192→256)
- **对比方法**：
  - 标准线性聚合(基线)
  - F-Mean混合神经元
  - Gaussian Support混合神经元
  - 三路混合神经元(线性、F-Mean和Gaussian)
- **评估指标**：清洁准确率、噪声准确率和鲁棒性分数ρ=噪声准确率/清洁准确率

## 4. 资源与算力
- 论文中未明确提及使用的GPU型号、数量或训练时长等算力信息。
- 在训练细节部分提到了学习率设置(标准参数10^-3，新参数10^-2)、梯度裁剪(最大范数1.0)、ReduceLROnPlateau调度器和早停策略(耐心10)。

## 5. 实验数量与充分性
- **实验数量**：4种聚合设置 × 2种架构 × 2种数据条件 = 16种实验配置
- **充分性**：
  - 在MLP和CNN两种架构上验证方法有效性
  - 在清洁和噪声数据上评估性能
  - 分析学习参数收敛情况，提供可解释性见解
- **客观性与公平性**：使用相同的优化器、学习率调度和早停策略，确保实验公平性

## 6. 论文的主要结论与发现
- **主要结论**：神经元级聚合是构建更具噪声容忍能力神经网络的有意义且未被充分探索的设计维度。
- **关键发现**：
  - 混合神经元在噪声数据上鲁棒性显著提升，三路混合达到0.991的鲁棒性分数，远高于标准基线的0.890
  - F-Mean混合在清洁数据上也表现出适度改进
  - 学习参数收敛到次线性聚合(p≈0.43-0.50)和高新颖性利用率(α≈0.69-0.79)
  - 三路混合在噪声环境下表现最佳，表明互补的聚合机制能更好地吸收噪声或不一致的证据

## 7. 优点
- **方法亮点**：
  - 提出两种新颖的可学习非线性聚合机制，保留与梯度训练的兼容性
  - 引入混合神经元概念，通过可学习参数在线性和非线性聚合之间插值，保持优化稳定性
  - F-Mean神经元计算开销小，易于集成到现有架构
- **实验设计亮点**：
  - 在MLP和CNN两种架构上验证方法有效性
  - 使用噪声数据集评估鲁棒性，而非仅关注清洁数据性能
  - 分析学习参数，提供对网络行为的可解释性见解

## 8. 不足与局限
- **实验覆盖局限**：仅在CIFAR-10和一种噪声变体上进行了验证，需要更广泛的基准测试
- **计算开销**：Gaussian Support神经元具有O(n^2)复杂度，可能限制其在大型网络中的应用
- **架构限制**：研究主要集中在MLP和CNN的分类器头部，其他架构设置可能暴露不同的权衡
- **未来方向**：需要扩展到更大的基准(如ImageNet)、医学成像任务和NLP问题，并与其它鲁棒聚合机制(如修剪均值和M估计器)进行比较

（完）
