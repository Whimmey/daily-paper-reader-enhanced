---
title: "RPiAE: A Representation-Pivoted Autoencoder Enhancing Both Image Generation and Editing"
title_zh: RPiAE：一种增强图像生成与编辑的表示中心自编码器
authors: "Yue Gong, Hongyu Li, Shanyuan Liu, Bo Cheng, Yuhang Ma, Liebucha Wu, Xiaoyu Wu, Manyuan Zhang, Dawei Leng, Yuhui Yin, Lijun Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19206v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 图像生成和编辑的自编码器
tldr: 扩散模型已成为图像生成和编辑的主流方法，但现有基于预训练视觉表示模型的tokenizer存在重建保真度低和潜在空间维度过高的问题。本文提出RPiAE，通过表示枢轴正则化训练策略和变分桥接技术，实现了语义保持、高保真重建和简化扩散建模，在文本到图像生成和图像编辑任务上表现优异。
selection_source: fresh_fetch
motivation: 现有基于预训练视觉表示模型的tokenizer因冻结编码器和过高维潜在空间导致重建保真度低，进而影响编辑质量。
method: RPiAE采用表示枢轴正则化训练策略和变分桥接技术，优化编码器以重建同时保留语义结构，并将潜在空间压缩为更适合扩散建模的紧凑形式。
result: 实验表明RPiAE在文本到图像生成和图像编辑任务上优于其他视觉tokenizer，并在基于表示的tokenizer中实现了最佳重建保真度。
conclusion: RPiAE通过创新的训练策略和架构设计，有效平衡了语义保持、重建保真度和扩散建模效率，提升了图像生成和编辑质量。
---

## 摘要
扩散模型已成为图像生成和编辑的主导范式，其中潜在扩散模型将去噪过程转移到紧凑的潜在空间以提高效率和可扩展性。最近尝试利用预训练的视觉表示模型作为标记器先验的方法，要么将扩散特征与表示特征对齐，要么直接重用表示编码器作为冻结的标记器。尽管这些方法可以提高生成指标，但由于冻结的编码器，它们通常面临重建保真度有限的问题，进而降低了编辑质量，同时过于高维的潜在变量也使得扩散建模变得困难。为解决这些局限性，我们提出了表示中心自编码器（Representation-Pivoted AutoEncoder），这是一种基于表示的标记器，能够同时提升生成和编辑能力。我们引入了表示中心正则化（Representation-Pivot Regularization）训练策略，使基于表示初始化的编码器能够在重建任务上进行微调，同时保留预训练表示空间的语义结构，随后通过一个变分桥将潜在空间压缩为更紧凑的形式，以实现更好的扩散建模。我们采用目标解耦的分阶段训练策略，依次优化生成可行性和重建保真度目标。这些组件共同产生了一种标记器，它能够保持强大的语义表达能力，实现精确的重建，并生成具有降低扩散建模复杂度的潜在变量。实验证明，RPiAE在文本到图像生成和图像编辑任务上优于其他视觉标记器，同时在基于表示的标记器中提供了最佳的重建保真度。

## Abstract
Diffusion models have become the dominant paradigm for image generation and editing, with latent diffusion models shifting denoising to a compact latent space for efficiency and scalability. Recent attempts to leverage pretrained visual representation models as tokenizer priors either align diffusion features to representation features or directly reuse representation encoders as frozen tokenizers. Although such approaches can improve generation metrics, they often suffer from limited reconstruction fidelity due to frozen encoders, which in turn degrades editing quality, as well as overly high-dimensional latents that make diffusion modeling difficult. To address these limitations, We propose Representation-Pivoted AutoEncoder, a representation-based tokenizer that improves both generation and editing. We introduce Representation-Pivot Regularization, a training strategy that enables a representation-initialized encoder to be fine-tuned for reconstruction while preserving the semantic structure of the pretrained representation space, followed by a variational bridge which compress latent space into a compact one for better diffusion modeling. We adopt an objective-decoupled stage-wise training strategy that sequentially optimizes generative tractability and reconstruction-fidelity objectives. Together, these components yield a tokenizer that preserves strong semantics, reconstructs faithfully, and produces latents with reduced diffusion modeling complexity. Experiments demonstrate that RPiAE outperforms other visual tokenizers on text-to-image generation and image editing, while delivering the best reconstruction fidelity among representation-based tokenizers.

---

## 论文详细总结（自动生成）

# RPiAE论文总结

## 1. 论文的核心问题与整体含义

### 研究动机和背景
- **核心问题**：现有基于预训练视觉表示模型的tokenizer存在两个主要局限：(1)由于冻结编码器导致的重建保真度有限，进而影响编辑质量；(2)过于高维的潜在变量使得扩散建模变得困难。
- **研究背景**：扩散模型已成为图像生成和编辑的主导范式，潜在扩散模型(LDM)通过将去噪过程转移到紧凑的潜在空间提高效率和可扩展性。现有方法主要分为两类：外部对齐方法(Ext-Aligned RM)将扩散特征与表示特征对齐，内部重用方法(Int-Reused RM AE)直接重用表示编码器作为冻结标记器。这些方法虽能提高生成指标，但存在重建保真度低和潜在空间维度过高的问题。

## 2. 论文提出的方法论

### 核心思想
- 提出表示中心自编码器(RPiAE)，一种基于表示的标记器，通过表示枢轴正则化训练策略和变分桥接技术，平衡语义保持、重建保真度和扩散建模效率。

### 关键技术细节
- **表示中心正则化(Representation-Pivot Regularization)**：
  - 使用冻结的枢轴副本编码器(PRE)作为语义参考
  - 通过枢轴正则化损失Lpiv = λpiv ||f - fp||²²监督可训练编码器的更新
  - 采用自适应权重λpiv平衡重建损失和枢轴正则化损失

- **变分桥(Variational Bridge)**：
  - 由编码器Be和解码器Bd组成
  - 将高维表示特征f压缩为紧凑潜在空间z
  - 引入KL正则化使潜在分布接近标准正态分布

- **目标解耦的分阶段训练策略**：
  1. **阶段I**：枢轴正则化编码器微调，训练Eθ和Dϕ
  2. **阶段II**：变分桥训练，冻结Eθ和Dϕ，优化Be和Bd
  3. **阶段III**：解码器专业化，固定Eθ和变分桥，微调Dϕ

## 3. 实验设计

### 数据集/场景
- **ImageNet-1K**：用于图像重建和类条件图像生成(256×256分辨率)
- **CC12M-LLaVA-Next**：用于文本到图像生成
- **OmniEdit**：用于图像编辑

### Benchmark
- **图像重建**：rFID、PSNR、LPIPS、SSIM
- **类条件图像生成**：gFID、IS、Precision、Recall
- **文本到图像生成**：GenEval、DPG-Bench
- **图像编辑**：GEdit-Bench-EN，使用EditScore-Qwen3VL-8B计算VieScore(G_SC、G_PQ、G_O)

### 对比方法
- **生成模型**：MaskGIT、LlamaGen、MaskDiT-XL、DiT-XL、SiT-XL
- **外部对齐的表示模型**：REPA-XL、LightningDiT
- **内部重用的表示模型**：SVG-XL、RAE-S、RAE-B、FAE-d32、VA-VAE

## 4. 资源与算力

- 论文中未明确提及GPU型号、数量和训练时长等具体算力信息
- 提供了部分训练配置，如批量大小(512-1024)、学习率(2×10⁻⁴)等，但缺乏硬件资源详细信息

## 5. 实验数量与充分性

### 实验数量
- 主要实验包括图像重建、类条件图像生成、文本到图像生成和图像编辑四大类任务
- 进行了多组消融研究，包括：
  - 目标解耦训练策略验证
  - 编码器和解码器结构影响
  - 潜在空间维度影响(d=32/64/128)
  - 枢轴正则化损失权重影响(wpiv=1/0.5/0.25)

### 充分性与公平性
- 实验覆盖多个任务和数据集，评估较为全面
- 使用标准评估指标，确保客观性
- 消融研究逐步验证了各组件的有效性
- 对比方法包括多种类型基线，确保公平性

## 6. 论文的主要结论与发现

### 主要结论
- RPiAE在文本到图像生成和图像编辑任务上优于其他视觉标记器
- 在基于表示的标记器中实现了最佳的重建保真度(rFID=0.50)
- 通过创新的训练策略和架构设计，有效平衡了语义保持、重建保真度和扩散建模效率

### 主要发现
- 表示中心正则化能够使编码器在重建任务上进行微调的同时保留预训练表示空间的语义结构
- 变分桥有效压缩了高维表示特征，使潜在空间更适合扩散建模
- 目标解耦的分阶段训练策略能够依次优化不同目标，避免相互干扰

## 7. 优点：方法或实验设计上有哪些亮点

### 方法亮点
- **表示中心正则化策略**：创新性地使用冻结的枢轴副本编码器作为语义参考，监督可训练编码器的更新
- **变分桥设计**：将高维表示特征压缩为紧凑的潜在空间，同时保留语义信息
- **目标解耦的分阶段训练策略**：依次优化不同目标，避免相互干扰

### 实验设计亮点
- 全面的评估指标：包括生成和编辑任务的多维度评估
- 深入的消融研究：验证了各个组件的有效性和必要性
- 与多种类型基线方法的对比：确保评估的全面性和公平性

## 8. 不足与局限

### 实验覆盖不足
- 主要实验集中在256×256分辨率的图像，未测试更高分辨率的表现
- 未探索不同类型的预训练表示模型对性能的影响

### 偏差风险
- 实验主要基于ImageNet等标准数据集，可能存在数据集偏差
- 评估指标可能不完全反映实际应用场景的需求

### 应用限制
- 模型参数量较大(Eθ:86.58M, Dϕ:415.35M, Be+Bd:47.41M)，部署资源要求高
- 训练过程复杂，需要多个阶段的训练和调参
- 未探索在计算资源受限环境下的性能表现

（完）
