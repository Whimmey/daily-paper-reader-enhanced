---
title: "UGID: Unified Graph Isomorphism for Debiasing Large Language Models"
title_zh: UGID：用于去偏大型语言模型的统一图同构
authors: "Zikang Ding, Junchi Yao, Junhao Li, Yi Zhang, Wenbo Jiang, Hongbo Liu, Lijie Hu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19144v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: Transformer去偏框架
tldr: 大型语言模型存在显著社会偏见，现有方法无法完全解决。本文提出UGID框架，将Transformer建模为结构化计算图，通过图同构约束实现内部表示级别的去偏。该方法联合约束注意力路由和隐藏表示，防止偏见迁移，同时引入对数空间约束和选择性锚点目标保持模型能力。实验证明UGID能有效减少偏见，保持模型安全性和实用性。
selection_source: fresh_fetch
motivation: 现有输出级或数据优化去偏方法无法完全解决大型语言模型中的社会偏见，因为偏见已嵌入内部表示。
method: UGID将Transformer建模为结构化计算图，通过图同构约束实现内部表示级别的去偏，联合约束注意力路由和隐藏表示。
result: 实验表明UGID能有效减少分布内和分布外设置下的偏见，显著降低内部结构差异，同时保持模型安全性和实用性。
conclusion: UGID是一种有效的内部表示级别去偏框架，能够减少大型语言模型中的社会偏见而不损害其一般能力。
---

## 摘要
大型语言模型(LLMs)表现出明显的社会偏见。基于输出级或数据优化的去偏方法无法完全解决这些偏见，许多先前的研究已经表明偏见嵌入在内部表示中。我们提出了用于去偏大型语言模型的统一图同构(UGID)，这是一个大型语言模型的内部表示级去偏框架，该框架将Transformer建模为结构化计算图，其中注意力机制定义了图的路由边，隐藏状态定义了图的节点。具体而言，去偏被表述为在反事实输入之间强制图结构不变，仅允许在敏感属性上存在差异。UGID联合约束了偏见敏感区域中的注意力路由和隐藏表示，有效防止了偏见在架构组件间的迁移。为了在不降低通用能力的情况下实现有效的行为对齐，我们在敏感logits上引入了对数空间约束，并采用选择性锚定目标来保留定义语义。在大型语言模型上的大量实验表明，UGID在分布内和分布外设置下都能有效减少偏见，显著降低内部结构差异，并保持模型的安全性和实用性。

## Abstract
Large language models (LLMs) exhibit pronounced social biases. Output-level or data-optimization--based debiasing methods cannot fully resolve these biases, and many prior works have shown that biases are embedded in internal representations. We propose \underline{U}nified \underline{G}raph \underline{I}somorphism for \underline{D}ebiasing large language models (\textit{\textbf{UGID}}), an internal-representation--level debiasing framework for large language models that models the Transformer as a structured computational graph, where attention mechanisms define the routing edges of the graph and hidden states define the graph nodes. Specifically, debiasing is formulated as enforcing invariance of the graph structure across counterfactual inputs, with differences allowed only on sensitive attributes. \textit{\textbf{UGID}} jointly constrains attention routing and hidden representations in bias-sensitive regions, effectively preventing bias migration across architectural components. To achieve effective behavioral alignment without degrading general capabilities, we introduce a log-space constraint on sensitive logits and a selective anchor-based objective to preserve definitional semantics. Extensive experiments on large language models demonstrate that \textit{\textbf{UGID}} effectively reduces bias under both in-distribution and out-of-distribution settings, significantly reduces internal structural discrepancies, and preserves model safety and utility.

---

## 论文详细总结（自动生成）

# UGID：用于去偏大型语言模型的统一图同构 - 论文总结

## 1. 论文的核心问题与整体含义

大型语言模型(LLMs)表现出明显的社会偏见，这些偏见不仅存在于输出层面，还嵌入在模型的内部表示中。现有去偏方法主要分为两类：模型外部方法（调整数据、推理或输出行为但不约束内部偏见表示）和模型内部方法（关注定位局部组件如注意力头或FFN向量）。研究发现，仅约束注意力机制在小规模模型（如GPT-2）中可有效减少偏见，但在扩展到十亿参数级别模型时效果显著减弱，这是因为偏见不仅存在于注意力机制中，还编码并放大在前馈网络(FFNs)中。因此，需要一种能够同时处理注意力路由和隐藏表示的去偏框架。

## 2. 论文提出的方法论

### 核心思想
将Transformer建模为结构化计算图，通过图同构约束实现内部表示级别的去偏。具体而言，将Transformer层建模为动态计算图G(x) = (V, E, H^l, A^l)，其中节点V代表token，节点特征H^l代表隐藏状态，加权边A^l代表注意力机制。目标是在反事实输入(x, x')之间强制图结构不变，仅允许在敏感属性上存在差异。

### 关键技术细节
1. **设计I：对齐路由（边）**
   - 使用组合拉普拉斯矩阵L = D - A捕获注意力图的拓扑结构
   - 引入注意力汇点掩码(M_sink)过滤掉第一个token
   - 引入自适应显著性权重(ω)聚焦于偏见枢纽
   - 边损失函数约束谱特征在反事实输入间保持一致

2. **设计II：防止偏见迁移（节点）**
   - 约束隐藏表示H^l在反事实上下文中保持一致
   - 防止偏见被存储在值向量中
   - 节点损失函数确保隐藏状态在反事实上下文中一致

3. **设计III：保留模型效用**
   - 对数空间引导：惩罚敏感属性上的概率发散
   - Top-K稳定性：将学生模型的注意力模式锚定到冻结的教师模型
   - 语义保留：应用对称的掩码KL散度保留一般知识

4. **设计IV：处理定义偏见**
   - 选择性对齐策略：构建目标对（刻板印象）和锚点对（定义）的混合数据集
   - 动态适应的损失函数，确保模型保留定义性语义（如"King"vs"Queen"）

## 3. 实验设计

### 数据集/场景
- 构建Few-Shot Intervention Dataset：包含10个性别反事实职业对用于去偏，4个定义锚点对用于安全
- 评估数据集：BBQ、CrowS-Pairs（刻板印象），BOLD、RealToxicityPrompts、HolisticBias（生成偏见分析）

### Benchmark
- 准确率差距（BBQ）
- 刻板印象分数（CrowS-Pairs）
- 情感/毒性方差
- 效用监控：Wikitext-2困惑度和BBQ非模糊准确率
- 结构同构性：∆Spec（谱发散）和∆Hidden（L2漂移）

### 对比方法
- CDA（Counterfactual Data Augmentation）
- KLAAD（Knowledge-Driven Latent Attention Alignment for Debiasing）
- Self-Debias（推理时去偏方法）

## 4. 资源与算力

论文中未明确提到训练过程中使用的GPU型号、数量或训练时长等具体算力信息。在附录的效率基准部分提到在单个NVIDIA A100 (80GB)上进行评估，但未详细说明训练所需的计算资源。

## 5. 实验数量与充分性

### 实验数量
- 在多个模型家族和规模上进行了实验：LLaMA-3 (8B, 8B-Instruct)、Qwen-2 (3B, 7B, 14B)、Gemma-2 (2B)
- 进行了消融研究，评估了每个设计组件的贡献
- 进行了鲁棒性诊断，包括模板方差和方向差距
- 进行了下游效用和操作效率评估

### 充分性与客观性
- 实验覆盖了多种模型规模和架构，证明了方法的通用性
- 进行了详细的消融研究，验证了各组件的有效性
- 使用了多种评估指标，包括行为指标和结构指标
- 实验设计较为客观，与多种基线方法进行了公平比较
- 通过激活修补等因果分析方法提供了更深入的机制验证

## 6. 论文的主要结论与发现

- UGID实现了去偏有效性和模型效用之间的优越平衡，显著优于最先进的基线方法
- UGID在分布内和分布外设置下都能有效减少偏见，显著降低内部结构差异
- UGID在跨不同社会类别的偏见缓解方面表现出强大的泛化能力
- UGID的优越性是规模不变的，在多个模型家族和规模上都实现了最优的帕累托前沿
- 研究发现偏见具有规模依赖性：在小规模模型中仅约束注意力可能有效，但在大规模模型中偏见会迁移到FFN组件
- UGID成功中断了偏见信号的层间放大，防止它们泄漏到最终输出

## 7. 优点

### 方法亮点
- 首次提出通过统一图同构实现去偏的框架
- 将去偏问题重新表述为计算图同构挑战
- 联合约束注意力路由和隐藏表示，防止偏见迁移
- 引入选择性锚点策略处理定义偏见，避免概念擦除
- 使用拉普拉斯谱约束精确对齐路由拓扑

### 实验设计亮点
- 在多种模型规模和架构上验证方法的通用性
- 使用结构同构性指标（∆Spec和∆Hidden）评估内部变化
- 进行详细的因果证据分析，通过激活修补验证偏见路径的精确中和
- 评估了模型对提示扰动的鲁棒性
- 测量了计算开销和资源效率，显示推理时无额外开销

## 8. 不足与局限

- 仅关注性别偏见，可能无法完全覆盖其他类型的偏见
- 锚点对集可能不够全面，可能无法捕捉所有性别特定语义的复杂性
- 方法可能对某些特定文化背景下的偏见处理不够充分
- 虽然在多种模型上进行了测试，但可能未覆盖所有可能的模型架构
- 计算图同构约束增加了训练复杂性
- 方法依赖于反事实对的构建，可能需要领域知识来识别适当的对
- 虽然在推理时没有额外开销，但训练阶段的计算成本可能较高

（完）
