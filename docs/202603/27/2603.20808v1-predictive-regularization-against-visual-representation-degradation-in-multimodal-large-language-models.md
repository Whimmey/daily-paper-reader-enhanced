---
title: Predictive Regularization Against Visual Representation Degradation in Multimodal Large Language Models
title_zh: 多模态大语言模型中针对视觉表征退化的预测正则化
authors: "Enguang Wang, Qiang Wang, Yuanchen Wu, Ke Yan, Xinbin Yuan, Shouhong Ding, Xialei Liu, Ming-Ming Cheng"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20808v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 多模态视觉表示
tldr: 多模态大语言模型在视觉-语言任务上表现出色，但其语言驱动训练会导致内部视觉表示退化。本文发现LLM中间层的视觉特征在全局功能和结构上均出现退化，这是由于模型为优化文本生成而牺牲视觉保真度。作者提出预测正则化方法，强制退化特征预测初始视觉特征，实验证明该方法能有效提升视觉-语言性能。
selection_source: fresh_fetch
motivation: 多模态大语言模型在语言驱动训练过程中存在视觉表示退化问题，影响其视觉理解能力。
method: 提出预测正则化(PRe)方法，强制退化的中间视觉特征预测初始视觉特征以保持视觉保真度。
result: 实验证明减轻视觉退化能有效提升多模态大语言模型的视觉-语言性能。
conclusion: 强大的多模态大语言模型需要同时具备跨模态推理能力和核心视觉能力，内部视觉表示的健壮性对全面的多模态理解至关重要。
---

## 摘要
尽管多模态大语言模型(MLLMs)在视觉语言任务上表现出色，但其语言驱动训练对内部视觉基础能力造成的代价尚不明确。本文中，我们进行了详细的诊断分析，揭示了一个普遍存在的问题：MLLMs中的视觉表征退化。具体而言，我们发现与初始视觉特征相比，LLM中间层的视觉表征在全局功能和补丁结构方面都出现了退化。我们将这一现象归因于由单一文本生成目标驱动的视觉牺牲，即模型为了优化答案生成而牺牲了视觉保真度。我们认为，一个强大的MLLM需要强大的跨模态推理能力和核心视觉能力，并提出预测正则化(PRe)方法，迫使退化的中间特征预测初始视觉特征，从而保持MLLM内部表征的固有视觉属性。大量实验证实，减轻这种视觉退化能有效提升视觉语言性能，强调了在MLLMs中培养强大的内部视觉表征对于全面多模态理解的关键重要性。

## Abstract
While Multimodal Large Language Models (MLLMs) excel at vision-language tasks, the cost of their language-driven training on internal visual foundational competence remains unclear. In this paper, we conduct a detailed diagnostic analysis to unveil a pervasive issue: visual representation degradation in MLLMs. Specifically, we find that compared to the initial visual features, the visual representation in the middle layers of LLM exhibits both a degradation in global function and patch structure. We attribute this phenomenon to a visual sacrifice driven by the singular text-generation objective, where the model compromises its visual fidelity to optimize for answer generation. We argue that a robust MLLM requires both strong cross-modal reasoning and core visual competence, and propose Predictive Regularization (PRe) to force degraded intermediate features to predict initial visual features, thereby maintaining the inherent visual attributes of the MLLM's internal representations. Extensive experiments confirm that mitigating this visual degradation effectively boosts vision-language performance, underscoring the critical importance of fostering robust internal visual representations within MLLMs for comprehensive multimodal understanding.

---

## 论文详细总结（自动生成）

# 多模态大语言模型中针对视觉表征退化的预测正则化论文总结

## 1. 论文的核心问题与整体含义

多模态大语言模型(MLLMs)在视觉语言任务上表现出色，但其语言驱动训练对内部视觉基础能力造成的代价尚不明确。本文发现MLLMs中存在一个普遍问题：视觉表征退化。具体表现为与初始视觉特征相比，LLM中间层的视觉表征在全局功能和补丁结构方面都出现了退化。这种现象是由于模型为了优化文本生成而牺牲了视觉保真度导致的。研究动机是探索这种视觉退化对模型性能的影响，并提出解决方案，因为一个强大的MLLM需要同时具备跨模态推理能力和核心视觉能力。

## 2. 论文提出的方法论

核心思想：
- 提出预测正则化(PRe)方法，强制退化的中间视觉特征预测初始视觉特征以保持视觉保真度
- 基于预测编码理论，认为高效的神经系统能够不断预测自身的低级信号以保持连贯的世界模型

关键技术细节：
- PRe方法使用一个轻量级的预测头(2层MLP)来强制中间层的退化视觉特征预测初始的锚定视觉特征
- 公式：L_PRe = -1/Np * Σ(cosine_similarity(f_pred(h_l^v,i), stopgrad(h_0^v,i)))
- 最终训练目标：L_total = L_LM + λ * L_PRe，其中λ是平衡超参数(默认设为0.5)
- 选择中间层作为目标层，因为MLLMs主要在浅层和中间层处理视觉信息，且全局语义退化在这些层最为明显

## 3. 实验设计

数据集/场景：
- 训练数据：LLaVA-558K和LLaVA-665K，分别用于预训练阶段和指令微调阶段
- 评估数据集：GQA、MMMU、RealWorldQA、TextVQA、AI2D、MMStar、ScienceQA、OCRbench、MMVP等

Benchmark：
- 线性探测实验：在CUB-200和ImageNet-100上评估视觉表征的功能有效性
- 补丁级语义分析：使用COCO-stuff数据集的分割掩码量化补丁表示的可分离性
- 统计特性分析：测量全局表示的几何复杂性和统计独立性

对比方法：
- 基线模型：使用CLIP或SigLIP作为视觉编码器，Vicuna或Qwen作为LLM
- 不同架构配置：冻结视觉编码器vs可训练视觉编码器
- 不同锚定特征选择：Pre-LLM、Pre-Proj、DINOv2等
- 不同正则化层选择：中间层vs最后层
- 不同正则化强度：λ=0.5, 1.0, 2.0

## 4. 资源与算力

论文中没有明确提到具体的GPU型号、数量和训练时长等算力信息。在补充材料中提到了一些训练配置：
- 使用DeepSpeed-ZeRO-3进行内存优化
- 第一阶段学习率为1e-3，总批大小为256
- 第二阶段学习率为2e-5(冻结视觉编码器)或2e-6(视觉编码器)和1e-5(其他组件)，总批大小为128
- 使用AdamW优化器，权重衰减和预热比例分别为0和0.03
- 每个阶段训练一个周期

## 5. 实验数量与充分性

实验数量：
- 在多个视觉编码器(CLIP、SigLIP)和LLM(Vicuna、Qwen)架构上验证
- 在9个主要基准测试上评估
- 进行了消融实验，包括不同正则化层选择、不同锚定特征选择、不同正则化强度等
- 在补充材料中还在3B LLM上进行了实验，并在更多数据集上验证

充分性与客观性：
- 实验设计较为全面，涵盖了不同架构、不同配置和不同任务类型
- 使用了多种评估指标，包括线性探测准确率、语义对比比等
- 进行了详细的消融实验，验证了不同设计选择的影响
- 提供了可视化结果，直观展示了方法的效果
- 实验对比公平，与基线模型在相同条件下进行比较

## 6. 论文的主要结论与发现

主要结论：
- MLLMs中确实存在视觉表征退化现象，表现为全局功能下降和补丁结构退化
- 这种退化是模型为优化文本生成而做出的视觉牺牲
- 提出的PRe方法能有效缓解视觉表征退化，提升视觉-语言性能
- 强大的MLLM需要同时具备跨模态推理能力和核心视觉能力

主要发现：
- 中间层的视觉表征退化最为明显，这与MLLMs处理视觉信息的主要区域一致
- 补丁级语义边界模糊是全局功能退化的微观原因
- 统计特性分析表明，中间层表征具有更高的几何复杂性和统计独立性，这是为语言任务优化的结果
- 视觉能力和语言能力在训练过程中呈现负相关，证实了视觉退化是语言驱动优化的副产品

## 7. 优点

方法亮点：
- 首次系统诊断了MLLMs中的视觉表征退化问题，提供了多层次的解释
- 提出的PRe方法简单有效，只需添加一个轻量级的预测头
- 基于预测编码理论，为解决视觉退化提供了理论依据
- 方法通用性强，适用于多种MLLM架构

实验设计亮点：
- 从宏观到微观全面分析了视觉表征退化现象
- 使用多种评估指标和方法验证了退化的存在和PRe的有效性
- 进行了详细的消融实验，验证了不同设计选择的影响
- 提供了可视化结果，直观展示了方法的效果
- 在多种架构和数据集上验证了方法的泛化能力

## 8. 不足与局限

不足与局限：
- 实验主要集中在视觉问答任务上，对其他视觉任务(如目标检测、分割等)的验证不足
- 使用内部表示作为锚点可能继承原始视觉编码器的偏差
- 没有探索对比学习等其他视觉表征学习方法与MLLMs的结合
- 方法在计算资源有限的环境中的适用性未充分探讨
- 对视觉退化的长期影响和不同训练阶段的动态变化研究不够深入
- 没有探讨PRe方法对模型推理速度和内存占用的影响

（完）
