---
title: "ConsRoute:Consistency-Aware Adaptive Query Routing for Cloud-Edge-Device Large Language Models"
title_zh: ConsRoute：面向云-边缘-设备大语言模型的一致性感知自适应查询路由
authors: "Haoyu Qiao, Hao Zhang, Shanwen Mao, Siyao Cheng, Jie Liu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21237v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: LLM的自适应查询路由
tldr: "ConsRoute是一个针对云-边缘-设备协同大语言模型推理的轻量级语义感知自适应路由框架。它通过重用LLM预填充阶段的隐藏状态作为查询表示，利用重排序器评估不同层级模型响应间的语义一致性，实现细粒度路由决策。贝叶斯优化动态平衡质量、延迟和成本，实验表明该方法能实现接近云的性能(≥95%)，同时降低近40%的端到端延迟和推理成本。"
selection_source: fresh_fetch
motivation: 大型语言模型的高推理延迟和成本限制了其在延迟敏感和资源受限场景的应用。
method: ConsRoute通过重用LLM隐藏状态作为查询表示，利用重排序器评估语义一致性，并采用贝叶斯优化动态调整路由阈值。
result: "ConsRoute实现接近云的性能(≥95%)，同时降低近40%的端到端延迟和推理成本，在响应质量和系统效率方面优于现有方法。"
conclusion: ConsRoute通过一致性感知的自适应路由有效平衡了大语言模型推理的质量、延迟和成本。
---

## 摘要
大语言模型(LLMs)提供了令人印象深刻的能力，但带来了显著的推理延迟和成本，这阻碍了它们在延迟敏感和资源受限场景中的部署。云-边缘-设备协同推理作为一种有前景的范式出现，通过跨层级动态路由查询到不同能力的模型。在本文中，我们提出了ConsRoute，一个轻量级、语义感知且自适应的路由框架，在最小化对响应质量影响的同时显著提高了推理效率。与依赖预测粗粒度输出质量差距的先前路由方法不同，ConsRoute利用重排序器直接评估不同层级模型生成响应之间的语义一致性，为路由提供细粒度的软监督信号。为了最小化设备端开销，ConsRoute重用LLM预填充阶段的隐藏状态作为紧凑的查询表示，避免了额外的编码器或推理过程。此外，这些表示被聚类，并采用贝叶斯优化来学习特定于聚类的路由阈值，在异构查询分布下动态平衡质量、延迟和成本。大量实验表明，ConsRoute实现了接近云端性能(≥95%)，同时将端到端延迟和推理成本降低了近40%，在响应质量和系统效率方面均持续优于现有路由基线。

## Abstract
Large language models (LLMs) deliver impressive capabilities but incur substantial inference latency and cost, which hinders their deployment in latency-sensitive and resource-constrained scenarios. Cloud-edge-device collaborative inference has emerged as a promising paradigm by dynamically routing queries to models of different capacities across tiers. In this paper, we propose ConsRoute, a lightweight, semantic-aware, and adaptive routing framework that significantly improves inference efficiency while minimizing impact on response quality. Unlike prior routing methods that rely on predicting coarse-grained output quality gaps, ConsRoute leverages a reranker to directly assess the semantic consistency between responses generated by models at different tiers, yielding fine-grained soft supervision signals for routing. To minimize device-side overhead, ConsRoute reuses hidden states from the LLM prefilling stage as compact query representations, avoiding additional encoders or inference passes. Furthermore, these representations are clustered, and Bayesian optimization is employed to learn cluster-specific routing thresholds that dynamically balance quality, latency, and cost under heterogeneous query distributions. Extensive experiments demonstrate that ConsRoute achieves near-cloud performance (>=95%) while reducing end-to-end latency and inference cost by nearly 40%, consistently outperforming existing routing baselines in both response quality and system efficiency.

---

## 论文详细总结（自动生成）

# ConsRoute 论文总结

## 1. 论文的核心问题与整体含义
- **研究动机**：大语言模型(LLMs)虽然能力强大，但推理延迟和成本很高，限制了在延迟敏感和资源受限场景（如移动设备和物联网系统）中的应用。
- **背景**：云-边缘-设备协同推理是一种有前景的解决方案，通过在不同层级部署不同规模的模型并动态路由查询来平衡性能和效率。
- **现有方法局限**：
  - 依赖粗粒度的质量差距预测，无法捕获响应间的细粒度语义一致性
  - 需要额外的编码器或外部API，增加设备端开销
  - 使用静态全局阈值，无法适应异构查询分布

## 2. 论文提出的方法论
- **核心思想**：ConsRoute是一个轻量级、语义感知且自适应的路由框架，通过评估不同层级模型响应间的语义一致性来做出路由决策。
- **关键技术细节**：
  - **语义一致性表示提取**：重用设备端LLM预填充阶段的隐藏状态作为查询表示，通过添加特定提示词引导模型关注一致性相关信息。
  - **一致性监督信号构建**：使用重排序器(reranker)直接评估不同层级模型响应间的语义一致性，结合数据增强（参考答案或高级LLM判断）构建软标签。
  - **轻量级一致性预测器**：训练一个小型MLP将查询表示映射到一致性预测分数。
  - **基于聚类的自适应阈值**：将查询聚类，使用贝叶斯优化为每个聚类学习特定的路由阈值，动态平衡质量、延迟和成本。
- **算法流程**：
  1. 离线阶段：构建语义一致性标签，训练一致性预测器，初始化聚类和阈值
  2. 在线阶段：对每个查询，提取表示，预测一致性分数，分配到最近聚类，应用对应阈值做出路由决策

## 3. 实验设计
- **数据集/场景**：
  - RouterBench数据集：包含36.5K来自8个NLP数据集的中英文查询
  - 具体任务：MMLU（通用知识）、GSM8K（数学推理）、HumanEval（代码生成）和MT-Bench（对话）
  - 部署环境：设备端（Qwen3-1.7B）、边缘端（Qwen3-14B）、云端（Qwen3-32B）
  - 网络条件：模拟"好"和"坏"两种网络环境
- **Benchmark**：准确率、端到端延迟、推理成本作为主要评估指标
- **对比方法**：
  - LLM-only：所有查询路由到大型语言模型
  - DLM-only：所有查询由小型语言模型处理
  - Edge-only：所有查询由边缘模型处理
  - RouteLLM（BERT）：使用微调的BERT编码器获取查询表示
  - RouteLLM（SW ranking）：使用文本嵌入模型检索历史查询
  - MixLLM：使用标签增强的BERT模型编码查询

## 4. 资源与算力
- **硬件配置**：
  - 设备端：Intel Core i5-12500H CPU + NVIDIA RTX 3050 GPU，运行Qwen3-1.7B
  - 边缘端：单NVIDIA RTX A6000 GPU，运行Qwen3-14B
  - 云端：两个RTX A6000 GPU，运行Qwen3-32B
- **训练时长**：论文中未明确说明训练时长

## 5. 实验数量与充分性
- **实验数量**：
  - 在多个数据集（MMLU、GSM8K、HumanEval、MT-Bench）上评估
  - 进行了消融研究，包括不同提示策略、不同监督源、标签增强、动态阈值等
  - 测试了网络条件敏感性、在线适应能力、效用超参数敏感性等
- **充分性与公平性**：
  - 实验设计较为全面，覆盖了不同任务类型、网络条件和系统配置
  - 与多种基线方法进行了公平比较
  - 提供了详细的消融分析，验证了各组件的贡献
  - 实验结果客观，通过图表清晰展示

## 6. 论文的主要结论与发现
- **主要结论**：
  - ConsRoute实现了接近云端性能（≥95%），同时将端到端延迟和推理成本降低了近40%
  - 在响应质量和系统效率方面均持续优于现有路由基线
  - 方法能够适应异构模型部署和变化的网络条件
- **主要发现**：
  - 语义一致性作为监督信号比传统的质量差距更有效
  - 重用LLM预填充隐藏状态作为查询表示显著降低了设备端开销
  - 基于聚类的自适应阈值能够更好地适应异构查询分布
  - 在线适应机制能够有效跟踪查询分布变化和网络条件变化

## 7. 优点
- **方法亮点**：
  - 创新性地使用重排序器评估语义一致性，提供细粒度监督信号
  - 重用LLM预填充隐藏状态，避免额外编码器，降低设备端开销
  - 基于聚类的自适应阈值设计，能够适应异构查询分布
  - 在线适应机制，能够动态调整路由策略以适应环境变化
- **实验设计亮点**：
  - 在多种真实场景和数据集上验证方法有效性
  - 与多种先进基线方法进行公平比较
  - 提供全面的消融分析，验证各组件贡献
  - 测试了方法在不同网络条件和模型配置下的鲁棒性

## 8. 不足与局限
- **实验覆盖局限**：
  - 实验主要在模拟环境中进行，缺乏真实大规模部署验证
  - 未充分探索方法在极端资源受限设备（如移动手机）上的表现
- **潜在偏差风险**：
  - 依赖预训练的重排序器和LLM判断，可能引入模型特定偏差
  - 计算成本估计可能简化了实际部署中的复杂因素
- **应用限制**：
  - 在线适应机制需要一定量的新数据才能有效调整，可能不适合快速变化的环境
  - 未考虑多用户、多任务并发场景下的路由策略
  - 方法在不同语言和文化背景下的泛化能力未充分验证

（完）
