---
title: Reservoir-Based Graph Convolutional Networks
title_zh: 基于液态池的图卷积网络
authors: "Mayssa Soussia, Gita Ayu Salsabila, Mohamed Ali Mahjoub, Islem Rekik"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24131v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 基于水库计算的图卷积网络
tldr: 本研究针对图卷积网络(GCNs)在处理复杂或动态数据时面临的过平滑和计算成本高的问题，提出了一种基于液态池的图卷积网络(RGC-Net)。该模型结合了液态计算与结构化图卷积，通过固定随机液态池权重和泄漏积分器增强特征保留，在分类和生成任务中实现了最先进性能，同时减少了过平滑问题并加快了收敛速度。
selection_source: fresh_fetch
motivation: GCNs在处理复杂或动态数据时面临过平滑和计算成本高的挑战，需要新的方法来有效捕获长距离依赖关系。
method: RGC-Net通过整合液态计算与结构化图卷积，使用固定随机液态池权重和泄漏积分器来增强特征保留和信息传播。
result: RGC-Net在分类和生成任务中实现了最先进性能，包括脑图演化，具有更快的收敛速度和减少的过平滑问题。
conclusion: RGC-Net成功地将液态计算与图卷积结合，为处理复杂图数据提供了高效且可扩展的解决方案。
---

## 摘要
消息传递是图神经网络(GNNs)的核心机制，通过聚合来自邻居节点的信息实现节点嵌入的迭代更新。图卷积网络(GCNs)通过将卷积操作适应于图结构来体现这种方法，使相邻节点的特征能够有效结合。然而，GCNs在处理复杂或动态数据时面临挑战。捕获长程依赖关系通常需要更深的层次，这不仅增加了计算成本，还导致过平滑问题，使节点嵌入变得难以区分。为了克服这些挑战，液态池计算已被集成到GNNs中，利用迭代消息传递动力学实现稳定的信息传播，无需大量参数调整。尽管有前景，现有的基于液态池的模型缺乏结构化的卷积机制，限制了它们准确聚合多跳邻域信息的能力。为了解决这些局限性，我们提出了RGC-Net(基于液态池的图卷积网络)，它将液态池动力学与结构化图卷积相结合。主要贡献包括：(i)重新设计的卷积框架，具有固定的随机液态池权重和泄漏积分器，以增强特征保留；(ii)用于图分类的健壮、可适应模型；以及(iii)由RGC-Net驱动的变换器，用于图生成，并应用于动态脑连接。大量实验表明，RGC-Net在分类和生成任务中取得了最先进的性能，包括脑图演化，具有更快的收敛速度和减少的过平滑问题。源代码可在 https://github.com/basiralab/RGC-Net 获取。

## Abstract
Message passing is a core mechanism in Graph Neural Networks (GNNs), enabling the iterative update of node embeddings by aggregating information from neighboring nodes. Graph Convolutional Networks (GCNs) exemplify this approach by adapting convolutional operations for graph structures, allowing features from adjacent nodes to be combined effectively. However, GCNs encounter challenges with complex or dynamic data. Capturing long-range dependencies often requires deeper layers, which not only increase computational costs but also lead to over-smoothing, where node embeddings become indistinguishable. To overcome these challenges, reservoir computing has been integrated into GNNs, leveraging iterative message-passing dynamics for stable information propagation without extensive parameter tuning. Despite its promise, existing reservoir-based models lack structured convolutional mechanisms, limiting their ability to accurately aggregate multi-hop neighborhood information. To address these limitations, we propose RGC-Net (Reservoir-based Graph Convolutional Network), which integrates reservoir dynamics with structured graph convolution. Key contributions include: (i) a reimagined convolutional framework with fixed random reservoir weights and a leaky integrator to enhance feature retention; (ii) a robust, adaptable model for graph classification; and (iii) an RGC-Net-powered transformer for graph generation with application to dynamic brain connectivity. Extensive experiments show that RGC-Net achieves state-of-the-art performance in classification and generative tasks, including brain graph evolution, with faster convergence and reduced over-smoothing. Source code is available at https://github.com/basiralab/RGC-Net .

---

## 论文详细总结（自动生成）

# 基于液态池的图卷积网络(RGC-Net)论文总结

## 1. 论文的核心问题与整体含义
- **核心问题**：图卷积网络(GCNs)在处理复杂或动态数据时面临两大挑战：(1)捕获长程依赖关系需要更深层次，导致计算成本增加；(2)深层网络会出现过平滑问题，使节点嵌入变得难以区分。
- **研究背景**：现有基于液态池(reservoir computing)的图神经网络缺乏结构化卷积机制，限制了它们准确聚合多跳邻域信息的能力。传统GCN在处理动态图数据时表现不佳，而液态池计算虽能提供稳定信息传播但缺乏结构化设计。

## 2. 论文提出的方法论
- **核心思想**：提出RGC-Net(Reservoir-based Graph Convolutional Network)，将液态池动力学与结构化图卷积相结合，解决GCNs的过平滑和计算效率问题。
- **关键技术细节**：
  - **输入层**：使用固定随机权重矩阵转换初始节点特征，实现维度调整
  - **图卷积液态池**：通过矩阵乘法迭代更新节点嵌入，使用归一化邻接矩阵和固定随机液态池权重
  - **泄漏积分器机制**：引入泄漏率α平衡保留初始嵌入和聚合邻域信息，公式为H(k+1) = ReLU((1-α)H(k) + α * Â * H(k) * W_res^l)
  - **线性输出层**：将最终图嵌入转换为任务特定输出维度
- **算法流程**：输入图→输入层转换→多轮图卷积液态池更新→输出层转换→任务特定输出

## 3. 实验设计
- **数据集/场景**：
  - 图分类任务：MUTAG(化学化合物)、PROTEINS(蛋白质结构)、DD(氨基酸距离)数据集
  - 图生成任务：EMCI-AD(阿尔茨海默病脑连接)、SLIM160(健康年轻人脑连接)和模拟纵向连接组数据集
- **Benchmark**：
  - 图分类：GCN、GAT、GraphSAGE、GIN、GraphESN
  - 图生成：Identity Function、RBGM、EvoGraphNet、GCN-Transformer
- **对比方法**：RGC-Net与TRGC-Net(可训练液态池权重的变体)进行对比，以及与传统GNN模型的性能比较

## 4. 资源与算力
- **训练时间**：RGC-Net在PROTEINS数据集上训练时间为1.83秒，比传统GNN快90%；在DD数据集上为7.73秒
- **内存使用**：在PROTEINS数据集上使用约1.78GB内存，与GCN相当
- **GPU使用**：与标准GNN相当或更低，如PROTEINS数据集上约0.0175GB
- **训练轮数**：图分类模型最多500轮，生成模型最多200轮，使用早期停止机制(耐心阈值5轮)
- **未明确信息**：论文未提及具体GPU型号和数量

## 5. 实验数量与充分性
- **实验数量**：
  - 图分类实验：在3个数据集上测试不同层数(1-5层)、迭代次数(1-5次)和泄漏率(0.7-1.0)
  - 图生成实验：在3个纵向连接组数据集上评估生成性能
  - 资源消耗比较：详细比较了不同模型的训练时间、每轮时间、训练轮数、内存和GPU使用
  - 超参数优化：使用网格搜索优化学习率、调度器步长、液态池迭代次数和泄漏率
- **充分性评估**：
  - 实验覆盖了不同类型和规模的图数据集
  - 使用3折交叉验证和3次随机初始化确保结果可靠性
  - 进行了详细的消融实验，测试不同超参数的影响
  - 客观比较了计算资源消耗，突显RGC-Net的效率优势

## 6. 论文的主要结论与发现
- RGC-Net在图分类任务上显著优于传统GNN模型和现有液态池GNN模型
- 在脑图演化预测任务上，RGC-Net-Transformer超越了RBGM和EvoGraphNet等SOTA方法
- 固定液态池权重在大多数情况下表现优于可训练权重(TRGC-Net)，支持非训练参数可带来更快收敛和更低资源消耗的假设
- 泄漏积分器机制有效减轻了过平滑问题，使模型能够更深而不损失节点特征区分度
- RGC-Net在大规模图数据上展现出更好的可扩展性，避免了传统方法的内存不足问题

## 7. 优点：方法或实验设计上有哪些亮点
- **方法创新**：首次将液态池计算范式应用于图卷积操作，创建结构化卷积机制
- **效率优势**：固定随机权重显著减少计算成本，训练时间比传统GNN快高达90%
- **设计亮点**：泄漏积分器机制平衡特征保留与信息聚合，有效控制过平滑
- **实验全面**：不仅评估性能，还详细比较资源消耗，提供完整效率分析
- **应用广泛**：在图分类和图生成两种任务中均展示有效性，特别是脑图演化预测
- **开源实现**：提供完整源代码，促进可复现性和进一步研究

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **模型局限**：RGC-Net不是置换不变的，可能影响其在具有不同节点顺序的图上的性能
- **超参数复杂性**：需要额外调整泄漏率和迭代次数，增加了模型调优的复杂性
- **应用限制**：实验主要集中在图分类和脑图演化预测，其他图学习任务(如节点分类、边预测)的适用性尚未验证
- **液态池固定性**：固定权重可能限制了在某些复杂或高度不规则结构上的适应性
- **实验覆盖**：虽然实验数量充足，但主要集中在生物医学和化学领域，缺乏更广泛的图数据类型验证
- **部署挑战**：虽然收敛更快，但在资源受限设备上的部署效率仍需进一步优化

（完）
