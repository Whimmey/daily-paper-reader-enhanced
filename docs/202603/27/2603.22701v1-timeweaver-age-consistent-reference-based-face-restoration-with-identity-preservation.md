---
title: "TimeWeaver: Age-Consistent Reference-Based Face Restoration with Identity Preservation"
title_zh: "TimeWeaver: 基于参考的年龄一致人脸修复与身份保持"
authors: "Teer Song, Yue Zhang, Yu Tian, Ziyang Wang, Xianlin Zhang, Guixuan Zhang, Xuan Liu, Xueming Li, Yasen Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22701v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 跨年龄人脸修复框架
tldr: TimeWeaver是首个支持跨年龄参考的人脸修复框架，解决了现有方法在参考与输入年龄不匹配时无法保持年龄一致性的问题。通过在训练中分离身份和年龄条件，并采用ID-Fusion模块和两种训练技术，TimeWeaver能够在保持身份一致性的同时精确实现目标年龄的修复效果，在视觉质量、身份保持和年龄一致性方面超越现有方法。
selection_source: fresh_fetch
motivation: 现有参考人脸修复方法假设参考与输入年龄对齐，无法处理跨年龄参考场景下的年龄一致性问题。
method: TimeWeaver通过训练时分离身份和年龄条件，使用ID-Fusion模块学习年龄鲁棒的身份表示，并在推理时采用Age-Aware Gradient Guidance和Token-Targeted Attention Boost技术引导采样。
result: 实验表明TimeWeaver在视觉质量、身份保持和年龄一致性方面均优于现有方法。
conclusion: TimeWeaver首次实现了支持跨年龄参考的人脸修复，为历史人脸修复和失踪人员检索等应用提供了有效解决方案。
---

## 摘要
最近的人脸修复进展已从视觉保真度转向身份保真度，推动了从无参考到基于参考的范式转变，后者依赖于同一人的参考图像进行条件修复。然而，这些方法假设参考图像和退化输入在年龄上是对齐的。当只有跨年龄参考可用时，如在历史修复或失踪人员检索中，它们无法保持年龄保真度。为解决这一局限，我们提出了TimeWeaver，这是第一个支持跨年龄参考的基于参考的人脸修复框架。给定任意参考图像和目标年龄提示，TimeWeaver生成兼具身份保真度和年龄一致性的修复结果。具体而言，我们在训练和推理过程中解耦身份和年龄条件。在训练期间，模型通过基于transformer的ID-Fusion模块，将全局身份嵌入与年龄抑制的面部标记融合，学习一种年龄鲁棒的身份表示。在推理期间，两种无需训练的技术——年龄感知梯度引导和标记目标注意力增强——引导采样朝向期望的年龄语义，实现对目标年龄提示的精确遵循。大量实验表明，TimeWeaver在视觉质量、身份保持和年龄一致性方面均优于现有方法。

## Abstract
Recent progress in face restoration has shifted from visual fidelity to identity fidelity, driving a transition from reference-free to reference-based paradigms that condition restoration on reference images of the same person. However, these methods assume the reference and degraded input are age-aligned. When only cross-age references are available, as in historical restoration or missing-person retrieval, they fail to maintain age fidelity. To address this limitation, we propose TimeWeaver, the first reference-based face restoration framework supporting cross-age references. Given arbitrary reference images and a target-age prompt, TimeWeaver produces restorations with both identity fidelity and age consistency. Specifically, we decouple identity and age conditioning across training and inference. During training, the model learns an age-robust identity representation by fusing a global identity embedding with age-suppressed facial tokens via a transformer-based ID-Fusion module. During inference, two training-free techniques, Age-Aware Gradient Guidance and Token-Targeted Attention Boost, steer sampling toward desired age semantics, enabling precise adherence to the target-age prompt. Extensive experiments show that TimeWeaver surpasses existing methods in visual quality, identity preservation, and age consistency.

---

## 论文详细总结（自动生成）

# TimeWeaver 论文总结

## 1. 论文的核心问题与整体含义
- **核心问题**：现有基于参考的人脸修复方法假设参考图像与退化输入年龄对齐，无法处理跨年龄参考场景（如历史修复、失踪人员检索）下的年龄一致性问题。
- **整体含义**：TimeWeaver是首个支持跨年龄参考的人脸修复框架，旨在解决现有方法在跨年龄场景下直接复制参考图像年龄特征而非保持目标年龄的问题，实现身份保真度和年龄一致性的双重目标。

## 2. 论文提出的方法论
- **核心思想**：在训练和推理过程中解耦身份和年龄条件，训练时专注于身份保持，推理时专注于年龄控制。
- **关键技术细节**：
  - **年龄鲁棒的身份表示**：
    - 使用预训练人脸识别模型(ArcFace)提取全局身份嵌入
    - 使用CLIP-ViT提取年龄抑制的面部标记，通过面部解析器创建掩码，抑制皮肤区域（年龄变化最显著区域）
    - ID-Fusion模块融合全局身份嵌入和年龄抑制的面部标记
  - **推理时的年龄控制**：
    - Age-Aware Gradient Guidance (AAGG)：计算年龄方向梯度，优化潜在空间
    - Token-Targeted Attention Boost (TTAB)：使用年龄特定标记的注意力图作为调制权重，集中更新在年龄相关区域
- **算法流程**：
  - 训练阶段：提取身份特征，通过ID-Fusion融合，注入到UNet中
  - 推理阶段：使用AAGG和TTAB技术引导采样朝向期望的年龄语义

## 3. 实验设计
- **数据集**：
  - 训练数据集：VGGFace2-HQ和CelebRef-HQ，共5,405个身份，178,877张图像
  - 测试数据集：
    - 同年龄测试：从CelebRef-HQ中选取150个身份
    - 跨年龄合成测试：从AgeDB构建的子集，100个身份，平均年龄跨度50.3年
    - 跨年龄真实世界测试：20个公众人物，拍摄年龄至少相隔20年
- **Benchmark**：
  - 视觉质量：PSNR、SSIM、LPIPS、NIQE、FID
  - 身份相似度：IDS（ArcFace嵌入的余弦相似度）
  - 年龄一致性：AGE（使用DEX年龄估计器的平均绝对误差）
- **对比方法**：
  - 无参考修复：CodeFormer、DifFace、DiffBIR
  - 基于参考的修复：DMDNet、RestorerID、Ref-LDM、FaceMe、InstantRestore

## 4. 资源与算力
- 使用4个NVIDIA RTX 4090 GPU
- 批处理大小：每GPU 12
- 训练迭代次数：270K
- 优化器：AdamW，学习率：4e-5
- 训练时长：未明确说明具体天数

## 5. 实验数量与充分性
- **实验数量**：
  - 同年龄和跨年龄（合成和真实世界）定量比较
  - 用户研究（50名志愿者）
  - 消融研究（身份表示、AAGG和TTAB机制）
  - 与两阶段修复-编辑管道的比较
  - 不同年龄跨度的鲁棒性测试（≤10、10~20、20~30、30~40、>40岁）
- **充分性**：
  - 实验覆盖了多种场景和数据集
  - 包括定量指标和定性比较
  - 进行了用户研究以验证人类感知
  - 消融实验验证了各组件的贡献
  - 实验设计客观公平，与多种最新方法进行了比较

## 6. 论文的主要结论与发现
- **主要结论**：
  - TimeWeaver是第一个支持跨年龄参考的人脸修复框架
  - 通过解耦身份和年龄条件，有效解决了身份-年龄冲突和跨年龄数据稀缺问题
  - 在视觉质量、身份保持和年龄一致性方面均优于现有方法
- **发现**：
  - 现有基于参考的方法在跨年龄场景下存在明显的年龄漂移
  - 两阶段修复-编辑管道在处理年龄编辑时存在局限性（结构扭曲、引入伪影、语义误解）
  - TimeWeaver在不同年龄跨度下保持鲁棒性

## 7. 优点：方法或实验设计上有哪些亮点
- **方法亮点**：
  - 首个支持跨年龄参考的人脸修复框架
  - 训练和推理阶段的解耦策略，有效处理身份-年龄冲突
  - ID-Fusion模块学习年龄鲁棒的身份表示
  - AAGG和TTAB两种训练-free技术实现精确年龄控制
- **实验设计亮点**：
  - 全面评估了同年龄和跨年龄场景
  - 使用多种定量指标和定性比较
  - 进行了用户研究验证人类感知
  - 消融实验验证了各组件的贡献
  - 与多种最新方法进行了公平比较

## 8. 不足与局限
- **实验覆盖**：
  - 实验主要在合成数据和有限的真实世界数据上进行
  - 可能存在数据集偏差（主要使用西方面孔）
  - 未充分探索不同种族和肤色的表现
- **偏差风险**：
  - 年龄估计依赖于DEX模型，可能存在误差
  - 用户研究样本量有限（50名志愿者）
- **应用限制**：
  - 计算成本较高（需要多GPU训练）
  - 对于极端年龄跨度或严重遮挡的情况可能表现不佳
  - 推理时间较长（约22.68秒）

（完）
