---
title: Unlocking Few-Shot Capabilities in LVLMs via Prompt Conditioning and Head Selection
title_zh: 通过提示词调节和头部选择解锁LVLMs的小样本能力
authors: "Adhemar de Senneville, Xavier Bou, Jérémy Anger, Rafael Grompone, Gabriele Facciolo"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24181v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: LVLM的提示 conditioning 和头部选择
tldr: 大型视觉语言模型(LVLMs)在零样本任务上表现良好，但在图像分类任务上落后于CLIP方法。本研究发现，通过提示条件和注意力头部选择，LVLMs的内部表示可以显著提升分类性能。作者提出的头部集成分类器(HEC)无需训练即可结合最具判别性的视觉和文本头部，在12个数据集上实现了小样本和零样本分类的最先进性能。
selection_source: fresh_fetch
motivation: 尽管LVLMs使用CLIP预训练的视觉编码器，但在图像分类任务上表现不佳，需要探索提升其分类能力的方法。
method: 提出头部集成分类器(HEC)，通过高斯判别分析选择最具判别性的视觉和文本注意力头，组合成无需训练的分类器。
result: HEC在12个数据集上实现了小样本和零样本分类的最先进性能，显著缩小了LVLMs与CLIP方法在分类任务上的性能差距。
conclusion: 通过提示条件和注意力头部选择，可以有效解锁LVLMs的小样本分类能力，使其在分类任务上达到或超过CLIP方法的性能。
---

## 摘要
当前大型视觉语言模型(LVLMs)在图像描述、视觉问答和OCR等许多零样本任务上表现出色。然而，这些模型在图像分类任务上表现不佳，性能不及基于CLIP的方法。值得注意的是，这一差距令人惊讶，因为许多LVLMs使用CLIP预训练的视觉编码器。然而，LVLMs并非受限于CLIP的独立视觉和文本编码器架构。在CLIP中，这种分离使分类偏向于类别名称匹配，而非联合视觉-文本推理。在本文中，我们表明尽管LVLMs的原始性能不佳，但它们可以通过提示词调节提高推理时的视觉特征类别可分性，并且LVLMs的内部表示，特别是注意力头，在零样本和小样本分类中可以超越模型本身的性能。我们引入了头部集成分类器(HEC)来弥合基于CLIP和基于LVLM的分类方法之间的性能差距。受高斯判别分析的启发，HEC对最具判别性的视觉和文本头进行排序，并将它们组合成一个无需训练的分类器。我们证明HEC在12个数据集上的小样本和零样本分类中达到了最先进的性能。

## Abstract
Current Large Vision Language Models (LVLMs) excel at many zero-shot tasks like image captioning, visual question answering and OCR. However, these same models suffer from poor performance at image classification tasks, underperforming against CLIP-based methods. Notably, this gap is surprising because many LVLMs use CLIP-pretrained vision encoders. Yet LVLMs are not inherently limited by CLIP's architecture with independent vision and text encoders. In CLIP, this separation biases classification toward class-name matching rather than joint visual-text reasoning. In this paper we show that, despite their poor raw performance, LVLMs can improve visual feature class separability at inference using prompt conditioning, and LVLMs' internal representations, especially attention heads, can outperform the model itself at zero-shot and few-shot classification. We introduce Head Ensemble Classifiers (HEC) to bridge the performance gap between CLIP-based and LVLM-based classification methods. Inspired by Gaussian Discriminant Analysis, HEC ranks the most discriminative vision and text heads and combines them into a training-free classifier. We show that HEC achieves state-of-the-art performance in few-shot and zero-shot classification across 12 datasets.

---

## 论文详细总结（自动生成）

# 论文总结：通过提示词调节和头部选择解锁LVLMs的小样本能力

## 1. 论文的核心问题与整体含义
- **核心问题**：当前大型视觉语言模型(LVLMs)在图像分类任务上表现不佳，性能不及基于CLIP的方法，尽管许多LVLMs使用CLIP预训练的视觉编码器。
- **研究背景**：LVLMs并非受限于CLIP的独立视觉和文本编码器架构，但CLIP的分离使分类偏向于类别名称匹配，而非联合视觉-文本推理。
- **研究动机**：探索如何提升LVLMs在图像分类任务上的性能，缩小与CLIP方法的差距，无需额外训练。

## 2. 论文提出的方法论
- **核心思想**：通过提示词调节提高LVLMs的视觉特征类别可分性，利用LVLMs内部表示特别是注意力头来提升分类性能。
- **关键技术细节**：
  - **提示条件**：任务条件(Task Conditioning)、领域条件(Domain Conditioning)和类别条件(Class Conditioning)
  - **头部选择**：使用高斯判别分析(GDA)选择最具判别性的视觉头(vision-heads)和文本头(text-heads)
  - **分类器组合**：HEC-V(视觉头集成)、HEC-T(文本头集成)和HEC-VT(两者结合)
- **算法流程**：
  1. 使用提示词条件化LVLMs
  2. 提取注意力头特征
  3. 使用GDA对头部进行排序
  4. 选择top-k头部进行集成
  5. 生成最终分类结果

## 3. 实验设计
- **数据集**：
  - 10个公开图像分类数据集：EuroSAT, UCF101, DTD, Caltech101, SUN397, OxfordPets, Stanford-Cars, Flowers102, Food101, FGVCAircraft
  - 2个额外细粒度分类数据集：CUB-200, Traffic-Signs
  - ImageNet作为通用分类数据集
- **场景**：
  - 视觉小样本分类(类别名称未知)
  - 文本零样本分类
  - 视觉-文本小样本分类
- **对比方法**：
  - CLIP-based方法：CLIP, TipAdapter, GDA, ProKeR
  - 视觉模型：DINOv1, DINOv2, DINOv3
  - LVLM-based方法：SAVs
  - 线性探测(Probing)方法

## 4. 资源与算力
- 所有实验都在单个NVIDIA V100 GPU上进行
- 未明确提到训练时长
- 计算复杂度分析表明HEC-V比线性探测更高效，复杂度降低因子为H²/L（对于Qwen2-VL约为28）

## 5. 实验数量与充分性
- **实验数量**：
  - 在12个数据集上进行了实验
  - 视觉小样本分类：N-way 4-shot，N等于每个数据集的总类别数
  - 文本零样本分类：10-way 0-shot，平均100个任务
  - 视觉-文本小样本分类：N-way 4-shot
  - 消融实验：不同提示条件、头部选择方法、集成策略等
- **充分性**：
  - 实验覆盖了多种场景和数据集
  - 进行了详细的消融实验验证各组件的有效性
  - 与多种基线方法进行了公平比较
  - 实验设计较为客观和全面

## 6. 论文的主要结论与发现
- **主要结论**：
  - HEC在12个数据集上实现了小样本和零样本分类的最先进性能
  - 通过提示条件和注意力头部选择，可以有效解锁LVLMs的小样本分类能力
  - HEC缩小了LVLMs与CLIP方法在分类任务上的性能差距
- **关键发现**：
  - 提示条件可以提高LVLMs的视觉特征类别可分性
  - LVLMs的内部表示，特别是注意力头，在分类中可以超越模型本身的性能
  - 领域特定的提示可以提取领域特定特征
  - 头部选择和集成策略对性能有显著影响

## 7. 优点
- **方法亮点**：
  - 无需训练即可提升LVLMs的分类性能
  - 结合了提示条件和注意力头部选择两种技术
  - 提出了三种不同的分类器(HEC-V, HEC-T, HEC-VT)适应不同场景
  - 使用高斯判别分析进行头部选择，理论基础扎实
- **实验设计亮点**：
  - 在12个多样化数据集上进行全面评估
  - 与多种基线方法进行公平比较
  - 进行了详细的消融实验验证各组件的有效性
  - 分析了计算复杂度，表明方法的高效性

## 8. 不足与局限
- **方法局限**：
  - 需要中间表示(注意力头)进行分类，对API使用有限制
  - 类别条件(Class Conditioning)仅适用于少量类别
  - LVLM推理比CLIP模型计算成本更高
- **实验局限**：
  - 实验主要在特定LVLM模型(Qwen2-VL, LLaVA-OV)上进行，泛化性有待进一步验证
  - 头部选择机制可能存在任务依赖性
  - 类别条件在大量类别时性能下降
- **应用限制**：
  - 需要标记的支持集进行头部选择
  - 在纯零样本场景下应用受限

（完）
