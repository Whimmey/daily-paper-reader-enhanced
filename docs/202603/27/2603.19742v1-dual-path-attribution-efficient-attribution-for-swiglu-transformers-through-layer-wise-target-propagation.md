---
title: "Dual Path Attribution: Efficient Attribution for SwiGLU-Transformers through Layer-Wise Target Propagation"
title_zh: 双路径归因：通过逐层目标传播实现SwiGLU-Transformers的高效归因
authors: "Lasse Marten Jantsch, Dong-Jae Koh, Seonghyeon Lee, Young-Kyoon Suh"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19742v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: Transformer模型分析
tldr: 本研究针对transformer大语言模型内部机制理解问题，提出了一种名为Dual Path Attribution (DPA)的高效归因框架。DPA通过解析SwiGLU Transformers的计算结构，在一次前向和反向传播中实现密集组件归因，无需反事实示例，在保持高忠实度的同时实现了O(1)时间复杂度，显著提升了模型可解释性效率。
selection_source: fresh_fetch
motivation: 理解transformer大语言模型的内部机制对其可靠部署至关重要，但现有归因方法在密集组件归因方面计算成本过高。
method: Dual Path Attribution (DPA)框架通过解析和线性化SwiGLU Transformers的计算结构，沿不同路径传播目标unembedding向量，实现高效的信息流追踪。
result: 实验表明DPA在标准可解释性基准上实现了最先进的忠实度，同时与现有方法相比具有前所未有的计算效率。
conclusion: DPA提供了一种高效且忠实的方法来理解transformer模型的内部工作机制，解决了密集组件归因的计算瓶颈问题。
---

## 摘要
理解基于transformer的大型语言模型(LLMs)的内部机制对于其可靠部署和有效操作至关重要。尽管最近的研究已经产生了大量试图平衡保真度和计算效率的归因方法，但密集组件归因仍然成本过高。在这项工作中，我们引入了双路径归因(DPA)，这是一个新颖的框架，可以在一次前向和一次后向传播中忠实地追踪冻结transformer上的信息流，而无需反事实示例。DPA分析性地分解并线性化了SwiGLU Transformers的计算结构，将其分解为不同的路径，沿着这些路径传播目标解嵌入向量，以获取每个残差位置的有效表示。这种以目标为中心的传播实现了相对于模型组件数量的O(1)时间复杂度，可扩展到长输入序列和密集组件归因。在标准可解释性基准上的大量实验表明，与现有基线相比，DPA实现了最先进的保真度和前所未有的效率。

## Abstract
Understanding the internal mechanisms of transformer-based large language models (LLMs) is crucial for their reliable deployment and effective operation. While recent efforts have yielded a plethora of attribution methods attempting to balance faithfulness and computational efficiency, dense component attribution remains prohibitively expensive. In this work, we introduce Dual Path Attribution (DPA), a novel framework that faithfully traces information flow on the frozen transformer in one forward and one backward pass without requiring counterfactual examples. DPA analytically decomposes and linearizes the computational structure of the SwiGLU Transformers into distinct pathways along which it propagates a targeted unembedding vector to receive the effective representation at each residual position. This target-centric propagation achieves O(1) time complexity with respect to the number of model components, scaling to long input sequences and dense component attribution. Extensive experiments on standard interpretability benchmarks demonstrate that DPA achieves state-of-the-art faithfulness and unprecedented efficiency compared to existing baselines.

---

## 论文详细总结（自动生成）

# 论文总结：双路径归因（DPA）

## 1. 论文的核心问题与整体含义

**研究动机和背景**：
理解Transformer大型语言模型(LLMs)的内部机制对于其可靠部署和有效操作至关重要。然而，现有的归因方法在密集组件归因方面计算成本过高，难以有效分析模型内部组件对预测的贡献。这一限制阻碍了对大语言模型内部工作机制的深入理解，影响了模型的可靠部署和有效操作。

## 2. 论文提出的方法论

**核心思想**：
Dual Path Attribution (DPA)框架通过解析和线性化SwiGLU Transformers的计算结构，将其分解为不同的路径，沿着这些路径传播目标unembedding向量，实现高效的信息流追踪。

**关键技术细节**：
- 将SwiGLU Transformers的计算结构分解为内容路径（传播表示）和控制路径（控制路由和转换）
- 通过逐层目标传播，将目标标记的解嵌入向量递归传播回Transformer模块
- 实现相对于模型组件数量的O(1)时间复杂度，与模型组件数量无关

**算法流程**：
1. **单次前向传播**：执行一次前向传播，缓存注意力权重、归一化输入、GLU预激活和门激活等所需激活
2. **自上而下的目标传播**：从输出层开始，递归地将有效目标向量传播回每一层
   - 首先通过GLU模块传播，分别计算上投影路径和门投影路径
   - 然后通过多头自注意力模块传播，分别计算值路径、查询路径和键路径
3. **分数计算**：计算各层次（输入标记、注意力头、MLP神经元）的归因分数

## 3. 实验设计

**数据集/场景**：
- **Known 1000**：用于事实序列完成，评估模型对事实知识的利用
- **SQuAD v2.0**：用于上下文支持的问答，评估模型对上下文信息的利用
- **IMDb**：用于情感分析，评估模型对情感信息的处理
- **Indirect Object Identification (IOI)**：用于电路发现，评估模型内部组件的因果关系

**Benchmark**：
- 归因保真度评估：使用comprehensiveness（破坏）和sufficiency（恢复）两个指标
- 组件级归因评估：评估内部组件（注意力头和MLP神经元）的重要性

**对比方法**：
- **基于梯度的方法**：Gradient、Input × Gradient、Integrated Gradients
- **基于注意力的方法**：Last-Layer Attention、Mean Attention、Attention Rollout
- **上下文混合方法**：Information Flow Routes
- **基于分解的方法**：DePass

## 4. 资源与算力

论文中提到的硬件资源：
- **GPU型号**：NVIDIA A100-SXM4-40GB
- **系统配置**：NVIDIA DGX系统
- **软件环境**：CUDA Version 12.2，NVIDIA Driver Version 535.161.08
- **实现框架**：PyTorch

论文未明确提到具体的GPU数量和训练时长，但提到在长上下文任务中，DPA比传统方法快约40倍。

## 5. 实验数量与充分性

**实验数量**：
- 在5种不同模型上进行了实验：Llama-3.1-8B-Instruct、Llama-2-7B-Chat、Qwen3-4B-Instruct、Mistral-7B-Instruct、Qwen2.5-32B-Instruct
- 在4种不同数据集上进行了评估：Known 1000、SQuAD v2.0、IMDb、IOI
- 进行了输入级和组件级的归因评估
- 进行了敏感性分析，评估不同权重参数对性能的影响

**充分性与公平性**：
- 实验覆盖了多种模型架构和数据集，确保结果的泛化性
- 使用了多种评估指标，确保评估的全面性
- 与多种基线方法进行了比较，包括简单基线和先进方法
- 在相同的硬件配置下进行所有实验，确保公平比较
- 进行了敏感性分析，验证了方法对不同参数设置的稳定性

## 6. 论文的主要结论与发现

**主要结论**：
- DPA在标准可解释性基准上实现了最先进的保真度
- 与现有方法相比具有前所未有的计算效率，时间复杂度为O(1)
- DPA能够精确隔离因果机制，直到单个注意力头和MLP神经元
- DPA在长上下文任务中表现出色，解决了传统干预方法的严重计算瓶颈

**关键发现**：
- DPA在输入级和组件级归因上都表现出色，在IOI和Known 1000基准上实现了零破坏和高恢复
- 在长上下文任务中，DPA比传统方法快约40倍，同时保持可比的内存占用
- 敏感性分析表明，对于输入级归因，中等权重(50-70%)表现最佳；对于组件级归因，较小权重(p≈0.2)表现最佳

## 7. 优点

**方法亮点**：
- **高效性**：实现了O(1)时间复杂度，与模型组件数量无关，解决了密集组件归因的计算瓶颈
- **无需反事实示例**：只需一次前向和一次反向传播，无需多个前向传播
- **精确性**：能够精确隔离因果机制，直到单个注意力头和MLP神经元
- **架构通用性**：核心方法论架构无关，可扩展到其他Transformer架构

**实验设计亮点**：
- **全面性**：在多种模型架构和数据集上进行了全面评估
- **多维度评估**：使用了多种评估指标，确保评估的全面性
- **深入分析**：进行了敏感性分析，验证了方法的稳定性和参数选择的影响
- **公平比较**：与多种基线方法进行了比较，确保公平性

## 8. 不足与局限

**架构特定性**：
- 当前数学分解明确针对标准密集SwiGLU Transformers，扩展到涉及离散路由的架构（如Mixture-of-Experts）或注意力无关范式（如State Space Models）需要手动推导新的逆函数

**近似局限性**：
- 在高曲率区域或由高度耦合、协同或 heavily非线性的组件交互驱动的区域，方法的归因保真度可能会降低
- 由于使用一阶近似，丢弃了二阶交叉导数和高阶动态

**范围限制**：
- DPA直接追踪到目标logit，而不是最终的post-softmax概率分布，无法轻松捕获通过抑制竞争token logit来增加目标token可能性的间接归因机制

**资源限制**：
- 尽管时间复杂度降低到O(1)，但在前向传播过程中缓存密集中间激活仍然是内存密集型的，在超长上下文窗口上应用仍需大量内存

**实验覆盖**：
- 虽然实验覆盖了多种模型和数据集，但主要集中在SwiGLU架构，可能无法完全推广到所有Transformer变体
- 实验主要集中在英文任务，对多语言能力的验证有限

（完）
