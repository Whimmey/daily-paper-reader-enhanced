---
title: "Photon: Speedup Volume Understanding with Efficient Multimodal Large Language Models"
title_zh: "Photon: 使用高效多模态大语言模型加速体积理解"
authors: "Chengyu Fang, Heng Guo, Zheng Jiang, Chunming He, Xiu Li, Minfeng Xu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25155v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 用于3D医学影像的高效多模态大模型
tldr: Photon是一种高效的多模态大语言模型框架，专门用于处理3D医学成像数据。通过可变长度token序列表示和自适应token调度技术，Photon显著降低了计算成本，同时保持了高准确性。其创新的梯度恢复机制和正则化方法确保了模型在压缩token时的稳定性和可靠性，在医学视觉问答任务中实现了最先进性能。
selection_source: fresh_fetch
motivation: 传统多模态大语言模型在处理3D医学成像时面临高计算成本问题，现有方法破坏了体积连续性并掩盖了细微发现。
method: Photon通过可变长度token序列表示3D医学体积，结合指令条件token调度、代理梯度传播和梯度恢复的反向传播规则，实现自适应token压缩。
result: Photon在多种医学视觉问答任务上实现了最先进准确性，同时显著降低了资源消耗并加速了训练和推理过程。
conclusion: Photon为3D医学成像理解提供了一种高效、准确且计算友好的多模态大语言模型解决方案。
---

## 摘要
多模态大语言模型在临床视觉问答任务中展现出巨大潜力，但扩展到3D成像受到高计算成本的限制。先前的方法通常依赖于2D切片或固定长度令牌压缩，破坏了体积连续性并掩盖了细微发现。我们提出了Photon，一个使用可变长度令牌序列表示3D医学体积的框架。Photon引入了基于指令的令牌调度和替代梯度传播，以在训练和推理过程中自适应地减少令牌，从而降低计算成本，同时缓解冗余令牌引起的注意力稀释。它包含带有梯度恢复的自定义反向传播规则，使离散令牌丢弃仍能实现可微分优化。为了稳定令牌压缩并确保视觉证据的可靠使用，Photon进一步应用了正则化目标，以减轻纯语言偏见并提高可靠性。在多样化的医学视觉问答任务上的实验表明，Photon在减少资源使用和加速训练与推理的同时，实现了最先进的准确性。

## Abstract
Multimodal large language models are promising for clinical visual question answering tasks, but scaling to 3D imaging is hindered by high computational costs. Prior methods often rely on 2D slices or fixed-length token compression, disrupting volumetric continuity and obscuring subtle findings. We present Photon, a framework that represents 3D medical volumes with token sequences of variable length. Photon introduces instruction-conditioned token scheduling and surrogate gradient propagation to adaptively reduce tokens during both training and inference, which lowers computational cost while mitigating the attention dilution caused by redundant tokens. It incorporates a custom backpropagation rule with gradient restoration to enable differentiable optimization despite discrete token drop. To stabilize token compression and ensure reliable use of visual evidence, Photon further applies regularization objectives that mitigate language-only bias and improve reliability. Experiments on diverse medical visual question answering tasks show that Photon achieves state-of-the-art accuracy while reducing resource usage and accelerating both training and inference.

---

## 论文详细总结（自动生成）

# Photon: 使用高效多模态大语言模型加速体积理解 - 论文总结

## 1. 论文的核心问题与整体含义

- **核心问题**：多模态大语言模型在临床视觉问答任务中展现出巨大潜力，但扩展到3D医学成像面临高计算成本的限制。现有方法通常依赖2D切片或固定长度token压缩，破坏了体积连续性并掩盖了细微的医学发现。
- **研究背景**：医学影像分析，特别是3D医学成像（如CT、MRI）的理解和问答，需要高效处理大量3D数据同时保持高准确性。
- **整体含义**：开发一种高效的多模态大语言模型框架，能够处理3D医学体积数据，减少计算成本，同时保持对关键医学信息的准确理解。

## 2. 论文提出的方法论

- **核心思想**：Photon框架使用可变长度token序列来表示3D医学体积，通过自适应token压缩减少计算成本，同时保持关键医学信息。
- **关键技术细节**：
  - **指令条件Token调度(ITS)**：
    - 指令感知显著性估计：计算指令token之间的中心性分数，然后计算视觉token与指令token之间的加权显著性分数
    - 实例感知阈值预测：从视觉显著性分数构建特征向量，预测实例特定的阈值
    - 基于阈值生成token保留概率和二进制掩码
  - **代理梯度传播(SGP)**：
    - 前向传播中根据保留掩码丢弃不重要的token
    - 反向传播中使用直通代理掩码将梯度恢复到底层保留概率
    - 构建任务相关的代理分数，用于指导token调度优化
  - **正则化目标**：
    - 软保留带：约束保留概率的平均比率在稳定范围内
    - 鲁棒性正则化：鼓励模型在视觉证据不足时产生更高的预测不确定性
    - 翻转正则化：防止模型对保留模式产生过度依赖
- **算法流程**：
  1. 输入处理：将3D医学体积分割成3D补丁，每个补丁嵌入为向量
  2. 视觉编码：使用3D视觉编码器处理视觉token
  3. 指令条件token调度：计算视觉token的显著性分数，预测实例特定阈值，生成保留掩码
  4. 代理梯度传播：在前向传播中应用硬选择，在反向传播中通过代理梯度优化
  5. 模型推理：根据保留掩码处理token，生成答案

## 3. 实验设计

- **数据集/场景**：
  - 3D-RAD：包含16,188个CT扫描，涵盖6个任务（异常检测、图像观察、医学测量、存在检测、静态时间诊断、纵向时间诊断）
  - DeepTumorVQA：基于9,262个腹部CT体积，包含355,962个训练和39,650个测试QA对
  - SLAKE：大规模双语医学视觉问答基准，支持多种成像模态
  - MedFrameQA：多图像医学视觉问答基准，包含2,851个临床VQA项目
  - NExT-QA：视频问答基准，包含5,440个真实世界视频和52,044个人类标注QA对
- **Benchmark**：
  - 主要使用3D-RAD和DeepTumorVQA作为评估基准
  - 评估指标包括准确率、BLEU、ROUGE、BERT Score等
  - 对于数值预测，使用平均相对准确率(MRA)
- **对比方法**：
  - 基础模型：Qwen2.5-VL
  - 3D医学MLLM：RadFM、M3D-L2、M3D-P3、OmniV、Merlin、CT-CHAT、Lingshu
  - 视觉token压缩方法：VisionZip、HiPrune

## 4. 资源与算力

- **训练资源**：使用16个NVIDIA H20 GPU，BF16精度，Deepspeed ZeRO2
- **推理资源**：使用8个NVIDIA H20 GPU
- **训练效率**：Photon比Qwen2.5-VL训练快约5.4倍
- **推理速度**：Photon达到4.12 tokens/s，比基线方法快约1.9倍
- **内存使用**：Photon推理时GPU内存使用约26.0GiB，比基线模型(134.2GiB)减少约80%

## 5. 实验数量与充分性

- **实验数量**：
  - 在3D-RAD和DeepTumorVQA两个主要基准上进行了全面评估
  - 进行了消融研究，评估各个组件的贡献
  - 进行了可视化分析，展示token压缩行为
  - 进行了零样本和域外评估
  - 进行了超参数分析和鲁棒性测试
  - 扩展到其他模态（MRI、X光、超声）和视频理解任务
- **充分性与公平性**：
  - 实验设计较为充分，覆盖了多种任务和数据集
  - 对比了多种基线方法，包括最新的3D医学MLLM
  - 消融研究验证了各个组件的必要性
  - 提供了统计分析和置信区间，确保结果可靠性
  - 零样本和域外评估验证了模型的泛化能力
  - 实验较为客观，使用了标准的评估指标和协议

## 6. 论文的主要结论与发现

- **主要结论**：
  - Photon在多种医学视觉问答任务上实现了最先进的准确性
  - Photon显著降低了计算资源使用，减少了GPU内存消耗（约减少2/3）
  - Photon加速了训练和推理过程（训练快5.4倍，推理快1.9倍）
  - 自适应token压缩能够保留关键的医学信息，同时减少冗余
- **关键发现**：
  - 指令条件token调度能够根据不同任务和指令自适应地保留相关视觉区域
  - 代理梯度传播确保了离散token丢弃的可微分训练
  - 正则化方法有效减轻了语言-only偏见，提高了可靠性
  - Photon在测量和推理任务上表现特别出色，提高了定量精度和空间分析能力

## 7. 优点：方法或实验设计上有哪些亮点

- **方法亮点**：
  - 使用可变长度token序列表示3D医学体积，避免了固定长度压缩的限制
  - 指令条件token调度根据不同指令自适应调整保留的token数量和位置
  - 代理梯度传播解决了离散token丢弃的可微分训练问题
  - 两阶段训练策略提高了效率，避免了过拟合
  - 多重正则化确保了模型的稳定性和可靠性
- **实验设计亮点**：
  - 在多个基准和数据集上进行了全面评估
  - 消融研究验证了各个组件的必要性
  - 可视化分析直观展示了token压缩行为
  - 零样本和域外评估验证了泛化能力
  - 扩展到多种模态和任务，证明了方法的通用性

## 8. 不足与局限

- **实验覆盖局限**：
  - 实验主要基于CT数据，对其他成像模态的验证相对有限
  - 虽然进行了零样本评估，但缺乏真实临床环境下的验证
- **性能局限**：
  - 模型在细粒度定量估计（如小病灶测量）上仍有挑战
  - 对罕见或解剖异常情况的处理能力有限
  - 模型可能依赖于训练数据中的常见模式，对罕见病变的识别能力有限
- **应用限制**：
  - 未充分探索模型在不同计算资源上的性能表现
  - 缺乏与医学专家的交互式评估和验证
  - 未充分探索模型在实时临床应用中的表现
- **其他局限**：
  - 模型复杂度可能影响部署到资源有限的临床环境
  - 对医学知识的理解和推理能力仍有提升空间

（完）
