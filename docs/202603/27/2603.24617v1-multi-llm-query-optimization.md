---
title: Multi-LLM Query Optimization
title_zh: 多大型语言模型查询优化
authors: "Arlen Dean, Zijin Zhang, Stefanus Jasin, Yuqing Liu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.24617v1"
tags: ["query:dl-cv"]
score: 9.0
evidence: 多LLM查询优化
tldr: 本文针对多大型语言模型(LLM)并行分类中的查询分配优化问题，提出了一种离线查询规划方法。通过将多类错误约束转化为成对比较并应用Chernoff集中界，构建了NP难问题的可行代理，设计了渐近完全多项式时间近似方案，实现了在保证可靠性的同时最小化查询成本的目标。
selection_source: fresh_fetch
motivation: 多LLM并行分类中如何最优分配查询以最小化成本同时保证可靠性是一个尚未充分解决的问题。
method: 通过将多类错误约束分解为成对比较并应用Chernoff集中界构建代理问题，并设计渐近完全多项式时间近似方案求解。
result: 所提出的代理问题具有闭式可解表达式且保证可行性，其最优解与真实最优解的比率随误差容限缩小而收敛到1。
conclusion: 所提出的AFPTAS算法能够在多项式时间内找到接近最优的查询分配方案，有效解决了多LLM查询优化问题。
---

## 摘要
并行部署多个大型语言模型(LLMs)来分类未知真实标签是一种常见做法，但在异构模型间最优分配查询的问题仍然理解不足。在本文中，我们制定了一个稳健的离线查询规划问题，该问题在满足状态误差约束的前提下最小化总查询成本，这些约束确保了每种可能真实标签的可靠性。我们首先通过从最小权集合覆盖问题的约简，证明了该问题是NP难的。为了克服这一难题，我们通过将多类误差的并集边界分解为成对比较，并结合Chernoff型集中边界，构建了一个代理问题。由此产生的代理问题在查询计数中具有闭式、乘法可分离的表达式，并且保证保持可行性。我们进一步表明，该代理在优化层面是渐近紧致的：随着误差容限的缩小，代理最优成本与真实最优成本的比率收敛到1，且具有显式速率$\left(\log\log(1/α_{\min}) / \log(1/α_{\min})\right)$。最后，我们设计了一个渐近完全多项式时间近似方案(AFPTAS)，该方案返回一个代理可行查询计划，其成本在代理最优值的$(1+\varepsilon)$因子范围内。

## Abstract
Deploying multiple large language models (LLMs) in parallel to classify an unknown ground-truth label is a common practice, yet the problem of optimally allocating queries across heterogeneous models remains poorly understood. In this paper, we formulate a robust, offline query-planning problem that minimizes total query cost subject to statewise error constraints which guarantee reliability for every possible ground-truth label. We first establish that this problem is NP-hard via a reduction from the minimum-weight set cover problem. To overcome this intractability, we develop a surrogate by combining a union bound decomposition of the multi-class error into pairwise comparisons with Chernoff-type concentration bounds. The resulting surrogate admits a closed-form, multiplicatively separable expression in the query counts and is guaranteed to be feasibility-preserving. We further show that the surrogate is asymptotically tight at the optimization level: the ratio of surrogate-optimal cost to true optimal cost converges to one as error tolerances shrink, with an explicit rate of $O\left(\log\log(1/α_{\min}) / \log(1/α_{\min})\right)$. Finally, we design an asymptotic fully polynomial-time approximation scheme (AFPTAS) that returns a surrogate-feasible query plan within a $(1+\varepsilon)$ factor of the surrogate optimum.

---

## 论文详细总结（自动生成）

# 多大型语言模型查询优化论文总结

## 1. 论文的核心问题与整体含义
- **研究动机**：并行部署多个大型语言模型(LLMs)来分类未知真实标签是一种常见做法，但在异构模型间最优分配查询的问题仍然理解不足。
- **核心问题**：如何在保证分类可靠性的前提下，最小化多LLM系统中的总查询成本。
- **研究背景**：实际应用中(如医疗诊断、内容分类、文档审查等)，组织经常使用多个LLM并行处理同一任务，但如何最优分配查询以平衡成本和准确性缺乏系统方法。

## 2. 论文提出的方法论
- **核心思想**：将多类错误约束转化为成对比较问题，并应用Chernoff集中界构建可求解的代理问题。
- **关键技术细节**：
  - 通过并集边界将多类误差分解为成对比较(Lemma 1)
  - 应用Chernoff集中界构建代理问题(Proposition 1)
  - 定义Chernoff亲和因子$M_{m}^{(y,y')}(s)$衡量模型对标签对的区分能力
  - 构建具有闭式、乘法可分离表达式的代理问题(Theorem 2)
- **算法流程**：
  - 设计渐近完全多项式时间近似方案(AFPTAS)(Algorithm 1)
  - 离散化Chernoff倾斜参数$s$为有限网格
  - 使用动态规划求解每个固定参数的子问题
  - 通过回溯恢复最优查询分配方案

## 3. 实验设计
- **数据集/场景**：论文未提供具体实验数据集或场景。
- **基准方法**：论文未明确提及对比的基准方法。
- **实验设置**：论文主要进行理论分析和算法设计，未描述具体实验设置。

## 4. 资源与算力
- **算力资源**：论文未明确提及使用的GPU型号、数量或训练时长等算力资源信息。
- **计算复杂度**：算法复杂度为多项式时间，具体为$O(K \cdot \log(1/\alpha_{\min}) \cdot 1/\varepsilon)$，其中$K$为模型数量，$\alpha_{\min}$为最小误差容限，$\varepsilon$为精度参数。

## 5. 实验数量与充分性
- **实验数量**：论文未提供具体实验数量或实验设计。
- **实验充分性**：论文主要贡献在于理论分析和算法设计，而非实证研究，因此无法评估实验的充分性、客观性和公平性。

## 6. 论文的主要结论与发现
- 证明了查询设计问题是NP难的(Theorem 1)
- 构建的Chernoff代理问题具有可行性保证(Theorem 2)
- 代理问题在优化层面是渐近紧致的，其最优解与真实最优解的比率收敛到1(Theorem 3)
- 设计的AFPTAS算法能够在多项式时间内找到$(1+\varepsilon)$因子接近最优的查询分配方案(Theorem 4)

## 7. 优点
- **理论创新**：首次系统性地解决了多LLM查询分配的优化问题，建立了坚实的理论基础。
- **方法创新**：创新性地将多类错误约束转化为成对比较，并应用Chernoff集中界构建可求解的代理问题。
- **算法效率**：设计的AFPTAS算法具有多项式时间复杂度，能够高效求解实际规模的问题。
- **理论保证**：提供了严格的理论保证，证明了代理问题的渐近紧致性和算法的近似性能。

## 8. 不足与局限
- **实证验证缺失**：论文缺乏实验验证，无法在实际数据上验证方法的有效性。
- **应用限制**：未考虑模型动态变化或查询成本变化的情况，可能限制在实际应用中的适用性。
- **假设条件**：依赖于模型输出的独立性和同分布假设，可能在实际场景中不成立。
- **计算效率**：虽然算法具有多项式时间复杂度，但实际计算成本可能随问题规模增加而显著提高。
- **扩展性**：未讨论方法在更大规模模型集合或更复杂分类任务上的扩展性。

（完）
